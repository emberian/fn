# BP ADU decoder guard closure

The complete `books/bp-adu.lisp` graph now has verified guards: **41 functions**,
including the public guard-`T` decoder and its typed streaming callees. Five
previously unverified decoder functions were closed by appending `verify-guards`
events. All existing source definitions and logical bodies remain byte-for-byte
unchanged. ACL2 assertions check the five entries' actual symbol class.

The [machine record](2026-09-18-bp-guards.json) retains before/after actual ACL2
world queries, the reproducible inventory driver, input hashes, exact version
and certification manifest. ACL2 8.7 / SBCL 2.6.8 passed eight scoped roots:

```sh
python3 tools/certify_books.py books/bp-adu tests/acl2/bp-adu-tests books/bp-receipt tests/acl2/bp-receipt-tests books/bp-receipt-records tests/acl2/bp-receipt-records-tests books/bp-outbound tests/acl2/bp-outbound-tests
```

The integrated manifest is
`build/acl2/certify-20260919T003826Z-38490/manifest.json` (UTC date). Inputs were
unchanged during certification. This is not a new full Makefile-root run.

The seven-book inventory distinguishes the remaining gap:

| Base book | Guard verified | Guard verification remains |
| --- | ---: | ---: |
| BP article ingress | 0 | 35 |
| BP ADU codec | 41 | 0 |
| Sender workflow | 109 | 0 |
| Sender journal records | 1 | 10 |
| Receiver decisions | 0 | 41 |
| Receiver journal records | 1 | 9 |
| Outbound projection | 9 | 2 |

This inventory covers definitions in those seven books, not all host entrypoints
or proof-only helpers. `:IDEAL` means guard verification is outstanding; it is
not a failed correctness theorem. The actual host remains an interpreted ACL2
bridge. The remaining 97 functions preclude a whole-BP-graph raw-execution claim.
Neither the table nor its counts measure a percentage of system correctness.
Work/heap bounds, authority, cryptography and physical durability remain separate.
