# Wave 1a: green-lane convergence at `60c5322`

This record covers the first integration of the 2026-09-18/19 review-driven
wave: the four lanes whose gates were green at handoff (assurance-tooling,
sender-proofs, host-repair, hygiene), merged into `dev` in that order. The
[machine record](2026-09-19-wave1a.json) holds the source digests, the remote
certification manifest fields, and the test result. The six remaining lanes
(crash-fidelity, nntp-served-path, twins-into-acl2, substrate, bp-primary-time,
wire-article-transfer) are in proof repair and are not part of this record.

## What was run

- **Certification** of every Makefile root on persvati (Linux x86_64, 24
  cores), using ACL2 8.7 on SBCL 2.6.8 built from the 8.7 release tag with the
  four community books fn includes certified in place. Every root exited 0,
  every nonce-tagged success marker was observed, every certificate digest was
  recorded, and the forbidden-facility audit found nothing. The runner was
  unchanged from the repository copy.
- **The same certification on the development laptop** under six concurrent
  repair lanes reached the same result on every root except
  `books/article-properties`, where `FN-AP-PARSE-LINES-FIELD-COUNT-BOUND`
  failed after 63,105,525 prover steps with 52 s spent in garbage collection.
  The identical book completed at 318,552,821 steps and passed on a quiet
  laptop at `a5a30d8` and on persvati here. This is recorded as an
  environmental nondeterminism of unknown cause; gates now run on quiet boxes.
- **The full Python suite** on the laptop: `python3 -m unittest discover -s
  tests -p 'test_*.py'` passed (count and time in the machine record) on
  Python 3.14.7. It includes the host-repair tests for the fourteen review
  defects it closed, the ledger tool's unit tests, and the reader and store
  suites over the repaired adapters.
- `make check` passed, including the new generated-ledger consistency check.

## What this record establishes

- The assumptions book, the generated ledger and its `make check` gate, the
  teeth test books, the sender replay-interpreter theorems and the
  durable-intent theorem, the index and checkpoint repairs, and the fourteen
  host repairs all certify or pass together on one tree.
- Registry events now name keystone theorems that exist and are not
  shape-suspect, checked mechanically.

## What it does not show

- Nothing about the six pending lanes: no crash-model fidelity, NNTP served
  path, byte-codec ownership, substrate, BP primary block, or wire and transfer
  results are in this tree.
- No physical durability, platform qualification, cryptographic or peer
  claim; the assumptions book names those as constrained functions and nothing
  discharges them.
- The laptop certification is not a passing record for this tree; persvati's is.
