# Four-node carried-media lab, 2026-09-20

A real run, and an incomplete one. `tests/evidence/2026-09-20-four-node-lab.json`
is the machine record the lab wrote; this file says what it means.

- Command: `python3 tests/bp-dtn7/run_four_node_lab.py`, mock BPA transport,
  Python 3.14.7, ACL2 through `tools/run_store.py` against the certificates
  installed in this worktree. Revision is in the JSON. 46.4 s.
- Status: **failed**, at `tests/bp-dtn7/run_four_node_lab.py:541` --
  `fn-workflow-preflight-history` refused relay-b's `attempt` record for work
  that same session had just enqueued (`JournalError('ACL2 rejected workflow
  durable history before publication')`).
- **Ten of the lab's twenty-one named assertions were checked and every one
  held**; the other eleven were never reached and nothing is claimed of them.
  Held: `window_1_delivers_both_letters`, `relay_a_accepts_both_letters`,
  `relay_a_archival_receipts_survive_kill`,
  `relay_a_journal_is_usable_after_recovery`,
  `relay_a_archival_receipts_unchanged_by_recovery`,
  `media_hop_accepts_both_letters`, `carried_media_is_not_modified_by_import`,
  `media_reimport_is_duplicate_not_a_second_acceptance`,
  `lost_receipt_regenerates_byte_identically`, `reimport_adds_no_receipt_record`.
- So the carried-media hop itself ran: relay-a exported a volume with no contact
  window open, relay-b imported both letters through
  `run_bp_receive.receive_bpa_request`, the volume was byte-identical after the
  import, a re-import was a duplicate that added no receipt record, and a
  deleted receipt regenerated byte-identically. The mid-forward SIGKILL of
  relay-a was reached and its three archival assertions held.
- Not reached, and therefore NOT evidence for anything: attempt expiry and
  retry, the reordered pair, the duplicate delivery, every destination
  assertion, and all of the local-number independence.
- A second reading from the same tree, recorded because it is the sharper
  statement of the blockage: after a journal reopen, `fn-workflow-work-status`
  answers `absent` for every work id in the replayed history, although
  `fn-workflow-install-replay` answered `ready`.
- Limitations of the lab itself are listed in the JSON's `limitations` array
  and are unchanged: mock BPA (not BPv7), no relay receipt kind, one
  application endpoint identity across all four nodes, trusted local A-POLICY,
  real local process death only.
