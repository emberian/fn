# Function prefix registry

Every executable definition in `books/` carries a `fn-` prefix followed by a
layer tag. This table is the authoritative map from tag to book and meaning.
Add a row before introducing a tag; a tag with no row is a review finding.
Counts are not maintained here; `tools/ledger.py` reports them.

| Tag | Books | Meaning |
| --- | --- | --- |
| `fn-state-`, `fn-pending-`, `fn-accept-`, `fn-install-`, `fn-allocate-`, `fn-initial-`, `fn-articles-`, `fn-membership(s)-`, `fn-next-`, `fn-bump-`, `fn-clear-`, `fn-find-`, `fn-all-`, `fn-advance-`, `fn-make-`, `fn-pair-`, `fn-string-`, `fn-octet-`, `fn-no-`, `fn-selection-` | `acceptance`, `acceptance-invariants` | Logical acceptance machine: staged allocation, durable completion, fences, primitive domains |
| `fn-ag-` | `acceptance-alloc` | Total executable helpers (`car`, `cdr`, `member`, `append`, `less`) that are their logical primitives by `mbe`; `append` executes as `fn-ag-append-exec` (`fn-ag-rev-onto`, constant stack); used in `:exec` branches across the tree |
| `fn-retain-` | `retention`, `retention-invariants` | Abstract retention accounting: pins, charges, evidence-gated release |
| `fn-rcl-`, `fn-rclb-`, `fn-rca-` | `reclaim-rule`, `reclaim-tombstone`, `store-reclaim`, `store-reclaim-buffer`, `store-reclaim-holders`, `reclaim-admission` | Content reclamation under D13 (STO-014): the operator's retention rule rows, the per-article decision over every holder, the tombstone a reclaimed payload becomes, and what reclamation keeps |
| `fn-node-` | `node`, `node-invariants`, `node-traces` | One-transaction composition of acceptance and retention; article-to-pin bindings; event dispatcher |
| `fn-record-` | `records-shape`, `records`, `records-invariants`, `records-canonicality`, `records-seam`, `records-attach`, `records-stamp` | Schema-0/1 transaction record grammar and owner-derived acceptance stamp over CBOR primitives. `records-shape` defines the record; `records` implements the codec; `records-seam` constrains the names callers use; `records-attach` attaches the implementation; `records-stamp` derives the stamp solely from the owner's observation |
| `fn-cbor-` | `cbor`, `cbor-invariants` | Deterministic CBOR primitives: uint32 and definite byte strings |
| `fn-oct-`, `fn-octets-`, `fn-octets$a-`, `fn-octets$c-` | `octets-stobj` | The octet buffer: an abstract stobj whose logical value is the octet list and whose executable is a resizable byte array with a fill count (`fn-octets-` the exports, `$a` the logical side, `$c` the concrete stobj, `fn-oct-` the abstraction lemmas and the derived readers a codec twin reads the buffer with) |
| `fn-pbb-` | `poster-bytes-buffer` | `fn-pb-existing-action` with the submitted payload in the octet buffer: the injection source walk and the payload compare by index, equal to the list function on the buffer's value |
| `fn-shb-` | `sha256-buffer` | SHA-256 of a prefixed octet buffer read by index over the word stobj of `sha256-stobj` (the third message reader: a short list prefix, then the buffer), with `fn-sha256-of-prefixed-buffer-is-sha256` equating it to the list model on every prefix and value, and `fn-shb-subject-id` the subject identity over the buffer the served POST calls |
| `fn-pull-` | `peer-pull`, `tests/acl2/peer-pull-tests` | The NEWNEWS pull feed (PRF-100): the round machine the owner's pull worker steps, the ack-bounded cursor close, the FNPL cursor journal (codec, envelope, scan, replay) and the pulled peers read from configuration rows |
| `fn-replay-` | `replay`, `replay-invariants` | Contiguous record replay into a node; typed ok/fault results |
| `fn-sf-` | `store-files`, `store-files-invariants`, `store-files-traces` | Immutable-file publication kernel: allocator frontier, staged record, barriers, crash constructor, recovery gate |
| `fn-bs-` | `byte-store`, `store-profile-namespace` (`fn-bs-profile-max-*` namespace-count accessors, D27), `byte-store-invariants`, `byte-store-programs`, `byte-store-scan`, `byte-store-profile-program`, `byte-store-profile-v1` (the pre-P6 profile relation, frozen), `byte-store-marker-program`, `byte-store-k0-marker` (`fn-bs-k0m-` helpers), `byte-store-k0-staging-error` (`fn-bs-k0e-` helpers), `byte-store-k0-authority-error` (`fn-bs-k0a-` helpers), `byte-store-k0-step-bridge-root` (`fn-bs-k0r-` helpers), `byte-store-k0-window` (`fn-bs-k0w-` helpers), `byte-store-k0-recover-program` (`fn-bs-k0v-` helpers), `byte-store-k0-pre-init` (`fn-bs-k0i-` helpers) | Byte-level storage model under the file kernel (crash model v2): inodes, directories, pending writes and entry operations, per-object fences, unit-granular torn crash images, syscalls with EIO/ENOSPC outcomes, and the host's syscall sequences as programs over it |
| `fn-bsrp-` | `byte-store-retention-publication`, `tests/acl2/byte-store-retention-publication-tests` | ACL2-authored retention Store event through the framed P-RECORD byte program, including the transaction-directory error cut |
| `fn-bsi-` | `byte-store-initializer` | Current `Store.initialize` fresh-path transcription: parent fences, fresh lock, metadata and configuration-history publication, and the complete initial byte image; existing/retry branches remain open |
| `fn-sn-` | `store-node`, `store-node-invariants`, `store-node-resolution`, `store-identity-sequence-invariants`, `store-sweep`, `store-observed`, `tests/acl2/store-node-index-tests` | Live file/node composition: pending record binding, actual completion, refusal and known-abort resolution, observed opening; and since D21 the two configuration fields beside the node -- the keyring and the served statement index -- with `fn-sn-set-keyring`, the carried agreement `fn-sn-indexedp` and the two served queries `fn-sn-statement-lookup` and `fn-sn-equivocatorp` |
| `fn-sis-` | `store-identity-sequence-invariants` | Local lemmas for the phase-aware Store journal cursor: one increment per successful identity event and a matching file completion without changing committed records |
| `fn-snt-` | `store-node-traces`, `store-node-invariants` | Trace relation and preservation for the composed store/node machine, and the replay-composition and frontier-advance lemmas the trace books spend (`store-node-invariants` has carried `fn-snt-` names since the replay-composition lemmas landed) |
| `fn-spc-` | `store-prepare-correspondence` | Executable live-store prepare projection and its equality to the replaying specification under the maintained store/node correspondence |
| `fn-sbud-` | `store-budget`, `owner-store-budget`, `store-budget-naming`, `tests/acl2/store-budget-tests`, `tests/acl2/owner-store-budget-tests`, `tests/acl2/store-budget-naming-tests` | M5 transaction budget: the committed-record count of the Store state, the budget derived from the persisted profile at open, the publication verdict, the operator headroom, the served article prepare that refuses at the budget (`fn-sbud-prepare`, installed by `fn-owner-prepare`), the staged record's sequence and transaction name the host writes (`fn-sbud-pending-sequence`, `fn-sbud-txn-name`), and the POST boundary over the profile's payload bound (`fn-sbud-post-boundary`) |
| `fn-profile-` | `store-profile-upgrade`, `tests/acl2/store-profile-upgrade-tests`, `store-profile-namespace` (`fn-profile-upgrade-keeps-namespace-counts`) | M5 offline profile upgrade: the upgrade relation `fn-profile-upgradep`, the operator verb's verdict `fn-profile-upgrade-verdict`, and the profile-dependent open gates the host calls (`fn-profile-txn-observation`, `fn-profile-replay-within-boundp`) with their monotonicity under an upgrade |
| `fn-snrt-` | `store-node-retention`, `tests/acl2/store-node-retention-tests` | P9 over the host-called store functions: refusal of an unaffordable obligation by `fn-spc-prepare`/`fn-node-prepare`, and `fn-sn-finish` keeping accepted articles and releasing an obligation only by its matching release |
| `fn-snrt-` | `store-node-resolution` (folded from `store-node-resolution-traces`, 2026-09-19) | Trace preservation including refusal and abort resolution |
| `fn-checkpoint-` | `checkpoint` | Logical checkpoint capture, restore and checkpoint-plus-suffix replay |
| `fn-cpc-` | `checkpoint-codec` | Canonical whole-state checkpoint bytes over the CBOR primitives: tagged node tree, header, both-direction canonicality, validation binding to capture, the FNCP frame magic and kind table (checkpoint, selection marker) |
| `fn-cpp-` | `checkpoint-publish` | Checkpoint generation machine: stage, barriers, link, marker selection, crash images with old/new and absent/present choices, recovery with distinct none/ok/corrupt/missing outcomes |
| `fn-cprt-` | `checkpoint-pack-retire` | Bounded selected-pack generation retirement, gap-aware monotone pack allocation and process-death namespace images |
| `fn-cc-` | `checkpoint-compaction` | Lossless canonical Store-event prefix packs, overlap validation, and prefix-plus-suffix reconstruction |
| `fn-ccp-` | `checkpoint-compaction-preservation`, `tests/acl2/checkpoint-compaction-preservation-tests` | M5 compaction preservation: the framed pack observe/coverage the host calls (`fn-ccp-observe-framed`, `fn-ccp-coverage-framed`), what a reclaim cut leaves (`fn-ccp-remove-names`, `-remove-pairs`, `-read`), and the theorem that the next open reconstructs the same record list |
| `fn-cverb-` | `store-compact-verb`, `tests/acl2/store-compact-verb-tests` | M5 `operator CONFIG store compact`: the verb's decision `fn-cverb-decide` (pack and reclaim, resume, or a named refusal), its temporary-space budget from the persisted profile, the compaction unit, and the open's inability to see a lost newest record (finding 3) |
| `fn-hm-` | `store-history-marker`, `tests/acl2/store-history-marker-tests` | M5 committed-history boundary: the marker's FNSM kind-3 codec, what it holds after a record commits (`fn-hm-after-commit`), the open's verdict over it (`fn-hm-open-verdict`), its byte program and cut table, and the crash/history model its keystones are stated over |
| `fn-hmr-` | `store-history-required`, `tests/acl2/store-history-required-tests` | D31 committed-history requirement: the profile-aware open verdict (`fn-hmr-open-verdict`, `:marker-missing` under `required`), the recovery catch-up frame (`fn-hmr-catch-up`), the `store upgrade-profile` gate (`fn-hmr-upgrade-verdict`), and the history model (commits, opens, resolutions, migrations) the A <= M <= D keystones are stated over |
| `fn-cpa-` | `checkpoint-auxiliary` | Versioned recovery-only comparison of replayed consumer and historical authorship projections with the Store opened from exact journal history |
| `fn-index-` | `index` | Derived group/number index and range queries |
| `fn-midx-` | `msgid-index` | Persistent Message-ID trie and canonical lookup correspondence |
| `fn-journal-` | `journal` | Historical isolated-slot journal experiment; not the adapter model |
| `fn-exchange-` | `exchange`, `exchange-invariants` | Bounded atomic fact-set admission and merge |
| `fn-transfer-` | `transfer`, `transfer-invariants`, `transfer-assembly-invariants`, `transfer-work`, `transfer-public-work`, `transfer-public-bound` | Fragment reservation, assembly, missing ranges, costed shadows and bounds |
| `fn-tj-` | `transfer-journal`, `transfer-journal-invariants` | Durable fragment journal around `fn-transfer-reserve`/`fn-transfer-add-chunk`: records with the kernel's outcome, replay as the fold of the transitions, the FNTJ frame family, typed faults, the `:unverified` candidate export |
| `fn-ct-` | `container`, `container-invariants` | Portable object container: articles with exact octets, content id and dependency ids, opaque unknowns; identity/dependency/size validation before allocation; publication through `fn-node-prepare`/`fn-node-complete`; OBJ-004 conflict evidence |
| `fn-wire-` | `wire`, `wire-invariants` | NNTP line framing, dot stuffing, bounded retained input |
| `fn-nntp-` | `nntp`, `nntp-overview`, `nntp-newnews`, `nntp-invariants`, `nntp-effects`, `nntp-pinned-msgid` | Reader command dispatcher, session cursor, projection, effects; the reader environment (clock observation and group-creation facts), the closed-form civil calendar, and the DATE/NEWGROUPS/MODE READER/OVER branches. `fn-nntp-dt-` is the RFC 5322 §3.3 date-time decoder NEWNEWS reads an article's `Injection-Date` or `Date` with; `fn-nntp-newnews-` is the budgeted scan and its two specification folds |
| `fn-nov-` | `nntp`, `nntp-overview`, `nntp-range-indexed` | RFC 3977 §8.3.2 overview projection over the proved article `fields` view: field escaping, `:bytes`/`:lines` metadata, the eight-field line, cleanliness, and pinned range rendering |
| `fn-ng-` | `nntp` | Guard-verified executable helpers for the NNTP graph |
| `fn-nntp-index-` | `nntp-index` | Index-backed twins of the NNTP number enumerations and the generation-bound index cache |
| `fn-gidx-` | `group-bucket-index`, `group-bucket-article`, `nntp` (LIST COUNTS lines), `nntp-list-counts` | Derived per-group buckets and local-number lookup through the pinned Message-ID trie; archive folds remain the authority |
| `fn-nlc-` | `nntp-list-counts` | Local lemmas and work counters for LIST COUNTS and the numbered Message-ID answer |
| `fn-olc-` | `owner-list-counts-read` | LIST COUNTS and Message-ID retrieval stated over the served step |
| `fn-xri-` | `tests/acl2/nntp-range-indexed-tests` | Bounded OVER/XOVER payload witness construction only |
| `fn-auth-` | `nntp-auth`, `nntp-auth-invariants`, `nntp-auth-fold` | AUTHINFO (RFC 4643) and STARTTLS (RFC 4642) as the outermost wrapper of the served command chain: the credential (name, principal, stored verifier, posting flag), the per-connection policy pinned at open, the session's cached name / authenticated subject / TLS layer / handshake state, the 480 gate, the posting allowance that follows the authenticated principal's credential, the capability labels gated on what the server will accept now, the closed effect enumeration that adds the `(:starttls)` handshake instruction to the NNTP effects, and the no-local-submission invariant over the complete served read |
| `fn-served-` | `served` | The served NNTP path as one logic-mode, guard-`t`-verified function: one socket read is the byte fold with `fn-auth-step` per framed event, with the reply concatenation, the partition law and the typed effect enumeration; the connection record (wire, auth session, pinned archive, pinned posting configuration, reader observation, injection clock) and the three projections the host may take of an effect list (reply octets, close, handshake); two stopping conditions, a closed wire and a pending TLS handshake |
| `fn-ideal-` | `ideal` | F_node, the ideal node functionality of [`specs/node-functionality.md`](../specs/node-functionality.md) section 1: the state record and the port dispatcher. SKELETON: only the reader port is real (it is `fn-served-step`); every other port is a stub returning the state with a `:todo` effect, and the robustness theorems of section 3 are written there as commented statements marked OPEN |
| `fn-dtn-` | (planned, `dtn-channel`) | The unauthenticated, adversarially scheduled bundle channel of [`specs/node-functionality.md`](../specs/node-functionality.md) section 5.4: the in-flight multiset and the adversary's delay, reorder, duplication, loss and injection. No book yet; the tag is registered so the skeleton above can name it |
| `fn-sys-` | (planned, `system`) | The composed system F_node × F_dtn: a finite map from endpoint id to node states plus one channel, and the end-to-end release theorem of section 5.4. No book yet |
| `fn-index-host-` | `host/index-host.lisp` | Trusted adapter that opens the index cache at a recovered generation and asks it; holds no enumeration logic |
| `fn-wildmat-` | `wildmat`, `wildmat-utf8-invariants`, `wildmat-parser-invariants`, `wildmat-matcher-invariants` | UTF-8 decoding, wildmat grammar parsing, dynamic-programming matcher |
| `fn-wm-` | `wildmat-matcher-invariants`, `wildmat-work` | Reference matcher and costed matcher shadow |
| `fn-article-` | `article`, `article-invariants`, `article-properties` | Bounded header/body article parser with exact source preservation |
| `fn-aw-` | `article-work-primitives`, `article-work-scanners`, `article-work`, `article-work-budget`, `article-public-work`, `article-public-bound` | Instrumented article parser shadow, value correspondence and work bounds |
| `fn-af-` | `article-fields` | Message-ID and Newsgroups field semantics over parsed views |
| `fn-frame-` | `frame-octets`, `frame-fields`, `frame-journal`, `frame`, `frame-invariants`, `frame-trailer` | The one durable frame grammar (magic, version, kind, bounded length, payload, 32-octet trailer), its journal field grammar, the constrained trailer function A-CRYPTO, and -- in `frame-trailer` -- `fn-frame-trailer`, the one owner of the integrity trailer that the Python bridge, the native host and the owner's feed all call |
| `fn-jpub-` | `journal-publish` | Immutable no-replace journal publication: stage/write, file barrier, link ambiguity window, namespace barrier, and the durable/refused/uncertain classification used by the native FNWF/FNRJ host |
| `fn-aj-` | `app-journal` | ACL2-owned native FNWF/FNRJ namespace, recovered frontier, aggregate accounting, initialization order, headroom, and authorized immutable publication operation |
| `fn-id-`, `fn-charge-` | `identity`, `identity-invariants` | Content-identity derivation (subject and archive obligation), the hexadecimal projection, and the per-payload charge policy |
| `fn-store-` | `store-config` | The configured group table and the name/code mapping shared by every adapter |
| `fn-cp-` | `consumer-position` | Experimental E2 v1 bounded cursor codec and finite register/ack/rebase/unregister decision kernel; its `:write` result is a proposal consumed by the Store and local-owner seam, not durable acceptance itself |
| `fn-cpe-` | `consumer-store-events` | Versioned, bounded E2 Store-journal event grammar and exact codec for bootstrap, register, ack, rebase, unregister and incarnation rollover; durability requires the Store finish/recovery join |
| `fn-csi-` | `consumer-store-invariants` | Completed Store prefix versus carried consumer projection, with explicit completing-window and recovery obligations |
| `fn-cei-` | `consumer-event-index` | Rebuildable four-octet radix lookup of exact committed Store events by journal sequence; correspondence to the authoritative event list is required before served use |
| `fn-col-` | `consumer-owner-local` | ACL2 local-owner command proposals and position query under a fixed local principal and immutable historical group-membership query |
| `fn-ncl-` | `consumer-local-control` | Bounded local-control consumer command/reply codec and CLI grammar; the native adapter transports these bytes to the owner |
| `fn-cpj-` | `consumer-poll-projection`, `tests/acl2/consumer-poll-projection-tests` | Read-only bounded projection of exact E2 cursor and schema-1 accepted article bytes; local Store provenance still requires the authenticated consumer poll caller |
| `fn-bp-` | `bp-workflow`, `bp-workflow-invariants`, `bp-workflow-transport-invariants`, `bp-workflow-binding-core`, `bp-workflow-binding-invariants`, `bp-workflow-records`, `bp-workflow-replay-status` | Sender workflow: work, attempts, intents, transport observations, journal replay, and the reopened image's read model (status across a restart, and whether a work was recovered or enqueued this session) |
| `fn-bpa-` | `bp-adu` | Canonical CBOR application data units: request and receipt |
| `fn-bpi-` | `bp-ingress` | Legacy article ADU ingress, routing and composed-store admission |
| `fn-bpr-` | `bp-receipt` | Receiver state: request context, receipt intent and decision, receipt ADU emission |
| `fn-bprr-` | `bp-receipt-records` | Receiver journal record application and replay |
| `fn-bprv-` | `bp-receiver-*-invariants`, `bp-receiver-evolving-*-invariants` | Receiver relation invariants over a Store, contexts, decisions and replay; the history-indexed relation over an evolving Store |
| `fn-bpr-live-` | `bp-receiver-evolving-store-invariants` | Live receiver trace model over the host's call sequence (`tools/run_bp_receive.py`): Store global, receiver global, journal on disk |
| `fn-bpnp-` | `bp-node-progress` | ACL2-owned one-event held progress selection and per-key dependency waits over the existing FNBS owner; the native service calls this outer step |
| `fn-bpnpp-` | `bp-node-progress-premises` | Local proof helpers showing the served `fn-bpnp-step` keeps its own guard premises; no executable behaviour |
| `fn-bpnpb-` | `bp-node-progress-bridge` | The effects bridge from the served `fn-bpnp-step` to `fn-bpn-step` and the release/receipt-prepare confinement predicate over every served event; no executable behaviour |
| `fn-bprt-` | `bp-route` | The BP route table (configuration rows written by `bp-route add`), its parser, and the routing decision `fn-bprt-next-hop` / the session gate `fn-bprt-offer-decision` the :session event consults (spec bp-node-machine 4.6) |
| `fn-bprts-` | `bp-route-step` | Proof-only names for the routed :session arm's scan and destination in the routing theorems over `fn-bpnp-step`; no executable behaviour |
| `fn-bpnr-` | `bp-node-rotation-codec`, `bp-node-rotation`, `bp-fnbs-replay-append` | BP journal rotation (N16): the kind-19 held-row checkpoint codec, generation naming and selection plan, recovery from a checkpoint and the publication phase driver |
| `fn-bpnrs-` | `bp-node-rotation-step` | Local proof helpers for the two equations between the served `fn-bpnp-step` and the rotation arms; no executable behaviour |
| `fn-bpnd-` | `bp-node-debt` | Exact received-FNBS physical record credit and held/attempt/handoff/fragment debt projections; served transitions carry the cached debt and update it from one row |
| `fn-bpo-` | `bp-outbound` | Outbound projection of sender work to request ADUs and receipt validation |
| `fn-bprq-` | `bp-request-plan` | The generic native request plan: the ACL2 attempt, its durable outcome, the FNBS key, the request ADU and destination that `bp-obligation request` executes; ION reuses its attempt constructor |
| `fn-bpio-` | `bp-ion-observation` | Pinned-ION observation decode and exact durable-attempt identity gate |
| `fn-bpiw-` | `bp-ion-workflow`, `bp-ion-workflow-replay` | Durable FNWF ION route and observed-ID replay overlay; replay against the live path |
| `fn-bprl-` | `bp-release`, `bp-release-invariants`, `bp-workflow-constructors`, `bp-release-replay-status` | Sender-side forwarding-obligation release: typed release evidence and term, the `:forward` pin undertaking, the release decision over the node image, the `:undertake`/`:release` journal interpreter, canonical receipt-ADU-to-intent constructors for the native workflow caller, and host-called status replay refinement |
| `fn-relay-` | `relay`, `relay-invariants`, `relay-crash-invariants` | Relay undertaking: receiver-then-sender composition over the receiver and sender public entry points, terms table, undertakings ledger, typed `:archived`/`:forwarding` receipts, crash-then-replay over both journals |
| `fn-bprv-` | `bp-receiver-*-invariants` | Receiver relation invariants over a Store, contexts, decisions and replay |
| `fnn-` | `host/native/*.lisp` (raw Lisp, not books) | The native host adapter: POSIX barriers and files, SHA-256, bounded libsodium Ed25519/SHA-512 observations, Roughtime CSPRNG/UDP acquisition, host-owned metadata JSON, socket loops, and `fnn-call`, the raw-Lisp `ec-call` into the `:program` host wrappers |
| `fn-bpc-` | `bp-primary-cbor` | Deterministic CBOR vocabulary RFC 9171 §4.3.1 needs over `fn-cbor-`: definite arrays and text strings, unsigned integers to 2^64-1 |
| `fn-bpp-` | `bp-primary`, `bp-primary-invariants` | BPv7 primary bundle block: flags, CRC-16/CRC32C, `dtn` and `ipn` endpoint IDs, creation timestamp, lifetime, fragment fields, bundle identity, §4.4 extension block data |
| `fn-bp-eid-` | `bp-eid-shape` | Text shape of an operator-enrolled BP endpoint ID (RFC 9171 §4.2.5.1 dtn/ipn, with fn's local canonical-form and length rules) and the VCHAR-only theorem `bp-boundary add` relies on |
| `fn-bpb-` | `bp-bundle`, `bp-bundle-invariants` | BPv7 whole bundle (RFC 9171 §4.3): the canonical block frame of §4.3.2 with its §4.2.2 CRC, the payload block, the indefinite-length bundle array, the §4.4 extension blocks as canonical blocks, and the bundle codec with bounds before allocation |
| `fn-bpbi-` | `bp-bundle-invariants` | Local proof vocabulary of the whole-bundle keystones: octet-list facts about `append` and `cons`, the `car`-of-`append` pair, and the block-list induction scheme the fold's round trip is proved by. Every name under it is `local`; none leaves the book |
| `fn-bpn-` | `bp-node`, `bp-node-records`, `bp-authored-wire`, `bp-status-report`, `bp-status-report-invariants`, `bp-report-deletion`, `bp-node-report-step`, `bp-report-outbox`, `bp-report-author`, `bp-report-observe` | fn as a BPv7 node: the configuration, authored and received bundles, durable creation sequence, §5.4 forwarding decisions, the bounded canonical §6.1.1 status-report codec, a durable held-deletion report intent, ACL2 administrative outbound authoring, and read-only received-report correlation; decoded reports have no release authority |
| `fn-bpnf-` | `bp-node-foundation`, `bp-node-receive-boundary`, `bp-node-fragment-family`, `bp-node-fragment-expiry`, `bp-fnbs-deletion-codec`, `bp-fnbs-deletion-publication`, `bp-node-fragment-plan`, `bp-node-fragment-replacement`, `bp-node-fragment-step`, `bp-fnbs-family-codec`, `bp-fnbs-family-replay`, `bp-fnbs-family-publication`, `bp-fnbs-codec`, `bp-fnbs-inspect`, `bp-fnbs-codec-invariants`, `bp-fnbs-byte-publisher`, `bp-fnbs-byte-invariants`, `bp-fnbs-replay`, `bp-fnbs-replay-invariants`, `bp-fnbs-namespace`, `bp-fnbs-publication`, `tests/acl2/bp-fnbs-*` | Received-bundle foundation and protected FNBS transitions. The fragment wrapper is the native service's called step: it proposes a kind-18 family row without retiring fragments, then atomically installs the whole row and retires the selected principal/coherence family only on durable publication. One ordered reducer replays kinds 5, 7, 18, and 10. Guard closure and the full native fragment fixture remain separate qualification obligations. |
| `fn-bpcd-` | `bp-clock-domain`, `tests/acl2/bp-clock-domain-tests` | Canonical Linux boot-ID validation, FNBS kind-6 clock-domain frame, exact read bound, and total selectors for the startup plan. The `fn-bpnf-clock-domain-*` functions in the same book gate recovery on the saved domain and mixed FNBS namespace evidence. |
| `fn-bpf-` | `bp-fragment`, `bp-fragment-invariants`, `bp-fragment-fast` | BPv7 fragmentation and ADU reassembly over identical-overlap covers; fragment primary blocks; cursor cutting refinement |
| `fn-sched-` | `scheduler`, `scheduler-invariants` | Durable contact/retry scheduler: contact-plan observations, bounded work queue, deterministic priority with a FIFO aging rule, selection composed with `fn-bp-step`, the stated unfair policy used for the starvation counterexample, and the proposed FNWF `:schedule` decision record |
| `fn-bpsc-` | `bp-contact-service`, `tests/acl2/bp-contact-service-tests` | Native FNBS contact-window decision for one ready BP peer, with bounded observed time and explicit open/closed/invalid outcomes |
| `fn-bpah-` | `bp-app-handoff`, `bp-release-authority`, `bp-fnbs-delivery-codec`, `bp-fnbs-delivery-replay`, `bp-fnbs-delivery-publication`, their ACL2 tests | Local FNBS held-item selection, policy-bound application receipt ingress, and exact kind-7 durable delivery-result publication/replay for the native A3 dispatch join |
| `fnn-bpnode-` | `host/native/bp-node.lisp` | Native single-owner BP application dispatch and delayed receipt outbox caller over FNBS, owner Store, FNRJ, and FNWF |
| `fn-clock-` | `clock`, `clock-invariants` | Host clock observations, Bundle Age anchors and the three-way bundle expiry decision |
| `fn-own-` | `owner`, `owner-invariants`, `owner-fault`, `owner-served-invariants`, `owner-numbering`, `owner-feed-subject` | The mutable service owner over the live `fn-sn` composition: committed view, per-connection version pins and NNTP sessions, one pending transaction, the proof-only completion ledger, clock observations and clock-stamped group-configuration facts; and in `owner-fault`, the HOST-FAULT transition `fn-own-fault` -- the RFC 3977 sec. 3.2.1 403 line, the close, and the connection, in-flight submission and pending transaction the owner forgets when the host abandons one connection rather than the service |
| `fn-anchor-` | `anchor`, `anchor-wire`, `anchor-record`, `anchor-invariants` | The external freshness anchor: the bounded deployed RoughTime v1 wire grammar, the statement and exact octets its two signatures cover, the strictly-newer interval order, the monotone rule for acceptance, restore and incarnation advance, fork evidence, and the FNAN durable record family |
| `fn-digest-`, `fn-sig-` | `crypto-seam` | Constrained digest and signature seam with shape-only constraints; tagged preimages; hex rendering |
| `fn-shs-` | `sha256-stobj` | SHA-256 executed over a word stobj (the schedule and hash state as `(unsigned-byte 32)` arrays, the message read in place from an octet list or a string), with `fn-sha256-stobj-is-sha256` equating it to the list model of `sha256`; `fn-sha256-stobj` is the realiser `crypto-attach` binds to the digest seams and `fn-sha256-of-string` the string entry the host calls |
| `fn-hsig-` | `hybrid-signature`, `hybrid-store`, `hybrid-store-injected`, `hybrid-store-invariants`, `host/hybrid-signature-host` | D09 ordered Ed25519/ML-DSA-65 key and signature shapes, exact authored-source signing preimage and conjunctive observed-verification gate; native authored-carrier injection through the ACL2 news agent while retaining the exact signed source; primitive correctness remains trusted |
| `fn-hl-` | `hybrid-lifecycle`, `host/hybrid-signature-host` | Operator-local per-principal current enrollment, ordered rotation and revocation tombstone; historical keyring snapshots remain unchanged |
| `fn-pa-` | `peer-authored-accept`, `tests/acl2/peer-authored-accept-tests` | Receiver-side carrier classification, exact current local enrollment selection and bound atomic kind-4 event construction for protected transit and served POST; the served POST's relayed refusal word (`fn-pa-served-word`); the C1 filing step every ingress takes first (`fn-pa-filing-plan`) |
| `fn-lb-` | `login-binding`, `tests/acl2/login-binding-tests` | The served POST posting policy `bound-logins`: a login's binding to a signing principal (the credential file's `signing` field), the gate over the in-flight submission's login and its FN-Authorship carrier, the verdict and its log line |
| `fn-ctl-`, `ct-`, `cat-`, `cvt-` | `control-classify`, `control-authority`, `control-visible`, `tests/acl2/control-tests`, `tests/acl2/control-authority-tests`, `tests/acl2/control-visible-tests` | Control-message recognition (RFC 5536 §3.2.3, RFC 5537 §5): `fn-ctl-classify` reads only the Control field (and Supersedes beside it); the fixed filing-group table `control.<verb>` / `control`. `control-authority` (C2/C3): `fn-ctl-authorize`, `fn-ctl-cancel-plan`, `fn-ctl-withdrawal-effect`, `fn-ctl-visible-articles`, `fn-ctl-journal-withdrawals`. `control-visible` (C3, D27): `fn-ctl-visible-add`, `fn-ctl-visible-extend`, the incremental visible list |
| `fn-ks-`, `kst-` | `key-statements`, `tests/acl2/key-statements-tests` | PRF-098: the owner-side key-statement executor (succession and revocation) in C2's `fn-ctl-authorize` shape: `fn-ks-statement`, `fn-ks-pop-source`, `fn-ks-pop-request`, `fn-ks-plan`, `fn-ks-execute`, `fn-ks-log-line` |
| `fn-pinv-`, `pit-` | `peer-invite`, `tests/acl2/peer-invite-tests`, `host/peer-invite-host` | Peering invitations (PRF-097, specs/peering.md §10): the signed invitation and acceptance documents (rendering, the body-line parser `fn-pinv-field` and its specification `fn-pinv-body-names-p`), the shared document checks (`fn-pinv-document`: carrier verified under its own key set, body names the principal and both keys, genesis binding `fn-prin-genesis-bindsp`), the issue/accept/confirm plans and the owner's enrolment steps (`fn-pinv-accept-step`, `fn-pinv-confirm-step`), the once-only consumption theorems over config's invitations slot, and the control request codec (kinds 9 to 11) |
| `fn-osp-` | `owner-signed-post`, `tests/acl2/owner-signed-post-tests` | The signed served POST over the owner: the ACL2-built kind-4 event names the POST's Message-ID and selected generation, the owner's `(:complete)` records its verdict and a later reader reports it, and a refused carrier renders its own 441 line |
| `fn-hsigi-` | `hybrid-signature-invariants` | Fixed-width projections and framing lemmas for the D09 authored-source subject |
| `fn-store-event-`, `fn-retention-event-` | `store-events` | Tagged article and retention Store events, shared sequence/transaction/generation coordinates and bounded event dispatch codecs |
| `fn-srci-` | `store-retention-codec-invariants`, `tests/acl2/store-retention-codec-invariants-tests` | Canonical retention event item encoding, bounded public decode round trip, and separation from the legacy article codec |
| `fn-nrt-` | `node-retention-transitions` | Node projection after authoritative retention undertaking/release transitions |
| `fn-prin-` | `principal`, `principal-invariants` | Principal ids from (public key, token), key succession chains, keyrings |
| `fn-stmt-` | `statement`, `statement-invariants` | Block-shaped statement header, item-sequence codec, content id, signing, receipt payloads |
| `fn-lace-` | `lace`, `lace-invariants` | Statement sets keyed by content id: merge, canonicity, cross-canonicity, equivocation, causal closure |
| `fn-pol-` | `policy`, `policy-invariants` | Group policy statements, the policy in force, authorization, policy term and receipts |
| `fn-stx-` | `statement-field`, `statement-transit` (designed, no book at this revision) | Statements on the wire: the detached `FN-Statement` field codec and its canonicality, the three-valued verdict a receiving node computes from the received bytes and its own keyring, the lace projected over the store with its carried index, the transit authority gate, the commits a batch carries, and the reader's `:fn-verified` rendering. Designed in [substrate transport](../specs/substrate-transport.md) |
| `fn-stxe-` | `stx-evidence-records` | Bounded Store event kind 2 for an acceptance-recorded statement verdict: common sequence/transaction/generation coordinates, exact Message-ID, three-valued verdict evidence, keyring generation and an opaque algorithm profile tag; unknown profiles are preserved and refused as authority; D09 selects the hybrid profile |
| `fn-th-`, `fn-thlc-` | `topic-history-metadata`, `topic-history-metadata-invariants`, `topic-history-authorship`, `topic-history-admission`, `topic-history-store-events`, `topic-history-prefix`, `topic-history-local-admin`, `topic-history-local-proposals`, `topic-history-local-control`, and their ACL2 tests | Bounded experimental FN-Topic codec, exact-source and historical authorship binding, local installation/anchor/report proposals, carried Store projection, and local FNCT control grammar; combined native qualification remains open |
| `fn-sti-` | `topic-history-prefix-invariants`, `topic-history-store-invariants`, and their ACL2 tests | Proof-side exact topic prefix and crash-image relation for the carried Store topic projection; no served history scan |
| `fn-thr-` | `topic-history-recovery-invariants` and its ACL2 test | Actual Store crash/recover and observed-open topic projection selector facts |
| `fn-hc-` | `hybrid-carrier` | Bounded portable FN-Authorship v1 carrier codec and exact authored-source projection; mutable relay fields are excluded from the signature subject |
| `fn-sob-` | `store-open-bridge`, `tests/acl2/store-open-bridge-tests` | The host's reopen `fn-cpo-open-observed` against the store-only reopen `fn-sn-open-observed`: the exact success condition of each, the shared file kernel, the configuration-journal conjunct `fn-sob-configured-openp`, and the K4 statements restated at the host's call (`<keystone>-of-host-open`) |
| `fn-sonb-` | `store-open-node-bridge`, `tests/acl2/store-open-node-bridge-tests` | The K4 node correspondence at the host's reopen: the configured replay `fn-cpr-replay` of a configuration journal whose every record names txid 0 (`fn-sonb-configured-before-eventsp`) is the fixed-table `fn-replay` under the final configuration, so `fn-snt-relation`, the exact-history node conjunct and the BP receipt theorem hold over `fn-cpo-open-observed` |
| `fn-spj-` | `source-projection-bridge`, `tests/acl2/source-projection-bridge-tests` | The carrier projection `fn-hc-authored-source` against the injection inverse `fn-inj-source-of`: two reserved header lines in front of a parsed source leave the projection unchanged, so for a source with its own Message-ID and Date the projection of the stored record is the projection of the recovered source |
| `fn-pb-` | `poster-bytes`, `poster-bytes-invariants`, `tests/acl2/poster-bytes-tests` | D25: the poster's bytes of an article (its octets without the injected Path, Xref, Injection-Date and Injection-Info fields, named by `fn-hc-`'s constants) and `fn-pb-existing-action`, the host-called duplicate-versus-conflict decision keyed on them |
| `fn-stxk-` | `stx-keyring-records`, `tests/acl2/stx-keyring-records-tests` | Bounded Store event kind 3 for an opaque keyring snapshot and the ordered identity replay context shared with kind-2 verdicts: snapshot generation must precede a verdict that names it, identical repeats are idempotent, conflicting bytes fault, rotation preserves historical verdicts, and unknown profiles never authorize; D09 selects the hybrid profile |
| `fn-stxa-` | `stx-accept-records`, `tests/acl2/stx-accept-records-tests` | Bounded Store event kind 4 for one atomic accepted-article commit: exact legacy fn-r bytes and exact kind-2 verdict bytes share parent sequence/transaction/generation, Message-ID, content identity, keyring generation and profile under one ACL2 binding predicate; standalone kind 2 remains inert evidence |
| `fn-sni-` | `tests/acl2/store-node-index-tests`, `tests/acl2/store-node-composite-index-tests` | Test-only: the principal, keyring, signed article and ordinary/composite Store runs with a nonempty carried index (D21); never in `books/` |
| `fn-toy-`, `fn-t-` | `tests/acl2/*-tests` | Test-only executable realisers attached with `defattach`, and test witnesses; never in `books/`.  `fn-t-anchor-{leaf-digest,sig-verify}` (`tests/acl2/anchor-teeth-tests`) realise the two A-CRYPTO seams of `books/anchor` so the keystones' own subjects evaluate |
| `fn-me-` | `membership-epochs`, `membership-epochs-invariants` | Group membership as epochs: commits over a base epoch, adopted chain and evidence set, the lace-shaped merge with explicit fork evidence, rosters, revocation knowledge, and the four-way admissibility decision for a late message. A policy model: no keys, no ciphertext, no digest |
| `fn-cfg-`, `fn-config-`, `fn-jrec-` | `config`, `config-invariants`, `config-records` | Durable configuration: the typed value (group-table history with created/retired generations and stamps, capacity, quota/policy/listener/peer/limit rows), the typed deltas and their admissibility, the configuration record and its canonical CBOR encoding, the replay fold into `(generation value)`, and the two-kind journal record over which `fn-config-aware-replay` agrees with `fn-replay` on transaction-only histories |
| `fn-ncfg-`, `fn-native-config-` | `native-config` | Bounded inert native operator configuration profile: restricted fn.toml lexical parsing, defaults, bounds, loopback and paired-resource validation, normalized host projections and the native-consumer availability gate |
| `fn-fap-` | `feed-auth-profile` | Bounded inert outbound AUTHINFO credential profile; command-renderable token bounds and canonical physical input grammar |
| `fn-native-auth-` | `native-auth-profile` | Bounded native AUTHINFO credential profile: canonical login-table parsing, exact hex widths, duplicate/legacy-cleartext refusal, construction of the existing `fn-auth-config`, and the explicit protected-transport prerequisite |
| `fn-nop-`, `fn-native-operator-` | `native-operator`, `tests/acl2/native-operator-tests` | Bounded ACL2 native operator grammar and tagged result: config-free help text, normalized init/run/post/status/recover plans, the store-marker names an `init` observation may lstat and the outcome it decides, and the five CLI outcome tags |
| `fn-nctrl-`, `fn-native-control-` | `native-control`, `tests/acl2/native-control-tests` | Sealed bounded AF_UNIX request/reply framing for exact local article submission, status classification, exit projection, and conservative lost-reply outcome |
| `fn-nls-`, `fn-native-live-status-` | `native-live-status`, `tests/acl2/native-live-status-tests`, `host/native-live-status-host` | The operator's status report (status, pins, obligations, peers) rendered once for the offline Store and the running owner, and the FNLS paged exchange that carries it over the control socket |
| `fn-native-admin-` | `native-admin-shape`, `native-admin-peer`, `native-admin`, `tests/acl2/native-admin-tests` | Bounded ACL2 administrative plan: group create/retire and capacity request normalization, the peer table's read side (`peer list`, its query predicate and its ACL2-rendered report), fixed configuration-record naming, and candidate configuration-history replay/open validation before immutable publication |
| `fn-nco-` | `native-config-observation`, `tests/acl2/native-config-observation-tests` | Bounded configuration-history observation: exact record decode, ACL2-derived generation/name binding, generation ordering, and the sole accepted contiguous `1..n` recovery plan |
| `fn-cnode-` | `node-config` | The configured node: a `fn-node` state paired with its `(generation value)` configuration, carrying as recognizer conjuncts that the acceptance group list is the allocation domain (every name ever created) and the retention capacity is the configured one; the lifted article transitions with the served-table admission check, the configuration transition, and the two-kind replay loop the host replays configuration history through |

| `fn-inj-` | `injection-shape`, `injection-path`, `injection`, `injection-invariants` | RFC 5537 §3.5 injection (and D32's supplied Path: the RFC 5536 §3.1.5 grammar, the Path-content offset, the insertion and its inverse): the injecting-agent configuration record, the Gregorian calendar of the 2000-2399 cycle and the RFC 5322 date-time rendering, the generated Message-ID, and the opaque decision record carrying either a refusal reason or the exact injected octets |
| `fn-mbx-` | `mailbox` | RFC 5322 §3.4 mailbox-list as RFC 5536 §§2.1, 3.1.2 restrict it for From: a bounded two-pass recognizer (a lexer to token kinds with comment nesting as a counter, then an automaton over the kinds), no obsolete syntax but `obs-phrase`, US-ASCII only; the keystone that every accepted value names an address |
| `fn-post-`, `fn-nntp-post-` | `nntp-post`, `tests/acl2/nntp-post-tests` | POST (RFC 3977 §6.3.1) composed over the reader dispatcher: the posting session (the reader session plus the awaiting bit), the result record carrying effects and a submission, and the two host entry points `fn-nntp-post-step` and `fn-nntp-post-outcome` |
| `fn-nntp-` | `nntp`, `nntp-invariants`, `nntp-effects` | Reader command dispatcher, session cursor, projection, effects |
| `fn-tcl-` | `tcpcl-records`, `tcpcl-octets`, `tcpcl-session`, `tcpcl-invariants`, `tcpcl-delivery`, `tcpcl-delivery-invariants` | TCPCLv4 convergence layer (RFC 9174): opaque message and session records, exact bounded octet grammar, session machine and C1 to C4 keystones, plus the host-called late delivery plan that releases a held END ACK only after durable acceptance, replaces it with XFER_REFUSE after definitive refusal, and withholds it after uncertainty. |
| `fn-path-` | `path` | RFC 5536 §3.1.6 / RFC 5537 §3.2 Path content as a bounded reader: entries, the `<path-identity>` syntax, the relaying-agent loop test of RFC 5537 §3.6 (excluding the tail-entry and anything after a POSTED diagnostic), the §3.2.1 step-3 diagnostic, and the Path/Date field projections a transit decision reads |
| `fn-pu-` | `path-update`, `path-update-tail` | What a relaying and serving agent changes in an accepted article (RFC 5537 §§3.2.1, 3.6, 3.7): a walk over the raw header lines that prepends the agent's `<path-identity>` and the §3.2.1 diagnostic to Path, removes every Xref with its continuation lines and copies every other octet; the four article-level keystones (nothing else changes, a second pass changes nothing, no Xref remains, every Path names the agent); in `path-update-tail`, the Path contents reader and the fifth (the received Path is kept behind the insertion) |
| `fn-cstr-` | `config-stream` | The two durable kinds as one sequence-ordered stream: the merge of the configuration-record file and the transaction journal by the sequence field each record carries, its losslessness and order keystones, and what a generation serves (a created group is served exactly from its generation, a retired one exactly below its retiring generation) |
| `fn-cpr-` | `config-physical-replay` | Recovery fold over the physical config directory and Store journal: independent dense sequences, config-before-event ordering at a shared transaction id, and admissibility against the reservations at each record's historical position |
| `fn-cpo-` | `config-observed` | Recovery-only observed-image entry for the ordered physical configuration and Store histories |
| `fn-sco-` | `store-checkpoint-open`, `tests/acl2/store-checkpoint-open-tests` | P3 Store checkpoint: capture of a record prefix (every replay fold's accumulator), extension over a suffix, the open from it (`fn-sco-open`), the select decision, the keystone `fn-sn-recover-from-checkpoint-equals-full-recover` |
| `fn-ock-` | `owner-checkpoint-open`, `tests/acl2/owner-checkpoint-open-tests` | The served owner's open from a Store checkpoint (`fn-ock-recover-extended`, keystone `fn-owner-recover-from-checkpoint-equals-full-recover`), its publication (`fn-ock-next-checkpoint`) and the K/2 policy (`fn-ock-publication-duep`) |
| `fn-ks-` | `acceptance-alloc` | Linear duplicate and subset checks through a local hash-set stobj (`fn-keyset`): `fn-ks-distinctp`, `fn-ks-subsetp`, the `:exec` paths of `fn-no-duplicatesp` and `fn-subsetp` (checkpoint-cost) |
| `lrt-` | `tests/acl2/linear-recognizers-tests` | Quadratic reference recognizers the linear `:exec` paths are evaluated against |
| `fn-scc-` | `store-checkpoint-codec` | P3 Store checkpoint bytes: postfix tree program for a stack machine, FNSC segment frames chained by `fn-frame-trailer` |
| `fn-bs-scp-` | `byte-store-state-checkpoint-program`, `tests/acl2/byte-store-state-checkpoint-program-tests` | The Store checkpoint's publish program (stage, write, fsync, rename, root fsync) and its old-or-new crash keystone |
| `fn-cst-` | `config-store-traces` | Phase-aware Store trace relation over the carried physical configuration and Store journal histories |
| `fn-ocl-` | `config-owner-live`, `config-owner-advance-invariants`, `config-owner-publish`, `config-crash-replay` | Historical owner relation across durable configuration completion and actual connection advance, retaining per-connection configuration pins; the host-called completion `fn-ocl-publish` and group-request delta `fn-ocl-request-deltas`; the crash headline over the recovery replay `fn-cpr-replay` |
| `fn-ocari-` | `config-owner-advance-reader-invariants` | Reader wire, recorded-verdict and archive-index pin preservation across actual configured-owner advance |
| `fn-ocfg-` | `owner-config`, `owner-served-invariants`, `owner-numbering`, `owner-config-observe` | The configured owner: a `fn-own` state paired with its live configuration, a per-connection pin table whose domain the recognizer holds equal to the open connections, and one staged configuration record; the `(:reconfigure id deltas)` event through the owner's single pending-transaction slot, the pin writes (open, advance, close) and `LIST ACTIVE` at the pin |
| `fn-scar-` | `served-carried`, `owner-served-carried`, `tests/acl2/owner-served-carried-tests` | The served read with the live node's `fn-node-statep` carried from the owner relation instead of evaluated per event: each definition equal to its reference (`fn-served-*`, `fn-auth-step-pinned`, `fn-own-finish-read`, `fn-ocfg-read-tls-prefix`) under that premise |
| `fn-mxc-` | `msgid-index-concrete`, `tests/acl2/msgid-index-concrete-tests` | The Message-ID trie walked by string index (`fn-mxc-get`, `fn-mxc-put`: `char` at depth i, no character list built); `fn-mxc-lookup`, `-extend`, `-build`, `-refresh` each equal to its `fn-midx-` reference with no hypothesis |
| `fn-pix-` | `peer-offer-indexed`, `tests/acl2/peer-offer-indexed-tests` | The IHAVE/CHECK offer decision with its history test answered by one trie lookup (`fn-mxc-lookup`, equal to `fn-midx-lookup`) in the owner's view trie when the session node holds the list the trie is keyed to; each definition equal to its reference (`fn-peer-history-hasp`, `fn-peer-decide-offer`, `fn-peer-command`, `fn-peer-step-pinned`) under the trie correspondence; and the reader's Message-ID retrieval chain (`fn-pix-msgid-retrieval-indexed` up to `fn-pix-peer-delegate-pinned`) with the lookup by index, each equal to its reference with no hypothesis |
| `fn-pgc-` | `peer-guard-carried`, `tests/acl2/peer-guard-carried-tests` | The peer arm of the served step with no node recognizer in its guard (D24): guard `fn-pgc-peer-sessionp`, the peer session recognizer without its node conjunct; offer decision through `fn-pgc-retain-admissiblep` (guard t); each definition equal to its reference (`fn-retain-admissiblep`, `fn-pix-decide-offer`, `fn-peer-command`, `fn-peer-step`'s transfer branch, `fn-peer-step-pinned`) under `fn-node-statep` of the session's node |
| `fn-oix-` | `owner-offer-indexed` | The owner's view-trie premise (`fn-scar-view-indexedp`) established at `fn-own-start` and kept by every `fn-ocfg-step` event, `fn-ocfg-observe` and the carried read |
| `fn-orec-` | `owner-recover-ocl`, `tests/acl2/owner-recover-ocl-tests` | The owner `fn-owner-recover` installs (`fn-cpr-replay`, `fn-cpo-open-observed`, `fn-own-start`, `fn-own-configure`, `fn-ocfg-make`) satisfies `fn-ocl-relation` and `fn-scar-view-indexedp` when the connection bound is natural and the open answers :ok; the first admin open after it keeps both |
| `fn-ccar-` | `owner-commit-carried`, `tests/acl2/owner-commit-carried-tests` | The owner's commit (`fn-own-finish`) with the history's contiguous sequence numbers (`fn-sf-record-listp` inside `fn-sn-statep`) carried instead of searched: `fn-ccar-seek` finds the completion record at its sequence position and reads its kind and pair through the carried accessors of `store-events-carried`; each copy of the completion gates, the two projection steps, the binding test, `fn-sn-finish`, `fn-own-complete` and `fn-own-finish` equals its reference, the gate is evaluated once per commit, `fn-ccar-ocfg-complete` equals `fn-ocfg-step` of `(:complete)`, and `fn-ccar-ocfg-prepare-identity` (guard-verified) equals `fn-ocfg-step` of `(:store (:prepare-identity e))` |
| `fn-evc-` | `store-events-carried`, `tests/acl2/store-events-carried-tests` | A Store event's kind and fields read by shape once it is recognized: `fn-evc-class-by-shape` and `fn-evc-field-by-shape` are O(1) and equal the recognizers and the `fn-store-event-sequence`/`-txid`/`-generation` dispatchers under `fn-store-event-p` (keystones); the carried accessors `fn-evc-sequence`, `-txid`, `-generation`, `-recordp`, `-retentionp`, `-stxep`, `-stxkp`, `-stxap`, `-consumerp`, `-topicp` are the references in the logic and the shape readings in execution, with guard `fn-store-event-p`; `owner-commit-carried` calls them |
| `fn-pcar-` | `owner-prepare-carried`, `tests/acl2/owner-prepare-carried-tests` | The owner's prepare (`fn-sbud-prepare`) with the history's txid fold replaced by the last record's txid: `fn-pcar-next-lower` equals `fn-sf-next-lower` from 0 with no hypothesis, and each copy of the candidate test, `fn-spc-stage-record`, `fn-spc-prepare`, `fn-opc-owner-prepare`, `fn-opc-prepare` and `fn-sbud-prepare` equals its reference |
| `fn-pcb-` | `peer-carriage-rows`, `peer-carriage`, `tests/acl2/peer-carriage-tests` | A peer boundary's opaque-carriage budget (typed rows and their extension), the carried usage as the projection of the committed carried composites and its owner cache, the budget-gated carried event and its trace bound, and the four refusal classes of a present carrier (PRF-099) |
| `fn-acar-` | `owner-advance-carried`, `tests/acl2/owner-advance-carried-tests` | The re-pin after a durable POST (`fn-own-advance-result`, `fn-own-outcome`) with the rebuilt session tested by `fn-scar-conn-boundedp` at the node the held session carries and the reader session opened with `fn-acar-nntp-projectionp` (`fn-nntp-projectionp` without its whole-archive `fn-statep`); equal to the reference when the connection holds a session and the view archive is an acceptance state, which `fn-ocl-relation` carries |
| `fn-rcon-` | `records-concrete`, `tests/acl2/records-concrete-tests` | The record recognizer over its strings in place (`fn-rcon-record-p`: the octet-domain test is `stringp`, the bounds are `length`, the ASCII walk is `fn-rcon-ascii-from` by index), equal to `fn-record-p` with no hypothesis, and the twins of `fn-store-event-p`/`-sequence`/`-txid`/`-generation`, `fn-sf-record-pair`, `fn-sn-record-bindsp`, `fn-cpe-projection-step` and `fn-th-prefix-step` with the recognizer replaced, each equal to its reference; `owner-commit-carried` and `owner-prepare-carried` call them |
| `fn-icar-` | `owner-intent-carried`, `tests/acl2/owner-intent-carried-tests` | The submission's intent identity (`fn-own-feed-intent-id`) digested once when `fn-owner-take` takes the submission and carried as the pair `fn-icar-carry-of` builds: `fn-icar-intent-id` equals the digest of the submission it is used for under `fn-icar-carryp`, which every carry the take builds satisfies, and the intent (`fn-icar-submission-intent`, the result and the records in one call) and the resolution records equal their references in `owner` |
| `fn-rcon-` | `records-codec-concrete`, `records-concrete`, `records-concrete-owner`, `records-attach-concrete` (no definitions), `tests/acl2/records-concrete-tests` | The record recognizer over its strings in place (`fn-rcon-record-p`: the octet-domain test is `stringp`, the bounds are `length`, the ASCII walk is `fn-rcon-ascii-from` by index), equal to `fn-record-p` with no hypothesis, and the twins of `fn-store-event-p`/`-sequence`/`-txid`/`-generation`, `fn-sf-record-pair`, `fn-sn-record-bindsp`, `fn-cpe-projection-step` and `fn-th-prefix-step` with the recognizer replaced, each equal to its reference; `owner-commit-carried` and `owner-prepare-carried` call them. The dispatchers at write (`fn-rcon-store-event-encode`, `fn-rcon-sbud-pending-sequence`, `fn-rcon-sf-record-dir-result`, `fn-rcon-sn-file-step`, `fn-rcon-sn-io`) and, in `records-concrete-owner`, the owner's transaction observation `fn-rcon-ocfg-io` (equal to `fn-ocfg-step` of `(:store (:io op result))`), each equal to its reference with no hypothesis; the host's store and owner wrappers call them. The codec's encoder `fn-rcon-record-encode-impl` (`records-codec-concrete`), `fn-record-encode-impl` with the concrete recognizer, equal to it with no hypothesis; `records-attach-concrete` attaches it to `fn-record-encode` in the host images |
| `fn-ocmt-` | `owner-commit-ocl` | `fn-cst-relation` across `fn-sn-finish` and `fn-ocl-relation` across the host's POST commit |
| `fn-opc-` | `owner-prepare-correspondence`, `tests/acl2/owner-prepare-correspondence-tests` | Fast shared-owner prepare lifted through the configured owner, its equality to the prior owner event under the maintained owner relation, and the exact pending-record projection the native writer calls |
| `fn-orpr-` | `owner-retention-preparation`, `tests/acl2/owner-retention-preparation-tests` | Exact configured-owner retention prepare projection and the host's staged versus refused Store branches |
| `fn-oag-` | `owner-agent`, `tests/acl2/owner-agent-tests` | The injecting agent a served POST names: `fn-oag-post-config`, the posting configuration the owner installs, whose agent is the replayed `path-identity` policy (the one slot a node's identity has), and the chain from `fn-ocfg-open`'s pin through `fn-ocfg-read-tls-prefix` down to `fn-inj-decide` that makes every injected article's Injection-Info name that agent |
| `fn-olog-` | `owner-log`, `tests/acl2/owner-log-tests` | The native owner's service log lines (served post, control post, accepted connection): ACL2-rendered, outcome word first and equal to the reply's, every field bounded and printable so a line is one line |
| `fn-osb-` | `owner-served-bound`, `tests/acl2/owner-served-bound-tests` | The served article bound the owner installs with its Store profile on every run path (PKT-103) |
| `fn-prov-` | `provenance`, `provenance-codec`, `tests/acl2/provenance-tests` | The typed provenance value the retention ledger's `evidence` slot carries: the four tagged records (`:post`, `:peer-transit`, `:bp-receive`, `:local`), the union recognizer `fn-provp` which accepts every string as the `:legacy` kind (so widening `stringp` to `fn-provp` changes no existing store, journal or theorem), `fn-prov-render` (the exact string each kind's writer produced before the widening) and `fn-prov-describe` (the lossless line `fn inspect` prints); and beside the record codec, the canonical CBOR octets, the wire string that rides in the record's existing `release-evidence` field, and the round trip in both directions |
| `fn-cfg-peer-` | `peer-config` | The peer record (name, path identity, transport, inbound accept-groups and limits, outbound feed-groups/streaming/backoff, auth) as an opaque record, its row codec into the configuration value's peers slot, `fn-cfg-peer-find` and `fn-cfg-peer-names`, and the `:set-peer`/`:remove-peer` delta constructors over config's two kinds |
| `fn-peer-` | `peer-inbound`, `peer-inbound-invariants`, `tests/acl2/peer-inbound-tests` | The inbound transit machine (specs/peering.md §2): the closed decision/reason enumerations, history = articles ∪ bindings, the offer and transfer decisions in RFC 5537 §3.6/§3.7 order, `fn-peer-transfer` into `fn-node-prepare`, the transit submission and session records, the IHAVE/CHECK/TAKETHIS/MODE STREAM step composed over the POST step, the RFC response tables, and keystones K1/K2/K3 |
| `fn-feed-` | (reserved) | The outbound feed machine of specs/peering.md §3 (packet K2, feed lane); registered here so no other cluster takes the prefix |
| `fn-defrecord-` | `defrecord` | `:program`-mode term and symbol plumbing behind the `fn-defrecord` and `fn-defrecord-export` macros; no definitional axiom, so no rule and nothing exported |
| `fn-deftransition-` | `deftransition` | The same, behind `fn-deftransition-closed` and `fn-deftransition` |
| `fn-drt-` | `tests/acl2/defrecord-tests` | The three throwaway records and the one transition that audit what the two macros generate; nothing outside that test book uses them |
| `fn-sn-` | `store-node`, `store-node-invariants`, `store-node-resolution`, `store-sweep`, `store-observed`, `tests/acl2/store-node-index-tests` | Live file/node composition: pending record binding, actual completion, refusal and known-abort resolution, observed opening; and since D21 the two configuration fields beside the node -- the keyring and the served statement index -- with `fn-sn-set-keyring`, the carried agreement `fn-sn-indexedp` and the two served queries `fn-sn-statement-lookup` and `fn-sn-equivocatorp` |
| `fn-frame-` | `frame`, `frame-invariants`, `frame-trailer` | The one durable frame grammar (magic, version, kind, bounded length, payload, 32-octet trailer), its journal field grammar, the constrained trailer function A-CRYPTO, and `fn-frame-trailer`, the one owner of the integrity trailer every host calls |
| `fn-ff-`, `fn-feed-filename-` | `feed-filename` | Bounded ACL2-owned FNFD peer-to-component codec: safe legacy leaf preservation, v1 lowercase-hex component layout, and refusal of invalid peer labels before host pathname construction |
| `fn-own-feed-` | `owner-feed`, `tests/acl2/owner-feed-tests` | The owner's half of the outbound feed (specs/peering.md §3, milestone 3): the feed table keyed by peer name with the peer record and the peer's `fn-feedp` in each entry, the RFC 5537 §3.6 scope decision (outbound wildmat, the Path loop check, the origin peer), the targets one article is offered to, the enqueue on a durable acceptance, the per-peer tick, the restart fence, the RFC 3977 §3.2 reply reader and the FNFD records each transition authorizes |
| `fn-fwi-` | `feed-wire-input`, `tests/acl2/feed-wire-input-tests` | Bounded, one-event-at-a-time NNTP command-line input over `fn-wire`: per-peer retained suffix, 512-octet chunk limit, explicit drain and invalid outcomes; it does not interpret reply codes. |
| `fn-fc-` | `feed-connection`, `feed-connection-invariants`, `tests/acl2/feed-connection-tests` | ACL2-owned outbound NNTP connection phase: exact 200/201 greeting, configured `MODE STREAM` emission, exact 203 streaming acceptance, and the ready/refused/closed transitions that gate `fn-owner-feed-connect` and normal feed reply delivery.  In `feed-connection-invariants` since T9c: the host's event view of one connection (`fn-fc-drive`, `fn-fc-drive-state`), the observation-only protected-channel property `fn-fc-gate-okp` and its stage functions, and `fn-fc-line-bodyp`. |
| `fn-fci-` | `feed-connection-invariants`, `tests/acl2/feed-connection-invariants-tests` | Carried validity of the outbound connection table: initialization, selected-state preservation, exact put/remove lookup, and table preservation, so served reply calls do not rescan all peer buffers. |
| `fn-feed-` | `peer-feed`, `peer-feed-invariants`, `tests/acl2/peer-feed-tests` | The outbound feed machine of specs/peering.md §3: the per-peer feed record (queue of (message-id, offer-state), backoff, in-flight offer, attempt counter), enqueue on a durable local acceptance, selection under the scheduler's contact window, the RFC 3977 §6.3.2 / RFC 4644 response-code map, the FNFD journal record kinds and their frame codec, the replay fold and the restart that resolves an in-flight offer by CHECK/IHAVE |

| `fn-bpaj-` | `bp-native-app` | Native BP request intent, exact evolving-Store binding, and receipt-journal replay join; host wrappers use the same dispatch definitions |
| `fn-bso-` | `byte-store-observation`, `byte-store-observation-scan`, and their ACL2 test books | Visible Store directory comparison across physical/model inode renaming: exact names and file octets plus the hard-link alias partition in root, transactions and staging. Its scanner theorem also requires identical ordered transaction names and a valid model scan. Unreachable inodes are outside this read-only observation. |

Host-only wrappers in `host/*.lisp` use `fn-store-`, `fn-bpreq-`, `fn-bpwf-`,
`fn-bprj-` and similar; they are `:program` mode and outside the proof boundary.
`host/config-host.lisp` marshals under `fn-cfg-host-`. The `fn-store-` tag is shared: `books/store-config` owns the group table under
it and `host/store-host.lisp` marshals under it. A host wrapper never decides
anything a book does not already decide.
