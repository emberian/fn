# Deputy report: store (store-files, store-node, store-observed, checkpoint, index, journal)

HEAD: `dep/store` (this commit), cut from dev a31ed5f, merged dev 8809807 (cache tooling). Conventions:
`docs/proof-style.md`. Board entries: `planning/deputies/BOARD.md` (2026-09-19 store).

## Per book (runner `tools/certify_books.py`, ACL2 8.7, this laptop; evidence `build/acl2/certify-2026*`)

Certified: store-files 2.6 s (certify-20260919T195713Z-91751); store-files-invariants 7.1, store-files-traces 0.85,
store-node 0.52, journal 0.20 and the four store-files test books (certify-20260919T195749Z-92441);
store-node-invariants 129.3, store-node-tests 1.3, index 0.54, index-tests 0.34 (certify-20260919T200822Z-12596);
checkpoint 2.35, checkpoint-tests 0.78 (certify-20260919T201141Z-42589); store-node-traces 5.7, store-node-resolution 1.1,
their three test books and store-node-guards-tests (certify-20260919T201308Z-51469).
Open: `books/store-observed` (failing form: the five accessor `verify-guards` of the open-result record lost their
argument to a hint-stripping script; repaired in source, not recertified; evidence certify-20260919T201506Z-61799),
and with it store-observed-tests, store-observed-traces (shim), store-observed-traces-tests, store-node-teeth-tests.
Not taken: the codecs merge (ca66782) named by the root; see the board NOTE for the exact next steps.

## Before/after (before: persvati `dev-0a16592` manifest `book_wall_seconds`; after: this laptop, so not one machine)

store-files 42.9 to 2.6; store-files-invariants 16.6 to 7.1; store-files-traces 1.4 to 0.85; store-node 0.31 to 0.52;
store-node-invariants 79.4 to 129.3 (regression: the local `fn-snx-core-definitions` theory opens the core
definitions the correspondence proofs need; the cure is core keystones for `fn-node-complete :durable`, below);
store-node-traces 6.5 to 5.7; store-node-resolution 3.6+0.5 to 1.1; checkpoint 1.1 to 2.4; index 0.28 to 0.54;
journal 0.16 to 0.20; store-observed 6.9+0.6 to open. Ledger: regenerated by `tools/ledger.py --write`
(`planning/ledger.md`); `make check` green. Whole-state recognizer calls on the executable path: 24 to 0
(fifteen `fn-sf-*` transitions, `fn-sn-{prepare,completion-enabledp,finish,io,recover}`, the two resolution gates,
`fn-sn-open-observed` twice). Books: 13 to 11 (two folds); test books unchanged in number.

## What changed and why

Records opaque with shape and accessor-of-constructor lemmas and the three forward-chaining shape facts (s1):
kernel state, composed state, open result, checkpoint, index entry. Recognizers carried under `mbe` with the
original total logic bodies (s4): no keystone statement moved. Export theories in every book (s2), proof vocabulary
under eight `deftheory` names. `-is-no-op` facts `:rule-classes nil` (s3, s7). `must-fail` teeth in five test books
are `assert-event` witnesses under `with-guard-checking :none` (s5). store-node includes store-files-invariants
(the kernel preservation keystones are its guard proofs); store-files includes replay directly.
Folds: store-node-resolution-traces into store-node-resolution (deleted; registries, Makefile, runner, SCN-015,
specs repointed); store-observed-traces into store-observed (shim kept for bp). journal: historical, export theory.
Statement changes (none to keystones): `fn-snt-node-reconstruct`/`fn-snt-acceptance-reconstruct` take the core
shape predicates as hypotheses; `fn-snt-state-reconstruction` concludes over `fn-sn-make`; two raw `caddr` typed
facts deleted; `fn-sn-open-observed-result-is-typed` added (`:rule-classes nil`).

## Proposal (exact edit, owner)

1. bp: `bp-receiver-evolving-store-invariants` includes `"store-observed"`; store then deletes the shim. Wrappers
   called from bp-ingress carry `(fn-sn-statep s)`, discharged from `fn-sn-*-preserves-state`.
2. host (host/store-node-host.lisp, reader-host): dispatch on `fn-sn-open-kind` under
   `fn-sn-open-observed-result-is-typed`, and on `fn-replay-result-kind` under `fn-replay-loop-result-is-typed`,
   instead of `fn-sn-open-okp`/`fn-replay-okp`, which each run a whole-state recognizer once per recovery.
3. core: export keystones for `fn-node-complete ... :durable` (article installed, archive pin, binding) so
   `fn-sn-actual-durable-completion-installs-record` stops opening `fn-accept-complete`; delete `replay-invariants.lisp`.
4. nntp: nntp-index uses the `fn-index-entry-*-of-fn-index-entry` lemmas or enables `fn-index-entry` locally;
   `fn-index-query-range` is withdrawn (local enable).
5. store, next wave: `fn-sf-history-recoverablep` re-replays the history per prepare (Theta(n^2)); under
   `fn-snt-relation` the live node is that replay, so the served check becomes the bound-candidate test.
6. K-series (specs/crash-model-v2.md s3.3): `fn-sf-crash-imagep` stays the interface predicate (K2 inhabits it);
   `fn-sf-crash` + `fn-sf-crash-realizes-every-admissible-image` are the K3 lemma; the two `fn-sf-unobserved-*`
   theorems and `fn-sf-crash-outside-namespace-window-keeps-image` become corollaries of K2; the two
   `fn-sf-completed-*-barrier-removes-*` are kernel shadows of K8; `fn-sf-stable-records-prefix-of-crash` and
   `fn-sf-surviving-candidate-is-exact-and-dominated` are the K5/K6 lemmas; `fn-sn-acknowledged-record-survives-
   observed-reopen` and `fn-snrt-acknowledged-record-retained-across-observed-reopen` are the K4 lemmas; checkpoint
   is unchanged (K12 after P8). Ready interfaces: carried `fn-sf-statep`, exported `fn-sf-{frontier-new,record-
   present}-visiblep` (used by `fn-bs-pending-matches-phase`), typed open result for the `fn-bs-scan-store` hookup.
