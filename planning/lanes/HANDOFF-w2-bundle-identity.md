# w2/bundle-identity

Branch `w2/bundle-identity`, worktree
`/Users/ember/dev/fn/build/lanes/w2-bundle-identity`, merged with `dev`
`72279c8` on 2026-09-20. The earlier revision of this file (written at branch
point `9321344`, before the deputies' realignment) is in the history of
`HANDOFF-w2-bundle-identity.md` at the worktree root; it has moved here.

## What the packet asked, and what moved

**Bundle identity, not the dtn7-rs BID, is the key the host stages and
deduplicates on.** The BID is retained as the transport handle for `/download?`
and `/delete?` and nothing else.

| Decision | Where it lives |
| --- | --- |
| the canonical primary-block identity and its octets | `fn-bpp-primary-identity-value`, `fn-bpp-primary-identity` in `books/bp-primary.lisp`, guard-verified |
| the bundle frame split (RFC 9171 §4.1 head, one CBOR item, exact octets to `fn-bpp-decode`) | `fn-bpi-host-primary-octets`, `fn-bpi-host-bundle-block` in `host/bp-ingress-host.lisp` |
| milliseconds, the DTN epoch, and the `fn-clock-observationp` | `fn-bpi-host-observation`; Python hands over raw `time.monotonic_ns()`/`time.time_ns()` and one configured error bound |
| identity + expiry in one answer | `fn-bpi-host-bundle-report` |
| the identity field in the FNBI frame | `books/frame.lisp`: `fn-frame-inbound-prefix`/`-open` carry a `:blob` identity field beside the BID text field, capped by `*fn-frame-max-identity*` (1152) |

Three outcomes stay distinct: `refused-identity:<reason>` (`:not-a-bundle`, a
codec reason, or `:anonymous`), `refused-expired` and `uncertain-expiry` all
leave the bundle staged nowhere and deleted nowhere; `run_bp_ingress.main`
maps them to `EXIT_REFUSED`, `EXIT_REFUSED`, `EXIT_UNCERTAIN`.

## The merge with dev (2026-09-20)

Four conflicts. All resolved towards the deputies' structure with the lane's
semantics re-applied.

- `books/frame.lisp`: codecs split the 1083-line book into `frame-octets`,
  `frame-fields`, `frame-journal` and `frame`, so the lane's whole pre-split
  file was one side of a single 900-line conflict. dev's split is taken; the
  two functions the lane changed survive in the auto-merged tail;
  `*fn-frame-max-identity*` is now defined in `frame.lisp` beside the inbound
  section, and both functions were already in that book's export theory.
  `docs/prefixes.md`'s `fn-frame-` row names the four books.
- `planning/ledger.json`, `planning/ledger.md`: dev's, regenerated by
  `tools/ledger.py --write`.
- `tests/test_bp_receive.py`: dev's derivation of the wrong subject (the label
  is inside the encoded identity now, so a wrong subject is a wrong identity
  rather than a wrong prefix) with the lane's `stage()`, which also registers
  the bundle octets the receiver reads.

## Proof style (docs/proof-style.md), for the surface this lane added

- `books/bp-primary.lisp` ends with an export theory withdrawing the identity
  projection under `fn-bpp-identity-vocabulary`; `bp-primary-invariants` opens
  it locally, and no includer inherits the definitions. The rest of that
  book's export policy (opaque records for the block, endpoint and time
  codecs) is item 4 of the bp deputy's proposal and is left to bp.
- `fn-bpp-primary-identity-ignores-destination-lifetime-and-crc-type-by-definition`
  is `:rule-classes nil`: it unfolds, it is not a proof event.
- `fn-bpp-primary-identity-is-octets` **lost its `(fn-bpp-blockp b)`
  hypothesis**. Every branch of `fn-bpc-enc` returns octets or nil, so
  `fn-bpc-enc-are-octets` is unconditional and no violating value exists;
  proof-style §5 says delete rather than leave unbitten. `specs/bp-primary.md`
  now says the theorem's content is that lemma at the identity value and that
  the lemma, not this restatement, is what to cite for the encoder.
- Teeth are ground witnesses, never a negated `must-fail`. The `(equal e e)`
  assertion that stood for the fragment collision is gone; in its place the
  ground identity *value* of a fragment (five items, none of them a payload
  length) and a separating witness for the total-ADU-length field. New
  violating values: a non-natural creation time for the `fn-bpp-blockp`
  hypothesis of `-value-is-shape`, and a forged source scheme tag for that of
  `-determines-adu-key` (`fn-bpp-eid-value` sends every non-`ipn`, non-`dtn:none`
  endpoint to the value of the `dtn` endpoint with the same scheme-specific
  part, so the forgery has the real identity and a different ADU key; only
  `fn-bpp-eidp` in the hypothesis rules it out).

## Findings that still stand

1. **The primary block does not determine a fragment's bundle identity.** RFC
   9171 §4.3.1 identifies a fragment by *this bundle's* payload length; the
   primary block carries the *total* ADU length. Two fragments of one ADU at
   one offset with different payload lengths share one identity here. Recorded
   in the book comment, in `specs/bp-primary.md`, and as the ground assertion
   described above. Whole bundles and fragments never collide (three-element
   versus five-element encoded array).
2. **Staging used to key on the BID**, so a redelivery under a fresh handle was
   a second staged frame. It is now one; and two bundles with identical
   payloads and different identities are two staged requests, which the old
   code could not express.
3. **The FNBI frame's BID is no longer part of what must agree on
   reconciliation.** Keeping the old whole-frame byte comparison would have
   fenced the journal on exactly the redelivery this packet exists to
   recognise.

## Certification and tests

**Certified on persvati, 2026-09-20.** `python3 tools/farm.py submit persvati
--jobs 12 --remote-root /home/ember/fn-lanes/w2-bundle-identity --closure
books/frame books/frame-invariants tests/acl2/frame-tests books/bp-primary
books/bp-primary-invariants tests/acl2/bp-primary-tests`, run
`run-20260920T043323Z-47ff`, exit 0, evidence
`build/acl2/certify-20260920T043327Z-2543449` (ACL2 8.7, SBCL, 12 jobs, a
co-tenant box at load 7.5). Thirteen roots, all `FN_CERTIFY_SUCCESS`, seconds
per root: `books/cbor` 0.13, `books/frame-octets` 0.40, `tests/acl2/bp-primary-tests`
0.26, `tests/acl2/frame-tests` 0.75, `books/cbor-invariants` 0.81,
`books/wildmat` 1.83, `books/frame-fields` 2.07, `books/bp-primary` 9.14,
`books/frame-invariants` 15.23, `books/frame-journal` 42.10,
`books/bp-primary-cbor` 76.63, `books/frame` 94.11,
`books/bp-primary-invariants` 758.38. **This is the first certification of
anything in this lane**; the branch-point handoff said so and it was true
until now.

The first run (`run-20260920T041641Z-91ed`, evidence
`build/acl2/certify-20260920T041644Z-2380284`) failed two roots, and both
failures were real defects in the lane's own additions rather than merge
damage:

- `fn-frame-inbound-open-of-prefix`, Subgoal 8'. With `fn-frame-field-octets`
  disabled by the theorem's own hint, the identity field's length stayed an
  opaque `(len (fn-frame-field-octets :blob ident))`, so the declared length
  in the header was not a natural the prover could see and
  `fn-frame-header-octets` could not fire. The text field has had a local
  length lemma since before this lane; the blob field now has its own (four
  octets of length prefix where text has two), `local`, so nothing leaves the
  book.
- `fn-bpp-primary-identity-determines-adu-key`: 805 s, 212M prover steps, and
  it still reverted to induction. Opening `fn-bpp-blockp` splits on
  `fn-bpp-eidp`, `fn-bpp-timep`, `fn-bpp-crc-typep`, `nth` and `floor` for
  both blocks at once, and by Subgoal 137.101.102.101.54' the goal was in
  `nth` vocabulary while the two `:use` instances were in accessor
  vocabulary, so they never matched. Rewritten to keep the record closed:
  one local `:rule-classes nil` step takes `(fn-bpp-eidp (fn-bpp-source b))`
  out of `fn-bpp-blockp`, a second takes the three head elements out of the
  encoded array (they are the endpoint value, the creation time and the
  sequence number whether or not the fragment flag is set; a mismatched flag
  makes the arrays different lengths and the hypothesis false), and the
  theorem itself runs with every definition disabled. **The statement is
  unchanged.** `books/bp-primary-invariants` went 805 s failing to 758 s
  certifying.

`python3 -m unittest tests.test_bp_receive tests.test_bp_receive_faults
tests.test_workflow_journal tests.test_bpa_dtn7` ran 57 tests in 425 s against
the certified books, 55 passing and two failing on the lane's own new tests.
**Both are closed on `w5/bp-receive-tests` (2026-09-20) and the four suites
are 57 of 57**, including `Dtn7IdentityDifferentialTests` -- against the mock
BPA, which is the scope caveat in the open list below. What the two were:

- `tests.test_workflow_journal.WorkflowJournalTests.test_a_frame_staged_under_a_different_identity_fences`
  expected `JournalFault: inbound name` and got `JournalFault: workflow
  journal is already owned` (an `flock` `BlockingIOError` underneath): the
  fixture staged the mismatched frame through its own open journal and then
  reopened the root while that handle still held the exclusive lock, so the
  claim was verified by nothing. The recovering owner is a later process and
  holds no lock of its own, so the fixture now closes its handle first. The
  claim is unchanged and reached: the reopen raises `inbound name`, with no
  image, an empty inbox and the lock released.
- `tests.test_bp_receive.ReceiveTests.test_store_commit_before_context_reopen_binds_existing_exact_record`
  called `ingest_bpa_adu` without its required `bundle`, a `TypeError` before
  the seeding ingress ran. It now seeds with a bundle of its own under a
  distinct creation sequence (a distinct identity from the receiver's), and
  the receiver binds the existing exact record with no second article.

The same lane found that `host/checkpoint-host.lisp` still named
`*fn-store-groups*`, deleted with the compiled group table in `4ba5599`: every
`Acl2Store` bridge on `dev` failed in its constructor, so no ACL2-backed
Python test could run. The three sites read `(fn-store-sn-domain state)` now.
Their `*fn-store-capacity*` is still the host constant where
`fn-store-sn-recover` opens the node with the *configured* capacity; that
mismatch is older than either lane and is left to the store-config owner.



## Open, for the next lane

- Run `tests/test_bpa_dtn7.py::Dtn7IdentityDifferentialTests` against the live
  pinned dtn7-rs agent. It needs a Cargo build of the pinned checkout
  (`tests/bp-dtn7/pin.json`), which no lane has had. Until then the BID-format
  agreement is checked only against the mock BPA over bundles the ACL2 encoder
  built: real evidence about the projection, no evidence that fn and `bp7`
  0.10.7 agree on a wire encoding either produced.
- Move `fn-bpi-host-primary-octets` into a book as a modeled bundle frame
  (RFC 9171 §4.1) with a round-trip theorem, so the split is a certified
  decision rather than a `:program`-mode host composition.
- Model the canonical block format (§4.3.2) and the payload block: that is
  what `fn-bpp-bundle-id` needs to become computable and what reassembly needs.
- Carry the decoded primary block, not only the identity, in the FNBI frame,
  so a relay has the source node ID and creation timestamp as values.
- bp owns the rest of `bp-primary`'s export policy (opaque records), and
  `bp-primary-invariants` still exports two accessor-equality rules that the
  ledger lints (`fn-bpp-decode-of-encode`, `fn-bpp-accepted-block-has-valid-crc`).
