You are lane config-consumer-catalog for fn (/Users/ember/dev/fn), wave 5 lane 5 (gpt-6's section 10, third: "then BP and the configuration/consumer paths"; section 2: "applies to consumer projections, capacity accounting, carriage usage, configuration replay"), on OPUS. Launched by the coordinator under D33 AFTER lanes checkpoint-pipeline and catalog-slice merged (you consume the delta and the projection roots). FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it, INCLUDING "Validation by batch" (no farm closure, no image; REPL on persvati; make check-lane; at most ONE targeted native module on hbox; READY FOR BATCH first in your LANEDUMP). Then read planning/design-2026-09-26-consolidation.md sections 1.4, 1.5, 2.1 (the R table: projection roots), 4.4 and 5; planning/evidence/caps-to-profile-2-2026-09-26.md and build/coordinator/LANEDUMP-caps-to-profile-2.md (PRF-186: the carried-invariant folds of the configuration replay; PKT-510: a configuration checkpoint and one replay per request: the operator's offline request was O(N^2) over N requests, 8,156 s for 1,100 at the base, 1,058 s after); planning/evidence/catalog-slice-2026-09-2N.md and checkpoint-pipeline-2026-09-2N.md (the delta grammar, fn-view-apply, the R table's roots and how the pipeline persists them); books/config-observed.lisp, config-owner-live.lisp, config-physical-replay*.lisp (the configuration log's replay and its fold), consumer-position.lisp, consumer-poll-projection.lisp, consumer-event-index.lisp (fn-cpe-projection-step, fn-cpe-projection-replay), owner-config-observe.lisp, the capacity vector books (fn-cvec-*, capacity-vector-2026-09-26.md), the carriage usage (fn-owner-carried-usage), host/native-operator-host.lisp and host/native/operator.lisp (the offline request: where the configuration log is replayed), host/native/consumer-local.lisp and host/owner-host.lisp fn-owner-consumer-local-* (the consumer's poll: ledger row 15), tests/test_native_consumer_profile.py, test_native_consumer_project_bounds.py, test_native_live_reconfiguration.py, test_native_operator_cli.py.
Base: the later of the two merges named above. Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/config-consumer-catalog /Users/ember/dev/fn/build/lanes/config-consumer-catalog dev`. Record: planning/evidence/config-consumer-catalog-2026-09-2N.md. LANEDUMP within the first hour: which projections become delta consumers and which roots the configuration checkpoint carries, BEFORE the first book edit.
USER-VISIBLE RESULT: an offline `operator` request costs the same at 1,100 prior requests as at 10 (PKT-510: the configuration log is not replayed per request: the configuration checkpoint is a projection root the pipeline persists and the open reads); a consumer poll at journal depth 10,000 costs what it costs at 128 (PKT-554's unmeasured row: the consumer projection is a catalog cursor advanced by the delta, never a replay); a live reconfiguration touching many entries (a policy change: the :policy delta) is applied in bounded quanta while serving continues, and every reader sees a consistent version within a command; capacity accounting and carriage usage advance from the committed delta's octets, never from a scan.
THE TASK, in order:
1. THE CONFIGURATION CHECKPOINT AS A ROOT (PKT-510, design 2.1 table R): the configuration fold's paused state is already a root; make the OPERATOR's offline request read it (the open installs the fold from the checkpoint and replays only the configuration records after it), with the theorem that the installed fold equals the replay of the whole log (PRF-186's carried folds are the reference: cite, extend); measure the 1,100-request series before and after (caps-to-profile-2's harness).
2. THE CONSUMER PROJECTION AS A DELTA CONSUMER: fn-cpe-projection-step over the committed row (the delta's :article/:retention/... case), its cursor a catalog column per consumer; the theorem: applying the deltas from the cursor equals fn-cpe-projection-replay over the suffix (the maintained-relation argument the byte-crash chain already makes for consumer replay: mandate section 6, cite the book); the poll (fn-owner-consumer-local-poll) reads the projection from the cursor: measure a poll at depth 128, 1,000 and 10,000 (tests/perf/native_consumer_poll_cost.py extended; the fixture's development profile caps the journal at 128: use a profile that does not, and say which).
3. CAPACITY ACCOUNTING AND CARRIAGE USAGE from the delta: fn-cvec-debt-extend and fn-owner-carried-usage consume the :article delta's octets and charge (hot-path-scans-2's carried folds are the base: the delta is what they fold over; the theorem: the carried figure equals the fold over the history, restated over deltas).
4. THE :policy DELTA, resumable: a reconfiguration whose effect touches rows [from, to) (a group's declaration change, a keyring generation) applied by fn-view-apply-step under a quantum from the profile (D27: exhausting a quantum yields, never truncates), the served version advancing only at the end (readers within a command see one generation); native: a reconfiguration during a POST load, every reader's OVER consistent (the mid-response invariant), the reconfiguration complete within a bounded number of quanta.
5. The deletion map in your record: the per-request replay of the configuration log, the consumer poll's replay over the suffix, the scan behind the capacity figure (each by name).
MAINTAINED RELATION AND ITS ENTRIES: the projection relations (the consumer cursor's, the configuration fold's, the capacity vector's) as conjuncts of R (design 1.5), established at open (the pipeline's R table and the journal suffix) and preserved by fn-view-apply's cases.
WITNESSES: a consumer at cursor 2 of a five-event history polled after three more commits (the projection equals the replay); a policy delta in two quanta with a reader between them; an offline request after 50 configuration records reading the root (the fold equals the replay); a must-fail per hypothesis.
NATIVE MODULES FOR THE BATCH: tests.test_native_consumer_profile, tests.test_native_consumer_project_bounds, tests.test_native_live_reconfiguration, tests.test_native_operator_cli, tests.test_native_consumer_inspect; the ONE you may run yourself: tests.test_native_live_reconfiguration. Fixtures: caps-to-profile-2's 1,100-request store (register /tank/fn/scratch/fixtures/caps-1100 with SHA256SUMS) and a consumer journal of depth 10,000 you build once and register.
ENVELOPE: about 150 tool calls; no farm runs; persvati REPL; one module.
IDS (the deputy assigns at launch): PRF x2 (the consumer projection over deltas; the configuration root's equality), SCN x2, PKT x1.
SEAMS: catalog-slice (the delta grammar and fn-view-apply live in books/catalog-delta.lisp: add your cases there with their {preserved} re-proved, through the deputy if a continuation is live); checkpoint-pipeline (the R table: add the configuration root's columns to F/R in ONE place); caps-to-profile-2 (merged: its PRF-186 folds are the reference); bp-catalog (lane 4) commits the BP rows you consume.
DELETION MAP (design 4.4's row, extended): fn-own-refresh's consumer and capacity comparisons; the per-request configuration replay; the poll's suffix replay.
Do NOT: add a second authority for a configuration decision (ACL2's fold is the one; the root is its cache); cap the consumer journal or the configuration log by a constant; edit planning/ledger.*; push; build an image or submit a closure.
Final message: under 20 lines with commit hashes: the roots and consumers, the theorems and host lines, the before/after figures (the 1,100-request series, the poll at three depths, the reconfiguration's quanta), the READY FOR BATCH list.
