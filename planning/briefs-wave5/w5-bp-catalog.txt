You are lane bp-catalog for fn (/Users/ember/dev/fn), wave 5 lane 4 (gpt-6's section 10, third: "then BP and the configuration/consumer paths"), on OPUS. Launched by the coordinator under D33 AFTER lane catalog-slice merged (you extend its catalog; nothing here starts before it). FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it, INCLUDING "Validation by batch" (no farm closure, no image; REPL on persvati; make check-lane; at most ONE targeted native module on hbox; READY FOR BATCH first in your LANEDUMP). Then read planning/design-2026-09-26-consolidation.md sections 1.1, 1.2, 1.4, 4.1, 4.2 and 5; planning/evidence/catalog-slice-2026-09-2N.md (the slice's record: fn-cat's exports, fn-held-p, the delta grammar, R); books/catalog*.lisp as merged; then the BP books that read a retained payload or the event index: books/bp-receipt.lisp (fn-bpr-event-article, equal to fn-cei-event-article by fn-bpaj-event-article-is-cei), bp-native-app.lisp and bp-native-app-fast.lisp (the reads of fn-record-payload), bp-ingress.lisp, bp-receipt-records.lisp, bp-workflow-records.lisp, bp-release.lisp, bp-outbound.lisp, bp-fnbs-*.lisp (the receiver journal, the family metadata with incremental coverage: gpt-6 section 6's "fragment-family metadata"), bp-node-job-offer.lisp (per-peer ready queues), the host host/native/bp-app.lisp, bp-service.lisp, bp-node.lisp, bp-obligation.lisp (every fnn-core call that hands a payload list), planning/evidence/bp-lifecycle-5-2026-09-26.md (SCN-077's receiver open of 1,311 held rows: 51.9 s, and what bp-crc-exec left), the ledger row 11.
Base: the catalog slice's merge commit. Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/bp-catalog /Users/ember/dev/fn/build/lanes/bp-catalog dev`. Record: planning/evidence/bp-catalog-2026-09-2N.md. LANEDUMP within the first hour: the BP row kinds you add to the catalog and the projections you make delta consumers, BEFORE the first book edit.
USER-VISIBLE RESULT: a BP transfer (a report R and its reply Q across two nodes, SCN-077's family, the M4 exchange) runs with NO octet-list copy of a retained payload on the BP path: the receipt, the workflow release and the outbound bundle read the article's bytes BY HANDLE from the arena through the catalog, the receiver's fragment-family coverage is a catalog projection consuming the committed delta (never rebuilt from the receiver journal at each open), and per-peer ready queues/cursors are explicit catalog cursors; measured: the receiver open of 1,311 held rows (51.9 s; 13 s after bp-crc-exec) and a 1 MiB transfer end to end (PKT-554's unmeasured row) before and after, with allocation per fragment.
THE TASK, in order:
1. INVENTORY, in the LANEDUMP: every BP read of fn-record-payload, fn-article-payload or fn-cei-* (the design's 4.2 row names the books), with its host entry; classify each as "reads bytes" (moves to the handle) or "reads metadata" (moves to a catalog column).
2. THE ROW KINDS: the BP receipt (kind 6?) and obligation/release records the Store already commits are catalog rows like any event: extend fn-cat's row grammar with their metadata columns (a new export only where a lookup needs one: the receipt by obligation identity, the release by receipt); the fragment-family metadata (family id, total, the coverage bitmap or ranges, the received count) as a projection root advanced by a :bp-fragment delta case (design 1.4's grammar extended; fn-view-apply-is-refresh's sibling theorem for the family projection: its application equals the fold over the receiver journal that bp-fnbs-replay defines: cite it as the spec, not restated).
3. THE READS BY HANDLE: fn-bpr-event-article and its callers take the row and the handle; the bundle encoder (fn-bpb-encode over the payload: bp-crc-exec's table-driven CRC-32C stays) reads the bytes from the arena by index into the outbound octet buffer (the pattern of the served ARTICLE's reference effect); the theorem per boundary: the bundle's octets equal the list model's over the materialized record (fn-held-wire).
4. PER-PEER READY QUEUES AND CURSORS: fn-bpnj-step's job offer reads a per-peer cursor cell of the catalog (a column, advanced by the offer's delta), never a scan of the obligations; the theorem: the offered job equals the reference's (fn-bpnj-step over the list) under R.
5. MEASURE (hbox, DTN developer image, matched to bp-lifecycle-5 and the ledger's BP rows): the receiver open of the 1,311-row fixture, a 1 MiB and a 10 MiB transfer end to end (two nodes on loopback, the bp-lifecycle harness), allocation per fragment, before and after.
MAINTAINED RELATION AND ITS ENTRIES: R extended with the BP projections (the family projection equals the journal fold; the cursors equal the offer fold), established at open (fn-cat-load-row per BP row and the receiver journal's replay as ONE fold that writes the projection: the byte-crash chain's maintained-relation argument for consumer replay is the pattern, mandate section 6) and preserved by the :bp-* delta cases.
WITNESSES: a two-fragment family received out of order (coverage after each delta; the reassembly's release); an obligation offered once per contact (PRF-103's witness re-run over the cursor); a receipt looked up by obligation identity; a must-fail per hypothesis.
NATIVE MODULES FOR THE BATCH: tests.test_native_bp_transit_identity, tests.test_native_bp_channel_admission, tests.test_native_bp_app_clock, tests.test_native_consumer_exchange_two_nodes (SCN-077's exchange), tests.test_native_source_corpus_bp; the ONE you may run yourself: tests.test_native_consumer_exchange_two_nodes (--images dtn,dtn-developer). Fixtures: /tank/fn/scratch/fixtures/bp-lifecycle-5-1311 (register with SHA256SUMS from bp-lifecycle-5's scratch).
ENVELOPE: about 150 tool calls; no farm runs (batch validation; the BP books certify on hbox's gate when the batch runs); persvati REPL (or hbox gate for BP books per BRIEF-COMMON) for each changed book and test book; one module.
IDS (the deputy assigns at launch): PRF x2 (the family projection's delta theorem; the bundle bytes by handle), SCN x1, PKT x1.
SEAMS: catalog-slice's fn-cat (extend the row grammar in ONE place, books/catalog-record.lisp, with its {preserved} obligations re-proved: coordinate through the deputy if the slice's continuation is live); bp-crc-exec (merged or running: read its LANEDUMP; the CRC stays theirs); config-consumer-catalog (lane 5) owns the consumer projection: the BP receipt's consumer-visible event is theirs to consume, yours to commit.
DELETION MAP (your record): fn-bpr-event-article's decode-at-put, every fn-record-payload read in the BP books (by name), the receiver journal's rebuild-at-open of the family coverage, the obligation scan per offer; KEPT as spec: bp-fnbs-replay's fold, fn-bpnj-step over the list.
Do NOT: change the bundle format, the CRC, the FNBS journal or the receipt records' wire encoding; add a second copy of a payload for the BP path; unify the FNBS, feed, configuration and Store journals (gpt-6 section 8: not now); edit planning/ledger.*; push; build an image or submit a closure.
Final message: under 20 lines with commit hashes: the row kinds and projections, the theorems and host lines, the before/after figures, the READY FOR BATCH list.
