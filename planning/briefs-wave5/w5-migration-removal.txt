You are lane migration-removal for fn (/Users/ember/dev/fn), wave 5 lane 6 (D34: fresh deploys, no migrations), on OPUS. Launched by the coordinator AFTER ember confirmed D34 (if your LANEDUMP carries a note that she did not, stop and write the state). FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it, INCLUDING "Validation by batch" (no farm closure, no image; REPL on persvati; make check-lane; at most ONE targeted native module on hbox; READY FOR BATCH first in your LANEDUMP). Then read planning/decisions.md D34 and D27; planning/design-2026-09-26-consolidation.md sections 4.7 and 6 (P-D: the export/import default); books/byte-store-frame.lisp (the format-7 tuples *fn-bs-meta-format-7-*, fn-bs-profile-from-format-7, the format-7 translation comment block, fn-bs-meta-format-7-valuesp and every reader that accepts a format-7 frame), store-profile-open.lisp (the format-7 arm at the open, the upgrade request), store-profile-upgrade.lisp (fn-profile-upgrade-verdict, the upgrade relation), byte-store-profile-program.lisp (P-PROFILE, PROFILE_CUTS), store-history-required.lisp (the :migrate arm, fn-hmr-upgrade-verdict), store-compact-verb.lisp (its format-7 preset arm), store-budget-naming.lisp (the format-7 presets by name), store-profile-namespace.lisp (the format-7 reader), native-operator.lisp (the `upgrade-profile`, `rollback-check`, `rollback-snapshot`, `needs-upgrade`, `repair-profile` verbs and their grammar), host/native/io.lisp (fnn-command-upgrade-profile, -upgrade-profile-open, fnn-upgrade-profile-write, fnn-command-rollback-check, -rollback-snapshot, -needs-upgrade, -repair-profile), packaging/upgrade-native.sh, packaging/install-native.sh (the versioned release directories fn-<rev>/ beside each other for rollback), docs/operator.md sections "Upgrade, and what a rollback loses" and the in-place upgrade steps, tests/test_native_profile_upgrade.py, test_native_rollback_history.py, test_native_stamp_migration.py, test_native_newnews_migration.py, test_native_history_required.py, test_native_operator_verbs.py (the verbs' grammar tests), tests/campaign/native_cuts.py PROFILE_CUTS, tools/native_program_check.py's program table, specs/storage.md's profile and upgrade lines (the STO/REQ lines that name an upgrade), planning/requirements.json (the HST/STO rows for upgrade and rollback), planning/evidence/node-hbox-bbf52159-2026-09-25.md (what the live node's deploy did: an in-place upgrade with the store kept).
Base: dev (HEAD at launch). Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/migration-removal /Users/ember/dev/fn/build/lanes/migration-removal dev`. Record: planning/evidence/migration-removal-2026-09-2N.md. LANEDUMP within the first hour: the complete list of what goes (by function, verb, file, test, spec line, requirement row) and the export/import archive's grammar, BEFORE the first edit.
USER-VISIBLE RESULT: the store has ONE format (8) and the code knows no other: a format-7 store is refused at open by name (`open refused reason=store-format`, exit 1) with the sentence "reinstall from the release and import"; there is no `upgrade-profile`, `rollback-check`, `rollback-snapshot`, `needs-upgrade` or `repair-profile` verb, no upgrade relation, no PROFILE program and no versioned release directory (a deploy is: stop, remove, install the release, init or import, start); and `store export DIR` / `store import DIR` carry the data that must survive a reinstall: the exact committed records (the codec seam's bytes, every kind, packs included), the profile, the store identity; a fresh node imports them and serves the same history (the Message-IDs, numbers, verdicts and consumer cursors equal), proved as a replay of the same records.
THE TASK, in order:
1. THE INVENTORY (LANEDUMP): grep for `format-7`, `format 7`, `upgrade-profile`, `upgrade relation`, `rollback`, `needs-upgrade`, `repair-profile`, `migrat` across books/, host/, tools/, packaging/, docs/, specs/, tests/, planning/requirements.json, tests/scenarios/catalog.json; classify each hit: remove, keep (a word that is not a migration: say why), or a spec line to rewrite.
2. REMOVE, in this order, certifying the closure each step (through the batch: REPL-admit, check-lane): (a) the verbs and their host commands and grammar (native-operator.lisp, io.lisp, docs/operator.md, the grammar book regenerated with tools/docs_check.py --write); (b) the upgrade relation and P-PROFILE (store-profile-upgrade.lisp, byte-store-profile-program.lisp, PROFILE_CUTS, the program table in native_program_check.py: five programs, PASS); (c) the format-7 tuples and the translation (byte-store-frame.lisp: the open refuses a format-7 frame by name; store-profile-open, store-history-required's :migrate arm, store-compact-verb's arm, store-budget-naming's presets by name, store-profile-namespace's reader); (d) the rollback verbs and fn-native-operator-history-* (native-operator.lisp; PRF-141's theorems go with their subject: retire the PRF row with its reason); (e) packaging/upgrade-native.sh, the versioned directories in install-native.sh (one libexec/fn/, replaced whole), the rollback sentences in docs/operator.md; (f) the tests: test_native_profile_upgrade, test_native_rollback_history, test_native_stamp_migration, test_native_newnews_migration, the :migrate cases of test_native_history_required, the verb grammar tests' rows: DELETED, not skipped; the requirement rows and SCN rows retired with a reason (the registry keeps ids stable: a retired row says so).
3. EXPORT/IMPORT (design P-D's default): books/store-export.lisp (prefix fn-sxp-): the archive is a directory of the record files' exact bytes in sequence order plus the profile frame and the identity, with a manifest of SHA-256s ACL2 computes (fn-sha256 attached); `store export DIR` (the store lock held, a fresh process) and `store import DIR` (an `init` with the profile then the records replayed through the ordinary publish program, one commit each, so every K0 program and cut is the existing one: NO new program); the theorem: the imported store's history equals the exported store's (fn-sf-records equal, hence every served fact by the existing theorems); the refusals by name: a manifest mismatch, a record out of sequence, a profile the codec cannot represent.
4. NATIVE: a store of 300 articles with a signed composite, a cancel, a retention event and a consumer registration exported, a fresh node imported, the two `status` and `inspect` observations equal and the numbers equal; a format-7 fixture store (the live node's bbf52159 profile kept as a fixture under /tank/fn/scratch/fixtures/format-7-store) refused by name at open.
MAINTAINED RELATION AND ITS ENTRIES: none new; the import establishes the Store's open relations through the ordinary init and publish entries (say which theorem covers the replayed commits: fn-sn-finish-preserves-state, the K0 record program).
WITNESSES: the export/import equality on a ground history of five records of four kinds; the refusals (manifest, sequence, profile); a must-fail per hypothesis of the equality.
NATIVE MODULES FOR THE BATCH: tests.test_native_operator_verbs, tests.test_native_operator_cli, tests.test_native_history_required (its remaining cases), tests.test_native_program_check, tests.test_native_recovery, plus the NEW tests.test_native_store_export; the ONE you may run yourself: tests.test_native_store_export. Fixtures: /tank/fn/scratch/fixtures/format-7-store (register; from the qualification's copies, never /tank/fn/node).
ENVELOPE: about 150 tool calls; no farm runs (the batch certifies the closure: byte-store-frame is high fan-in, so the deputy may run your batch alone; say so in READY FOR BATCH); persvati REPL for every changed book and test book; one module.
IDS (the deputy assigns at launch): PRF x1 (the export/import equality), STO x1 (one format; export/import), HST x1 (the deploy is a reinstall), SCN x2, PKT x1; the retired rows keep their ids with a retirement note.
SEAMS: release-product (lane 7) writes the docs a stranger installs from: hand it the exact `store export/import` grammar in your LANEDUMP; public-node (lane 8) deploys from the release: no upgrade path exists for it; checkpoint-pipeline's schema 3 (its schema-2 refusal is the same rule: one format).
DELETION MAP (design 4.7's first row): every item by name in your record, with the test that covered it and what now covers the property (a fresh install plus import).
Do NOT: keep a reader for format 7 "for one release"; keep a verb that refuses by name where D34 says the verb goes (the OPEN refuses; the verb does not exist); add a migration for the checkpoint file (schema 2 is refused, the journal replays); touch /tank/fn/node or its backups; edit planning/ledger.*; push; build an image or submit a closure.
Final message: under 20 lines with commit hashes: what went (counts by kind), the export/import theorem and host lines, the native result, the READY FOR BATCH list.
