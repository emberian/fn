You are lane adapter-retirement for fn (/Users/ember/dev/fn), wave 5 lane 3 (gpt-6's section 7: "the native interface still contains an internal wire protocol that can be retired"), on OPUS. Launched by the coordinator under D33. FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it, INCLUDING "Validation by batch" (you submit no farm closure and build no image: REPL-admit every changed book and test book on persvati, run the test books there, make check-lane, at most ONE targeted native module on hbox, and end your LANEDUMP with READY FOR BATCH). Then read planning/design-2026-09-26-consolidation.md sections 1.3, 1.4, 4.3, 4.4, 4.6 (your deletion map: the mailboxes by name, the typed results) and 6; planning/review-2026-09-26-gpt6-consolidation.md section 7; host/native/owner.lisp in full (fnn-owner-name-list, fnn-owner-config-names, fnn-owner-feed-flush, fnn-owner-drain-one, fnn-owner-complete-bound-submission, fnn-owner-complete-bp-transit-submission, fnn-owner-attempt, fnn-owner-publish-captured, fnn-owner-serve-client, fnn-owner-handle-chunk, every fnn-owner-*-global and fnn-global reader), host/native/feed-service.lisp and pull-service.lisp (their fnn-global reads), host/native/io.lisp (fnn-core, fnn-core-state, fnn-core-buffer-state, fnn-global, fnn-call), host/owner-host.lisp in full (every f-put-global: fn-owner-install-effects, fn-owner-take, fn-owner-feed-install-feed, fn-owner-sco-*, fn-owner-reconfigure*, fn-owner-log-*, fn-owner-install-profile), books/owner-served-carried.lisp (fn-served-reply-octets, the effects), books/owner-feed-*.lisp (the feed port result fn-own-feed-port-*), books/owner-log.lisp, tests/test_native_owner.py, tests/test_native_friends_feed.py, tests/test_native_peer_pull.py, tools/host_shape_check.py, tools/transcribe_check.py (what they check about host files).
Base: dev (HEAD at launch), or the catalog slice's merge if it has landed (then the token and the delta exist; otherwise your typed results carry today's values and the slice adopts them: say which in your LANEDUMP). Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/adapter-retirement /Users/ember/dev/fn/build/lanes/adapter-retirement dev`. Record: planning/evidence/adapter-retirement-2026-09-2N.md. LANEDUMP within the first hour: the typed result shapes (each a fn-*-p recognizer in books/owner-results.lisp) and the list of every mailbox you retire, BEFORE the first host edit.
USER-VISIBLE RESULT: nothing on the wire changes; what a reviewer sees: no global result mailboxes (every `f-put-global 'fn-owner-*` in host/owner-host.lisp and every `fnn-global` read in host/native/*.lisp gone except the log line's queue), no LF-joined name lists split in raw Lisp (fnn-owner-name-list deleted), no frame fetched by index against a separate peer list (the feed flush consumes ONE FeedPublication value: peer_reference, sealed_frame_plan, completion_token), no completion by history search (the store finish callback carries the token when the slice has landed; before that, it carries the pair fn-owner-pending-sequence returns, typed), no refresh-by-rediscovery on the host's side (the host never compares two ACL2 values: it executes what ACL2 returns); and the measured allocation per POST from the join/split and the mutex-held time per feed flush and per served step, before and after.
THE TASK, in order:
1. books/owner-results.lisp (prefix fn-ores-, register in docs/prefixes.md): the typed results as ACL2 values with recognizers, one per wrapper step, exactly as design 4.6 lists them: FeedPublication {peer_reference, sealed_frame_plan, completion_token}, SubmissionTaken {id, msgid, octets-range (the buffer range, or the octets until ingress-span-2 lands: say which), groups, intent, transitp, peer}, ServedStep {reply-range or reply-octets, closep, starttlsp, submittedp, log-line}, Capture {count, fill, roots, frontier, budget}, ConfigResult {octets, reason}. Each wrapper in host/owner-host.lisp RETURNS its value (`(value result)`) instead of writing globals; the theorem per shape: the value's fields equal the globals' old values under the same owner (a by-definition equation per wrapper, named `-by-definition`; the wrapper is :program mode, so the equation is over the :logic function it calls: name it).
2. host/native/owner.lisp, feed-service.lisp, pull-service.lisp: every fnn-global reader replaced by a field read of the returned value after ONE recognizer check at the boundary (fnn-owner-result, the pattern of fnn-owner-action's keyword check); fnn-owner-name-list and fnn-owner-config-names deleted (ACL2 returns a list of strings: fn-owner-group-codes already takes names as octets; the config names wrapper returns strings); fnn-owner-feed-flush consumes the FeedPublication's sealed_frame_plan (a list of (peer . frame) pairs from ACL2, no index); the log line stays a queue (PKT-508) but is a field of ServedStep, not a global.
3. The mutex discipline (gpt-6 section 7, second paragraph): keep the single mutation owner; separate the bounded semantic step (under the mutex) from the I/O that consumes its plan (outside it where the contract permits: the reply write already is, per exposure-reply-size; the feed append already is); measure the mutex-held time per served step and per feed flush with the ledger's harness (owner CPU under the mutex vs wall) before and after; do not change what runs under the mutex without a named contract line in your record.
4. tools/host_shape_check.py and tools/transcribe_check.py: they must stay green; if a check enumerates the globals, update it to enumerate the typed results (say what the check now proves).
5. The record's deletion map (design 4.6): every mailbox by name, every reader that moved, the two removed paths (the name-list grammar, the by-index fetch), and the measurements.
MAINTAINED RELATION AND ITS ENTRIES: none new; the owner's fn-ocl-relation is untouched. What you owe is the by-definition equations per typed result (the value equals what the globals held) and the native modules unchanged.
WITNESSES: per shape a ground value that passes its recognizer and a malformed one the host refuses by fault (fnn-fault, exit 4: uncertain, refused and accepted stay distinct); the feed flush on a two-peer table (two pairs, appended in the value's order); the served step's log line reaching the queue.
NATIVE MODULES FOR THE BATCH: tests.test_native_owner, tests.test_native_friends_feed, tests.test_native_peer_pull, tests.test_native_operator_verbs (status, config), tests.test_native_log_sink, tests.test_native_checkpoint_auto (the capture's typed value); the ONE you may run yourself: tests.test_native_owner. Fixtures: none new.
ENVELOPE: about 150 tool calls; no farm runs (batch validation); persvati REPL for the results book and its test book; hbox one module.
IDS (the deputy assigns at launch): PRF x1 (the typed results' by-definition equations as events of one target), HST x1 (a host-shape requirement line: no global result mailboxes), SCN x1, PKT x1 (what remains).
SEAMS: catalog-slice (the token and the delta become fields of SubmissionTaken and ServedStep when it lands: leave the two fields nil-able and say so); egress-span (the reply range: read its LANEDUMP; do not touch its writer); checkpoint-pipeline (Capture is what fn-owner-sco-capture returns: coordinate the field names in your LANEDUMP); ingress-span-2 (the octets-range).
DELETION MAP: design 4.6's row, in your record, by name.
Do NOT: change the served bytes, the feed frames or the journal formats; decide anything in raw Lisp that ACL2 decided (the recognizer check is a shape check, never a semantic one); add a second mailbox; hold the mutex longer than today; edit planning/ledger.*; push; build an image or submit a closure.
Final message: under 20 lines with commit hashes: the shapes, the mailboxes retired (count and names), the before/after allocation and mutex figures, the READY FOR BATCH list.
