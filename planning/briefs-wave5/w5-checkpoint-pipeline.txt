You are lane checkpoint-pipeline for fn (/Users/ember/dev/fn), wave 5 lane 1 (gpt-6's order: "first: stable snapshots and bounded checkpoint publication, the operating-lifetime limit"), on FABLE because the checkpoint schema is a format seam and the K0 crash argument passes through it. Launched by the coordinator under D33. FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it; this lane CERTIFIES ITSELF (it changes the checkpoint codec, a high-fan-in seam: the three-run rule applies) and runs at most the native modules named below. Then read planning/design-2026-09-26-consolidation.md sections 0, 2 (all of it: the schema, the pipeline, the theorem, the crash points), 4.5 (your deletion map) and 6; then planning/evidence/checkpoint-capture-stream-2026-09-26.md (the stream entry over fn-octets-pub, the budget, the N = 40,000 result and its "Not done"), rep-wave-d-2 section 3 and rep-wave-d-3 sections 1 to 3 (the buffer writer and reader, PKT-307), build/coordinator/ground-truth-checkpoint-capture.md (the path, trigger to file; read the named functions yourself), books/store-checkpoint-open.lisp (fn-sco-capture, -extend, -finalize, -freeze, -thaw, fn-sn-recover-from-checkpoint-equals-full-recover), store-checkpoint-codec.lisp (the FNSC frame, fn-scc-decode-segments), store-checkpoint-buffer.lisp, store-checkpoint-reader.lisp (fn-sccr-admit-segment, PRF-135), owner-checkpoint-stream.lisp (fn-ock-publication-stream, fn-ock-capture-budget, fn-ockb-file-len), byte-store-state-checkpoint-program.lisp (fn-bs-scp-program, the five cuts), host/native/owner.lisp fnn-owner-publish-captured and fnn-owner-maybe-publish, host/native/io.lisp fnn-state-checkpoint-plan/-load/-write, fnn-command-state-checkpoint, fnn-write-staged-at, fnn-plan-write-all, host/store-node-host.lisp fn-store-sco-publish-plan/-decode/-segment-admit, host/owner-host.lisp fn-owner-sco-capture/-publication-done/-due, tests/campaign/native_cuts.py STATE_CHECKPOINT_CUTS, tools/native_program_check.py, tests/test_native_state_checkpoint.py, tests/test_native_checkpoint_auto.py.
Base: dev (HEAD at launch). Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/checkpoint-pipeline /Users/ember/dev/fn/build/lanes/checkpoint-pipeline dev`. Record: planning/evidence/checkpoint-pipeline-2026-09-2N.md. LANEDUMP within the first hour: the four tables' exact row grammars and the batch step's signature BEFORE the first book edit.
USER-VISIBLE RESULT: a node holding N articles publishes its automatic checkpoint in time proportional to the metadata (seconds at N = 40,000, where today it is 54 s and linear in the payload octets), with the publication buffer holding ONE segment (SEG octets, a profile constant) instead of the whole file (315 MB at 40,000), a file that carries every payload ONCE (about 1.05 file octets per payload octet where today it is 2.1: PKT-307/PKT-314 retired), and reopens from it in the time the arena copy takes; when the file would exceed the profile's checkpoint budget OR the staged file's space plus the maintenance reserve, the owner defers BY NAME before allocating anything (CHECKPOINT deferred reason=exceeds-budget|exceeds-space estimate=E budget=B) and keeps serving; `store checkpoint` (the verb) is the SAME pipeline in a fresh process.
THE TASK, in order:
1. THE SCHEMA (design 2.1): books/store-checkpoint-tables.lisp (prefix fn-sct-, register it in docs/prefixes.md): the four tables P (payload extents: file-local index, offset, length, the bytes), E (one row per committed event: kind, txid, generation, stamp, provenance, charge, Message-ID, groups+numbers, the payload reference as a FILE-LOCAL p, visibility, and, until the catalog slice lands, the record's OTHER fields as today's record minus its payload), R (the projection roots: the configuration fold paused at S with its node's article entries as (sequence . p) references, the identity context, the consumer cursor, the topic prefix state, the scalar totals), F (schema 3, store identity, S, frontier txid, keyring and policy generation, profile generation, the writer's source revision, the segment counts per table, the digest genesis). Each table is its own run of FNSC segments with the SAME frame (header 37 octets, chunk, chained trailer), so fn-sccr-admit-segment and fn-scc-decode-segments's refusals are reused, not restated. THE VALUE THE TABLES MEAN is fn-sco-capture: state and prove `fn-sct-tables-of-capture` (the tables built from a capture) and `fn-sct-capture-of-tables` (the capture rebuilt from the tables) as inverses, with the file-local p translated to the fresh arena's handle by the identity on a fresh arena (say so in the theorem: never assume it for a live arena). The record's payload bytes: today's record holds an octet list; P holds those bytes once; two rows whose payloads are equal lists get ONE p when they are the same object (the composite and its carried record are distinct objects today; say what you dedupe and prove the load restores the same lists).
2. THE PIPELINE (design 2.2): books/owner-checkpoint-pipeline.lisp (prefix fn-ockp-): `fn-ockp-capture` (the O(1) stable root: S, the arena fill or, before the catalog slice, the record count and the roots; NO record list handed to the thread), `fn-ockp-batch` (rows [i, i+B) of E and their P extents into fn-octets-pub, at most SEG octets per segment, returning the frames written and the next i: the resumable step), `fn-ockp-decide` (the resource decision BEFORE any allocation: the estimate from carried scalars, never a walk (`fn-ockb-file-len` is retired: give each row its encoded size as a carried column, advanced at commit, so the estimate is O(1)); against the profile's checkpoint budget (fn-ock-capture-budget, kept), the publication buffer's budget (SEG: a constant of the profile) and the space (the capacity vector's statvfs figure plus fn-smr's reserve)); `fn-ockp-validate` (the reader's admission of the segment just written before the next batch). The host: fnn-owner-publish-captured calls capture/decide/batch in a loop OFF the mutex, writing each batch's frames through fnn-write-staged-at between the created and written cuts (as fnn-plan-write-all does today), then the rename and the root fsync as today; fnn-command-state-checkpoint runs the same loop under the store lock. Backpressure: the deferral names its reason and both numbers; `status` carries it (STO-024's words plus `exceeds-space`); the due path stays blocked (fn-ock-publication-blockedp, kept).
3. THE THEOREM (design 2.3): KEYSTONE `fn-sct-load-of-publish-is-the-capture`: loading the file the pipeline wrote (any batching B, any SEG the profile admits) gives the tables whose value is fn-sco-capture of the captured history; then fn-sn-recover-from-checkpoint-equals-full-recover and fn-sco-open transfer unchanged (cite; do not restate). Keep PRF-133 and PRF-135 as the frame-level theorems, re-targeted to the table codec (say which statements move and why each is the same property). Refusals agree: what the codec refuses the pipeline refuses (the pattern of fn-sccb-plan-refuses-what-the-codec-refuses).
4. THE CRASH POINTS (design 2.4): fn-bs-scp-program UNCHANGED (its :write-all is the host's loop of writes, torn at unit granularity); tools/native_program_check.py PASS over the same six programs; tests/campaign/native_cuts.py's verify_state_checkpoint_cut_map unchanged; ONE new native case in tests/test_native_checkpoint_auto.py: a kill between two batches (a developer selector FN_NATIVE_CHECKPOINT_BATCH_FAULT=k:kill, through fnn-developer-selector) reopens with the OLD checkpoint (the created cut's verdict) and the staged file is swept by fn-bs-recover-stage-cleanup-program.
5. THE READER: books/store-checkpoint-reader.lisp extended to schema 3 (the tables) beside schema 2 for ONE release? NO: D34 (fresh deploys, one format) means schema 3 replaces schema 2; a schema-2 file is refused by name (`reason=checkpoint-schema`) and the journal replays; say so in specs/storage.md and delete the schema-2 writer (fn-sccb-plan's tree encode, fn-sco-freeze/-thaw, fn-sco-index-records, fn-ockb-file-len, fn-ock-publication the list entry): the deletion map's row 4.5 in your record with what you removed, by name.
6. MEASURE (hbox, matched to checkpoint-capture-stream section 5 and rep-wave-d-3 section 5, under systemd-run -p MemoryMax=40G, the launcher's dynamic space UNCHANGED): the load to N = 40,000 x 2 KiB on tmpfs with every CHECKPOINT line (octets, ms), VmHWM; the reopen from the last automatic checkpoint; the N = 10,000 x 32 KiB point (the wave-d fixture copy: file octets per payload octet, `store checkpoint` wall and max RSS, the reopen's wall and VmHWM); the publication buffer's fill at its largest (SEG); the throughput gate's checkpoint phase (record, not compare, under load).
MAINTAINED RELATION AND ITS ENTRIES: the tables' correspondence with the capture (`fn-sct-tables-of-capture`) is established at every publication from the owner's carried state (fn-owner-sco-capture, under the mutex) and checked by the reader's load (`fn-sct-load` refuses by name a file whose tables do not close: rows referencing a p past P's count, a frontier below S); it is a derived structure, never authority: the journal replay stays the reference (STO-011).
WITNESSES (teeth per BRIEF-COMMON's rule): a ground history of two retention events, two configuration records and two articles (one a signed composite) published at B = 1 and SEG = 64 (many batches, many segments), loaded and compared to fn-sco-capture; the refusal at the budget boundary both sides; a row referencing p = count (refused by name); the space deferral with the capacity vector's figure below the estimate; a must-fail per hypothesis of the keystone.
NATIVE MODULES FOR THE BATCH: tests.test_native_state_checkpoint (the verb: five cuts through both entries; extend its source test to the table subjects), tests.test_native_checkpoint_auto (the automatic publication, the deferral, the new batch-kill case), tests.test_native_checkpoint (unchanged). Fixtures: /tank/fn/scratch/fixtures/checkpoint-pipeline-n10k-32k (a copy of /tank/fn/scratch/rep-wave-d/work/n10000-32k/m/store with SHA256SUMS; register it) and the chain fixture the ledger used.
ENVELOPE: about 200 tool calls; three farm runs on persvati (--affected-by the books you change: store-checkpoint-open's closure reaches owner-invariants: count and say); every book under 10 s; hbox under systemd-run only; one 40G unit at a time; never /tank/fn/node.
IDS (the deputy assigns at launch; state in your LANEDUMP which you took): PRF x2 (the keystone; the pipeline's refusal theorem), STO x1 (the schema-3 line after STO-024 in specs/storage.md), SCN x2 (the pipeline; the batch-kill cut), PKT x2 (what remains; a decision packet if the dedupe rule or the space contract needs ember).
SEAMS: the catalog slice (lane 2) re-targets E's loader to fn-cat-load-row and P's to the arena; keep `fn-sct-load` parametric in the row sink (a function of the row and the payload bytes) so that lane changes one function, not the file. egress-span and ingress-span own the served buffer fn-octets; you own fn-octets-pub only. served-path-scale-2's fn-sopc-classified-open owns the open's classification: add your `checkpoint-schema` reason through it.
DELETION MAP (design 4.5): the 7-tuple value, fn-sco-freeze/-thaw, fn-sco-index-records, the tree codec fn-scc-encode/-decode-tree over the whole value, fn-sccb-plan's whole-file encode, fn-ockb-file-len, fn-ock-publication (list): removed by name in your record; fn-sco-capture, fn-sco-open, fn-bs-scp-program, the FNSC frame and fn-sccr-admit-segment: kept.
Do NOT: raise the dynamic space, K, SEG or any timeout to make a measurement pass; cap stored data; keep a schema-2 reader "for now"; hold the mutex through the encode; weaken a theorem; edit planning/ledger.*; push.
Final message: the LANEDUMP summary with commit hashes, under 25 lines: the keystone and its host lines, the file octets per payload octet before/after, the capture and reopen figures before/after with VmHWM, the manifests, the native SHAs, what is not done.
