You are lane public-node for fn (/Users/ember/dev/fn), wave 5 lane 8 (D36: a public node), on OPUS. Launched by the coordinator ONLY AFTER ember answered the certificate question (Let's Encrypt on fn.fg-goose.online through which challenge and which renewal hook, or her own certificate and key: the file paths) and the storage tier question (which pool and dataset the node's store lives on, its quota, and whether it is the 91-percent-full `tank` the ledger measured 446 ms POSTs on), AND after lane release-product's tarball exists with its SHA. If your LANEDUMP carries no answers from the deputy, stop and write the state: this lane does nothing without them. FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it; the mandate's rule that no lane deploys, publishes or messages is LIFTED FOR THIS LANE ONLY for the host fn.fg-goose.online and the peers spwashi and pug, by D36, and for nothing else (the live LAN node /tank/fn/node stays out of bounds; you do not touch it, migrate it or reuse its store: D34, a fresh deploy). Then read planning/decisions.md D36, D34, D35, D23 (carriage), D29 (control scope), D32 (Path); planning/evidence/release-product-2026-09-2N.md (the tarball, its SHA, the stranger's commands), friends-peer-2026-09-26.md and friends-peer-2 (the friends' nodes: their peer names, wildmats, what they run), public-exposure-2026-09-26.md and exposure-reply-size (PRF-161, PRF-182: the limits a stranger-facing port has: the accept backlog, the flood result, the reply size), planning/nntp-gap-inventory-2026-09-26.md (the ten-first list for a public node with friends, which of them landed), docs/install.md and docs/operator.md (as release-product wrote them: you follow them exactly; a step they lack is a finding against them), packaging/fn.service, host/native/tls.lisp (the certificate and key the node loads; what it refuses), books/public-exposure.lisp and public-exposure-reply.lisp (the limits by name), the peer-invite grammar (host/native/peer-invite.lisp, docs).
Base: dev (HEAD at launch), unchanged by this lane except its record. Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/public-node /Users/ember/dev/fn/build/lanes/public-node dev`. Record: planning/evidence/public-node-2026-09-2N.md (a node record in the form of node-hbox-bbf52159-2026-09-25.md: the revision, the tarball SHA, the profile, the store's dataset, the peers, the exposure limits on, the certificate's issuer and expiry, every command run and its log SHA).
USER-VISIBLE RESULT: fn.fg-goose.online answers NNTP over TLS (563) and STARTTLS (119) to the public with the exposure limits on, serves the groups ember declares, is peered with spwashi's and pug's nodes (each an invited peer: invite, accept, a first article each way, the feed journals showing the obligations released), and ember reads it from a real newsreader (tin or pan, over TLS, with her account) and from the human web client; a stranger's flood is refused by name and the node keeps serving the friends (public-exposure's measurement repeated against the public address); `status` and `health` read clean; the node record says what it carries and what it does not.
THE TASK, in order:
1. THE HOST: where fn.fg-goose.online resolves to is ember's answer (hbox behind a port forward, or another machine): install ONLY from the release tarball (release-product's file and SHA), as docs/install.md says, into a fresh directory on the storage tier ember named; init with the profile ember names (the scale profile, or the default: write which and why in the record); the certificate as she answered; the unit enabled; firewall rules for 119 and 563 (and nothing else) written into the record.
2. THE LIMITS: the public-exposure profile on (PRF-161's limits by name: the per-address connection cap, the accept backlog, the greeting refusal, the reply-size bound), verified with public-exposure's harness against the public address from persvati (the flood, the 500-connection case, the reply-size case), the figures in the record beside the LAN ones.
3. THE PEERS: invite spwashi and pug by the peer-invite grammar (the invitations sent by the channel ember names: never by this lane's guess); on acceptance, a first article each way, the feed journals' obligation lines, NEWNEWS pull if their nodes are INN (peer-pull's findings); what INN sends that fn refuses (the inventory's transit gaps) observed and recorded, not fixed here.
4. THE READERS: ember's account (keys-and-accounts: the login binding), tin and pan over TLS (the inventory's client-compatibility rows: what each needed; a missing LIST OVERVIEW.FMT or DATE is a finding for the inventory's ranked list, not this lane's fix), the human web client behind loopback (M6's client) if release-product packaged it.
5. THE RECORD: the node record; a packet per finding (a docs step missing, a client expectation missing, a limit too tight or too loose, the pool's POST latency at the public node).
MAINTAINED RELATION AND ITS ENTRIES: none; this lane proves nothing. What it owes: every command it ran, in order, with log SHAs; the exposure figures; the peering evidence (both directions); the client evidence.
WITNESSES: the flood refused by name while a friend's POST is accepted in the same minute; a peer's article visible to ember's newsreader after her next GROUP (the catalog slice's pinned-view rule if merged; if not, after a reconnect, and say so); the certificate's chain verified by openssl s_client from persvati.
NATIVE MODULES FOR THE BATCH: none (the node is the evidence); the qualification record of the tarball's revision is release-product's.
ENVELOPE: about 100 tool calls; the public host under a user unit with MemoryMax from the profile; never /tank/fn/node; no farm runs; no image builds (the tarball is the image).
IDS (the deputy assigns at launch): PKT x3 (findings), SCN x1 (the public node's scenario row).
SEAMS: release-product (the tarball and the docs: a step they lack is their finding); the NNTP inventory's ranked list (each client gap you hit is a row there: cite, do not fix); friends-peer (the friends' details).
DELETION MAP (design 4.7's third row): the LAN-only node as the only deployment; the record replaces node-hbox-bbf52159 as the current node record ONLY if ember says the LAN node retires (D34: fresh deploys; the LAN node is not migrated).
Do NOT: touch /tank/fn/node or its store; send an invitation, a message or an article to anyone but spwashi and pug and ember's own account; open any port but 119 and 563; reuse the LAN node's keys or certificate; push.
Final message: under 20 lines: the host, the tarball SHA, the profile and dataset, the peers' state, the exposure figures, the client findings, the packets.
