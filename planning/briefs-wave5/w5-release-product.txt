You are lane release-product for fn (/Users/ember/dev/fn), wave 5 lane 7 (D35: the release is the product), on OPUS. Launched by the coordinator AFTER lane release-openbsd reported (its findings on the OpenBSD VM, the runpath check tools/runpath_check.py, the platform argument of packaging/release-tarball.sh) and AFTER lane migration-removal merged (no upgrade verb exists for the docs to name). FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it, INCLUDING "Validation by batch" (no farm closure; you build NO image on your own beyond the tarball's build, which is the product: say in READY FOR BATCH which image the batch must build from the tarball recipe). Then read planning/decisions.md D34 and D35; planning/design-2026-09-26-consolidation.md sections 4.7 and 5; planning/evidence/release-openbsd-2026-09-26.md and build/lanes/release-openbsd/LANEDUMP.md (what ran on the VM, the first blocker if any, the runpath findings); packaging/release-tarball.sh, install-native.sh, fn (the launcher), fn.service, fn-native.service.in, net.fn.plist, fn.toml.example; tools/build_native_host.sh; tools/runbooks/hbox-image-build.sh and hbox-node-deploy.sh (the frozen image directory and the deploy as done today); host/native/build.lisp (save-exec; the image's foreign libraries: crypto.lisp, tls.lisp, signatures.lisp load with :dont-save t and revalidate at start); docs/operator.md in full (what a stranger reads today), docs/README.md; planning/evidence/friends-peer-2026-09-26.md (the Linux tarball a friend installed); planning/evidence/node-hbox-bbf52159-2026-09-25.md (the deploy's steps); tests/test_native_image_profiles.py (production vs developer), tests/test_native_raw_scripts.py.
Base: the later of the two merges named above. Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/release-product /Users/ember/dev/fn/build/lanes/release-product dev`. Record: planning/evidence/release-product-2026-09-2N.md. LANEDUMP within the first hour: the tarball's layout per platform and the stranger's ten commands, BEFORE the first edit.
USER-VISIBLE RESULT: a stranger with a Linux x86-64 or an OpenBSD amd64 machine downloads fn-<rev>-<platform>.tar.gz and SHA256SUMS, verifies, unpacks, and with the documented commands (fewer than ten) has a node: init, start under systemd (Linux) or rc.d (OpenBSD), post and read over TLS from another machine, export and reinstall; the tarball bundles its runtime (the SBCL image, OpenSSL 3.5 and libsodium built for that platform), carries its source revision (`fn --version`), and `tools/runpath_check.py` proves no Python is on the path a deployed node or its operator verbs execute (Python stays for clients and tests); the docs are written for a person who has never seen this repository.
THE TASK, in order:
1. ONE TARBALL PER PLATFORM: packaging/release-tarball.sh PLATFORM REV (linux-x86-64, openbsd-amd64: the recipe release-openbsd found, or its first blocker written into the record as the reason the OpenBSD tarball is not shipped yet) producing fn-<rev>-<platform>.tar.gz with: bin/fn (the launcher, no Python), libexec/fn/ (the production image and its core, the foreign libraries it revalidates at start, source-revision), share/fn/ (fn.toml.example, the unit or rc.d script, docs/), and SHA256SUMS at the top; the build runs on hbox under swarm-build from a `git archive` of REV (never a worktree), certifies the default profile's closure from the cache, acquires and validates the artifact set (tools/proof_artifacts.py), builds the production image, and refuses to package an image whose closure is not green at REV's digest (green_check's line in the record).
2. NO PYTHON IN THE RUNPATH as a check in the tarball build and in `make check`: tools/runpath_check.py over the unpacked tarball (release-openbsd's tool: extend it to the packaged layout), failing on any python, any tool needing it, or any absolute path outside the tarball except the platform's libc; its output in the record.
3. INSTALL AND OPERATE, fresh (D34): packaging/install-native.sh installs into ONE directory (no versioned siblings), refuses an existing store of another format (the open's refusal), and the documented reinstall is stop, remove, install, import, start; the unit files and the rc.d script from the tarball; `fn operator CONFIG init|run|status|export|import` as the whole daily grammar a stranger needs.
4. DOCS FOR A STRANGER: docs/install.md (new; linked from docs/README.md and docs/operator.md's head): what fn is in five lines, the download and verification, the install per platform, the first node (init, TLS certificate: a self-signed one the tarball's tool makes, or the operator's), peering with a friend (the invite grammar peer-invite has), export/import, what to do when the log says a refusal by name (a table of the reasons `status` and the log print, from the books' tables: generate it with the docs_check tooling so it cannot drift); every command in it exists with the grammar the docs give (NNT-032's check: tools/docs_check.py).
5. THE REHEARSAL, on isolated copies (never /tank/fn/node): on hbox, unpack the Linux tarball into /tank/fn/scratch/release-product/, install, init, start under a user unit, post and read over TLS from persvati (the tarball peering friends-peer did), export, remove, reinstall, import, read the same article; on the OpenBSD VM if release-openbsd brought it up; every step's log SHA in the record.
MAINTAINED RELATION AND ITS ENTRIES: none; this lane proves nothing new in ACL2. What it owes: the tarball's image is built from a green closure at REV (green_check cited), the runpath check, the rehearsal's logs.
WITNESSES: the runpath check failing on a planted python symlink in a scratch copy of the tarball and passing on the real one; the install refusing a format-7 store fixture; `fn --version` printing REV.
NATIVE MODULES FOR THE BATCH: tests.test_native_image_profiles, tests.test_native_raw_scripts, tests.test_native_operator_cli, plus the NEW tests.test_release_tarball (the tarball's layout, SHA256SUMS, the runpath check, `fn --version`; it needs the tarball built: the batch builds it from your recipe). Fixtures: the format-7 store fixture migration-removal registered.
ENVELOPE: about 150 tool calls; hbox under swarm-build for the tarball build and systemd-run --user for the rehearsal node; the OpenBSD VM under release-openbsd's 8G unit; no farm runs.
IDS (the deputy assigns at launch): HST x2 (the release layout; no Python in the runpath), SCN x1, PKT x1.
SEAMS: release-openbsd (its VM, its runpath tool, its rc.d script: continue, do not redo); migration-removal (the export/import grammar); public-node (lane 8) deploys THIS tarball: your record names the exact file and its SHA.
DELETION MAP (design 4.7's second row): the developer checkout as a deployment (hbox-node-deploy.sh's steps that copy a worktree: replaced by the tarball), packaging/upgrade-native.sh (gone with migration-removal), any docs sentence that tells the operator to run a Python tool on the node.
Do NOT: package a developer image; ship an image whose closure is not green at REV; write docs that name a repository path; touch /tank/fn/node; push.
Final message: under 20 lines with commit hashes: the tarball(s) and their SHAs, the runpath check's output, the rehearsal's result per platform (or the OpenBSD blocker), the READY FOR BATCH list.
