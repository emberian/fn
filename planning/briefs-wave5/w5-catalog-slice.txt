You are lane catalog-slice for fn (/Users/ember/dev/fn), wave 5 lane 2 (gpt-6's order: "second: one concrete committed catalog and byte owner as a vertical slice"), on FABLE because this is the representation and invariant-ownership seam. Launched by the coordinator under D33 AFTER ember confirmed D33 and PKT-293 (gpt-6's answer: two views, the shared transition) and AFTER lane checkpoint-pipeline merged (you re-target its loader). If your LANEDUMP carries a note from the deputy that ember overruled PKT-293, stop and write the state. This lane ABSORBS the queued w3-rep-wave-d-4 brief (build/coordinator/queue/w3-rep-wave-d-4.txt): read it for gpt-6's refinements; where it and this brief differ, this brief wins, and say so in your record. FIRST read /Users/ember/dev/fn/build/coordinator/BRIEF-COMMON.md completely and follow it; this lane CERTIFIES ITSELF (the record representation is the highest fan-in definition in the tree). Then read planning/design-2026-09-26-consolidation.md in full (sections 1, 3, 4.1 to 4.4, 6: the interface you implement, the prototype's finding that decides its form, your deletion maps); planning/review-2026-09-26-gpt6-answers.md section 1; planning/evidence/rep-wave-d-2026-09-25.md sections 2 to 4 (the arena and its consumers, the program), rep-wave-d-2 sections 1 and 2 (the interface finding: the transition and the served machine read bytes; the two views), rep-wave-d-3 section 3; books/payload-arena.lisp (all of it), proto-catalog.lisp, proto-catalog-fold.lisp, proto-catalog-arena.lisp (the attachable generic and how a consumer is certified once), consumer-event-index.lisp and -store-invariants.lisp (the catalog-to-be: fn-cei-*, fn-ceis-indexedp), owner.lisp (fn-own-refresh, fn-gidx-refresh, fn-own-inflight, fn-own-sub-*, fn-own-step, fn-own-complete), owner-commit-carried.lisp (fn-ccar-seek, fn-ccar-own-finish), store-node.lisp (fn-sn-finish, fn-sn-prepare, fn-sn-completion-record, fn-stx-verdict-of-octets), store-files.lisp (fn-sf-finish, fn-sf-completion), control-visible.lisp (fn-ctl-refresh-*), group-bucket-article.lisp (fn-gidx-entry-number-article), nntp-responses.lisp (fn-nntp-msgid-retrieval-indexed, fn-nov-body-line-count), host/owner-host.lisp (fn-owner-prepare-buffer, fn-owner-existing-action-buffer, fn-owner-finish-submission, fn-owner-take, fn-owner-recover*, fn-owner-sco-capture), host/native/owner.lisp (fnn-owner-attempt, fnn-owner-finish-submission, fnn-owner-drain-one, fnn-owner-handle-chunk), the ledger planning/performance-2026-09-26.md rows 1, 5, 7, 8, 9 and "The next five fix lanes" (over-number-index and post-identity-index are ABSORBED by this slice: their walks are catalog columns; check whether either lane has merged and reuse its index if so).
Base: dev (HEAD at launch). Worktree: `git -C /Users/ember/dev/fn worktree add -b lane/catalog-slice /Users/ember/dev/fn/build/lanes/catalog-slice dev`. Record: planning/evidence/catalog-slice-2026-09-2N.md. LANEDUMP within the first hour: the held record's shape, fn-cat's exports with their :logic bodies, fn-cat$corr's conjuncts, the PreparedCommit's fields and the delta's grammar, BEFORE the first book edit; the deputy reads it.
USER-VISIBLE RESULT (one native vertical slice, measured against the wave-d baseline at N = 10,000 x 2 KiB and x 32 KiB and the ledger's N = 10,000 rows): ordinary and signed POST -> interned held record (payload in the arena, byte facts and context decided once) -> completion BY TOKEN (no history search) -> the committed delta applied to the served view -> pinned ARTICLE and OVER by number and by Message-ID from catalog columns and the handle -> checkpoint (the pipeline's E rows are the catalog's rows, P is the arena) -> reopen establishing the relation; with sixteen bytes per retained octet gone (5.55 GB live at 10,000 x 32 KiB to about 0.6 GB), POST flat in N (10.8 ms owner CPU at 10,000 to about 4.5 ms), OVER 2,000 rows under 1 s (17.7 s today), ARTICLE by number 2.4 ms (7.2 ms), and a long-lived reader's GROUP showing a peer's new article (the NNTP inventory's finding: a connection's pinned view advances between commands, C3 kept within a command).
THE INTERFACE YOU IMPLEMENT is the design's section 1, in the form section 3's prototype settled: `fn-cat` is a `defabsstobj ... :attachable t` whose :logic functions ARE the list model (the held-record list; the specs fn-cei-article-records-for, fn-gidx-build, fn-ctl-refresh-visible restated over sequences as the :logic bodies of the lookups) and whose foundation is the table stobj `fn-cat$c` (arrays by sequence, a hash table for Message-IDs, per-group number arrays, the scalar cells); `fn-cat$corr` carries every cross-field invariant (design 1.1), proved once per export; no book above books/catalog.lisp names fn-cat$c or fn-arena$c. The byte owner is `fn-arena` behind it (design 1.2); the held record is `fn-held-p` (design 1.1: payload a natp handle; byte-derived facts: body start, body line count, control target, source digest, class; context: verdict with its keyring generation); the wire record `fn-record-p` and the codec seam are UNCHANGED (the codec never sees a handle). PreparedCommit and completion by token: design 1.3 (fn-cat-prepare, fn-cat-complete, fn-cat-abandon; a stale token refused by name; recovery interns from durable evidence, never by token). The delta: design 1.4 (fn-delta-p with the :article, :withdraw, :withdraw-pending, :redecide, :policy (resumable, fn-view-apply-step under a quantum) cases; the pinned view is a VERSION: GROUP and LISTGROUP advance a connection's version between commands, C3 within a command; the cancel-after-target case is the witness that a reader at v sees the article until it advances past w).
THE ORDER (each step a certified, measured increment on the branch; the deputy merges at a step boundary if your budget ends): (1) the interface in the LANEDUMP; (2) books/catalog-record.lisp (fn-held-p, fn-held-wire = alpha, intern: `fn-cat-intern wire fn-octets fn-arena` from the buffer at prepare and `fn-cat-intern-list` from a decoded wire record at open, computing the byte facts and the context under the generation in force; the context theorem: between prepare and finish the keyring generation is unchanged (the phase gate fn-sn-completion-enabledp) OR the finish re-decides: choose, state, prove); (3) books/catalog.lisp: fn-cat$c, fn-cat$corr, the exports and their obligations (the {correspondence}/{preserved}/{guard-thm} events as defabsstobj-missing-events prints them; the pattern of payload-arena.lisp and proto-catalog.lisp), the opened logical view; (4) books/catalog-commit.lisp: PreparedCommit, prepare/complete/abandon, the shared transition fn-sn-finish-held over the held record's metadata (parametric in the payload: shares the tuple accessors, never a predicate whose meaning silently changes); (5) books/catalog-delta.lisp: fn-delta-p, fn-view-apply, fn-view-apply-step, KEYSTONE fn-view-apply-is-refresh (design 1.4), the version semantics of the served view; (6) books/catalog-relation.lisp: fn-cat-owner-relation R (design 1.5), established at init, open-from-journal (fn-cat-intern-list per decoded record, fn-arn-store-corr-of-open's shape) and open-from-checkpoint (the pipeline's fn-sct-load re-targeted: E rows to fn-cat-load-row, P to the arena), preserved by every :protect export composed with its transition (the table in 1.5); (7) the served reads: ARTICLE/HEAD/BODY by number and Message-ID and OVER as reference effects from the catalog's columns and the handle (the body line count is a column: no walk), the bytes the host writes from the handle equal fn-served-reply-octets of the reference (egress-span's keystone shape; coordinate with egress-span's writer: build/lanes/egress-span/LANEDUMP.md); (8) the host: fn-owner-prepare-buffer, fn-owner-existing-action-buffer, fn-owner-finish-submission (the token from the prepare through fnn-owner-attempt's callback), fn-owner-recover*, fn-owner-sco-capture (the O(1) root), the served reads; (9) MEASURE on hbox (tools/rep_measure.py and the ledger's perf.py, systemd-run -p MemoryMax=40G, the wave-d fixtures and the ledger's n10k, matched image pairs): live heap after a full GC, allocation per POST, records visited per POST/OVER row/completion, OVER 40 and 2,000, ARTICLE by number, greeting, reopen from journal and from checkpoint with VmHWM, the capture, at N = 1,000 and 10,000 x 2 KiB and x 32 KiB.
MAINTAINED RELATION AND ITS ENTRIES: R = fn-cat-owner-relation (design 1.5): alpha(C, A) = the old logical Store; established at the four entries named there (fresh init, open from journal, open from checkpoint, recovery after a cut), preserved by fn-cat-complete, -withdraw, -redecide (with their {preserved}) and the arena's seals; say in your record which entry establishes it and which transitions preserve it, by theorem name.
WITNESSES: per keystone a reachable non-degenerate witness (a three-article history with a signed composite and a cancel committed after its target; a second connection pinned before the cancel reads the article, advances at GROUP, reads it withdrawn) and a must-fail per hypothesis; the stale-token refusal (a token from an abandoned attempt cannot complete the next); the context theorem's negative (a keyring change between prepare and finish is either impossible by the phase gate, proved, or re-decided, witnessed); the delta's :policy case in two quanta; corrupted-state and mutation witnesses labelled separately.
NATIVE MODULES FOR THE BATCH: tests.test_native_owner, tests.test_native_reader_index (OVER by number), tests.test_native_visibility_join (441 across withdrawal and reclamation: NNT-019 must hold unchanged), tests.test_native_hybrid_author (signed POST), tests.test_native_recovery, tests.test_native_state_checkpoint, tests.test_native_checkpoint_auto, plus one NEW module tests.test_native_catalog_view (the pinned view advancing at GROUP; the cancel-after-target case over two connections). Fixtures: /tank/fn/scratch/fixtures/rep-wave-d-n10k-{2k,32k} (register from /tank/fn/scratch/rep-wave-d/work with SHA256SUMS) and the ledger's n10k.
ENVELOPE: about 250 tool calls (this is the largest lane of the wave: commit at every step); four farm runs on persvati (records-shape is NOT changed: the wire record stays; the closure is your new books plus the host-facing carried books you retire; count and say); every book under 10 s; hbox under systemd-run only; never /tank/fn/node.
IDS (the deputy assigns at launch): PRF x4 (fn-cat$corr's establishment at open; fn-view-apply-is-refresh; the completion-by-token theorem; the served bytes' equation), REP x1 (REP-011 becomes implemented: cite; a new REP for the catalog if the registry wants one), STO x1 (the catalog as the served store's executable), NNT x1 (the pinned view advances at GROUP/LISTGROUP: a REQ line in specs/nntp.md under the reader's view), SCN x3, PKT x3.
SEAMS: checkpoint-pipeline (merged before you; its fn-sct-load is parametric in the row sink: you supply fn-cat-load-row and the arena); egress-span (the reply as a buffer range: its writer drains what you hand it as (:write-payload h kind); do not touch its writer); ingress-span-2 (the wire span: the prepare interns from the buffer range it seals; do not touch fnn-owner-attempt's fill); adapter-retirement (lane 3) takes the token and the delta as typed results: leave the wrapper signatures where the design 4.6 table says and write the exact hunk in your LANEDUMP if you must touch a mailbox; over-number-index and post-identity-index (if running): their LANEDUMPs say what they built; the catalog's number arrays and Message-ID table SUPERSEDE them: reuse, do not duplicate.
DELETION MAP (design 4.1 to 4.4, your record must name each by name): fn-sn-event-index and fn-ceis-* (31 theorems), fn-own-view-index/-group-index/-withdrawals/-withdrawn/-raw/-verdicts, the Store tuple's fields 6, 8, 9; fn-retain-known-id-scanp, fn-find-article, fn-acceptedp on POST; fn-gidx-find-number-entry, fn-nntp-index-entry-available per row; fn-served-make-conn-group-indexed; fn-ccar-seek, fn-sn-completion-record on the served path and the fn-ccar-*/fn-pcar-* twins that only relocated one lookup; fn-own-refresh and fn-gidx-refresh, fn-midx-refresh, fn-ctl-refresh-*; fn-own-conn-archive as a copy; the octet-list payload in the held record, the acceptance state's article and pending payloads, fn-nov-body-line-count's walk, fnn-octet-list per payload. KEPT as spec: fn-sf-records, fn-record-p and the seam, fn-cei-article-records-for, fn-cei-build, fn-gidx-build, fn-ctl-refresh-visible/-withdrawals, fn-own-refresh (the delta's meaning), fn-sn-finish over wire records, fn-sn-find-record.
Do NOT: replace fn-record-payloadp globally (rep-wave-d-2 section 1: the codec's domain stays the wire record); keep a list payload on the hot path "for now"; thread fn-arena through fn-sn-finish, the replay or the served machine (the transition is parametric in the payload); add a second authority for identity, charges or visibility; move an existing theorem's statement (state new ones over the new subjects and equate); count a proof-search refusal as teeth; quote the arena's amortized doubling as a latency bound; claim a number not measured under the baseline's conditions; edit planning/ledger.*; push.
Final message: the LANEDUMP summary with commit hashes, under 30 lines: the step reached, R and its entries by theorem name with the host lines, the manifests, the before/after rows (live heap, POST, OVER, ARTICLE, reopen, capture), the native SHAs, what is not done.
