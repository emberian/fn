# v0 scoreboard

One row per property of [plan §2.1](plan-2026-09-22-trajectory.md). A row is
DONE when the theorem is over the function the host calls, its teeth book has
a reachable witness and one must-fail per hypothesis, and the property was
observed on the deployed image; otherwise the row names its obstruction. Lanes
update their cells when they land; the audit records
(`planning/v0-audit-*.md`) carry the evidence behind each cell. The "observed
on image" cell holds one sentence for the latest image and is replaced, not
appended to: earlier observations stay in their immutable records under
[`evidence/`](evidence/), and the computed coordinates (implemented, proved,
qualified, deployed) are in [the current view](current.md).

| P | theorem | host-called subject | teeth | observed on image | obstruction |
| --- | --- | --- | --- | --- | --- |
| P1 protected channel | `fn-auth-gated-command-is-refused-and-not-performed` (nntp-auth.lisp:1613) over `fn-auth-step`; MISSING the 480/483 statement at the served level | host calls `fn-auth-step-pinned` (owner-host.lisp:1240); the bridge (nntp-auth-invariants.lisp:232) is never applied to the gate | nntp-auth-teeth-tests: 49 must-fail, one per hypothesis | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): matrix AUTH-GATED-A/B 480 before login and every TLS/AUTHINFO transit row as on 4eca4148, auth 4/4, starttls 2/2, auth_admin_fidelity 3/3, protected_peering 5/5, tls_transport 2/2, relocation 2/2, and the web reader logged in over STARTTLS with `LIST COUNTS` matching its page. | none named; [audit](v0-audit-p1-p6.md) |
| P2 240 after consumed completion | `fn-own-240-follows-consumed-completion` (owner-served-invariants.lisp, subject `fn-own-finish`); `fn-own-consumed-completion-is-240-or-uncertain` (owner-invariants.lisp: after a consumed completion no word is a refusal); `fn-post-outcome-store-refusal-kinds-are-distinct`, `-is-not-uncertain` (nntp-post.lisp); PRF-070 | host calls `fn-own-finish` for the article completion (owner-host.lisp `fn-owner-finish-submission`, callback of native `fnn-owner-attempt`) and `fn-own-outcome` (`fn-owner-outcome`); refusal kind relayed from the ACL2 step that refused | owner-tests: W2 witness + 4 must-fail; nntp-post-tests: a line per kind + 5 must-fail; owner-log-tests updated | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): cut campaign 50/50 and probe 40/40 with every present repost answered `already stored here`, marker cuts 20/20 through both entries, and the production kill run 0 torn, 0 lost-240, 0 reused over 206 POSTs. | wording of refusal lines is ember's (duplicate vs conflict varies with the repost's Injection-Date second); uncertainty raised as a condition outside the Store word still closes without a line; retention/identity/config and `store post` finish words still host phase comparisons; capacity compare in host; EIO rows on developer image only |
| P3 reading resumes | `fn-own-pinned-view-survives-other-post` (owner-served-invariants.lisp): after any writer events (`:take`, `:begin`, `:store`, `:complete`) through `fn-ocfg-step` and `fn-own-outcome` for another connection, a reader connection's served chunk answers with the same effects and consumed prefix (peer connections read the live node by design: scope, not a gap); local numbers never reused over the same calls (books/owner-numbering.lisp, PRF-002): `fn-own-served-watermarks-never-decrease` (every group's next local number in the committed view is at least what it was) and `fn-own-served-local-number-is-never-reassigned` (a (group . number) naming an article keeps its Message-ID), both under `fn-own-relation`, citing `fn-state-has-fresh-local-numbers`; NEWNEWS PRF-052 certified over the acceptance stamp | owner-host.lisp:1240 `fn-ocfg-read-tls-prefix` itself; writer events through `fn-ocfg-step` (owner-host.lisp:209; `fn-opc-prepare` equals the `:store :prepare` event under the relation); outcome through `fn-ocfg-with-owner` (`fn-owner-replace-core`); the numbering theorems speak of the committed view every reader pins on open/advance | owner-served-invariants-tests: witness (reader 3 at v2 across connection 4's 240, view moves to v3), 3 must-fail (poster repinned; `(:advance 3)` event; peer connection IHAVE 335 then 435); owner-numbering-tests: witness (two POSTs in fn.letters across a completion, numbers 3 and 4, watermark 3 to 5, advanced reader GROUP 211 4 1 4 and STAT 3/4), 3 must-fail (relation on each keystone: node archive wound back, 240 answered, fn.letters 1 given again, watermark 3 to 2; holder hypothesis); writer-events hypothesis has no tooth (no violating value found, redundancy not proved); nntp-newnews-tests 12 must-fail | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): reader_index 5/5, served_differential 7/7, the matrix reader rows unchanged from 4eca4148, and the web reader's unread counts equal to the node's `LIST COUNTS` before and after a mark. | none named |
| P4 one owner decides duplicate vs conflict | D25 as the injection inverse (2026-09-24, lane d25-injection-inverse): `fn-pb-a-resend-at-any-clock-is-answered-already-stored`, `fn-pb-a-changed-source-is-answered-conflict`, `fn-pb-an-existing-action-writes-nothing` over `fn-own-outcome` (poster-bytes-invariants.lisp:318,350,384), on `fn-inj-source-of-inverts-the-injection` (injection-invariants.lisp:811): the subject is the poster's source, recovered exactly from injection recipe v2 at every clock reading, Date present or absent; v1 records read only where unambiguous (`fn-inj-source-of-a-v1-record`, :741), otherwise compared as octets. RFC 5537 §3.5 item 11 one theorem per case (:867,881,912). Refines the byte-identity `fn-sn-existing-action` (:227) | `fn-pb-existing-action` (poster-bytes.lisp:80) at owner-host.lisp:364,1328 and store-node-host.lisp:497,624; its word goes through native `fnn-owner-attempt` to `fn-own-outcome` (`fn-owner-outcome`) | poster-bytes-tests: real Store and owner witnesses (Date-less resend: old decision `:conflict`, new `:duplicate`; changed byte, changed/removed Date, a poster Date equal to fn's generated one, another agent, v1 records, new Message-ID), 13 must-fail; injection-tests: the four inverse cases and item 11 on the INN lab's article, 4 new must-fail | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): POST-DUPLICATE-A/B answer `already stored here`, the D25 review matrix passes 21/21 and the probe's 18 present reposts are duplicates, but one payload through `store post` then `operator post` (either order) is now a conflict (finding F3, the test expects a duplicate). | a full two-node matrix and the operator campaign on an image with this change are owed; transit compares octets again (a relayed copy with another Path is a conflict), and a relay projection belongs to the relayed-workflow outcome; resends straddling the upgrade of Date-less v1 records answer conflict |
| P5 a fault costs one connection | `fn-ocfg-open-at-the-bound-refuses` (at `max-conns` the open answers nothing and returns the configured owner unchanged) and `fn-ocfg-fault-keeps-every-other-connection` (owner-served-invariants.lisp) with `fn-ocfg-fault-is-own-fault`; `fn-own-fault-keeps-every-other-connection` (owner-fault.lisp:138), K4 (owner-invariants.lisp:1836) | owner-host.lisp:1218 `fn-ocfg-open` and :1273 `fn-ocfg-fault`, both theorems over the called wrapper; natively only a connection-local fault continues (host/native/owner.lisp:629-648) | owner-served-invariants-tests: open witness (2 of 2 open), 2 must-fail (below the bound; identifier counter wound onto an open connection); fault witness and 1 must-fail (the faulted connection); owner-fault-tests: a reachable witness for each of the seven PRF-040 owner-fault events and 4 must-fail (other = id; submission of another connection; submission of the faulted connection; a reader faults while connection 4 holds the transaction); the `(fn-own-inflight o)` hypothesis of `-clears-the-faulted-submission` and `-keeps-another-connections-submission` is redundant, proved by two test theorems (kept in the statements); PRF-040 now lists `fn-own-fault-reply-is-not-a-post-outcome` | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): owner 18/18 and control 14/14, POST-CONCURRENT accepted in the matrix, and every developer EIO, control-fault and test-stop campaign row ends client 3, recover 0 with the resubmission a duplicate. | P5 overclaims: a fault inside a shared owner action stops the service with exit 4 (deliberate, AGENTS.md recovery rule); ember: restate P5? |
| P6 live reconfiguration | `fn-ocl-no-reader-observes-a-half-change` (config-owner-publish.lisp, no hypothesis) over staging `fn-ocfg-step` then `fn-ocl-publish`; `fn-ocl-publish-installs-the-whole-staged-record` (published config = `fn-cfg-apply-record` of the whole record, under `fn-ocl-config-historyp`); request: `fn-ocl-request-refused-over-a-reader-in-the-group`; headline 2 `fn-ocl-crash-at-any-instant-recovers-the-live-generation` (config-crash-replay.lisp, hypothesis `fn-ocl-config-historyp`): before the durable write the journals replay `:ok` to the live configuration, after it a replay of the appended journal is `:fault` or exactly the whole record applied at its generation, after `:durable` the installed journals replay `:ok` to the published configuration; equation `fn-ocl-cpr-replay-ok-is-config-replay-ok` (cpr `:ok` implies config-only `:ok`, same configuration; converse false) | staging owner-host.lisp:213 (`fn-owner-step`); publication owner-host.lisp:271 calls `fn-ocl-publish` and installs its owner unconditionally; delta owner-host.lisp:247 calls `fn-ocl-request-deltas` (host twin `fn-owner-config-deltas` deleted); model equation `fn-ocl-publish-agrees-with-ocfg-complete-on-readers`; recovery owner-host.lisp:173 `(fn-cpr-replay config-records records)` in `fn-owner-recover` (native owner.lisp:467), the headline's subject; staging and close through `fn-ocfg-step`, publication `fn-ocl-publish` | config-owner-publish-tests: reachable witness (reader mid-`GROUP`, create staged by a second connection, admin close, :durable, same reply to the rest of the command, new connection serves the group) and 7 must-fail: one per hypothesis (6) plus the false owner conjunct; config-crash-replay-tests: reachable witness (2 configuration records, 2 Store events, create staged, admin closed, `:durable`) cut at four instants and replayed by `fn-cpr-replay` (generation 2, 2, 3, 3), a skipped-generation record recovers `:fault`; 3 must-fail (history hypothesis, the called `:ok`, the false converse on a descending-txid journal) | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): CFG-LIVE accepted and CFG-LIVE-REFUSE refused in the matrix, live_reconfiguration 11/11 and operator_verbs 19/19 including the D23 peer-list rows. | native image run owed (test_native_live_reconfiguration on an image carrying `fn-ocl-publish`) |
| P7 two nodes exchange both ways | K2 in/out, K3, K5 (port form `fn-feed-port-replay-is-live-modulo-inflight`), PRF-058 octets exist; MISSING loop freedom over the host's filtered `fn-own-submission-targets` and the Path-tail clause | inbound, octets, K5, TLS over host-called functions; outbound loop over a filtered callee; `fn-own-feed-accept` uncalled | PRF-058/047/051/K5 have teeth; PRF-042 loop and history theorems 0 must-fail | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): all TRANSIT-*-AB/BA rows as on 4eca4148 and peering 5/5; the INN lab, rerun on the same image after its `operator-post-feeds-inn` assertion was corrected to RFC 5537 §3.5 item 11 (no Injection-Date on an article that supplied Date and Message-ID), holds 33 of 33 ([post-d25b](evidence/post-d25b-tests-2026-09-25.md)). | none named; [audit](v0-audit-p7-p9.md) **D23 (lane/d23-nntp-relay, [evidence](evidence/d23-nntp-relay-2026-09-24.md)):** an unenrolled relay whose boundary lists the author (`carries-principal` rows) stores and relays a signed article with a `carried` verdict; unlisted is still 439 `local-enrollment`. Proved over `fn-pa-current-plan`/`fn-pa-carried-event`/replay; the three-node native chain is written but NOT RUN (no hbox image of this branch). |
| P8 signature verdict visible | PRF-026: `fn-sn-finish-of-a-kind-4-acceptance-records-its-verdict`, `fn-own-reader-opened-after-completion-pins-the-finished-verdicts`, `fn-own-read-hdr-fn-verified-is-the-pinned-verdict` (p8-verdict); signed served POST: `fn-osp-signed-post-finish-records-its-verdict`, `fn-osp-reader-after-signed-post-reports-its-verdict`, `fn-osp-served-refusal-renders-its-reason` (books/owner-signed-post, [p8-signed-post](evidence/p8-signed-post-2026-09-24.md)); PRF-048 injectivity and indexedp exist. Premise not proved: the published completion record is the event ACL2 built; the `:verified` token is a witness fact (no fn-stxe codec round trip) | reader: `fn-own-read` (owner.lisp chunk via `fn-ocfg-read-tls-prefix`); writer: `fn-own-step (:complete)` (owner-host.lisp `fn-owner-finish`) and `fn-own-outcome` (`fn-owner-outcome`), with the served POST arm (host/native/owner.lisp:1173) and bound submission (:1247) calling `fnn-owner-attempt-served` (:884), the transit attempt plus ACL2's `fn-pa-served-word` | owner-verdict-read-tests: six must-fails, four reader premises without; owner-signed-post-tests: reachable served-POST trace (POST, take, plan, kind-4, complete, 240, HDR verified) and ten must-fails | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): hybrid_author 8/8 with `FN_D23_VERIFY=1`, fn_verify 17/17, and the web reader showing node verdict `absent` for an unsigned article. | no non-fn verifier; library trust not a named assumption; `fn-sig-verify` unattached (ember); an unenrolled signer's POST is refused `local-enrollment`, not stored unsigned (decision recorded, pending ember) **D23:** `HDR :fn-verified` gains `carried HEX` (never `verified`, `fn-osp-carried-record-reads-carried`, owner-level witness in owner-signed-post-tests); `fn_verify.py` exits 3 on it (fake-node test). Served POST keeps D02 (`fn-pa-current-plan-without-carried-list-never-carries`). |
| P9 keep until release, refuse unaffordable | `fn-retain-admit-refuses-unaffordable-obligation` exists; MISSING the same over `fn-node-prepare`/`fn-sn-prepare`, and never-removed-without-release over `fn-sn-finish`; not in any PRF row | `fn-retain-admit`'s refusal branch unreachable from the host (`fn-node-prepare` refuses first, node.lisp:185) | reachable witness, 0 must-fail | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): CAP-REFUSE-A/B refused and CAP-SET accepted in the matrix, the kill run's stores closed under the 128 budget, and a copied 4eca4148 store (plain and packed) gains its history marker at its first commit and is refused `history-short-of-marker` when its newest transaction file is lost. | none named |
| P10 every cut is a model crash point | K1-K4, K5 (`fn-bs-stable-prefix-retained-by-byte-crash`), K8, K7 proved; K6 partial; K0 at all 25 cut coordinates: 18 POST cuts from any related state (byte-store-k0, -k0-staging), the 7 recovery cuts from every crash image of any related state (byte-store-k0-recovery: `fn-bs-crash-image-relates-to-recovery-entry-kernel`, `fn-bs-recover-program-keeps-relation-at-every-cut`, `fn-bs-host-recovery-sweep-starts-related`), every injected EIO arm and the link error arm (`fn-bs-k0-record-link-error-arm-relation`); general per-step K0 `fn-bs-program-step-preserves-relation` ABSENT | byte-model programs, tied to the native host only by `tools/native_program_check.py`; the recovery entry kernel is the host-called `fn-cpo-open-observed` output (`fn-bs-host-reopened-kernel-is-the-recovered-kernel`; io.lisp:713 called at :1478, store-node-host.lisp:156), not `fn-sn-open-observed` which K4 is stated over; 4 PRF-041 reach orphans | K5 3, K8 6, K6 63 must-fails; K0 recovery 4 witnesses (2 POST cuts x link/rename kept or lost), 9 must-fails | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): every cut passes at runtime (25 cuts and 10 compaction cuts through both entries, 5 marker cuts ×2 ×2) and production refuses 30/30 selectors, but the ACL2 differential and served crash model fail at `finish-consumed` and `finish-durable` because the byte model has no committed-history marker (finding F1, class a), so P10 is not claimed on this image. | general per-step K0; recovery inside a live process (admin.lisp:107 re-runs `fnn-recover` on a non-crash store) has no entry theorem; no way to kill the production image at a cut; 6 checkpoint cuts and ~12 BP/TCPCL/owner kill points without an `fn-bs-*` coordinate; T16 has ext4/tmpfs only; [audit](v0-audit-p10-p11.md) |
| P11 bundles across an outage | D23 relay trust as four predicates (PRF-075): `fn-bpaj-carried-source-decision` is host-called through `fn-bpah-request-trustedp` and the transit plan; a receipt releases only through `fn-bpah-receipt-release-record` (books/bp-release-authority, host-called by `fnn-bpnode-receipt-result`): the delivering neighbour's own EID or a `bp-boundary-releases-for` row for the issuer, and the exact held obligation; carriage alone releases nothing (`fn-bpah-carrier-without-release-row-releases-nothing`); a third way, a receipt signed by the issuer's enrolled hybrid keys (frame books/bp-signed-receipt, preimage `fn-bpsr-signed-preimage`), releases through any carrier (`fn-bpah-signed-receipt-releases-through-any-carrier`), a failing signature releases nothing (`fn-bpah-unverified-signed-receipt-releases-nothing`) and `require-signed-receipts` on a carrier refuses bare receipts (`fn-bpah-required-signature-refuses-bare-receipts`); the listed-relay profile stays a stated delegation (`fn-bpah-trusted-bare-receipt-releases`); crypto primitives under A-CRYPTO, see [signed receipts](evidence/signed-receipts-2026-09-25.md); release rows change no request (`fn-bpah-request-trust-ignores-release-rows`); `fn-bpnp-step` exists and is host-called; 5 of 29 spec §5 theorems exist; T1-T3, T5, T6 replay ABSENT; PRF-045 model-only; `fn-bprl-release-removes-the-forward-pin` exists | host calls `fn-bpnp-step` (bp-service.lisp:172); the no-release and lifecycle keystones are over the lower `fn-bpn-step` with a state-only bridge; PRF-046 has 0 events and says "calls fn-bpn-step" | guard-premises 5, machine-teeth 6, release 7 must-fails; N03/N04/N05 partial, N09 present; 14 N and all 24 BP-R labels MISSING; BP-R17 present through `fn-bpnp-step` (keystone `fn-bpnp-step-busy-delivery-defers`, books/bp-node-busy-delivery; positive trace and six must-fails plus four teeth in bp-node-counterexamples-tests; [record](evidence/bp-busy-delivery-2026-09-24.md)); `fn-bpnf-operationp` names `:conflict` | On c3420013 ([qual c3420013](evidence/qual-c3420013-2026-09-25.md)): bp_node 19/19, N08 1/1, bp_app 5/5, bp_obligation 4/4, and `bp-obligation request` through zero, one and two dtn7 relays reaching `receipted pinned=no` while an unauthorized origin is refused `source-not-carried`. On 18c91321 ([qual 18c91321](evidence/qual-18c91321-2026-09-24.md)): bp_node 17/17 on fn-host-dtn-developer and N08 1/1 on fn-host-developer, bp_app 5/5 (arity defect gone), contact_relay 1/1, bp_service 16/16 and receive_integrity 4/4 on the DTN developer image, fragment 1/1, app_journal 10/10. Current coordinates: [current view](current.md#p11); history: [evidence records](evidence/). [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) On 18c91321 ([qual 18c91321](evidence/qual-18c91321-2026-09-24.md)): bp_node 17/17 on fn-host-dtn-developer and N08 1/1 on fn-host-developer, bp_app 5/5 (arity defect gone), contact_relay 1/1, bp_service 16/16 and receive_integrity 4/4 on the DTN developer image, fragment 1/1, app_journal 10/10. [m4-app-receipt on 39eb05fb](evidence/m4-app-receipt-2026-09-24.md): through dtn7-rs 0.21.0 with one and with two relays (relay-a SIGKILLed mid-transfer by PID, restarted), `bp-service` is uncertain then accepted on resume, fn B's `bp-node serve` takes custody once and its application answers `request-refused` once (source EID is not the neighbour's enrolled EID), no receipt, A stays `pinned=yes`; the direct control runs request-accepted, receipt-accepted at A, `pinned=no`; bp_node 17/17 (`f9dd875a`), bp_service 16/16, relay 1/1, contact 2/2, receive_integrity 4/4 on the new DTN developer image; mock four-node fails at relay-a recovery on an ACL2 replay defect (`fn-bpiw-replay-records` never fences before a recovery outcome). Before that, [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) bp-node-counterexamples-tests: N11 witness per hypothesis (event kind by a six-field recovery event; next-arrival reached through recovery; epoch/next-op/bound corrupted, unreachable), kind-14 authority witness and six refusals; [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) [d23-bp-carrier on dc27d36d](evidence/d23-bp-carrier-2026-09-24.md): through dtn7-rs 0.21.0 with one and two relays (and the four-node lab, 6/6), B's relay boundary carries `dtn://sender/` and A's carries `dtn://receiver/`; B decides `carried ... author=sender-author`, `request-accepted` once, the receipt crosses back, A `receipt-accepted`, `pinned=no` (reports `5ce88b7a`, `cc27514315b13ba3`, four-node `a1d0ff86`); without the carries row: `refused reason=source-not-carried`. [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) On 18c91321 ([qual 18c91321](evidence/qual-18c91321-2026-09-24.md)): bp_node 17/17 on fn-host-dtn-developer and N08 1/1 on fn-host-developer, bp_app 5/5 (arity defect gone), contact_relay 1/1, bp_service 16/16 and receive_integrity 4/4 on the DTN developer image, fragment 1/1, app_journal 10/10. [m4-app-receipt on 39eb05fb](evidence/m4-app-receipt-2026-09-24.md): through dtn7-rs 0.21.0 with one and with two relays (relay-a SIGKILLed mid-transfer by PID, restarted), `bp-service` is uncertain then accepted on resume, fn B's `bp-node serve` takes custody once and its application answers `request-refused` once (source EID is not the neighbour's enrolled EID), no receipt, A stays `pinned=yes`; the direct control runs request-accepted, receipt-accepted at A, `pinned=no`; bp_node 17/17 (`f9dd875a`), bp_service 16/16, relay 1/1, contact 2/2, receive_integrity 4/4 on the new DTN developer image; mock four-node fails at relay-a recovery on an ACL2 replay defect (`fn-bpiw-replay-records` never fences before a recovery outcome). Before that, [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) On 4eca4148 ([qual 4eca4148](evidence/qual-4eca4148-2026-09-24.md)): bp_node 19/19 incl. N07 and N11 and N08 on both developer images, bp_app 5/5, contact_relay 1/1; dtn7 labs not run here. [d23-issuer-authority on 95069712](evidence/d23-issuer-authority-2026-09-24.md): through one dtn7-rs relay with A's boundary carrying B but not releasing for it, B `request-accepted`, A `release carried-not-released`, `receipt-refused`, `pinned=yes` (report `81fd81fa`); with `releases-for` B: `listed-issuer`, `pinned=no` with one and two relays (`e2ff6959`, `0ac5e5bf`); direct control `self-issued`, `pinned=no`; the verdict is A's kind-7 detail. [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) On 18c91321 ([qual 18c91321](evidence/qual-18c91321-2026-09-24.md)): bp_node 17/17 on fn-host-dtn-developer and N08 1/1 on fn-host-developer, bp_app 5/5 (arity defect gone), contact_relay 1/1, bp_service 16/16 and receive_integrity 4/4 on the DTN developer image, fragment 1/1, app_journal 10/10. [m4-app-receipt on 39eb05fb](evidence/m4-app-receipt-2026-09-24.md): through dtn7-rs 0.21.0 with one and with two relays (relay-a SIGKILLed mid-transfer by PID, restarted), `bp-service` is uncertain then accepted on resume, fn B's `bp-node serve` takes custody once and its application answers `request-refused` once (source EID is not the neighbour's enrolled EID), no receipt, A stays `pinned=yes`; the direct control runs request-accepted, receipt-accepted at A, `pinned=no`; bp_node 17/17 (`f9dd875a`), bp_service 16/16, relay 1/1, contact 2/2, receive_integrity 4/4 on the new DTN developer image; mock four-node fails at relay-a recovery on an ACL2 replay defect (`fn-bpiw-replay-records` never fences before a recovery outcome). Before that, [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) bp-node-counterexamples-tests: N11 witness per hypothesis (event kind by a six-field recovery event; next-arrival reached through recovery; epoch/next-op/bound corrupted, unreachable), kind-14 authority witness and six refusals; [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) [d23-bp-carrier on dc27d36d](evidence/d23-bp-carrier-2026-09-24.md): through dtn7-rs 0.21.0 with one and two relays (and the four-node lab, 6/6), B's relay boundary carries `dtn://sender/` and A's carries `dtn://receiver/`; B decides `carried ... author=sender-author`, `request-accepted` once, the receipt crosses back, A `receipt-accepted`, `pinned=no` (reports `5ce88b7a`, `cc27514315b13ba3`, four-node `a1d0ff86`); without the carries row: `refused reason=source-not-carried`. [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) On 18c91321 ([qual 18c91321](evidence/qual-18c91321-2026-09-24.md)): bp_node 17/17 on fn-host-dtn-developer and N08 1/1 on fn-host-developer, bp_app 5/5 (arity defect gone), contact_relay 1/1, bp_service 16/16 and receive_integrity 4/4 on the DTN developer image, fragment 1/1, app_journal 10/10. [m4-app-receipt on 39eb05fb](evidence/m4-app-receipt-2026-09-24.md): through dtn7-rs 0.21.0 with one and with two relays (relay-a SIGKILLed mid-transfer by PID, restarted), `bp-service` is uncertain then accepted on resume, fn B's `bp-node serve` takes custody once and its application answers `request-refused` once (source EID is not the neighbour's enrolled EID), no receipt, A stays `pinned=yes`; the direct control runs request-accepted, receipt-accepted at A, `pinned=no`; bp_node 17/17 (`f9dd875a`), bp_service 16/16, relay 1/1, contact 2/2, receive_integrity 4/4 on the new DTN developer image; mock four-node fails at relay-a recovery on an ACL2 replay defect (`fn-bpiw-replay-records` never fences before a recovery outcome). Before that, [dtn-node-image on 0b0de690](evidence/dtn-node-image-2026-09-24.md): the DTN image now carries the node (`bp-node`, `bp-obligation`, `bp-app`, owner, operator config, signatures); on `fn-host-dtn-developer` native N08 passes (log `ce722164`) and test_bp_node_native is 17/17 (`dd717ba9`); the relay contact passes cut, restart, delivery and expiry on `fn-host-dtn` (`ed137cae`); bp_service 16/16, app_journal 10/10, bp_contact 2/2, receive_integrity 4/4, image_profiles 12/12; dtn7-rs: a severed `bp send` is uncertain (exit 3), its RETRY re-offers the same creation/sequence (ACL2 `fn-bpn-host-authored-retry`), dtnd delivers once after the carrier SIGKILL, and the return bundle from the enrolled carrier is admitted by `bp receive ... STORE`. Before that, [m4-dtn-n08 on dev 1b734868](evidence/m4-dtn-n08-2026-09-24.md): native N08 (death after a durable kind 8, retry delivers once, receiver holds one copy, a third run makes no attempt) passes on the default developer image (log `0cb3ed9a`) and cannot run on the DTN developer image, which has no `bp-node`/`bp-obligation` verb; test_bp_node_native 17/17; bp_service 16/16, receive_integrity 4/4, bp_contact 2/2, app_journal 10/10 on the DTN images; the relay contact with a boundary-trusted `bp-node serve` receiver now delivers after the interrupted contact and fails only at the expiry step (a tick at wall 3600001 reports no `status=expired`; not diagnosed); real BPA (dtn7-rs 0.21.0): an fn-authored bundle crosses a severed contact, a retry and a SIGKILLed carrier and dtn7d delivers it once, but the return bundle is refused at fn's receive boundary on the DTN image, and the Python-host exchange lab passes 8/8 with receipt return. Earlier: matrix 47bdb9a4 ([run 165406Z](evidence/matrix-47bdb9a4-2026-09-24.md), report `33a79636`) and 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md)): BP-* not exercised (no native phase); [native subsets 47bdb9a4](evidence/native-subsets-47bdb9a4-2026-09-24.md): bp_node 15/16, bp_app 1/4 (receive arity), relay 0/1; [1a9dd747](evidence/native-subsets-1a9dd747-2026-09-24.md) and 6c0626c5 superseded; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin) | open after D23: bp-node serve's own forwarding still expects the application peer as TCPCL neighbour (routing, not trust); an anonymous ingress reports `reason=generation`; kind-8 death no longer strands the row: under the default retry policy (spec bp-node-machine §4.3.1, adopted by the coordinator 2026-09-24, pending ember) recovery keeps the uncertain attempt, the next session to the peer re-offers the same held bundle up to 3 times, then the row is held and reported stranded (lane bp-kind8-retry: `fn-bpnp-forward-scan-offers-the-only-candidate`, `fn-bpnp-step-session-offer-is-the-scan-choice`, `fn-bpnp-forward-image-keeps-the-held-primary`, `fn-bpnp-step-attempt-persist-preserves-the-retry-bound`; N08 flipped positive; native N08 passes on the default developer image of 1b734868). Open: the receiver absorbs a duplicate with XFER_ACK, not XFER_REFUSE 1, and the sender collapses refusal reasons; multi-candidate fairness (N03/N04); no theorem that replayed attempt epochs precede the recovery epoch; a reassembled fragment's id is fresh again at the receiver; closed by dtn-node-image: the DTN image carries the node, `bp receive` admits against a STORE, `bp send` severed is uncertain and retries keep identity, and the relay expiry was a test error (an age-anchored bundle ages by CLOCK_BOOTTIME only); still open: a native fn application receipt through a real BPA, a four-node real-BPA run, frozen DTN images from a shared cut, a keystone (not only teeth) for `fn-bpn-host-authored-retry`, and `bp send` has no durable resolution record so RETRY is operator-named; an unenrolled channel keeps custody with no principal and is refused at the application, not the CL; N16 (rotation) is still inexpressible: no generation transition, held-row checkpoint codec, selection namespace or generation-authority recovery exists (spec 3.6, slice E) On the frozen DTN images of c3420013 ([qual-dtn c3420013](evidence/qual-dtn-c3420013-2026-09-25.md)) every DTN module passes on `fn-host-dtn-developer` (bp_app 5/5 and bp_obligation 4/4 now on that image), `fn-host-dtn` fails only on refused developer selectors, and through dtn7-rs (0, 1, 2 relays, four nodes, interrupted contact) a SIGKILLed first hop leaves A uncertain, resume is accepted, B decides once and the receipt releases the pin only for a neighbour or `releases-for` issuer. |

Decisions: taken 2026-09-24 ~22:30 UTC as D23 (relay trust by carrier allowlist), D24 (peer node check carried), D25 (duplicate versus conflict keys on the poster's bytes), D26 (the ratchet's number is the 2-job measurement) in planning/decisions.md; the adopted defaults (P5's sentence, D02 over served POST, the kind-8 default with duplicates acknowledged, the five specific-purpose group names allowed) stand until ember says otherwise. Still ember's: none pending.

Image under test and deployed node: see [the current view](current.md) (generated; `make check` fails when it is stale).
