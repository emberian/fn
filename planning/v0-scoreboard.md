# v0 scoreboard

One row per property of plan §2.1 (`planning/plan-2026-09-22-trajectory.md`).
The "observed on image" cell cites the dated evidence record that observed
the property on a named image. Lanes fill their own rows; merge rows, never
overwrite another lane's cell.

| property | observed on image | record |
| --- | --- | --- |
| P2 | **1a9dd747 pair: cut half holds, wire half fails.** After a kill at all 20 cuts of `native_cuts.py` through both entries, the acknowledged article rereads byte-identical by `inspect` and `ARTICLE` (40/40 operator-campaign observations, run twice). No `240` preceded any death, and a SIGKILL of the production owner after a `240` rereads identical. At the NNTP wire: an uncertain outcome is a closed connection with no reply, not `441 ... do not repost` (W1, 9 EIO cuts); an EIO at `finish-consumed`/`finish-durable` answers `441 ... refused` for a durable article (W2); store refusals and duplicate reposts carry no reason (W3). The operator CLI keeps 0/1/3 distinct in every row. | [campaign-1a9dd747-2026-09-24](evidence/campaign-1a9dd747-2026-09-24.md) |
| P10 | **1a9dd747 pair: passes on the developer twin, not selectable on production.** Every cut taken names a `fn-bs-*-program` coordinate (`verify_native_cut_map` and the image-gated test pass); 20/20 cuts agree with the table on the developer image (core `9b38ba45…`). The production image (`fn-host` `60e14e2a…`, core `12ece6cc…`) refuses every cut selector at start (27/27, exit 5, store unchanged) and was killed only at two command boundaries, both passing. Five model sites (`frontier-created`, `frontier-written`, `record-created`, `record-written`, `record-stage-unlinked`) have no host cut. No ACL2 differential ran. | [campaign-1a9dd747-2026-09-24](evidence/campaign-1a9dd747-2026-09-24.md) |
