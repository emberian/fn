# v0 scoreboard

One row per property of [plan §2.1](plan-2026-09-22-trajectory.md). A row is
DONE when the theorem is over the function the host calls, its teeth book has
a reachable witness and one must-fail per hypothesis, and the property was
observed on the deployed image; otherwise the row names its obstruction. Lanes
update their cells when they land; the audit records
(`planning/v0-audit-*.md`) carry the evidence behind each cell.

| P | theorem | host-called subject | teeth | observed on image | obstruction |
| --- | --- | --- | --- | --- | --- |
| P1 protected channel | (a) `fn-auth-step-pinned-protected-only-refuses-authinfo-before-tls` and `fn-served-dispatch-of-authinfo-on-a-clear-connection-is-483-and-changes-nothing`; (b) `fn-auth-step-pinned-gated-command-is-refused-and-not-performed` and `fn-served-dispatch-of-a-gated-command-is-480-and-changes-nothing` (whole connection unchanged, only effect the 480 line); (c) `fn-auth-step-pinned-login-installs-the-credential-posting-flag`, `-post-by-a-principal-without-the-flag-is-440`, `-post-by-a-principal-with-the-flag-is-delegated` (all books/nntp-auth-invariants.lisp) | `fn-auth-step-pinned`, called at served.lisp:617 by `fn-served-dispatch`, reached from owner-host.lisp:1240; 480/483 proved from the `fn-auth-step` keystones through the bridge (nntp-auth-invariants.lisp:232) | nntp-auth-teeth-tests keystones 11 to 17: reachable witness each, 44 new must-fail (one per hypothesis) with evaluated counterexamples; PRF-031/-039 cite certify-20260924T105650Z-3582222 and certify-20260924T145426Z-1436530 | 483-then-login on da5fd8cb via node_probe; 480-before-login never; not on 1a9dd747; matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): AUTH-GATED-A/B 480 before login, AUTH-LOGIN 281, AUTH-WRONG 481, AUTH-WITHDRAWN, AUTH-POST 240 (clear loopback under `[auth] required`, not `protected_only`: the 483 clause is not observed); TRANSIT-TLS-AB/BA and TRANSIT-AUTHINFO-AB/BA accepted (feed over STARTTLS+AUTHINFO to a `protected_only` target, clear offer 502); TRANSIT-TLS-WRONG-ANCHOR and TRANSIT-AUTHINFO-WRONG refused, nothing delivered 483-then-login on da5fd8cb via node_probe; 480-before-login leaving no article: `tests/test_native_auth.py` `test_restricted_command_before_login_is_480_and_leaves_no_article`, not yet run (no image); nothing on 1a9dd747 | observation on the next image; (c) proves the auth layer delegates a flagged POST, not that the POST succeeds |
| P2 240 after consumed completion | `fn-own-240-follows-consumed-completion` (owner-served-invariants.lisp): a 240 from `fn-own-outcome` after `fn-own-finish` means `fn-sn-finish` consumed the enabled completion, exactly one pair was appended to the store's successes and the ledger, and the completed record is an article record with the in-flight submission's Message-ID and payload; FALSE over the host's own `:durable` word (finding: owner-tests `*own-240*` is a 240 for the injected article over `<three@example>`'s completion) | outcome: owner-host.lisp:1034 `fn-own-outcome`; the word: `fn-own-finish`, which the host does not call yet (`fn-owner-finish`, owner-host.lisp:495-507, still compares phases; PRF-015 event carries `pending_subject`); p2-wire owns the host change | owner-served-invariants-tests: reachable witness (connection 4's own article, 240), the `*own-240*` counterexample asserted, 2 must-fail (relation: malformed session answers 403 both ways; 240: `*own-taken*`) | 1a9dd747: crash half holds at all 20 cuts (40/40); wire half FAILS: W1 uncertain = closed connection with no reply (9 EIO cuts), W2 EIO at finish answers `441 refused` for a durable article, W3 no refusal reason; [campaign](evidence/campaign-1a9dd747-2026-09-24.md); matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): POST-COMMIT 240 then POST-FRESH reread on a new connection, POST-DUPLICATE 441, OUT-UNCERTAIN rc=3 (developer postpublish fault; an exit code, not the wire reply), OUT-RECOVER rc=0; CRASH-* not exercised (the native slice has no crash phase) | W1/W2/W3 are host defects (owner.lisp:1376/1514, io.lisp:1605/1614 + owner.lisp:744, nntp-post.lisp:280); ember: does a refusal name duplicate vs conflict on the wire? |
| P3 reading resumes | `fn-own-pinned-view-survives-other-post` (owner-served-invariants.lisp): after any writer events (`:take`, `:begin`, `:store`, `:complete`) through `fn-ocfg-step` and `fn-own-outcome` for another connection, a reader connection's served chunk answers with the same effects and consumed prefix; local numbers never reused is still only at the acceptance layer (PRF-002 keystones), no served-path statement; NEWNEWS PRF-052 certified over the acceptance stamp | owner-host.lisp:1240 `fn-ocfg-read-tls-prefix` itself; writer events through `fn-ocfg-step` (owner-host.lisp:209; `fn-opc-prepare` equals the `:store :prepare` event under the relation); outcome through `fn-ocfg-with-owner` (`fn-owner-replace-core`) | owner-served-invariants-tests: witness (reader 3 at v2 across connection 4's 240, view moves to v3), 3 must-fail (poster repinned; `(:advance 3)` event; peer connection IHAVE 335 then 435); nntp-newnews-tests 12 must-fail | V0-POST-CONCURRENT checks liveness only, never the unchanged view; not on 1a9dd747; matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): NEWNEWS-STAMP-A/B accepted (whole-history NEWNEWS includes the accepted article when its payload date differs), NEWNEWS-FUTURE empty, NEWNEWS-SYNTAX 501; POST-CONCURRENT is liveness only; local-number non-reuse not observed | none named |
| P4 one owner decides duplicate vs conflict | the three `fn-sn-existing-action-...-by-definition` theorems (store-node-existing-invariants.lisp:6,17,28); `fn-store-article-match` host twin is gone | store-node-host.lisp:456,577; owner-host.lisp:324,1166 | store-node-existing-tests: 5 must-fail | no image run since T3 landed; not on 1a9dd747 (matrix row v0_matrix.py:372 is the duplicate POST); matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): second POST of the same Message-ID 441 (POST-DUPLICATE); operator resubmission of identical octets rc=0 DUPLICATE, different octets rc=1 REFUSED (OUT-REFUSED); transit 435/438/439 on duplicates (TRANSIT-DUPLICATE, CHECK-DUP, TAKETHIS-DUP) and INN-DUPLICATE-FN 435; the wire does not say which | none: a native run is owed; `:conflict` becomes `:refused` in raw Lisp at host/native/owner.lisp:713 |
| P5 a fault costs one connection | `fn-ocfg-open-at-the-bound-refuses` (at `max-conns` the open answers nothing and returns the configured owner unchanged) and `fn-ocfg-fault-keeps-every-other-connection` (owner-served-invariants.lisp) with `fn-ocfg-fault-is-own-fault`; `fn-own-fault-keeps-every-other-connection` (owner-fault.lisp:138), K4 (owner-invariants.lisp:1836) | owner-host.lisp:1218 `fn-ocfg-open` and :1273 `fn-ocfg-fault`, both theorems over the called wrapper; natively only a connection-local fault continues (host/native/owner.lisp:629-648) | owner-served-invariants-tests: open witness (2 of 2 open), 2 must-fail (below the bound; identifier counter wound onto an open connection); fault witness and 1 must-fail (the faulted connection); owner-fault's own K-FAULT hypotheses still 0 must-fail; PRF-040 now lists `fn-own-fault-reply-is-not-a-post-outcome` | test_native_owner.py:246 last run 09-21; not on 1a9dd747; matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): nothing: no row injects a fault into a served connection | P5 overclaims: a fault inside a shared owner action stops the service with exit 4 (deliberate, AGENTS.md recovery rule); ember: restate P5? |
| P6 live reconfiguration | `fn-ocl-no-reader-observes-a-half-change` (config-owner-publish.lisp, no hypothesis) over staging `fn-ocfg-step` then `fn-ocl-publish`; `fn-ocl-publish-installs-the-whole-staged-record` (published config = `fn-cfg-apply-record` of the whole record, under `fn-ocl-config-historyp`); request: `fn-ocl-request-refused-over-a-reader-in-the-group` | staging owner-host.lisp:213 (`fn-owner-step`); publication owner-host.lisp:271 calls `fn-ocl-publish` and installs its owner unconditionally; delta owner-host.lisp:247 calls `fn-ocl-request-deltas` (host twin `fn-owner-config-deltas` deleted); model equation `fn-ocl-publish-agrees-with-ocfg-complete-on-readers` | config-owner-publish-tests: reachable witness (reader mid-`GROUP`, create staged by a second connection, admin close, :durable, same reply to the rest of the command, new connection serves the group) and 7 must-fail: one per hypothesis (6) plus the false owner conjunct | test_native_live_reconfiguration passed on 295bbe35; not on 1a9dd747; matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): NODE-INIT/CONFIG/STATUS/START/STOP, GROUP-CREATE/SERVED/RETIRE/UNKNOWN, CAP-SET, PEER-ADD/LIST/REMOVE/ABSENT, AUTH-PASSWORD/LIST all through `packaging/fn-native` on the image with no Python in the node; CFG-LIVE accepted (group declared on the running owner served 211), CFG-LIVE-REFUSE refused; AUTH-NEW not built; "no reader half-sees" not observed Not yet observed on an image carrying `fn-ocl-publish`. | headline 2 (`fn-ocfg-crash-at-any-instant-recovers-the-live-generation`) is over `fn-cnode-config-replay`, which no host line calls (recovery calls `fn-cpr-replay`, owner-host.lisp:173): pending_subject in PRF-028; native image run owed |
| P7 two nodes exchange both ways | K2 in/out, K3, K5 (port form `fn-feed-port-replay-is-live-modulo-inflight`), PRF-058 octets; outbound loop freedom now over the host-called functions (`fn-own-submission-targets-are-feed-targets`, `fn-own-submission-never-targets-a-loop`, `fn-own-transit-outcome-never-enqueues-on-the-origin`, books/owner-feed-subject) and the Path-tail clause (`fn-peer-relayed-octets-keep-the-received-path-tail`, keystone `fn-pu-relay-article-keeps-the-received-path-tail`) | inbound, octets, Path tail, K5 and TLS over host-called functions; outbound loop over `fn-own-submission-targets` (owner-host `fn-owner-submission-intent`, `fn-own-feed-durable`) and `fn-own-transit-outcome` (owner-host:995); `fn-own-feed-accept` uncalled, its two theorems `-sibling` | new keystones 1 must-fail per hypothesis (owner-tests, owner-feed-tests, peer-inbound-tests); `-never-offers-a-loop` 3; `-omit-no-offerable` 1 of 3, the other 2 hypotheses proved redundant; PRF-042 loop and history still 0 must-fail | 1a9dd747 join: TLS+AUTHINFO feed both ways (indirect), kill -9 then exactly one copy; loop, TLS-negative and octet-diff rows not run; matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): both ways, offer 335, transfer 235, IDENTICAL (only Path and Xref differ), duplicate 435, loop 437 then 430, CHECK/TAKETHIS 238/239 and 438/439; FEED-QUEUE/OFFER/ONCE/JOURNAL accepted (ONCE: kill after ACK, not re-offered); INN 9 rows held (33/33 findings); TRANSIT-INDEPENDENT not exercised (no native phase) | none of design; PRF-042 teeth; native loop/TLS-negative rows on the next image; FNFD physical correspondence (T16); [audit](v0-audit-p7-p9.md), persvati runs 144845Z-a330, 145112Z-797e |
| P8 signature verdict visible | kind-4 finish keystone `fn-sn-finish-of-a-kind-4-acceptance-records-its-verdict` (+ other-msgid sibling), reader pin `fn-own-reader-opened-after-completion-pins-the-finished-verdicts`, served `fn-own-read-hdr-fn-verified-is-the-pinned-verdict` (PRF-026, was 0 events); PRF-048 injectivity exists | `fn-sn-finish` (owner `(:complete)`, host/owner-host.lisp `fn-owner-finish`) and `fn-own-read` (host/native/owner.lisp `fn-owner-chunk` via `fn-ocfg-read-tls-prefix-is-full-read`); framing of the HDR line is a premise, not proved from the wire | tests/acl2/owner-verdict-read-tests: reachable witness on the T10a trace; must-fail for finish gate, `fn-stxa-p`, pre-completion reader, article absent, read cut before LF, item not `:fn-verified`; NO must-fail yet for auth gate, peer, awaiting-article, wire-closed premises | `HDR :fn-verified` answered `0 verified keyring 1` at B on 1a9dd747 and survived restart; verification only through fn's own binary; matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): STX-* not exercised (no native phase) | no non-fn verifier and no signing client; NNTP POST of an FN-Authorship article takes `fnn-owner-attempt` (host/native/owner.lisp:1173) not `fnn-owner-attempt-transit` (:746), so no durable verdict (gap, not decided in decisions.md); primitive-observation trust not a named `A-*` assumption; `fn-sig-verify` unattached (ember); persvati run-20260924T144834Z-7de2, 145302Z-8c12, 145434Z-8eca |
| P9 keep until release, refuse unaffordable | PRF-004: `fn-spc-prepare-refuses-unaffordable-obligation` (charge over the room the capacity leaves: the store state is returned unchanged), `fn-node-prepare-refuses-unaffordable-obligation`, `fn-node-prepare-refuses-whatever-retain-admit-refuses` (the equation that carries `fn-retain-admit-refuses-unaffordable-obligation` to the called function), `fn-sn-finish-keeps-accepted-articles`, `fn-sn-finish-releases-an-obligation-only-by-its-matching-release` (corollary: no arm releases an `:archive` pin) | `fn-spc-prepare` at store-node-host.lisp:472 (`:refused` at :475, native `fnn-bridge-prepare` io.lisp:810); `fn-sn-finish` at store-node-host.lisp:546 | `tests/acl2/store-node-retention-tests`: reachable store (capacity 10, 2 reserved) refuses charge 9 and stages 8; article, undertaking and release arms keep the article and archive pin; 8 must-fail, one per hypothesis; identity/consumer/topic arms unwitnessed. Certified persvati `certify-20260924T144414Z-1366014` (book 2.4 s, tests 2.5 s), `certify-20260924T144502Z-1374723` (retention-tests 0.5 s) | `V0-CAP-REFUSE` refused on 915 only; matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): CAP-REFUSE-A/B refused (capacity 1, article refused before it is written), CAP-SET accepted; NODE-REINIT-SAFE accepted (a refused second init leaves the recovery line unchanged) | not observed on an image; charges are abstract units (physical bytes open); no archive-release event in v0, so articles are kept outright |
| P10 every cut is a model crash point | K1-K4, K5, K7, K8 proved; K6 partial; K0 restricted to cut coordinates proved (lane p10-k0, `books/byte-store-k0`): `fn-bs-k0c-cut-pair-is-previous-pair` (any program: a cut's pair is the preceding pair), from an arbitrary related state under the typed input and an absent stage: P-FRONTIER staged-durable/durable/reserved, P-RECORD staged-durable/linked/attempted/durable/completing, P-FINISH every pair; lane p10-k0-b (`books/byte-store-k0-staging`), same premises: frontier-created/-written/-replaced/-attempted, record-created/-written/-stage-unlinked/-staging-cleaned (staging-only step lemmas: create, non-authority write, unlink, staging fence each keep the relation outside the recovery window; the pair-6 authority-quiet premise restated and proved there), error arms: (:frontier-replace :error) after a failed or issued rename, (:frontier-dir :error) at frontier-barrier, (:record-link :error) after a failed link, (:record-dir :error) at record-barrier (an EIO at any other campaign cut fences with no observation, so the state is the related cut pair); 18 of 25 cut coordinates covered; general `fn-bs-program-step-preserves-relation` ABSENT; open: every P-RECOVER cut (recover-replayed, recover-barrier x5; needs the entry theorem from `fn-sn-open-observed`'s scanned image), recovery-stage-unlinked only given a related sweep entry (K-sweep), the issued-link error arm | byte-model programs, no host line calls them; `tools/native_program_check.py` (in `make check`) checks statically that fnn-advance-frontier, fnn-publish, fnn-finish, fnn-recover, fnn-sweep-staging perform the programs' steps in order (5/5 PASS; two opaque ACL2 callbacks trusted; `frontier-barrier`/`record-barrier` injection-only); PRF-041 byte-model events carry a model-only `pending_subject` | K0 (`tests/acl2/byte-store-k0-tests`, retained-history witnesses): 8 must-fails for the first ten cuts, plus 24 per-hypothesis must-fails for the eight lane-b cuts and 6 for the error arms, with a reachable witness moving each error arm's kernel to :fenced-*; K5 3, K8 6, K6 63 must-fails; K5, K8 now PRF-041 events | 1a9dd747: 20/20 cuts pass on the developer image, every cut names an `fn-bs-*-program` coordinate; production refuses every selector (27/27 exit 5) and took only two boundary kills; five model sites have no host cut; no ACL2 differential ran; [campaign](evidence/campaign-1a9dd747-2026-09-24.md); matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): CRASH-* not exercised (no native phase); OUT-UNCERTAIN then OUT-RECOVER and NODE-STOP then recover are the only death-adjacent rows 1a9dd747: 20/20 cuts pass on the developer image; the five model sites without a host cut (frontier-created/-written, record-created/-written, record-stage-unlinked) now have `fnn-at` selectors and table rows (25 POST/RECOVERY cuts), not yet run on an image; production refuses every selector; no ACL2 differential ran; [campaign](evidence/campaign-1a9dd747-2026-09-24.md) | general K0; the P-RECOVER entry theorem and its six cuts; the issued-link error arm; no way to kill the production image at a cut; 6 checkpoint cuts and ~12 BP/TCPCL/owner kill points without an `fn-bs-*` coordinate; T16 has ext4/tmpfs only; [audit](v0-audit-p10-p11.md) |
| P11 bundles across an outage | over the called `fn-bpnp-step`: `fn-bpnp-step-base-event-refines-fn-bpn-step` (bridge: on `(:base E)` exactly `fn-bpn-step`'s base and effects, or nothing), `fn-bpnp-step-emits-no-release-and-no-receipt-prepare` (every event, no hypotheses), `-preserves-guard-premises`, `-progress-preserves-held`, `-progress-issued-unchanged-or-pending-dispatch` (books/bp-node-progress-bridge.lisp, PRF-046 now has 7 events); lifecycle and send-authorization keystones hold of the served step only on `(:base E)`; 5 of 29 spec §5 theorems; T1-T3, T5, T6 replay/continuation ABSENT; PRF-045 marked model-only (the served step allocates no sequence) | host calls `fn-bpnp-step` (bp-service.lisp:172); the lower keystones are bridged for `(:base E)`; the served forwarding arm's own `:cl-send` (kind 8) has no authorization keystone | bp-node-counterexamples-tests: bridge witness + 2 must-fails (one per hypothesis), confinement witnesses + release mutant; §11.1 added N05 (attempt half), N06 (machine half), N08 (stranded negative), N11 (refusal half), BP-R02, R06, R07, R15, R16 with must-fails; present N03, N04, N09, N15 (unlabelled); 10 N labels and 19 BP-R traces not expressible through `fn-bpnp-step` today, each with its missing transition named at the end of the book | only N03 and the interrupted fragment ran on 1a9dd747; no interrupted contact on a real BPA; `books/scheduler` has no native caller, `tools/scheduler.py` keeps a durable log in Python (twin); matrix 1a9dd747 ([run 143359Z](evidence/matrix-1a9dd747-2026-09-24.md), report `10744a97`): BP-* not exercised (no native phase) | death after kind 8 strands the row (N08 witness: replayed `:forwarding` row never re-offered); the retry/duplicate-control policy is ember's decision; recovery flag in `fn-bpnp-step` compares the first effect, not its tag, so recovery keeps the session table (no served effect today) |

Decisions for ember: P2 refusal reason on the wire; P5 restatement; P8 non-fn verifier and `fn-sig-verify` attachment; P11 retry policy after a possible send.

Image under test: `1a9dd747` ([record](evidence/native-cut-1a9dd747-2026-09-24.md)).
Deployed node: `da5fd8cb`.
