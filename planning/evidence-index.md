# Evidence index

One row per record in `tests/evidence/`, and for the deploy gate's records in `planning/evidence/`. "Covers" and "Does not show" are
qualitative; exact root/test/function counts belong to each record's own JSON
file and to planning/ledger.md, never repeated here as prose. Records are
ordered by their position in the project's own narrative
([now.md](now.md)/[assurance-closure.md](archive/assurance-closure.md)); several share
the calendar date 2026-09-18 because that date compresses many batches.

A record cites the certification run behind it by run id. Those runs write
`build/acl2/certify-<UTC>-<pid>/`, which is ignored and lives only on the
machine that ran it, so the **manifest** of every cited run is committed at
`planning/evidence/manifests/<run-id>.json`: the requested books, the
per-book verdict, the source and certificate digests, the ACL2 build, and
the revision, host and times. The **logs** are not committed and die with
their directory; each manifest's `archived_from` says where its log was.
`python3 tools/evidence_manifests.py check` is in `make check` and fails on
a newly cited run with no committed manifest. 177 runs cited before
2026-09-21 were already gone when the archive was built and are named in
`planning/evidence/manifests/LOST.txt`; a row there is a claim a reader
cannot check, and its owner re-runs it or walks it back.

| Evidence record | Source revision | Covers | Does not show |
| --- | --- | --- | --- |
| [dregg-e1-payload-budget.md](evidence/dregg-e1-payload-budget.md) (under `planning/evidence/`; a synthetic Mini/fn integration measurement, not certification) | Mini `e542eb5` (`implement/fn-evidence`); fn fixture branch `implement/mini-evidence`; native fn signing image source `329a51a2` | Source-owned bounded Mini one-event evidence export, read-only Lean re-admission and exact historical receipt/call comparison; five native tamper refusals; exact hybrid fn carrier and ACL2 BP ADU size under existing limits | No live/private Mini history, later-event witness, Mini proof certification, current T10 receiver binding, BP delivery, fn retention, consumer ack, application reply, or current authority to execute |
| [dregg-e1-consumer-p1.md](evidence/dregg-e1-consumer-p1.md) (under `planning/evidence/`; a synthetic Mini consumer operation experiment) | Mini `4b48047` native host, separately typechecked Lean decision proof; public P0 package from the preceding row | Two different-source calls signed against one pre-state: one atomic binding and Q accepted, stale other refused; exact reply recovered after reopen; changed-source conflict retained separately; unavailable current grant refused | Trusted synthetic fn attribution, no native E2 fetch/cursor/ack, no exact fn article provenance or reply posting, no process-death/power-loss CAS claim |
| [2026-09-23-human-web-client.md](../tests/evidence/2026-09-23-human-web-client.md) | client source based on `2c916fc7`; native image digest in record | Separate loopback web client over actual NNTP/HTTP sockets; accepted/refused/uncertain POST; scratch frozen native owner POST and readback | Public deployment, deployed-node TLS/auth, signature verdict, consumer ack, ACL2 proof |
| [2026-09-23-human-web-followup.md](../tests/evidence/2026-09-23-human-web-followup.md) | client source digest and frozen native image in record | Exact in-process submission and outcome reuse under concurrent POST, browser POST/redirect/GET and keyboard/visual check, scratch native readback | Persistent client deduplication across restart, deployed TLS/auth, signature verdict, public HTTP qualification |
| [2026-09-18-integrated.md](../tests/evidence/2026-09-18-integrated.md) | source/dependency digests only (no git revision recorded in this early record) | First integrated batch: acceptance/node initial-state and transition preservation, article-to-pin binding as a string triple, wire/CBOR primitives, the isolated-slot journal experiment, exchange merge, an experimental NNTP reader, independent `nntplib` traffic | Journal execution by the composed node (unexecuted); durable completion/recovery stay abstract inputs; no physical disk or host/milestone claim |
| [2026-09-18-storage.md](../tests/evidence/2026-09-18-storage.md) | `22f929a` | Complete primitive/record round trips, typed replay proofs, a real immutable-file store with durable allocation, a reader over recovered state, a maximum-profile reopen test | Replay validity does not prove every acknowledged physical history is reconstructible; no record-level accepted-input canonicality yet; platform/power-loss qualification |
| [2026-09-18-articles.md](../tests/evidence/2026-09-18-articles.md) | `7a018da` | Exact successful-parse source preservation, general retention-release invariants, LISTGROUP ranges/cursors, independent client traffic | Syntax parser has no required-field semantics; release authorization/evidence are inputs, not proved authorization; RFC READER bundle incomplete; no full installed-system source audit |
| [2026-09-18-wildmat-storage.md](../tests/evidence/2026-09-18-wildmat-storage.md) | `fe02b6e` | UTF-8 wildmat/filtering, explicit command bounds, two recovery hardening fixes, an executable file-publication kernel with initial fence/gate proofs and crash traces | Full adapter refinement; file kernel does not prove preservation for every transition/crash; calls existing replay with supplied groups/capacity rather than carrying its own configuration proof |
| [2026-09-18-assurance.md](../tests/evidence/2026-09-18-assurance.md) | `94e08a1` (original run; corrected in place) | General finite file traces, article recognizer/bounds, UTF-8/parser/DP correspondence, transfer state/assembly/hot-path work, record canonicality, CBOR/record guards, injected-fault and process-death coverage, bounded small-state exploration | The original run's three stale test expectations (corrected, not retroactively called passing); physical barriers/filesystem isolation/stable storage remain assumptions; no freshness anchor, checkpoint/compaction, signing schema, complete NNTP posting or deployment claim |
| [2026-09-18-composed-store.md](../tests/evidence/2026-09-18-composed-store.md) | `80afcbe008f3475d27d4376e1b9f205eb080a9e2` | Actual node traces preserving prior bindings; mixed live file/node traces (refusal, known abort, uncertain commit, recovery); observed-image loader; independent client traffic over reopened state | Not a proof of syscalls, cryptographic primitives, peer honesty or storage hardware; no byte-accurate physical reservation, rollback freshness, physical checkpoint/compaction, native signatures or full NNTP posting |
| [2026-09-18-reader-checkpoint-index.md](../tests/evidence/2026-09-18-reader-checkpoint-index.md) | mixed: 10 unchanged roots from the frozen composed-store batch plus a separately corrected ingress-book run (initial run's aggregate result is explicitly not called successful) | Logical checkpoint capture/suffix restore; derived group/number index soundness/completeness against source memberships; legacy BP article admission; remaining NNTP/article-field guards; socket/partition tests and independent client traffic | Physical checkpoint encoding/publication/selection; host index adoption; complete RFC injection; signed provenance; actual BP workflow/receipt composition |
| [2026-09-18-article-work.md](../tests/evidence/2026-09-18-article-work.md) | source/dependency digests (no git revision recorded) | Public article-parser work *value* correspondence for every ACL2 input, unconditional input/rejection-path work theorems, executable boundary checks at several length/count thresholds | The *cost* side is an instrumented shadow, not a measurement of the real parser (see the hygiene-lane annotation added 2026-09-18: the closed-form ceiling is roughly 16,000x the real traversal cost); integer bit complexity, memory, GC, evaluator overhead and host I/O are outside the model |
| [2026-09-18-fields-transfer.md](../tests/evidence/2026-09-18-fields-transfer.md) | `ddeb657` | RFC 5536 Message-ID/Newsgroups grammar over preserved views, transfer staging (reservation, gaps, duplicates, conservative overlap conflicts), file-model transition/crash preservation, a real recovery-fault mutation-gate fix | Article-field checks are a routing/identity subset, not full injection/authority; transfer outputs are unverified candidates, never receipts; **the recorded `transfer.lisp` source digest predates a later rewrite of that book and no longer matches it (annotated in the record, 2026-09-18 hygiene pass)** |
| [2026-09-18-bpv7-transport.md](../tests/evidence/2026-09-18-bpv7-transport.md) | `4daf02d7ea927e9293753b2a5c4497457f6e5a40` (pinned dtn7-rs) | Actual two-node BPv7 transport for opaque payloads: queue-while-absent, restart survival via inventory, non-destructive download, explicit delete, duplicate suppression | Not yet an end-to-end fn exchange; no application acceptance, receipt authorization, retention handoff, quota, liveness or power-loss durability claim |
| [2026-09-18-bp-ingress.md](../tests/evidence/2026-09-18-bp-ingress.md) | dtn7-rs `4daf02d7ea927e9293753b2a5c4497457f6e5a40`; fn `f0c1c759fc5190ef895018b7253112b9c05d7759` | Two real loopback BPAs carrying an exact legacy article into one fn receiver; queue/forward-on-contact, restart/inventory rediscovery, duplicate-BID recognition | Inbox-only ingress rejects nonempty workflow record histories until the ACL2 replay bridge is integrated; emits no receipt; no hostile-input, power-loss, authenticated-transport or RFC-injection claim |
| [2026-09-18-bpa-boundary.md](../tests/evidence/2026-09-18-bpa-boundary.md) | dtn7-rs `4daf02d7ea927e9293753b2a5c4497457f6e5a40`; fn `27071be6437758c4f4047a90d9431298574ecd9f` | Bounded pinned BPA receive path: capped inventory/BID/bundle/article sizes, receiver restart, article acceptance, distinct-BID exact-article retry, mock HTTP boundary cases | Not a proof of the upstream BP parser or physical durability; per-operation timeouts do not bound a whole slow transfer; still uses a BPA fixture as sender and emits no fn receipt |
| [2026-09-18-bp-workflow.md](../tests/evidence/2026-09-18-bp-workflow.md) | `b4b3444` (record) / `67c320f` (subsequent full Python suite) | ADU codec round trips and canonicality; sender journal adapter executing actual ACL2 workflow decisions: durable attempt intent/outcome, expire/reopen, recovery of an unresolved intent | Live test records a BPA callback, not a sent bundle; receiver request persistence, receipt regeneration and the actual return BP path are separate; no authenticated remote receipt or power-loss guarantee |
| [2026-09-18-bp-exchange.md](../tests/evidence/2026-09-18-bp-exchange.md) | `b44651279549dc20a763a802df241ed78977ff9c` (fn); pinned BPA `4daf02d7ea927e9293753b2a5c4497457f6e5a40` | Full actual fn-to-BP-to-fn-to-receipt round trip: durable sender work, outage/restart, explicit retry, receiver decision, lost receipt, distinct-BID duplicate, receipt regeneration, durable sender acceptance of the return | Does not establish BPA recovery for every internal cut (one restart window left a bundle unforwarded, recovered only through fn's own retry, not BPA-internal); A-POLICY trusted, receipts unsigned; no LTP, relay, fairness or mission-profile claim |
| [2026-09-18-bp-composition-assurance.md](../tests/evidence/2026-09-18-bp-composition-assurance.md) | `ff381d1` (base); `185f1e6e055e582095bd6304688bf8c89fa8099f` (Python suite manifest) | Joint pending/durable sender-work binding invariant over actual transitions and finite traces (with a fabricated-but-unbound witness); receiver Store/context/decision/replay invariants against a **fixed Store**; five actual receiver SIGKILL cuts with exact recovery checks | The fixed-Store theorem does not compose with an evolving Store (the receiver process-death cuts check disk reopen only, consistent with the review's D4 crash-model finding); no journal byte/live/replay refinement or authenticated policy binding |
| [2026-09-18-bp-guards.md](../tests/evidence/2026-09-18-bp-guards.md) | `input_base_revision a56b080` | Complete guard verification of the BP ADU decoder book, including the public guard-`T` decoder and typed streaming callees | Six other inventoried BP base books still have unverified functions (see planning/ledger.md for the current count); this is not a raw-execution claim for the whole BP call graph |
| 2026-09-19 | `60c5322` | [wave1a](../tests/evidence/2026-09-19-wave1a.md) | Green-lane convergence: assumptions, ledger, teeth, sender replay/intent theorems, index/checkpoint, host repairs; all roots certified on persvati; Python suite green | Nothing about the six pending repair lanes; laptop certification of this tree is not a passing record |
| 2026-09-19 | `4af825a` | [wave1b](../tests/evidence/2026-09-19-wave1b.md) | Base-book lanes converged: crash fidelity, wire/article/transfer, NNTP served path, ACL2-owned framing and identity; all roots certified on persvati at `03f15fa`; Python suite green on Linux | Nothing about substrate or BP primary/clock; macOS Python not re-run since `60c5322` |
| [deploy-cce4b11-2026-09-20.md](evidence/deploy-cce4b11-2026-09-20.md) (under `planning/evidence/`, written by `tools/deploy_gate.py`, not a certification record) | `cce4b11` | One commit deployed to persvati and run as a real service: certificate pairs taken by content from the host's own gate, a two-group store, accepted/refused/uncertain distinct in the exit codes, a raw-socket NNTP transcript, `kill -9` from inside an open POST, recovery through the real path, and a reread of the acknowledged article through a fresh server | No certification is re-established here and no book is proved; the owner did not start on this commit so the served evidence is the reader's; no concurrent session, no independent `nntplib` decoder (the host has only Python 3.13), no newsreader (none installed), one kill point only, and a SIGKILL is not a power loss |
| [wave-realignment-2026-09-19.md](evidence/wave-realignment-2026-09-19.md) (under `planning/evidence/`; a batch record, not a certification record, and no ACL2 process was started to write it) | `0bd0b5c`..`c8886ee`, gated at `7a9e89a` | The proof-style realignment of all seven book clusters and the four server waves: certify wall before and after per cluster, rules withdrawn, whole-state recognizers taken off the executable paths (34 calls to 0 across core and store), `must-fail` forms converted to witnesses, the farm gate's 210-of-213 with the three failing roots and their owner, the 331-test Python run with its seven failures, and eight named defects with their causes (including seven theorems that were false as written, each corrected by an exhibited counterexample) | No gate exists for `dev` at `c8886ee` or any revision after `7a9e89a`; eight of the tree's 221 roots have never been gated at all; no single machine certified the whole tree, so "before" is a farm manifest and "after" is a laptop run; nothing here re-establishes a certificate or advances a proof target |
| [native-matrix-915-2026-09-22.md](evidence/native-matrix-915-2026-09-22.md) (under `planning/evidence/`; five `tools/v0_matrix.py --backend native-operator` runs, not a certification record) | driver `2202a95f` then `783db508`; image `915d5c72` | The full v0 matrix driven against the immutable native production image on persvati: 123 of 192 rows reached with no Python in the node; the driver had never started a native node before (`nohup VAR=x`); a host fault was being read as the uncertain outcome; the native outbound feed delivers live posts to the peer; the native node has no path identity of its own, so loop suppression cannot fire | Nothing is published as the current matrix; the image is 210 commits behind `dev`, so AUTH, live administration and STARTTLS rows wait for a newer image; init, uncertain outcome, `peer list`, capacity refusal, crash, BP, statements, media, scale, INN and independent clients are not reached by the native slice |
| [native-freeze-c28ffc30-2026-09-22.md](evidence/native-freeze-c28ffc30-2026-09-22.md) (under `planning/evidence/`; seven hbox certification runs, no image) | `c28ffc30` from `dev` at `dbf2e1ab` | The image closure of `dev` taken from 111 to 130 of its 164 books at one frozen origin: the bounded CBOR profile's cost on goals that only dispatch on a record kind, and eight repairs under it, each naming the form it fixes | No image, no image identity, no runtime qualification; two books still red and 34 blocked behind them; the counts are certificates at one origin with one ACL2 executable and are not a coverage claim |
| [native-freeze-01fbdad4-2026-09-22.md](evidence/native-freeze-01fbdad4-2026-09-22.md) (under `planning/evidence/`; the second pass, no image) | `01fbdad4` from `dev` at `27dc5a21` | Why the closure was red: five commits on 2026-09-21 landed behaviour into books whose invariants books did not certify afterwards, so statements about `fn-sn-finish`, `fn-replay-apply-record`, `fn-sn-recover` and the AUTHINFO session were never proved in the shape the machine has, and several were false on arms that did not exist when they were written.  `books/peer-config`, `books/peer-inbound`, `books/nntp-auth`, `books/served`, `books/served-tls-prefix`, `books/native-auth-profile`, `books/native-auth-admin`, `books/store-node-invariants`, four BP receiver books and `tests/acl2/peer-inbound-tests` certify; the closure is 140 of 164 in one run (`certify-20260922T115409Z-2912983`), up from 130 | No image, no image identity, no runtime qualification; `books/store-node-traces` is red on the trace relation itself and `books/checkpoint` on the same store-event vocabulary, and everything left in the closure is above one of them; `fn-sn-finish-preserves-indexedp` no longer claims the accepted-statement arm (PRF-023) |
| [native-freeze-dabebb84-2026-09-22.md](evidence/native-freeze-dabebb84-2026-09-22.md) (under `planning/evidence/`; the fourth pass, images built) | `dabebb84` on `w31/freeze-3`, carrying `dev` at `2788d4cb` | The image closure certifies in one run at one frozen origin, 165 of 165 (`certify-20260922T200011Z-3216833`), after three repairs: `books/checkpoint-codec` (the encoder's value stated once so the round trips hold it and `fn-frame-item` closed; the generation keystone restated over the store-event projections the journal compares, since over the article accessors it was false of a retention event), `books/store-prepare-correspondence` (reconfiguration keeps the deferred and completing links) and `books/owner-prepare-correspondence` (the configured open's reader arm keeps the owner relation).  Production, developer and DTN images built and frozen under `build/images/dabebb84.../` with a build-source manifest and hashes, against a locally built OpenSSL 3.5.8 | No runtime qualification; the frozen launchers name the unfrozen cores; OpenSSL 3.5.8 is a local build inside the trust boundary; the counts are certificates at one origin with one ACL2 executable and are not a coverage claim |
| [fn-client-915-2026-09-22.md](evidence/fn-client-915-2026-09-22.md) (under `planning/evidence/`; a client exercise against a deployed node, not a certification record) | client from `dev` at `3f2d1c6e`; image `915d5c72` | The first run of `tools/fn_client.py` against a node rather than `tests/fake_node.py`: the frozen 915 image on persvati over the `ssh -L` tunnel docs/operator.md prescribes, `groups`, `read` with its watermark held across three runs, `post` read back by Message-ID and by a fresh reader, `--json` on each, duplicate identity refused (1), unknown group 411 (1), no listener uncertain (3), `nntplib` agreeing octet for octet, and the client identical on CPython 3.9 through 3.14; four client defects fixed (a node that accepts and then closes came out as a traceback on the refused exit, an IPv6 node address was unreachable, an unsaveable watermark crashed after the articles were out, a negative `--since` reached the wire) and the same connect gap closed in `tools/node_probe.py`; the image answered `381` to `AUTHINFO USER` with no STARTTLS offered and the client sent no password | STARTTLS, AUTHINFO, `--credentials` and the protected channel are still only exercised against the fake, because the image predates them; no concurrency, no article gaps, no recovery claim, no newsreader, and no book is certified or theorem proved by any of it |
| [owner-defects-2026-09-22.md](evidence/owner-defects-2026-09-22.md) (under `planning/evidence/`; a host repair against a deployed node, not a certification record) | host from `dev` at `13c5ea43`; image `915d5c72` | The three node findings the client exercise left, and a fourth handed over by `w32/operator-verbs`: a POST past the 32 768-octet body limit reproduced to the exact octet (32 760 answered `441 ... exceeds the configured size`, 32 772 stopped the process), traced to a host arm that faulted on the prefix count `fn-served-step-counted` is entitled to return when `fn-wire-after-line` closes the wire mid-article, now carried as the next step's input under a 511-octet bound; one clock reading per run traced to a host that called `fn-owner-observe` once at startup, now taken at open and before every chunk as `tools/run_owner.py` already did, at gettimeofday resolution; the accept queue of 1 raised to a named, documented 16; and `FN_NATIVE_CONTROL_TEST_STOP`, a SIGSTOP between a durable control submission and its reply that called itself developer-only in a docstring and was honoured by a production image, now gated on the serialized image profile the way `FN_NATIVE_POST_FAULT` is, answering the caller `:fault` (exit 4) instead.  The model's own answer to an oversize article -- 61 of 130 octets consumed, `(:reply :begin-article :reply :close)`, `441 posting failed; the article was not received` -- read out of the certified `books/served-tls-prefix`, and a raw-Lisp test of the three deployed functions with its teeth shown by patching the source back | No image was built and no image ran the fix, so the two image-driven tests have never executed and nothing about the composed node is witnessed; no book changed, so nothing was certified and no theorem is claimed; the backlog change is structural and untested; the cut gate has run in no image either, and the one test that arms the cut now skips for want of a developer image rather than running against the wrong one; the TLS arms are untouched and unexercised; DATE still answers the accept time for the life of one connection, which is D10-a's pin and not this defect |
| [inn-lab-dabebb84-2026-09-22.md](evidence/inn-lab-dabebb84-2026-09-22.md) (under `planning/evidence/`; `tools/inn_lab.py` on hbox, not a certification record; findings in its `.findings.json`) | image `dabebb84` (the hbox node's), launcher and drivers from `8a5f502a`; INN 2.7.4 | The native owner against a real InterNetNews over loopback, every reply quoted from a byte-transparent relay: fn's feed into innd by IHAVE (335/235), innfeed into fn (238/239), 435 for a second offer both ways, 437 for a Path loop both ways, innd's history across SIGKILL, fn's articles across SIGTERM and recover; the served octets compared header by header. Three fn findings violated: `operator post` relays an article with no Path (innd 437), and fn serves a transit article without its own path identity and with INN's Xref (RFC 5537 3.7 steps 6, 7) | One host, loopback, through a relay; no TLS or AUTHINFO; one peer, so fn's outbound render of a transit article is unobserved; image provenance beyond its digests |
| [inn-lab-da5fd8cb-2026-09-23.md](evidence/inn-lab-da5fd8cb-2026-09-23.md) (under `planning/evidence/`; `tools/inn_lab.py` on hbox, not a certification record; findings in its `.findings.json`) | image `da5fd8cb` from `dev` `2e53fae2` (the freeze of 2026-09-22), launcher and drivers from `9895e0b9`; INN 2.7.4 | The first record's scenarios rerun on the first image carrying the T13-conform repairs, plus a `From: yue` POST: 33 of 33 held. The three fn findings of `inn-lab-dabebb84` are gone: `operator post` now crosses to innd injected (Path `fnA.hbox.test!not-for-mail`, Injection-Date, Injection-Info; 335/235), fn serves innfeed's article with `Path: fnA.hbox.test!!inn.hbox.test!...` and no Xref, and `From: yue` draws `441 posting failed; From is not a valid mailbox list` and is not served | One host, loopback, through a relay; no TLS or AUTHINFO; one peer, so fn's outbound render of a transit article is unobserved; nnrpd's copy of the operator's article not read back; image provenance beyond its digests and source manifest |

| [matrix-witnesses-da5fd8cb-2026-09-23.md](evidence/matrix-witnesses-da5fd8cb-2026-09-23.md) | saved production image `da5fd8cb` / `2e53fae2`; matrix test overlay from `df5097b6`, file digests in record | Native `[store] path` init and status on an isolated scratch store; wildcard config rejected with usage exit 5; ACL2 scanned a real protected-feed FNFD commit and reconstructed its queued owner entry | Protected feed did not offer or deliver on this image; no acknowledged FEED-ONCE or mid-transfer death witness; no proof of image/source correspondence beyond the cited freeze |
| [matrix-protected-f0b8b166-2026-09-23.md](evidence/matrix-protected-f0b8b166-2026-09-23.md) | frozen production/developer image pair from `f0b8b166`, exact launcher/core/runtime and harness file digests in record | Five saved-image protected tests passed: reciprocal STARTTLS/AUTHINFO and reconnect octets; wrong-secret and wrong-anchor refusal; post-ack sender death with no reoffer; durable `:feed-sent` sender death, ACL2 FNFD replay to `:queued`, production restart to `:done`, and one recipient article | One host and filesystem, process death rather than power loss; first resumed `CHECK` not observed on the wire; tests and ACL2 replay are evidence, not a proof of OpenSSL or crash durability |

Every `.json` companion of a row above is the authoritative machine-readable
record; this table summarizes it and is not a substitute for reading it.
- [node-hbox-dabebb84-2026-09-22](evidence/node-hbox-dabebb84-2026-09-22.md) — the first native node deployed on hbox from the dabebb84 image; the probe from the Mac holds on every row; the full matrix on the image with its two live-configuration disagreements by name
| [agents-on-hbox-2026-09-22.md](evidence/agents-on-hbox-2026-09-22.md) (under `planning/evidence/`; a client exercise against a deployed node, not a certification record) | client from `dev` at `16c1bbfa`, lane `t13/agents`; image `dabebb84` on hbox | The first agent use of the hbox node: yue and tulip each over STARTTLS against the pinned certificate with their own login, `groups`, `read --all`, `post`, `show`, `read --new` from a fresh state file, a reply with `References`, a post in `fn.announce`, and each agent's watermark showing it only the other's new article; `--json` on each; wrong password 481 (1), unknown group 441 (1), duplicate Message-ID 441 (1), `--plain` 480 (1, no AUTHINFO sent), a foreign `--cafile` refused (1) after the fix; `nntplib` on 3.12 reading the same article byte-identical; two client defects fixed (a failed certificate verification reported uncertain, lost the 382 and was followed by a cleartext QUIT; `show --json` by Message-ID said number 0) and the zsh-broken `$NODE` idiom corrected in docs/agents.md; the node accepted `From: yue` with no address | `--credentials` against a node, folded headers through the text rendering, an uncertain post, concurrency, a restart, off-LAN reach and a newsreader are not exercised; no book is certified or theorem proved by any of it |
| [campaign-dabebb84-2026-09-22.md](evidence/campaign-dabebb84-2026-09-22.md) (under `planning/evidence/`; a runtime campaign on an image, not a certification record) | image `dabebb84` (developer and production) on hbox; driver from `dev` at `16c1bbfa` | The 14 cuts of `tests/campaign/native_cuts.py` driven through the operator verb over fresh stores, twice, with identical per-cut results: every SIGKILL cut leaves the candidate where the table says, `operator recover` exits 0 and changes no transaction byte, the prior article and every surviving candidate read back byte-identical by `store inspect` and NNTP `ARTICLE`, and resubmission answers `DUPLICATE` exactly when the candidate survived. The developer post and control faults each give exit 3 and a clean recover, and the production image faults (4) on `FN_NATIVE_POST_FAULT`. Seven findings named with their lines: the cuts arm only `store post`/`store recover`, never the served owner (F1); `.allocation-` stages are never swept and 65 deaths make the store unopenable (F2); the control stop is a racy group SIGSTOP (F3); production reports a durable article as fault (F4) and a refused control fault as uncertain, then stops (F5); three more developer selectors are honoured by production (F6); the recovery cut has no position in the recovery program (F7) | No ACL2 differential (`byte-store-keystones` is red and outside the image closure); nothing on the served path but the control stop; process death, not power loss; one filesystem; the DTN image and `FN_NATIVE_AUTH_ADMIN_FAULT` not run; nothing certified, no theorem |
| [campaign-da5fd8cb-2026-09-23.md](evidence/campaign-da5fd8cb-2026-09-23.md) (under `planning/evidence/`; a runtime campaign on an image, not a certification record) | image `da5fd8cb` (developer and production; sources `dev` at `2e53fae2`) on hbox; driver from lane `campaign/da5fd8cb` | The 16 cuts of `tests/campaign/native_cuts.py` run twice with byte-identical tables, now each taken both by the served owner (`operator CFG run`, which dies at the cut; its client answers 3 in every post row) and by the `store` entry: the candidate is where the table puts it, `operator recover` exits 0 and changes no transaction byte or the frontier, both articles reread equal to their references by `store inspect` and NNTP `ARTICLE`, and the same-entry resubmission is a duplicate exactly when the candidate survived. The recovery program's own cuts are selectable and die before the sweep. The control stop answers 3 in 10 of 10; the production image refuses every developer selector and the `store post` FAULT argument at start with exit 5 and the store unchanged; 65 deaths at the allocation cut leave one stage and the store opens; the sweep fidelity tests pass on the image. F1 and F3 to F7 of the first campaign are witnessed fixed and F2 witnessed fixed and changed; the driver's reread now judges the owner's injected article (H1); the two write entries store different octets for one payload, so a cross-entry retry is refused and `store post` writes an uninjected article in every image (N1) | No ACL2 differential; process death, not power loss; one filesystem; the DTN image not run; three selectors run only against the production gate; F2's over-bound directory built from files; nothing certified, no theorem |
| [certificate-cache-2026-09-23.md](evidence/certificate-cache-2026-09-23.md) (under `planning/evidence/`; a tooling measurement with real ACL2 on persvati, and one plain-roots run `certify-20260923T012310Z-2197907`) | `dev` at `1554e2cd` plus lane `tooling/cache`; ACL2 8.7 at `/home/ember/fn-gates/toolchains/w25/acl2-literal` | ACL2 8.7 with checksum book-hashes includes and extends a closure whose certificates come from several origins, with those origins on disk, removed or edited, because it compares sub-books by familiar name, annotations and book-hash and never by full-book-name; `install-set` now composes such a closure from snapshot origins and the farm's plain-roots preflight no longer requires the remote origin, so the peer-inbound roots installed 74 dependencies and certified only their 3 books | One chain and one diamond on one host and toolchain; no `--pcert` and no native image from a composed closure; the live-worktree exclusion is kept as policy, not shown necessary; the relocatable `project-dir-alist` route is measured but not adopted |
- [node-hbox-da5fd8cb-2026-09-23](evidence/node-hbox-da5fd8cb-2026-09-23.md) — the hbox node upgraded in place to the second image built from dev; the freeze in 1 min 44 s; the probe holds on every row; the store and the agents' posts kept
