# Backlog from the hourly sweep, 2026-09-25

The sweep of `build/coordinator/NIGHT.md`. Each line is one packet: what it
is, where it lives, the source (a commit or a record), and a mark.

- OWNED:<lane> means a running lane covers the packet.
- GOAL-UNOWNED means ember's overnight goal covers it and no running lane
  owns it.
- OUT-OF-GOAL means neither.

Items that a later commit closed stay listed as closed, with the hash that
closed them, so the next sweep does not reopen them.

**First sweep.** It covered dev `dev~700..dev`, from `45872c8c` (first) to
`24591e11` (last); the base is `5b633769`. It also covered spike/mega
`0e2173ab..b4d0184c`: the operator and reader spikes. The records read are
`planning/evidence/*-2026-09-25.md`, the three design docs dated 2026-09-25,
the twins and fan-in audit, the gpt-6 decisions review, D27 to D32, and the
two spike records.

**Next sweep.** Sweep 2 covered dev `24591e11..483987b1` and spike/mega `b4d0184c..f6d947d0`; sweep 3 covered dev `483987b1..abcb21d2`; sweep 4 covered dev `abcb21d2..51808446`; sweep 5 covered dev `51808446..54cf01c2`; sweep 6 covered dev `54cf01c2..6c6a6f35`; sweep 7 covered dev `6c6a6f35..081eb72e`; sweep 8 covered dev `081eb72e..7f7f980c`; sweep 9 covered dev `7f7f980c..88ec3ef0` (spike/mega unchanged at `f6d947d0` since sweep 2). Run `git log --format='%h %s%n%b' 88ec3ef0..dev` and `git log ... f6d947d0..spike/mega`, read the evidence records and LANEDUMPs merged since (expected: the running lanes bp-lifecycle-5 (PKT-308, PKT-294 item 1), capacity-vector (PKT-323, PKT-263 steps 3 to 6), hot-path-scans (PKT-324, PKT-301), rollback-history (PKT-327, PKT-287), signed-history-index (PKT-330, PKT-223, PKT-291 (2), PKT-190), pack-chain-join (PKT-332, PKT-168, PKT-197), commit-regression (PKT-348; its brief's PKT-283/PKT-284 collide with existing lines), the qualification qual-b6759850 (PKT-321, PKT-300, PKT-249, PKT-271, PKT-319); and the queued rep-wave-d-4 (PKT-328, PKT-307, PKT-314, PKT-317, PKT-293), pack-chain-open (PKT-331, PKT-347), outcome-algebra (PKT-329, PKT-295, PKT-246, PKT-275), ten-second-4 (PKT-326); any answer ember gives in planning/decisions-for-ember-2026-09-26.md closes or reopens its WAITS ON EMBER line), dedupe against this file, and append numbers from PKT-353 (PKT-321 to PKT-332 are the lanes' reserved ids above; PKT-164 to PKT-179 the wave-2 briefs'; PKT-236, 244, 246, 263, 291, 293, 294, 307 and 308 the continuations'; PKT-292 was released untaken; width-3 takes new ids from the coordinator), and update the line below.

last swept: 88ec3ef0 / f6d947d0

## Bounds (D27)

- [ ] PKT-001 P5, the namespace counts: `*fn-cp-max-consumers*` 256; `*fn-bpn-machine-max-records*` 4096 and received 2x; `*fn-nco-max-config-observations*` and `*fn-ff-max-observations*` 8192; `*fn-native-auth-max-credentials/-lines/-octets*`; `*fn-ncfg-max-octets/-lines*`; `*fn-pol-max-members*` 64; `*fn-th-max-*`; `*fn-cpa-clone-max-*`; `*fn-cpp-max-generations*` 4096. P1's five namespace fields exist, but no book reads them — books named in design §1.3 — design-2026-09-25-bounds.md §5 row 5 — OWNED:bounds-p5
- [ ] PKT-002 P5, chained packs: `*fn-cc-max-events*` 4096 and `*fn-cc-max-octets*` 4 MiB, so `store compact` refuses `:exceeds-compaction-unit` above 4096 transactions — books/checkpoint-compaction.lisp, store-compact-verb — dbf731d2, bounds-p1 record — OWNED:bounds-p5
- [ ] PKT-003 P5, the keyring snapshot as deltas: `*fn-stxk-max-octets*` 131072 and `-snapshot*` 65536 cap the principal count — books/stx-keyring-records.lisp — design §1.2 — OWNED:bounds-p5
- [ ] PKT-004 P5, the BP widths: `*fn-bpa-max-octets*` 65538 and `*fn-bpa-max-article*` 32768 (a BP-carried article is capped at 32 KiB), `*fn-bpb-max-data/-input*` 1 MiB, `*fn-bpf-max-length*` and `-fragments*` 65538 x 64 — books/bp-adu, bp-bundle, bp-fragment — design §5 row 5; 3169d50b — OWNED:bounds-p5 (the design row includes the BP widths; check that the lane brief does)
- [ ] PKT-005 P6: the u64 uint family, record schema 2 and frontier-3. The u32 caps the txid, sequence, frontier and charge, so profile T is capped at 2^32-1 — books/cbor.lisp, records*, byte-store-frame (frontier) — design §5 row 6; build/lanes/bounds-p6/LANEDUMP.md — OWNED:bounds-p6
- [ ] PKT-006 P6, segmented articles: an article is still one record payload in one bounded u32 read (`fnn-read-bounded-fd`, `fn-store-profile-read-bound`); new books/segments.lisp — design §2.4 item 3 — OWNED:bounds-p6
- [ ] PKT-007 P6: generic CBOR `*fn-cbor-max-bytes*` and `-max-input*` (65535/65538) still cap any codec that uses the generic entry, including config `*fn-cfg-max-octets*` 65538 and `*fn-stxe-max-octets*` 65538 — books/cbor.lisp:25,28 — design §1.2 — OWNED:bounds-p6 (config/stxe callers: see PKT-013)
- [x] PKT-008 Frame `:blob` is still 131,072, a data cap. It needs a per-schema `(:blob . W)` width, the frame-invariants round trips, the frame-journal length lemma and the `fn-nctrl-max-frame-for A` read bound. `operator post` carries at most 131,072 octets. A 516-root freeze — books/frame-octets.lisp:32, frame-fields.lisp (`fn-frame-specp`, `-field-okp`, `-parse`), frame-invariants, frame-journal; host `fnn-control-read-frame`, `fn-native-control-host-max-article` — fe9f28bf; bounds-join §3 — GOAL-UNOWNED — closed b0517f04 (f5d0c571: per-schema blob width)
- [x] PKT-009 The control-frame payloads follow the blob: `*fn-nhctrl-max-payload*` 65,536 caps control articles when hybrid control is loaded, and `*fn-nctrl-max-payload*` is 262,666 — books/native-hybrid-control.lisp, native-control.lisp — bounds-p2, bounds-join records — GOAL-UNOWNED — closed b0517f04 (f5d0c571)
- [x] PKT-010 Journal payloads that derive from the blob or are fixed: `*fn-frame-max-receipt-payload*` 269958, `-workflow-payload*` 16342, `*fn-tj-max-payload*` 262161 (two blobs), `*fn-aj-max-records*` 4096, `*fn-aj-*-aggregate*` — books/frame-journal.lisp:36-37, transfer-journal.lisp:66, app-journal.lisp — design §1.2 (meant to follow P2; no record changed them) — GOAL-UNOWNED — closed b0517f04 (tj and workflow payloads; the receipt and app-journal remainder is PKT-152)
- [x] PKT-011 The pre-reservation figures: `fn-sbud-verdict-at` presents 65,538 for an article (`*fn-store-article-publication-figure*`) and 196,608 for kind 4 (`*fn-store-accepted-statement-publication-figure*`). A composite above 196,608 is now reachable, and the history pre-check then relies on the publish path's own accounting. Derive the figures from A and G — books/store-events.lisp `fn-store-publication-ceiling`, store-budget-naming `fn-sbud-verdict-at` — signed-path record §1 and §5; e100f135 — GOAL-UNOWNED — closed 03be0a78 (a9ed4dc2: the history gate charges each article its own worst case, `fn-sbud-article-figure`; the A-and-G figure was rejected as non-monotone; kind 4 and peer-carried go to width-producers-2)
- [ ] PKT-012 The header caps are kept as work bounds: `*fn-article-max-header-octets*` 16384, `-header-lines*` 256, `-fields*` 64 and `*fn-af-max-field-value-octets*`. Under D30 repair A, a fixed total-work limit on one monolithic request is a data limit. Owed: a header parse whose work theorem is linear in the header (the certified fuel is quadratic), or a resumable parse — books/article.lisp:16-19, article-fields.lisp:15, tests/acl2/article-work-tests.lisp — bounds-p2 "Kept"; review-2026-09-25 §2.A — GOAL-UNOWNED
- [ ] PKT-013 The group-name ceiling is 256 (the config label width), not the wire's 460. Raising it fails `fn-cfg-labelp-of-record-group-name` and needs `*fn-cfg-max-label*` raised. The config caps `*fn-cfg-max-rows*` 1024, `-items*`, `-deltas*` 64 and `-octets*` 65538 are unclassified — books/records-shape `*fn-record-max-group-name*`, config.lisp:45-55, the native-admin typed-delta keystone — bounds-join §1; bounds-p2 finding 2 — GOAL-UNOWNED
- [ ] PKT-014 BP data caps that no packet names (the grep of `defconst *fn-*-max*`): `*fn-bpn-machine-max-jobs*` 64, `*fn-bpn-machine-max-octets*` 16 MiB, `*fn-bpnf-max-held-image*` 131072, `*fn-bpn-lifecycle-max-payload*` 134144, `*fn-bpn-evidence-max-records*` 4096, `*fn-bpnp-max-routes*` 64. Classify each as data or work; move the data ones to the profile — books/bp-node-machine.lisp:18-21, bp-node-foundation.lisp:9, bp-node-machine-codec.lisp, bp-receive-evidence.lisp:20, bp-node-progress.lisp:14 — this sweep — GOAL-UNOWNED (after bounds-p5)
- [ ] PKT-015 The host dup the design listed: the TCPCL MRU literal 1048576. No packet took it — host/native/tcpcl.lisp:45 — design §1.3 dup — GOAL-UNOWNED
- [ ] PKT-016 `--dynamic-space-size` is a data bound under D27: the launcher's 32000 MB, and the tools' 8000 MB cap. It should come from the profile or the operator — native launcher; tools/acl2_slots.py — a056d45c, 19cf7397, rep-heap record — GOAL-UNOWNED
- [ ] PKT-017 Python data caps: `MAX_INBOUND_AGGREGATE` 64 MiB (tools/workflow_journal.py); `MAX_TRANSACTION_COUNT` 128 and `DEFAULT_CONFIG` in the format-7 `SUPPORTED_PROFILES`; `command_post` reads at most `max_payload_bytes` 32,768 — tools/run_store.py `_config_from_metadata`, `command_post` — bounds-join §4 — GOAL-UNOWNED (after python-store-f8)
- [ ] PKT-018 Should T and H move from the offline profile to a configuration record, now that P3 decouples open from them? Also, the default profile values — design §2.1, §5 "Open" — OUT-OF-GOAL (decision)
- [x] PKT-019 The kind-4 literal caps (32,768 / 65,538 / 196,608) — closed e100f135
- [x] PKT-020 The `*fn-hsig-*` host literals 32768/34820, fn_verify ARTICLE_MAX and the signature-command 32768 — closed 6166adc1, 9c462d19
- [x] PKT-021 `*fn-store-capacity*`, R with one valid value, the profile relation against the kind table — closed fe9f28bf
- [x] PKT-022 Owner stack exhaustion at 2 MiB (`fn-frame-split`, `fn-ag-append`); TLS oversize stopping the owner; the 60 KiB and 200 KiB signed POSTs — closed 636a6768, e100f135

## Representation (D27)

- [x] PKT-023 Wave B, boundary 6: the octets abstract stobj `fn-octets`, with the preservation proof for each export, the first consumer keystone over `fn-record-payloadp`, and host `fnn-octets-fill` — books/octets-stobj.lisp (new), host/native/io.lisp `fnn-octet-list` :838-844, owner-host `fn-owner-prepare` — design-2026-09-25-representation.md §5 — OWNED:rep-octets-stobj — closed aa899a3b
- [ ] PKT-024 Wave B, boundary 7: the record codec over the buffer (`fn-rcs-encode-into`, `fn-rcs-decode-exact`), with abstraction theorems against the records-seam names. The host record write and recovery decode (io.lisp 710-723) are switched to it. The encoder output is still a list — books/records-stobj.lisp (new) — design §5; rep-records-2 record — GOAL-UNOWNED (after rep-octets-stobj)
- [ ] PKT-025 Wave B, boundary 8: the frame codec over the buffer (`fn-frs-seal-into`, `fn-frs-decode`), with the trailer computed from the buffer — books/frame-stobj.lisp (new), frame-fields.lisp:306,:317, frame.lisp:151, io.lisp:950, owner-host.lisp:1894 — design §5 — GOAL-UNOWNED (after rep-octets-stobj)
- [ ] PKT-026 Wave B, boundary 9: the article parser over the buffer (`fn-ars-parse`, `fn-ars-parse-work`), with the PRF-016 work theorems restated over the twin — books/article-stobj.lisp (new), article.lisp:324, owner-host `fn-owner-chunk` :1414 — design §5 — GOAL-UNOWNED (after rep-octets-stobj)
- [ ] PKT-027 Boundary 3 follow-on: SHA-256 read straight from the buffer. `fnn-trailer`, `fnn-subject-id`, `fnn-obligation-id` and the carried intent still hand ACL2 an octet list — books/sha256-stobj.lisp, host/native/io.lisp — rep-sha256 record — GOAL-UNOWNED (after rep-octets-stobj)
- [ ] PKT-028 Wave C, boundary 10: the owner state as the abstract stobj `fn-owner`, holding payloads as byte arrays. `fn-owner-corr` must be preserved for prepare, commit, outcome, read and recover, with `fn-ocl-relation` and `fn-sn-statep` folded in — books/owner-stobj*.lisp (new), every host entry in host/owner-host.lisp — design §5 — GOAL-UNOWNED (after wave B)
- [ ] PKT-029 The payload arena `fn-payloads`, and record strings kept in the arena or as base strings (4 B per char on SBCL). Measured effect: 12.9 GB to 0.8 GB at the scale ceiling. Needs a records-shape freeze (624 roots) — books/records-shape `fn-record-payload` — rep-heap record — GOAL-UNOWNED (wave C)
- [ ] PKT-030 The intent identity as an ACL2-held field of the in-flight submission, carried by `fn-own-step-preserves-relation`. Today it rests on a host single-writer discipline — books/owner.lisp `fn-own-sub-*`, owner-host `fn-owner-take` :670 — rep-intent record — GOAL-UNOWNED (wave C)
- [ ] PKT-031 The checkpoint carries the whole record list, so it is as large as the history. The kernel should carry a count instead — `fn-sco-capture`, `fn-sf-records` — bounds-p3 finding 3 — GOAL-UNOWNED (wave C)
- [ ] PKT-032 The group-name grammar converts every name to a list (`fn-record-group-namep`). The wire token becomes a string before lookup. `fn-own-refresh`, `fn-midx-refresh` and OVER bucket resolution walk character lists (an `fn-own-refresh` twin is needed). The host converts group names at io.lisp:133,136,676 — books/records-shape, group-bucket-article, msgid-index, owner — rep-records-2 record; design §2 — GOAL-UNOWNED
- [ ] PKT-033 Recovery decodes every record from a freshly built list, and `fnn-octet-list` conses one list per octet argument — host/native/io.lisp :676,:710,:722,:838 — design §1, §5 — GOAL-UNOWNED (with PKT-024)
- [ ] PKT-034 `fn-pa-filing-plan` (control C1) runs `fn-article-parse` over an octet list and has no concrete twin — books/control-classify.lisp, `fn-pa-filing-plan` — control-c1 finding 6 — GOAL-UNOWNED (with PKT-026)
- [ ] PKT-035 The N16 rotation codec runs on octet lists — books/bp-node-rotation-codec.lisp — 97643d20 — GOAL-UNOWNED
- [ ] PKT-036 The BP, TCPCL and feed wire codecs run on lists. Measure first, then `bp-stobj` and `feed-stobj` after boundary 6 — `fn-bpn-receive` (bp-node.lisp:446), `fn-peer-relayed-octets` (peer-inbound.lisp:243) — design §5 "Not on this profile" — GOAL-UNOWNED (after rep-octets-stobj)
- [ ] PKT-037 Fold `records-attach` and `records-attach-concrete` into one attachment (a freeze edit). The leaf `mbe` freeze batch: `fn-record-octet-stringp`, `fn-record-metadata-bytes-p`, `fn-octet-listp`, `fn-midx-lookup` — books/records-attach*.lisp, records-shape, acceptance-alloc, msgid-index — rep-records-2 record; design §5 — GOAL-UNOWNED (ride the wave-B freeze)
- [ ] PKT-038 The prepare's guard `fn-sf-record-valuesp` still names the list dispatchers. This is guard and logic only; nothing runs it per POST — books/owner-prepare-carried.lisp:34,60,70 — representation record — OUT-OF-GOAL
- [x] PKT-039 SHA-256 on words, the records recognizer and codec twins, the Message-ID lookup and the heap census — closed 3b9eebe4, e8789315, 44aaf698, a056d45c

## Owner and checkpoint

- [x] PKT-040 The served owner opens from the checkpoint and publishes the next one at K/2. It carries the node invariant through replay — host/native/owner.lisp:460 `fnn-owner-install`, `fn-owner-recover`, config-physical-replay `fn-cpr-apply-event` — 825f85cf findings 1, 2, 7 — OWNED:owner-checkpoint-open — closed 69fa464a
- [ ] PKT-041 In checkpoint mode, `fnn-open-live-store` callers (pack, compact, owner) still rebuild all N records' octets — `fn-store-sco-prefix-octets`, `fnn-open-live-store` — bounds-p3 finding 4 — GOAL-UNOWNED (after owner-checkpoint-open)
- [ ] PKT-042 The checkpoint is trusted for records 0..S-1, so damage below S goes unseen while the marker count binds it — `fn-sco-select`, store-history-marker — bounds-p3 finding 5 — GOAL-UNOWNED
- [ ] PKT-043 The whole-file statement over the host's range loop: only the pairwise `fn-bs-read-ranges-concatenate` exists, and the reach baseline admits it as model-only — host/native/io.lisp:1578 `fnn-state-checkpoint-segments`, books/byte-store-range-read — b36c6e02; bounds-p3 finding 8 — GOAL-UNOWNED
- [ ] PKT-044 The checkpoint codec's round-trip hypotheses have no must-fail: the u64 widths, and `fn-scc-treep` beyond the rational case — `fn-scc-decode-segments-of-segments` tests — bounds-p3 teeth — GOAL-UNOWNED
- [ ] PKT-045 The owner's octet cache assumes committed records grow append-only within one process. That is stated, not proved — `fn-sbud-bytes-extend`, owner install — bounds-p1 limits — GOAL-UNOWNED
- [ ] PKT-046 `fn-ocl-relation` across staging, `:close` and `fn-ocl-publish` as the host calls them. Only the open was proved — books/owner-recover-ocl.lisp — ea0b5787 — OUT-OF-GOAL
- [ ] PKT-047 Three scratch-only hbox patches that the P3 image closure needed are not on the branch (b91af70e, 59c1b559, 2288c05d); the coordinator should inspect them. P3's N=2048/4096 native figures are incomplete — /tank/fn/scratch/bounds-p3/ — bounds-p3 record — OUT-OF-GOAL (deputy)
- [x] PKT-048 Refusing to serve when the checkpoint is missing — decided and closed at 825f85cf as fall back and report

## History reclamation (D13)

- [ ] PKT-049 History reclamation under D13 — specs/storage.md history classes, checkpoint-compaction — 0f059fea, design §3 — OWNED:reclaim-d13
- [ ] PKT-050 Covered files are checked at open, not ruled out by proof — books/checkpoint.lisp:159-172, host/checkpoint-host.lisp — f2a0bdfc — GOAL-UNOWNED (after reclaim-d13)
- [x] PKT-051 A lost newest transaction file was undetectable at open — closed 0f059fea (the marker); no operator compact entry — closed 617116de

## BP node and DTN

- [x] PKT-052 Durable budgets (kind 20) and receipts sent from `bp-node serve`, not from `bp-contact tick` — books/bp-node-progress, bp-fnbs-*; host/native/bp-node.lisp — qual-dtn-c3420013 "lacks"; lane LANEDUMP — OWNED:bp-budgets-receipts — closed 9c1c110a
- [x] PKT-053 A base receipt transfer that ends uncertain is node-wide (the native module's 11 FAIL, exit 3); it should be connection-local. Needs a model change and its theorem (`fn-bpnp-uncertain-receipt-transfer-keeps-the-job-owed`) — lane LANEDUMP finding — OWNED:bp-budgets-receipts — closed 9c1c110a (0acdb88e)
- [ ] PKT-054 "Offered once per contact" as an ACL2 theorem over the host driver loop. The lane records it as not claimed — build/lanes/bp-budgets-receipts/planning/evidence/bp-budgets-receipts-2026-09-25.md:56,217 — lane record — GOAL-UNOWNED (after bp-budgets-receipts)
- [x] PKT-055 Routed queued jobs: FNBS base jobs (`bp-obligation request`, receipts and reports from `bp-contact tick`, `bp-service`) keep the CONTACT-HOST:PORT they were queued with. Route them with `fn-bprt-outbound-choice` at queue time, and rerun the dtn7 lab showing "pin stays, no-route logged" — books/bp-route.lisp, host/bp-native-app-host.lisp, bp-contact — bp-routing finding 1; 02f9766b — GOAL-UNOWNED — closed c64720c4
- [x] PKT-056 The 6-field `:session` form and `:resume` are not gated by the route. An unrouted older row blocks younger rows under the same dispatch key — `fn-bpnp-step-session-offer-is-the-scan-choice`, dispatch key — bp-routing findings 2, 3 — GOAL-UNOWNED — closed c64720c4 (c671e9e9)
- [x] PKT-057 N16-F1: after a rotation and reopen, the first new operation reuses the rotation's operation id. The checkpoint's `prior` must be the rotation's id, and `fn-bpnr-recover-from-checkpoint-equals-full-recover` should be restated with the event's epoch — books/bp-node-rotation*.lisp, `fnn-bps-open` — n16-rotation record; 97643d20 — GOAL-UNOWNED — closed acac63d8 (6591fb74)
- [ ] PKT-058 The rest of N16: the empty new-generation premise is not modelled; spec §3.6's four-authority table, kind-19 chunks plus a manifest (kind 20 is now bp-budgets-receipts', so renumber), the `:quiesce` event and obligations 3 to 6 are open; the Store marker driver `fn-cpp-marker-driver-*` is not in the DTN image; the K0 byte model is not instantiated for rotation — specs/bp-node-machine.md §3.6, books/bp-node-rotation, host/native/bp-service.lisp — n16-rotation "Open" — GOAL-UNOWNED
- [x] PKT-059 N16 housekeeping: old generation directories are never removed, and a killed rotation's stray `.bp-generation-<pid>-<hex>` is never removed — host/native/bp-service.lisp — n16-rotation "Open" — GOAL-UNOWNED — closed acac63d8 (0cceac9c; the killed rotation's kept directory is PKT-199)
- [ ] PKT-060 N16's journal fill past 8,192 received finals with a rotation in between has not run natively. It needs a multi-bundle sender or a profile-set record limit (after PKT-001) — tests/test_bp_node_native — n16-rotation "Open" — GOAL-UNOWNED
- [x] PKT-061 BP fragmentation (a goal item). Outbound fragments should be sized by the receiver's MRU rather than 65538 x 64 — books/bp-fragment.lisp, host/native/tcpcl.lisp — design §2.3 BP row; goal — GOAL-UNOWNED (after bounds-p5) — closed acac63d8 (5262daec: the send side sized by the MRU; receiver caps PKT-185, held transit PKT-198)
- [ ] PKT-062 Production DTN labs: every lab runs only on the developer DTN image because its setup uses `store post`. Build the setup from production verbs — tools/*lab*.py, tests/test_bp_*_native — qual-dtn-c3420013 "lacks" — GOAL-UNOWNED
- [ ] PKT-063 `bp-boundary show` does not render the `receipt-signer` or `require-signed-receipts` rows. The receipt signer directory is positional — host/native bp-boundary, books/native-admin-peer — signed-receipts findings 5, 7 — GOAL-UNOWNED
- [ ] PKT-064 The with-issued waits slot, still open from the machine-gaps lane — books/bp-node-machine-gaps.lisp — 80af52a3 — GOAL-UNOWNED (confirm it is still open)
- [ ] PKT-065 The DTN profile deployed, and signed receipts plus `bp-obligation recover` labs run on a frozen image — current.md P11/M4 next gates — OUT-OF-GOAL (deputy: cut, qualify, deploy)
- [x] PKT-066 N16 inexpressible; BP-R17 busy; `:conflict` in operationp; the resume and recover verbs; the ION retry live/replay mismatch; death after kind 8 stopping the node — closed 97643d20, aabcf98b, 409ee487

## Control (D29)

- [x] PKT-067 C2, the authority rows for the verbs C3 needs, and C3, cancel as a withdrawal record hidden from newly published views. The early-cancel test is visible(T then C) = visible(C then T). A pinned reader keeps its view; a fresh reader after the cancel sees it withdrawn — books/control-*, `fn-ctl-authorize` — D29; design-2026-09-25-control-messages.md — OWNED:control-c2c3 — closed 613f4f17 (C2 at 9b94b69b; C3 served at 613f4f17; owner relation remainder PKT-206)
- [x] PKT-068 A signed control article (FN-Authorship) is refused `:control-signed`, not filed, because hybrid-store requires the record's groups to equal the source's Newsgroups. C2 needs signed controls to be filed — books/hybrid-store.lisp, control-classify — control-c1 finding 3 — OWNED:control-c2c3 — closed b94f1015
- [x] PKT-069 `fnn-owner-complete-bound-submission` with a commit callback is a second route into the Store; any future caller must file first. Make that structural, not a convention — host/native/owner.lisp — control-c1 finding 4 — GOAL-UNOWNED — closed 8f973b6f (7c80e4f6)
- [x] PKT-070 No native case drives a control bundle through BP transit (owner.lisp:1370) — tests/test_bp_*_native — control-c1 finding 5 — GOAL-UNOWNED — closed acac63d8 (native 26/26 with the control-article case, log 7810d607)
- [ ] PKT-071 C4, group control by article, and feed suppression of withdrawn targets — D29 — OUT-OF-GOAL (deferred by D29)
- [x] PKT-072 The 437 transit line hid its reason — closed d0d73413 (see PKT-104 for the native run)

## Peering

- [ ] PKT-073 Peering with strangers: invite/accept, succession, revocation and pull feeds, with a carriage resource policy and the three-way verdict. No dev lane exists; the packets come from spike/peering's deferral list when it lands — spike/peering (running) — goal — GOAL-UNOWNED (after spike/peering)
- [ ] PKT-074 The three-node D23 chain has never run natively: an unenrolled relay listing the author stores and relays with `carried`, and an unlisted author gets 439 — tests (written) — 15128285; current.md — GOAL-UNOWNED
- [ ] PKT-075 Audit T2: three dead `fn-pix-` copies and their theorems; restate `fn-pgc-decide-offer-is-pix-decide-offer` — books/peer-offer-indexed.lisp:134,209,277, peer-guard-carried.lisp:116 — audit packet 7 — OUT-OF-GOAL
- [ ] PKT-076 Store identity (a decision candidate from LIST COUNTS) — b021f7b7 — OUT-OF-GOAL

## Marker (D31)

- [x] PKT-077 A durable marker-required state after migration, so a missing marker is damage. The requirement must not become durable before a valid marker exists — books/store-history-marker.lisp, `fn-hm-open-verdict` — D31; review §3 — OWNED:marker-required — closed 5cfd0519
- [x] PKT-078 Recovered success without a later commit: record durable, death before the marker, recovery, the client resolves "already stored", no new commit, the newest file deleted, recovery again. Protect it with a marker catch-up at recovery — review §3 case 2 — OWNED:marker-required — closed 5cfd0519
- [ ] PKT-079 A cheaper publication program against the frozen A <= M <= D. It currently costs 326 to 388 ms per 32 KiB commit on ZFS. Options are a shared later barrier or bounded group commit, both refining the same contract; moving the count into the allocation barrier is not allowed — books/byte-store-marker-program.lisp, host/native/io.lisp `fnn-mark-committed` — D31 step 3; 0f059fea — GOAL-UNOWNED (after marker-required)
- [x] PKT-080 closed by lane k0-rest: the coverage generic over the :root name (fn-bs-k0s-root-rename-pendingp), the :rename arm onto any :root name but the frontier, fn-bs-k0-state-checkpoint-cuts-relation-by-step and fn-bs-k0-profile-cuts-relation-by-step (planning/evidence/k0-rest-2026-09-25.md)
- [ ] PKT-081 The NNTP probe has no recorded wire run of the five marker cuts. The silent staging-cleanup swallow (S1) is still a node defect that the tests pin — tests/campaign/native_nntp_post_probe — probe-tests-d25 limitations — OUT-OF-GOAL (qualification; S1 flagged)
- [x] PKT-082 F1, the byte model had no marker (closed 396ac0b6); the marker-replaced commutation (closed 824e8f8c); the marker error arms, universal (closed a361b6a8)

## K0 corollaries

- [x] PKT-083 closed by lane k0-cuts: the :ok root barrier from any related state (fn-bs-k0s-root-fence-preserves-relation) and fn-bs-k0-frontier-durable-cut-relation-by-step (planning/evidence/k0-cuts-2026-09-25.md)
- [x] PKT-084 closed by lane k0-cuts: the four prefix corollaries by the step (fn-bs-k0p-stage-pairs-by-step) (planning/evidence/k0-cuts-2026-09-25.md)
- [x] PKT-085 closed by lane k0-cuts: fn-bs-k0-record-linked-cut-relation-by-step, inputs by fn-bs-k0p-record-link-inputs (planning/evidence/k0-cuts-2026-09-25.md)
- [ ] PKT-086 Step kinds the general K0 does not cover: `:mkdir` and `:link-eexist` (the pre-init relation); every step in the recovery window (`fn-bs-replay-matches-scan` per step, P-RECOVER); the in-process re-recovery at admin.lisp:107 — books/byte-store-k0*.lisp — k0-corollaries; 4e03bd3e. The `:staging` error outcome closed by k0-steps; the `:transactions` and `:root` error outcomes closed by k0-rest (fn-bs-k0a-authority-fsync-error-preserves-relation) — GOAL-UNOWNED. Recovery window, the recover program by the step and the admin.lisp:107 re-recovery closed by k0-recovery (fn-bs-k0w-step-preserves-relation, fn-bs-k0v-recover-program-by-step, fn-bs-k0v-host-rerecovery-keeps-relation-at-every-cut); :mkdir/:link-eexist kept by the pre-init relation (fn-bs-k0i-step-preserves-pre-init). Still open: the frontier publication's pairs from its link to the end of init, barrier errors in the window with an authority entry pending, (:recover) from :replaying with one pending
- [x] PKT-087 The K4 node correspondence: the b5724fce bridge does not carry `fn-sn-open-observed-success-has-live-history-relation`, `fn-bpr-live-receipt-regenerated-after-restart`, or the node conjunct of `fn-sn-open-observed-success-exact-history`, because `fn-snt-relation` does not hold of the host's configured node. The corollaries add `fn-sob-configured-openp` — books/store-open-bridge.lisp; PRF-007/036/041 — b5724fce — GOAL-UNOWNED — closed 5aeba0a1 (a74f7581; follow-ups PKT-217)
- [x] PKT-088 The general per-step K0, the redundant coverage hypothesis, nine per-cut corollaries, and K4 stated over an uncalled function — closed 766381c8, a361b6a8, b5724fce

## Python host twins

- [ ] PKT-089 The last twin (#8): `metadata`'s `b"unsigned-legacy-v0"` provenance label, which the two BP drivers read. Build `fn-prov-make-bp` evidence; this changes the stored BP evidence bytes — tools/run_store.py:1578, tools/run_bp_ingress.py, run_bp_receive.py, books/provenance.lisp — d0d73413; python-twins record — OWNED:python-store-f8
- [x] PKT-090 The Python store suite is red under P1's opaque profile: 17 failures and 19 errors, "written under a different store format", "post returned 4" — tools/run_store.py, run_owner.py; tests.test_store* — fe9f28bf; bounds-join §4 — OWNED:python-store-f8 (confirm the brief covers it) — closed cd9b8204
- [x] PKT-091 The native host keeps its own verdict-to-text table (a host-Lisp twin; one owner per decision) — host/native/io.lisp:2011 `fnn-validate-post-boundary` — python-twins finding 1 — GOAL-UNOWNED — closed cbd78ffc (d895f676)
- [x] PKT-092 13 red cases in test_store_corruption reference a function that does not exist on dev — tests/test_store_corruption.py — d0d73413 — OUT-OF-GOAL — closed 5c520e61 (tests.test_store_corruption 7/7 ok in the harness-repair laptop tables, before and after)
- [x] PKT-093 Twins 1 to 7 — closed d0d73413

## Operator surface (spike/operator deferrals, packaging/fn `SPIKE`)

- [x] PKT-094 Live status, obligations and pins while an owner runs: today the answer is "store is already locked" and headroom cannot be observed. Needs a control-socket status query — packaging/fn status/top; books/native-control — spike-operator findings — OWNED:live-status — closed 0680226a
- [x] PKT-095 Refused POSTs are never logged, on either path, so a refusal-rate alert has no signal. Owner-log lines should be rendered by `fn-owner-log-line` — books/owner-log.lisp, host/native/owner.lisp — spike-operator finding — GOAL-UNOWNED — closed ae6e6961 (794f0ff5)
- [x] PKT-096 The `[alerts]` and `[ops]` tables are stripped before the image reads fn.toml. Needs the config grammar, plus an `operator CONFIG show` projection to replace the awk — books/native-config.lisp — spike-operator deferrals 1-2 — GOAL-UNOWNED — closed 8f973b6f (7c80e4f6)
- [x] PKT-097 The mission table (small-community / relay / archive) is three rows in bash. Needs a named, validated fn.toml plus profile — books/native-config.lisp, byte-store-frame profile — deferral 3 — GOAL-UNOWNED — closed 8f973b6f (7c80e4f6)
- [ ] PKT-098 A node-health verdict to back doctor's checks and thresholds and the alert rules (fault, rate, headroom) — new book; packaging/fn doctor/check — deferrals 4-5 — GOAL-UNOWNED
- [x] PKT-099 A `store needs-upgrade` answer to replace the scratch-init format test. Rollback soundness (the bounds-join §5 condition) should be a checked predicate — books/store-profile-upgrade, byte-store-frame — deferrals 6-7 — GOAL-UNOWNED — closed ae6e6961 (d265fddd)
- [ ] PKT-100 A restore verb. `checkpoint clone` refuses every news-only store `:unbootstrapped` (there is no consumer incarnation), so no fresh-identity restore exists for NNTP-only nodes — books/checkpoint-auxiliary.lisp, host/native/checkpoint.lisp — deferral 8; finding — GOAL-UNOWNED
- [x] PKT-101 Log rotation: the owner should reopen its log on SIGHUP (copy-and-truncate can lose a line). Node-probe assertions should replace grepping reply codes — host/native/owner.lisp; tools probe — deferrals 10-11 — GOAL-UNOWNED — closed 8f973b6f (7c80e4f6; tools/node_probe.py used at 5895211a)
- [x] PKT-102 `principal set-password` answers `restart-required` even with no owner running — host/native/operator.lisp, books/native-auth-admin — spike-operator finding — GOAL-UNOWNED — closed ae6e6961 (794f0ff5)
- [x] PKT-103 The developer `owner run` path does not install the profile's served bound: a 40 KiB article passes the wire and gets an unnamed 441, and `test_article_over_the_body_limit_is_refused_and_the_owner_survives` fails. Also, developer `store ROOT init` takes flag words (`--max-article-octets`) as group names — host/native/owner.lisp (developer owner run), io.lisp:2852 `fnn-command-init` — signed-path §5; large-article §4.1, §5 — GOAL-UNOWNED — closed ae6e6961 (794f0ff5)
- [x] PKT-104 The 437 transit refusal line has had no native run — host/native/owner.lisp `fnn-owner-transit-complete` — python-twins record — GOAL-UNOWNED — closed ae6e6961 (794f0ff5)
- [x] PKT-105 `status` should print the pessimistic open-cost figure beside the profile values — `fn-bs-profile-report` — design §3.3, §5 — GOAL-UNOWNED (after owner-checkpoint-open) — closed fbbb6622 (ea66da80)
- [x] PKT-106 The deployed node's offline 7-to-8 upgrade (written, not executed), and the rehearsal step "POST 3 MiB, restart" on the upgraded store — bounds-join §5 — OUT-OF-GOAL (deputy deploy) — closed 85bb3ba2 (the node moved 7 to 8 unmarked; the required step is PKT-235)

## Reader (spike/reader deferrals)

- [x] PKT-107 D32: a client-supplied Path is accepted and prefixed, the supplied tail kept as poster source under D25, a supplied Xref still refused, and tin's post, reply and cancel work — books/injection, poster-bytes, nntp-post; specs/nntp.md — 24591e11; spike-reader — OWNED:path-and-login — closed 09c0af20
- [x] PKT-108 The login-to-principal binding: a login posting with another principal's key is verified for that key, and the node relates no AUTHINFO user to a principal — `fn_web.Signer`, books/nntp-auth, hybrid enroll — spike-reader deferral 1 — OWNED:path-and-login — closed 09c0af20 (4d848b4d)
- [ ] PKT-109 The grammar of the `withdrawn` verdict token, and the reader reading `HDR :fn-control`. The spike's `revoked <hex> keyring G` grammar comes from spike/peering — `parse_verdict_hdr`, books/nntp-responses — spike-reader deferral 2 — GOAL-UNOWNED (after control-c2c3)
- [x] PKT-110 XPAT works but CAPABILITIES does not list it; a second pattern argument is not ORed; matching is case-sensitive. The web layer builds the wildmat itself (it should be the node's) — books/nntp-*, wildmat; tools/fn_web.py — spike-reader tin findings, deferral 4 — GOAL-UNOWNED — closed cbd78ffc (fe6c8dcf, d895f676)
- [ ] PKT-111 The tin client phase and four `V0-CLIENT-TIN-*` rows in a matrix run (needs tin on PATH on hbox) — tools/v0_matrix.py, tools/spike_tin_drive.sh — spike-reader — GOAL-UNOWNED (after path-and-login)

## Cost (D26)

- [x] PKT-112 Carry the kind: `fn-store-event-sequence` and `-txid` run the whole `fn-stxa-p` recognizer to read one field, and `fn-rcon-store-event-sequence`, `-txid`, `-p` and `-generation` each re-walk the payload. This is why a 200 KiB signed POST is 2.2x plain (3.8x TLS) — books/store-events.lisp, records-concrete.lisp, records-codec-concrete.lisp — e100f135; signed-path §2, §5; rep-records-2 — GOAL-UNOWNED — closed db87dc76 (cf7c41ff; the replay remainder is PKT-224)
- [x] PKT-113 `fn-own-submission-targets` parses the Path at both the intent and the resolution (1 to 4 percent) — books/owner.lisp, owner-intent-carried — rep-intent record — GOAL-UNOWNED — closed db87dc76 (542c2045)
- [ ] PKT-114 Books over 10 s: store-node-invariants (11.4 to 13.6 s, split near event 50; 93 local events, opens the record vocabulary) and owner-invariants (10.4 to 12.1 s, 3.5 s is the include). In the NEAR band: config-owner-live, native-admin-peer (8.2 s slowest), peer-inbound. native-admin varies 8.3 to 15.1 s at the same bytes (not investigated) — books/* — ten-second-2; signed-path §4; bp-routing — GOAL-UNOWNED
- [ ] PKT-115 The IHAVE/CHECK per-event config check (50 percent of what remains) — books/peer-inbound, owner — 3dee88fe — OUT-OF-GOAL
- [x] PKT-116 bp-node-progress-guards, bp-node-rotation, bp-node-progress-invariants, store-node-traces and native-admin under 10 s; the fasl cache — closed 34ec37c1, 2b8164ec

## Tooling and registry

- [ ] PKT-117 Nothing certifies host-file changes that a test book loads: the cache key does not cover host/store-host.lisp as loaded by provenance-tests — tools/certify_books.py — d0d73413 — OUT-OF-GOAL
- [ ] PKT-118 The fan-in splits never ran a pcert Convert wave. 77 unresolved (file, name) pairs remain in the include closure — tools/certify_books.py --pcert — fanin-packets record — OUT-OF-GOAL
- [ ] PKT-119 Audit packet 3: move the RFC 5536 grammar out of records-shape into a leaf book (spec edits drop from 619 roots to 18). Batch it with the next records-shape freeze (PKT-037) — books/records-shape.lisp — audit packet 3 — OUT-OF-GOAL (cheap if it rides wave B)
- [ ] PKT-120 Stale prose: T7 `fn-store-txn-prefix-reclaim-plan` in proofs.json's progress_note. The rep-heap record's REOPEN_PLACEHOLDER and MAKECHECK_PLACEHOLDER. Ledger warnings: bare-general-claim on the intent teeth, and accessor-equality on the rcon twins — planning/proofs.json, planning/evidence/rep-heap-2026-09-25.md:169,206 — audit; bd227366 — OUT-OF-GOAL
- [ ] PKT-121 Rewarm the persvati fasl cache; old entries have no fasl until they are recertified — tools/certs — fasl-cache record — OUT-OF-GOAL

## Other

- [ ] PKT-122 Audit T1: delete the uncalled `fn-bpiw-attempt-record` and its test theorem (7 roots) — books/bp-ion-workflow.lisp:61, bp-request-plan-tests.lisp:122 — audit packet 7 — OUT-OF-GOAL
- [ ] PKT-123 Audit T3: `fn-sn-existing-action` is uncalled on the served path. Move it to poster-bytes as the reference and delete store-node-existing-invariants (279 roots; ride the next store-node change). Also the `fn-sn-make-v3`/`-v4`/`-v5` constructors — books/store-node.lisp:65,194-210 — audit packet 8 — OUT-OF-GOAL
- [ ] PKT-124 1070 unreached definitions, and the `-by-definition` lemmas: five small ones plus two `fn-hsig-authorization-binds-*` at 420 roots each — audit §1b, §1c appendix B — OUT-OF-GOAL
- [x] PKT-125 The consumer_e2 signed poll with the undated source (F6) — tests/test_native_consumer_e2.py:298 — qual-c3420013 — OUT-OF-GOAL — closed 4aa3ab61 (322c8ea2 dates the signed-poll default source; C14 fixed at 95eeee4c)
- [ ] PKT-126 The component test "TLS reset preserves the selected library identity" failed on hbox; not investigated — native_hybrid_signatures — bounds-p4 limitations — OUT-OF-GOAL
- [ ] PKT-127 The five special-purpose group names are ember's call — 91bdf0e9 — OUT-OF-GOAL (decision)
- [x] PKT-128 qual-c3420013 F2 (INN Injection-Date) and F3 (cross-entry conflict) — closed cc5dbb3c; the header projections related (T5) — closed b5724fce; the EID with a space — closed 6373d09d

## Data constants on dev (`defconst *fn-*-max*` in books/, 200 lines)

The work bounds that stay: the NNTP syntax caps, wildmat, the feed wire chunk, the admin and operator argv, auth name, statement and stx caps, the key and signature octets, the anchor wire, TCPCL u16/u32/u64, the poll scan, retry counts, backoff, `*fn-sn-max-staging-observation*` (sweep rounds), `*fn-nctrl-max-active-clients*`, `*fn-store-event-max-octets*` (node-generated, fixed width), and `*fn-bpsr-*` and `*fn-hc-*` (fixed signature widths). The following are data and not owned by P5 or P6:

- PKT-129 = PKT-008 `*fn-frame-max-blob*`
- PKT-130 = PKT-009 `*fn-nhctrl-max-payload*`, `*fn-nctrl-max-payload*`
- PKT-131 = PKT-010 `*fn-frame-max-receipt-payload*`, `*fn-frame-max-workflow-payload*`, `*fn-tj-max-payload*`, `*fn-aj-max-records*`, `*fn-aj-*-aggregate*`
- PKT-132 = PKT-012 `*fn-article-max-header-octets*`, `-header-lines*`, `-fields*`, `*fn-af-max-field-value-octets*` (work today; data under D30-A)
- PKT-133 = PKT-013 `*fn-record-max-group-name*`, `*fn-cfg-max-label*`, `*fn-cfg-max-rows*`, `-items*`, `-deltas*`
- PKT-134 = PKT-014 `*fn-bpn-machine-max-jobs*`, `-octets*`, `*fn-bpnf-max-held-image*`, `*fn-bpn-lifecycle-max-payload*`, `*fn-bpn-evidence-max-records*`, `*fn-bpnp-max-routes*`
- PKT-135 `*fn-feed-max-payload*` 1024, `*fn-sched-max-payload*` 4096, `*fn-anchor-max-payload*` 1024, `*fn-cp-max-token*` 512, `*fn-th-topic-max-octets*` 1024: classify (each is probably a fixed record shape, which is work) — books/peer-feed.lisp:850, scheduler.lisp:420, anchor-record, consumer-position, topic-history-store-events — this sweep — GOAL-UNOWNED (classification only)
- PKT-136 `*fn-stxe-max-octets*` 65538 and `*fn-stxe-max-detail*` 8192 (evidence records) — books/stx-evidence-records.lisp:15 — this sweep — GOAL-UNOWNED (with PKT-007)
- PKT-137 `*fn-ncfg-default-max-connections*` 32 is a default the operator can change, not a cap — OUT-OF-GOAL
- PKT-138 `*fn-nntp-max-article-number*` 2^31-1 is RFC 3977 §6. It stays a wire limit, but a group past it cannot be served. Record it as an RFC limit — OUT-OF-GOAL

## Appended by the deputy from merge messages (2026-09-25 evening)
- [ ] PKT-139 `*fn-bpn-machine-max-records*` 4096 is a fixed retained-data cap on the base BP journal (D27); a never-answering neighbour fills it in about 2000 passes (2 records per pass) — books/bp-node-machine.lisp — bp-budgets-receipts finding 7 — GOAL-UNOWNED (bp-routing-2 / bounds-residual)
- [ ] PKT-140 Once per contact is a host loop fact, not a theorem — host fnn-bpc-drive-contact — bp-budgets-receipts — GOAL-UNOWNED (bp-routing-2)
- [x] PKT-141 The checkpoint carries the whole record list and the Store open re-encodes the covered prefix (the kernel should carry a count); a publication blocks served commands 6 to 12 s under the owner mutex (extension and encoding outside it); the suffix is replayed twice on a checkpoint open — books/checkpoint*, host/owner-host.lisp — owner-checkpoint-open findings 1 to 3 — GOAL-UNOWNED (one lane: checkpoint-cost) — closed d917aab1 (one open, a count in the file, publication off the mutex; the kernel list is PKT-031)
- [x] PKT-142 `fn-article-listp` (books/acceptance.lisp:45-52) is Θ(S²) and runs once per open inside fn-cnode-statep; make it linear (carried or indexed). P3's N=2048 logs in hbox /tank/fn/scratch/bounds-p3/logs/ — coordinator note — GOAL-UNOWNED (checkpoint-cost) — closed d917aab1 (linear list checks; the two node checks are PKT-189)
- [ ] PKT-143 The cheaper marker publication program: marker n written with the next reservation under one root fsync (marker is 2 of 7 fsyncs per commit on ZFS, ~27 %) — books/byte-store-marker-program.lisp — marker-required design — GOAL-UNOWNED (store-durability)
- [x] PKT-144 Three native neighbour tests fail on source-text drift (fn-sn-io, peer-report, non-ASCII in native-control.lisp) — tests/ — marker-required — GOAL-UNOWNED (small fix lane) — closed cfe625a9
- [ ] PKT-145 The status codec is on octet lists (a D27 twin); each page re-renders the whole report under the owner lock — live-status — GOAL-UNOWNED
- [x] PKT-146 C3 owner wiring: fn-own-refresh with K1 restated over the carried withdrawn set (needs the view's verdicts plus the config-history txid ordering) — books/owner-invariants.lisp, control-visible — control-c3 (continuation owed) — GOAL — closed 645505a8 (K1 restated over the visible state; the journal and txid facts are PKT-206)
- [ ] PKT-147 spike/control findings: a signed article naming a group the node does not serve is refused by hybrid-author (cause unfound); after an executed rmgroup the group still answers 211 and is in LIST ACTIVE (C4, out of goal on dev) — spike-control record — GOAL-UNOWNED (the first), OUT-OF-GOAL (the second)
- [ ] PKT-148 spike/bp deferrals: routed queued jobs as a theorem (offered only to the routed hop), exact fragment reassembly, the durable contact-time route, held-transit fragmentation, a crash point for directory removal, two module tests rewritten, ION via a dtn7-to-ION UDP hop — spike-bp record — GOAL-UNOWNED (bp-routing-2, bp-n16-prod)
- [ ] PKT-149 tools/certify_books.py refuses skip-proofs and lacks :skip-proofs-okp, so the D28 spike allowance is not real — tools — spike/control — OUT-OF-GOAL (told the spike lanes via the coordinator)
- [x] PKT-150 status-join: the offline status's `checkpoint-file` line (fnn-state-checkpoint-file-report) dropped when live-status's ACL2 report replaced the printed lines; put it into the report through the observation — books/native-live-status.lisp, host/native/io.lisp — deputy, live-status merge — GOAL-UNOWNED — closed fbbb6622 (ea66da80)
- [x] PKT-151 PKT-011 blocked by PRF-072: pre-check figures from the profile make fn-profile-upgrade-keeps-verdict false (raising A or R without H makes the history check stricter); options: a hypothesis on the theorem, or fn-profile-upgradep refusing such upgrades (changes the bounds-join deploy procedure) — books/store-events.lisp, store-profile-upgrade — bounds-blob — WAITS ON EMBER — closed 03be0a78 (the per-article figure is profile-independent, so `fn-profile-upgrade-keeps-verdict` keeps its statement; `fn-profile-upgrade-keeps-article-verdict`; decided by the coordinator as recommended)
- [ ] PKT-152 Remaining data caps from bounds-blob: the receipt journal's two 131,072 blobs (app store record, request data), the app-journal count 4,096 and aggregates 16/64 MiB (needs the profile in the receipt codec, the Python bridge and fn-aj-statep); the BP held bundle image *fn-bpn-machine-max-job-octets* 131,072 can take (:blob . W) — bounds-blob — GOAL-UNOWNED
- [ ] PKT-153 Reader pins have no durable form (reclaim-d13 finding) — GOAL-UNOWNED (reclaim-host decides)
- [ ] PKT-154 control-c3 findings: the BP native-app author path files a signed control article by Newsgroups (control-c3b); a signed control article stored under Newsgroups before C1 may no longer replay (control-c3b checks a copy of the live store) — GOAL (control-c3b)
- [ ] PKT-155 *fn-store-format-id* in books/store-config.lisp is unreferenced after python-store-f8 (remove in a book batch); the Python read-bound pre-checks that remain are listed in planning/evidence/python-store-f8-2026-09-25.md — python-store-f8 — GOAL-UNOWNED (Python host's last twin)
- [x] PKT-156 status renders max-history-octets=0 and history-marker=0 (fn-nls-nat renders 0 for 2^40 and for the word unmarked); tests/test_native_io_progress.sh fails in fn-store-profile-max-transactions — bounds-profile findings — GOAL-UNOWNED (status-join) — closed fbbb6622 (c2963b6d)
- [ ] PKT-157 bounds-profile remainder: consumers (field 9; replay refuses past 256, 407 dependents), BP rows (field 10; eviction design, PKT-139), policy members (field 13 inside a signed statement's codec: DECISION for ember), *fn-cpp-max-generations* (after bounds-p5), BP ADU bounds (BP ingress has no profile) — GOAL-UNOWNED
- [ ] PKT-158 path-and-login open: the INN lab's supplied-Path scenario unrun; the Store's verdict record does not carry the login; the tin matrix rows live only on spike/mega — GOAL-UNOWNED (reader-control-surface)
- PKT-162 (done by test-latency: every Acl2Store takes a pool slot, shared per process tree, with the heap cap): tools/frame_bridge.py starts ACL2 directly (`acl2` on PATH), bypassing the slot pool and the 8,000 MB heap cap (`--dynamic-space-size 32000` seen on the laptop during the python-store-f8 rerun, 2026-09-25 18:25 UTC): route the Python bridge through tools/acl2 (or apply acl2_slots.apply_heap_cap in its Popen env) until the Python host is retired. (coordinator, from the rerun's process list)
- [x] PKT-163 (done: saved bridge image, one bridge per CLI process, pooled; store modules 23 to 32 s, test_bp_node_native 114 s on hbox; tools/test_budget.py enforces 300 s per module; planning/evidence/test-latency-2026-09-25.md) Test iteration time is pathological (ember, 2026-09-25): the Python store modules take 1857 to 2283 s each (about 90 s per ACL2 boot at include-book, a boot per test, bridges outside the pool: PKT-162); test_bp_node_native 909 to 1134 s. Target: each module under 3 minutes, a single test under 20 s cold, and a per-module wall-time budget enforced so it cannot regress — tools/*_bridge.py, tests/ — OWNED:test-latency
- [x] PKT-176 (done by harness-repair) The Python owner bridge did not boot on dev: host/owner-host.lisp called fn-rcon-ocfg-io, fn-rclb-existing-action and the fn-octets stobj, which only host/native/build.lisp included. owner-host.lisp now includes them itself; tools/build_lists_check.py applies its `included` rule to every bridge load list (tools/bridge_image.KINDS). Behind it: Acl2Owner.prov_post read the store bridge's uninstalled config (every intent refused), feed_filename_decode fed octets to the symbol reader, the conflict word was `refused`, and an 81 KiB boot transcript deadlocked tests/test_owner.py's stderr pipe for 300 s — planning/evidence/harness-repair-2026-09-25.md
- [x] PKT-177 (done by harness-repair; completes PKT-162) Eleven launchers started a bare ACL2 outside the pool and heap cap (auth_secret, stx, run_reader, host_check, run_simulator, cert_alists, proof_artifacts, and four test drivers). tools/acl2_slots.py run/popen/tree_slot is the one launch path; tests/test_acl2_launchers.py reads the code for any other, in make check.
- [x] PKT-178 (harness-repair; native sweep done by harness-repair-2, no order-dependent leftover in 60 native modules, 416 tests; remainder is PKT-248) Shared test worlds: tools/test_budget.py --order reverse finds a test relying on an earlier one's leftovers; the machine's git configuration leaked into synthetic repositories (commit.gpgsign: test_triage over budget). Open: a reverse-order sweep of every module in the qualification, and native image modules are outside the ACL2 launcher rule (hbox cgroup only).
- [x] PKT-179 (harness-repair; the cross-process cache done by harness-repair-2 1b49bbdc: content-addressed build/cache/ledger-tree, make check 90.5 s to 72.5 s warm, outputs identical) make check cost: 141 s to about 100 s by removing repeated work (load_tree x5 in teeth_check, closure edge resolution in proof_cost, per-token stats in cite_check, two tree analyses in harness_check), outputs identical. Open: each checker process still analyses the tree once (~5 s x 6 processes); a cross-process cache keyed by content would take most of it.
- [ ] PKT-173 peering-compose decision packets for ember (planning/evidence/peering-compose-2026-09-25.md): (a) packet 7, declined key statements at reopen: IMPLEMENTED as the recorded disposition (decided under the configuration in force at the statement's txid), switch `*fn-ks-reopen-policy*` :recorded | :current in books/key-statements.lisp; (b) packet 4, hold :unverified evidence as a charged record: recommended NO (keep refusing; the verdict names the class); (c) packet 5, pull cursor: recommended keep the FNPL files (cut campaign green at six cuts); (d) packet 8, pull over TLS + principal binding: designed (reuse fn-fc-step as the preamble), NOT implemented -- continuation lane — OWNED:peering-compose

## Sweep 2 (wave-2 deputy, 2026-09-25 ~23:40 UTC): dev 24591e11..483987b1, spike/mega b4d0184c..f6d947d0

Sources: 394 dev commits (61 merges), the 55 evidence records added or changed in the window, the 50 LANEDUMP copies in build/coordinator/, the spike/mega records spike-storage and spike-mission at f6d947d0, and the Fable mandate (§5 to §12 and the coordinator's reconciliation). Packets that are a wave-2 brief's headline carry the brief's reserved number (PKT-164 to PKT-179) and are not repeated here; the lines below are the specific findings. Existing packets closed in the window are ticked above with their hash.

### Bounds (D27)

- [x] PKT-180 The width-boundary brief's "blocker" (saved format-8 presets at R = 17,138,486 invalid at open) was already repaired at the bounds-p6 merge: `fn-record-encoded-octets-ceiling` is back at the u32 overhead (1,083) with a separate wide ceiling, and `fn-bs-profile-v1-valid-stays-valid` has a witness at R = 17,138,486. Still owed: the native open of a bbf52159-written store and the status comparison on an image — books/byte-store-frame.lisp, byte-store-profile-v1.lisp — 0a117169 — OWNED:width-boundary — closed 03be0a78 (bbf52159 stores open byte-unchanged under the u64 schema on the native gate)
- [ ] PKT-181 Charge above 2^32-1 is refused at the POST boundary, and the retention ledger's codecs have not been audited for u64 charge — books/retention*, the POST boundary — bounds-p6 finding 3 — OWNED:width-boundary
- [ ] PKT-182 Overbound frames: a request frame past the owner's read bound reads as `:overbound` and is answered `refused`, but a client still writing sees a closed connection and reports uncertain (exit 3); this is reachable for an article about 17 MB past A — host/native owner frame read, `fnn-control-read-frame` — bounds-blob §6 — GOAL-UNOWNED (§5.7)
- [ ] PKT-183 Composed only in prose: the namespace after an accepted publication has GENERATION entries (contiguity), which the writer and listing keystones need; the admin's serialized credential file is checked (`:fault :serialized-profile`), not proved, within the loader's octet bound — books/native-auth*, config namespace — bounds-profile "Not done" — GOAL-UNOWNED (§6)
- [ ] PKT-184 `fn-owner-live-post-config` still hands the control-signed path the codec ceiling (`fn-owner-post-config`), not the profile's served bound (`fn-osb-config`) — host/native/hybrid-control.lisp — operator-verdicts "Not done" — GOAL-UNOWNED (§5.5)
- [ ] PKT-185 The BP receiver's fragment caps stay: `fn-bpf-inputsp` and `fn-bpf-fragmentp` keep 65,538 x 64, and `fn-bpf-canvas`'s guard is the capped recognizer, so the uncapped exactness theorem is logical only; the sender's `*fn-bpb-max-data*`/`-input*` 1 MiB and the ADU read bounded by the transfer MRU argument remain — books/bp-fragment*.lisp, bp-bundle, host bp-service run — bp-n16-prod "What remains" — OWNED:bp-lifecycle (supersedes PKT-004's BP rows, which pack-chain-serve's brief does not cover)

### Representation (D27)

- [ ] PKT-186 At N = 10,000 the native POST path stalls: a profile of the stalled run is 79 % in `SB-IMPL::QUERY-FILE-SYSTEM` (fstatat, PARSE-NAMESTRING, WILD-PATHNAME-P) in the per-transaction file publish path, whose cost grows with the files in the transaction directory; the rep-wave-c after-image never finished N = 10,000 within 600 s — host/native/io.lisp publish path — rep-wave-c §7 — GOAL-UNOWNED (§7; rep-wave-d's N = 10,000 baseline will hit it first)
- [ ] PKT-187 `fn-owner-peer-carrier-form` (`fn-pa-carrier-form`, D23) runs `fn-article-parse` over a payload list on every served POST, beside the C1 filing plan (PKT-034); both are boundary 9 — host/owner-host.lisp, `fnn-owner-attempt-transit` — rep-wave-c §5 — OWNED:rep-wave-d
- [ ] PKT-188 The control request is built by client and owner as an octet list and `fn-frame-decode` walks it as conses; the native bounds-blob run stops at 290,000 octets for that reason — books/frame-fields.lisp `fn-frame-decode`, host control read — bounds-blob §6 — OWNED:rep-wave-d (boundary 8)

### Owner and checkpoint

- [ ] PKT-189 Two quadratic node checks at open: `fn-node-articles-have-archive-bindingsp` (`fn-retain-find-id` per article) and `fn-articles-freshp` with `fn-all-article-memberships` rebuilt per article; with the file decode and `fn-record-p` they keep the checkpoint open at full-replay cost (7.7 s against 7.8 s at N = 4096; reopen about N^1.6) — books/node*, acceptance — checkpoint-cost finding 1 — GOAL-UNOWNED (§7)
- [ ] PKT-190 Each connection's greeting costs 1.4 s at N = 4096 under the owner mutex: whole-state work on a served path (AGENTS.md) — host/native/owner.lisp greeting, owner open-connection — checkpoint-cost finding 2 — GOAL-UNOWNED (§7)
- [ ] PKT-191 At N = 100k (spike/storage): 86 ms per POST, full replay 3,870 s, and the checkpoint capture exhausts 32 GB — `fn-sco-capture`, the owner commit path — spike-storage record, 082ba341 — GOAL-UNOWNED (§7, §8)
- [ ] PKT-192 Untoothed hypotheses: the history hypothesis of `fn-ock-next-checkpoint-is-the-capture`, and the `:ok` hypothesis of the Store-open replay conjunct — books/owner-checkpoint-open.lisp, store-checkpoint-open — owner-checkpoint-open Teeth; checkpoint-cost LANEDUMP — GOAL-UNOWNED (§6)

### History reclamation (D13)

- [ ] PKT-193 The spike/storage keystones dev owes for reclaim: the archive release as a Store event (`fn-node-statep` relaxed to pins or releases; the release frees its charge; D13's authority policy for an operator release); `fn-spk-reclaim-preserves-decisions` including the checkpoint commute; the stub identity invariant; the stub codec round trip and the D25 keystone under a constrained `A-DIGEST-COLLISION-FREE`; the `FN-Reclaimed:` refusal checked only at the owner's prepare, and `store post` can still store a tombstone-shaped payload — spike-storage deferrals 3 to 6; 7d6a13c3 — OWNED:reclaim-lifecycle
- [ ] PKT-194 The reclaimed record's own replay step (`fn-accept-complete` of the tombstone-staged record equals reclaiming the original) is unproved (the direct proof split on `floor`); articles with an authorship verdict are refused rather than reclaimed with the verdict kept — books/store-reclaim.lisp — reclaim-d13 "Not done" 4, 7 — OWNED:reclaim-lifecycle
- [x] PKT-195 The maintenance reservation: admission must leave room for the release, the rotation and the cleanup debt across Store and BP namespaces (an explicit reservation invariant) — mandate §8 — OWNED:reclaim-lifecycle — closed for the Store 3ba3f9c6 (PRF-129, books/store-maintenance-reserve.lisp: every admission other than a release leaves one release record, 4,096 octets and one transaction; `fn-cverb-pack-fits-the-disk`, `fn-rclp-pack-fits-the-disk`); the BP namespace's rotation reserve is PKT-299
- [ ] PKT-196 History compaction as a semantic summary (distinct from packing and reclamation): it must preserve future duplicate/conflict decisions, allocation high waters, key/policy evidence, obligations, consumer scope, topic/control dependencies and the marker's meaning; no lane builds it, and pack-chain-serve must not describe `store compact` as it — mandate §8 — GOAL-UNOWNED (§8)
- [ ] PKT-197 The chain's keystones from spike/storage: the link codec round trip, PRF-073 over a chain, the crash keystone for each link and selection cut, a chain-length bound in the profile (the spike walks up to 10^6 links in host code) — books/pack-chain*, checkpoint-publish — spike-storage deferral 8 — OWNED:pack-chain-serve

### BP node and DTN

- [ ] PKT-198 Held-transit fragmentation: `fn-bpnp-forward-scan` records an `:mru` wait for a held row larger than the session MRU and never cuts it; only the base-job send (`fnn-bps-send-effect`) fragments. No per-fragment custody: a partial send is `:uncertain` and the whole job is retried. `fn-bpfs-plan` has no D27 twin — books/bp-node-progress.lisp `:session` arm, bp-fragment-send — bp-n16-prod "What remains" — OWNED:bp-lifecycle
- [ ] PKT-199 N16 remainder: a killed rotation's unselected new-generation directory is kept and never retired; the removal model is an alist, not K0's byte store; `fn-bpnr-retired-namep`/`-retired-names`/`-retire-ops` are not guard-verified. (The merge-doubled block in host/native/bp-node.lisp that the bp-lifecycle brief lists was removed at the merge, test_bp_node_native 27/27, log 04446209.) — books/bp-node-retire.lisp — bp-n16-prod "Not done"; acac63d8 — OWNED:bp-lifecycle
- [ ] PKT-200 Exit codes after routing: a `:failed` connect exits 0 where nine bp_service and one bp_contact case expect 3 (qual C1); `bp-obligation request` with no route exits 1 after a durable attempt (qual C2; "is exit 1 right" is the BP owner's question); verbs without a Store (`bp-service run/resume`, `bp-contact tick`) keep the queued address; a no-route request needs a new request once a route exists — host/native/bp-*.lisp, tests — qual-bbf52159 C1, C2; bp-routing-2 §9 — OWNED:bp-lifecycle
- [x] PKT-201 A durable re-route record: a table change after queueing holds the job `:route-changed`, and because the lower machine offers the first queued job for a peer it also holds younger jobs to that peer (head-of-line) — books/bp-node-machine.lisp, bp-node-contact-driver — bp-routing-2 §1, §9 — OWNED:mission-four-node (selection fairness) — closed b007bb82 (the head-of-line: `fn-bpnj-contact-next` offers the first ready job; the held job's durable re-route stays with PKT-148)
- [ ] PKT-202 One transit decision for NNTP and BP (K6): `operator run` and `bp-node serve` each take the store's writer lock, so a relay carries two stores bridged host-side (`store post` in, `store inspect` out, `operator post`) — host/native/owner.lisp:460, bp-node.lisp:703 — spike-mission deferral 1 — GOAL-UNOWNED (§9; mission-four-node will meet it)
- [ ] PKT-203 A BP store cannot enrol an author: `hybrid-enroll` needs an owner's control socket and the DTN image has none — host/native/hybrid-control.lisp, build-dtn — spike-mission deferral 4 — GOAL-UNOWNED (§9)
- [ ] PKT-204 The mission's return receipt past B (NNTP has no application receipt; C's receipt releases B's BP obligation while A's obligation to B is B's feed reply), and contacts as a modelled plan rather than a TCP proxy pair and dtn7 static routes — tools/mission_lab.py — spike-mission deferrals 2, 5 — OWNED:mission-four-node
- [ ] PKT-205 `bp-node-progress-guards` (10.94 s) and `bp-node-retire` (10.79 s) are include time: move `fn-bpnr-selection-plan`/`-plan-*` into the codec book so the retire book stops loading the machine — books/bp-node-rotation*, bp-node-retire — bp-n16-prod certification — GOAL-UNOWNED (§14)

### Control (D29)

- [ ] PKT-206 C3 owner relation: `fn-own-relation` carrying WS = journal(...) and the configuration-txid order (the journal keystone's hypotheses are discharged by nothing); owner-level teeth for `fn-own-read-archive-command-is-the-pinned-dispatcher` (a reachable owner witness, a must-fail per hypothesis); the served numbering keystone's "nothing" arm has no witness — books/owner-invariants.lisp, control-served — control-c3e "Still not done"; control-c3b 5 — GOAL-UNOWNED (§5.4, §6)
- [ ] PKT-207 C3 cost: a Message-ID-to-txid index (`fn-ctl-record-txid` decodes composites per walk); the owner's IHAVE/CHECK history test falls to its scan once any target is withdrawn (a raw trie beside the visible one restores the fast path) — books/control-*, peer-offer-indexed `fn-pix-history-hasp` — control-c3b, c3d — GOAL-UNOWNED (§7)
- [ ] PKT-208 A view whose group index is nil pins no control, so it answers the plain 423/430 rather than "withdrawn"; `:control-signed` stays in the served-reason tables with nothing producing it (an unreachable branch: mark it or remove it) — books/nntp-post.lisp `*fn-pa-served-reasons*`, control-served — control-c3e, control-c3 finding 4 — GOAL-UNOWNED (§5.4)
- [ ] PKT-209 A proof that the carrier render preserves the Control field (where the signed source and the filing plan disagree the constructors build no event); the live reconfiguration reply omits the refusal reason (`:no-such-grant`); operator verbs `control evidence MSGID` and `control log` do not exist — books/control-classify, hybrid-store, native-admin — control-c3 finding 2; control-c2c3 finding 4; spike-control "Also not done" — GOAL-UNOWNED (§5.4, §11)
- [ ] PKT-210 Two-node control cases (authority, no grant on two nodes, the kill between arrivals, a reader pinned on the other node): the two-store harness has no enrolment or grant step — tests two_store_join — control-c3b "Not done" — OWNED:source-corpus (cancel order across two nodes)

### Peering

- [ ] PKT-211 Succession-era invitations (a principal whose current keys are not its genesis keys) are refused `genesis`; the carriage trace theorem fixes one budget (not a lowered-budget sequence); `peer list` does not render the budget; no lab row reaches the unsupported-profile or signature-failed class natively — books/peer-invite*, peer-carriage — peer-invite, peer-carriage "Not done"; spike-peering Limitations — GOAL-UNOWNED (§5.4)
- [ ] PKT-212 A refused kind-3 commit (Store budget) surfaces as the transit attempt's refusal after its kind-4 composite committed; `tools/fn_verify.py` does not render `revoked` — host/native/owner.lisp `fnn-owner-key-statement`, tools/fn_verify.py — peer-keys "Not done" — GOAL-UNOWNED (§5.7, §11)
- [ ] PKT-236 peering-tls-pull, not finished: (a) `peer accept` still configures no peer on the acceptor's side: the invitation has no inviter address (its `Host`/`Port` fields are the INVITEE's, which `peer confirm` uses). Design: invitation kind `invitation fn-peering-v2` adding `Inviter-Host`/`Inviter-Port` (v1 still decodes, and accepts without configuring), `peer invite` gains the inviter's HOST PORT words (books/native-operator.lisp grammar), and an accept record plan beside `fn-pinv-confirm-record-plan` that consumes nothing and publishes one `(:set-peer INVITER ...)` record bound `(:principal INVITER)` with the invitation's Groups as inbound, plus the fold keystone and its witnesses — a wire change between nodes, so both ends must run the v2 image; (b) the pull's duplicate-replay bound after a close cut is proved (PRF-100) and observed natively only in the clear: through TLS the recording proxy cannot count ARTICLE commands; (c) no native case runs the loopback-lab exception against a server whose `protected_only` is false; (d) a peer record carries one credential policy for both directions (the outbound profile), so a credentialed pull needs `outbound` groups and B also feeds A; a pull-only credential slot would separate them — books/peer-invite.lisp, books/peer-pull-session.lisp, books/peer-config.lisp — peering-tls-pull "Not done" — GOAL-UNOWNED (§12 packet 8)
- [ ] PKT-213 Pull liveness: a peer that keeps listing a Message-ID it cannot produce (430) holds the cursor until it stops listing it — books/peer-pull*, scheduler-peers — peer-pull "Not done" — GOAL-UNOWNED (§5.7)

### Marker (D31)

- [ ] PKT-214 U1b: a hand copy of the kept format-7 `config.json` over a *required* store still opens under c3420013 and drops the requirement; `rollback-check` now refuses it by name, but the file copy itself is unguarded (the mandate: a refusing verb is not a proof that file replacement is impossible) — store config, history-required — qual-bbf52159 U1b; node-hbox-bbf52159 "Next" — GOAL-UNOWNED (§5.6)
- [ ] PKT-215 Stated marker limits: a shared-lock reader may serve a record above the marker until the next writable open; K0 is not stated at the catch-up cuts (the catch-up is the marker program after the recovery barriers) — books/store-history-required.lisp, byte-store-k0-marker — marker-required "Stated limits" — GOAL-UNOWNED (§5.6, §6)

### K0

- [ ] PKT-216 K0 gaps not in PKT-086: the root barrier's error outcomes over a pending frontier rename (only `:ok` is a step kind); the administrative publication at admin.lisp (config directory, staging cleanup) has no byte program, so the relation at line 107 is a hypothesis of the host theorem — books/byte-store-k0*.lisp, host/native/admin.lisp — k0-cuts, k0-recovery "Not done" — GOAL-UNOWNED (§6)
- [ ] PKT-217 PKT-087 follow-ups: the BP receipt theorem over `fn-cst-relation` for a store reconfigured after an event; a sharper reconfiguration hypothesis than txid 0 (for example "no capacity reduction after a release"); whether the deployed node's configuration journal satisfies `fn-sonb-configured-before-eventsp` (not checked) — books/store-open-node-bridge.lisp — k0-steps LANEDUMP — GOAL-UNOWNED (§6)

### Python host and harness

- [ ] PKT-218 Tests older than the behaviour, unchanged across two qualifications: live_reconfiguration (live `peer list` answered; `-over`), control's `operator post` with a supplied Path (D32 accepts), key_statements' carried IHAVE without a carriage budget, hybrid_author's article cap, consumer_project_bounds `codec` vs `limit`, the kill harness's over-limit band (33,600 octets under a 16 MiB profile) — tests/test_native_* — qual-bbf52159 C3 to C7, C9 — OWNED:harness-repair (key_statements' carried case given its `peer budget` at 0a1f2d54; the rest open)
- [x] PKT-219 `native_developer_selectors_raw` fails on `fnn-core` undefined in `fnn-recovery-test-fault` (a raw-harness stub gap beside C13); python-store-f8's modules not shown green (test_store_corruption's fixed family, three test_checkpoint and three test_store cases that timed out booting) — tests/native_*_raw.lisp, tests/test_store* — operator-verdicts LANEDUMP; python-store-f8 "Not yet shown green" — OWNED:harness-repair — closed 5c520e61 (hbox raw_scripts 9/9, C13 retired; the store modules green on the laptop)

### Operator surface

- [ ] PKT-220 Offline-only status verbs: `bp-obligation status`, `store retention` (and `operator status` in the mission lab) stop the serving node to read; the health states "outstanding receipt debt" and "stranded transfer" need them live — host/native bp-obligation, retention — spike-mission deferral 3 — OWNED:operator-health
- [ ] PKT-221 A login-to-principal binding change needs a restart (credentials are startup-pinned) — books/login-binding.lisp, host auth — path-and-login LANEDUMP — GOAL-UNOWNED (§11)

### Reader

- [x] PKT-222 The XPAT served-step theorem is stated at `fn-nntp-step-pinned`, not lifted to the host-called `fn-served-step`; NNTP has no thread command (a deferral the reader's threading must answer) — books/nntp-*, served — reader-surface LANEDUMP; cbd78ffc — OWNED:reader-daily — closed b6c6e19a (PRF-122 over the served chain `fn-owner-chunk` to `fn-nntp-step-pinned`; threads are XPAT References over a stated window)

### Cost (D26)

- [ ] PKT-223 The signed POST's identity prepare still replays the appended history (`fn-ccar-sn-prepare-identity` keeps `fn-sf-history-recoverablep` over `(append records (list event))`: O(N·L) per signed POST); the article prepare avoids it under the owner relation, the identity event has no such correspondence — books/store-prepare-correspondence, carry-kind — carry-kind "Not done" — GOAL-UNOWNED (§7)
- [ ] PKT-224 `fn-replay-apply-record` and `fn-replay-identity-step` still dispatch through `fn-stxa-p` and decode the composite on the prepare, the gate and the finish (carried twins over `fn-store-event-p` are the continuation) — books/replay.lisp — carry-kind "Not done"; db87dc76 — GOAL-UNOWNED (§7)
- [ ] PKT-225 Books over 10 s not in PKT-114, from the window's runs: config-owner-live 12.3 to 15.1 s (scope the global `fn-ocl-projectionp-of-a-visible-state` / `fn-ocl-projection-keeps-state-fields` rules to their hints), consumer-store-invariants 12.1, bp-node-forwarding-teeth-tests 11.9, bp-node-busy-delivery 11.99, bp-route-step 11.54, bp-report-guards 11.1, nntp-auth 11.24, nntp-list-counts-tests 11.04, native-admin-peer 10.4, topic-history-store-invariants 10.4, store-reclaim 10.8; the proof_cost baseline was widened under load (`--allow-regression`) — books/* — 67c6eaa5; ten-second-3, control-c3b, checkpoint-cost LANEDUMPs — GOAL-UNOWNED (§14)

### Qualification

- [ ] PKT-226 What bbf52159's qualification did not cover: the pull between copies was plaintext with no auth on B; the key-statement rehearsal did not revoke; per-cut evidence is coordinate-precise only on the developer twin; one run on one box over loopback, process death not power loss — the next cut's qualification lane — qual-bbf52159 "What this does not establish" — GOAL-UNOWNED (§15)
- [ ] PKT-227 The spike/mission lab's four nodes have been serving on hbox for about 14 h (`/tank/fn/gates/spike-mission-0e2173ab`); stop them with the lab's `down` when the mission lane no longer needs them — mandate reconciliation — OUT-OF-GOAL (deputy housekeeping)

### Decisions (mandate §12; now.md §5)

- [ ] PKT-228 §12.2 News-only restore and numbering (PKT-100): an older backup reissues article numbers readers saw (RFC 3977 §6). (a) advance every group's counter past a gap, (b) a new numbering epoch, (c) same-identity restore only from a known-latest backup — books/checkpoint-auxiliary.lisp:75 `fn-cpa-rollover-proposal` — operator-verdicts "Not done" — WAITS ON EMBER — packet written 7cd09f39 (planning/decisions-packets-2026-09-25.md packet 2: recommends a witness-driven `store rebase` with a durable per-group numbering floor, `:same-history` only when the witness matches, `:uncertain` without one; option (b) rejected, (c) kept only as the witness-checked branch; the docs warning is PKT-243)
- [ ] PKT-229 §12.3 The policy-member count (field 13, 64) sits inside a signed statement's codec; a per-node limit would let two nodes disagree on one statement's validity. Keep a protocol work bound, or version the codec — books/policy `fn-pol-policy-of-items` — bounds-profile "Not done" — WAITS ON EMBER — packet written 7cd09f39 (decisions-packets packet 3: keep 64 as the schema-v1 protocol bound, field 13 reserved (admitted only at >= 64, never a validity input); found: field 13's reader `fn-bs-profile-max-policy-members` has no caller, so the advertised knob does nothing today, and no served path reaches policy)
- [ ] PKT-230 §12.4 Refused signed articles: hold them with a durable `:unverified` verdict naming the class (a new kind-4 composite, charged before promised, D23 kept), or keep refuse-and-log — books/peer-authored-accept, stx-verify — peer-carriage "Not done" — WAITS ON EMBER — packet written 71701343 (PKT-173 b: recommends keep refusing; the seven-class verdict is proved, PKT-240)
- [ ] PKT-231 §12.5 The pull cursor as `<store>/pull/*.fnpl` files or a Store record family (about 25 books; the configuration history would use `max-config-generations`) — books/peer-pull* — peer-pull "Not done" — WAITS ON EMBER — packet written 71701343 (PKT-173 c: recommends keep the FNPL files; the six-cut native campaign is green)
- [x] PKT-232 §12.6 The added keystone hypothesis: `fn-bs-profile-admits-every-article-record` requires the record to fit u32 widths, stated not proved; width-boundary's PRF-123 discharges it at the host's call or implements frontier-3 — books/byte-store-frame — 0a117169 — OWNED:width-boundary (returns to ember only if the producer theorem fails) — closed 03be0a78 (PRF-123 discharges the width hypothesis at the host's sequence; frontier-3 is PKT-244's)
- [x] PKT-233 §12.7 A declined key statement that is still the newest record is decided again at the next open, so a `keys` grant added across a restart lets it act: durable decline, or explicit re-evaluation — books/key-statements.lisp `fn-ks-recover` — peer-keys continuation 2 — WAITS ON EMBER (peering-compose implements the switch) — closed 4b710f03 (the recorded disposition: the open decides under the configuration at the statement's txid, a decline replays as a decline natively; ember's default `*fn-ks-reopen-policy*` is PKT-173 a, the re-evaluation verb PKT-241)
- [x] PKT-234 §12.8 Plaintext pull: the pull client has no TLS arm; a loopback lab exception, if any, must be explicit in the spec — host pull client — peer-pull "Not done" — OWNED:peering-tls-pull (PKT-236; designed in the peering-compose record, packet 8) — closed 3bb735fa (the pull runs over STARTTLS as its own principal; a clear transport with a credential is refused before dialling except the named loopback-lab exception in specs/peering.md, NNT-023, PRF-125; the remainder is PKT-236)
- [ ] PKT-235 The live node's `store upgrade-profile --history-marker required` "after a day of service", with a fresh snapshot (after it, c3420013 cannot open the store); the mandate withdrew the deputy's deploy authority, so this is ember's go — /tank/fn/node — node-hbox-bbf52159 "Next"; mandate reconciliation — WAITS ON EMBER

## Sweep 3 (wave-2 deputy, 2026-09-26 ~00:25 UTC): dev 483987b1..abcb21d2

Sources: 11 dev commits (2 lane merges: peering-compose 71701343, visibility-join ebae2d0e; the sweep-2 commit c086f0e4; the decision packets 7cd09f39; two ledger regenerations), the records planning/evidence/peering-compose-2026-09-25.md and visibility-join-2026-09-25.md, LANEDUMP-peering-compose.md and LANEDUMP-visibility-join.md, and planning/decisions-packets-2026-09-25.md. spike/mega is unchanged at f6d947d0. Edited in place above: PKT-233 closed (4b710f03); PKT-228 and PKT-229 carry the 7cd09f39 packets; PKT-230 and PKT-231 carry PKT-173's recommendations; PKT-234 moved to peering-tls-pull; PKT-218 notes one repaired test. PKT-173 stays as the merge wrote it. The pull cut campaign's process-kill-only scope (after-write survived through the page cache) is PKT-226's class, not a new packet.

- [ ] PKT-164 (visibility-join's reserved id, recorded in its record) A privileged identity query for the unresolved lost-reply case: answer the D25 question (`already stored` / `different article`, nothing else) before the login and posting gates for a resend under a held Message-ID, or keep dev's default (the client reports unresolved, exit 4; the operator settles it with `store inspect`). The coordinator recommends no: 440 first as RFC 3977 orders it, and a held Message-ID must not be probeable by an unauthorized poster — host `fnn-owner-attempt-served` order, books/login-binding.lisp, NNT-013 — visibility-join record "PKT-164"; ebae2d0e — WAITS ON EMBER
- [ ] PKT-237 The reclaimed-tombstone arm of SCN-060 (a lost reply, then reclamation, then `reconcile` answered 441 by the tombstone digest) has no native run because dev has no host reclaim verb; proved and witnessed only in books/visibility-join (`fn-vj-reclamation-keeps-a-held-message-id-answered`). Run it natively once the per-article release verb lands — tests/test_native_visibility_join.py — visibility-join "Not done" — OWNED:reclaim-lifecycle (the verb; the native case follows it)
- [ ] PKT-238 The login-rebinding case (a login bound to a principal after the lost reply draws `441 ... this login posts only articles signed by its bound principal` from the login gate, before the Store's decision) is not run natively; the client contract calls it unresolved, the same class as the 440 case that was run — tests/test_native_visibility_join.py, host `fnn-owner-attempt-served` — visibility-join "Login rebinding"; LANEDUMP Open — GOAL-UNOWNED (§5.1, §15)
- [ ] PKT-239 "The same source is `:duplicate` across a completion" is witnessed, not proved in general: `fn-statep` carries no Message-ID uniqueness invariant, so the keystone concludes only membership in `(:duplicate :conflict)`. Owed: the uniqueness invariant, established and preserved by `fn-sn-finish`, and the sharpened keystone — books/visibility-join.lisp, the Store state invariant — visibility-join LANEDUMP Open; ebae2d0e — GOAL-UNOWNED (§5.1, §6)
- [ ] PKT-240 `fn-pcb-admission-verdict` (seven classes, keystone `fn-pcb-admission-verdict-names-its-class`) has no host caller; the transit refusal detail still prints `fn-pcb-refusal-class`. Wire the served transit log to the verdict (with a theorem equating the two on the refusal arms) or mark it unreached — books/peer-carriage.lisp, host/native/owner.lisp `fnn-owner-attempt-transit` — peering-compose "Assurance chains" 3, "Not done" — GOAL-UNOWNED (§5.4)
- [x] PKT-241 No explicit re-evaluation of a declined key statement: re-deciding under today's grants now needs a new statement; a `keys redecide MSGID` operator verb was named and not built. Goes with ember's choice of the `*fn-ks-reopen-policy*` default (PKT-173 a) — books/key-statements.lisp, native-operator — peering-compose packet 7 "Rejected alternative", "Not done" — WAITS ON EMBER — closed fc99c076 (superseded: the `:current` switch is gone and `keys redecide MSGID` is specified, not built, as PKT-325)
- [ ] PKT-242 The invitation carries no inviter address, so `peer accept` configures no peer on the acceptor's side (only `peer confirm` does, in one record); and the confirmed peer is a clear transport (TLS for it is `peer add` of the same name). Owed: `Inviter-Host`/`Inviter-Port` as a versioned invitation field (a wire change between nodes), `accept` configuring the peer, the confirm keystones' witnesses extended — books/peer-invite.lisp, invitation codec — peering-compose "Not done"; LANEDUMP Open — OWNED:peering-tls-pull (brief task 3)
- [ ] PKT-243 docs/operator.md "Back up" teaches copy-back (stop, copy the store directory) with no warning that restoring an older copy reissues article numbers readers saw (RFC 3977 §6, §6.1.1.2); decisions-packets packet 2 lists the warning as work that continues without the decision — docs/operator.md:1192 "Back up" and "Recover" — 7cd09f39 — GOAL-UNOWNED (§12.2; a docs-only step)
- [ ] PKT-245 ML-DSA-65 signing is randomized: a retrying client must resend the saved signature bytes and never re-sign (a re-signed source is a changed source, REFUSED by hybrid-author after PKT-166); docs/agents.md must say so and the web client must freeze the signed bytes of its drafts (visibility-join's `post --draft` does for the client's own) — docs/agents.md, tools/fn_client.py, tools/fn_web.py — source-corpus merge 6cfe702e — OWNED:source-corpus-2 (the docs sentence), GOAL-UNOWNED (the web client's drafts of signed posts, if it ever signs)
- [ ] PKT-246 BP carriage of the source corpus: the refusal of every A-injected element (source-corpus-2 native-3/native-4) was mission-signed's layer 1 (the transit lookup read the Message-ID with the injecting check), fixed by `fn-bpaj-transit-article-fields`; on the dev 43fad7ab merge the six unsigned elements arrive at B as A's records with B's splice (source-corpus-2-2026-09-26/native-5.log). The signed carrier followed once mission-signed-2 (PRF-132) bound kind-4 acceptances: all seven arrive (native-7.log at 1ec78422). Still open here: the FNCT conflict word (packet in the record; default append :conflict, a refusal, exit 1), waiting on the coordinator — books/native-control.lisp, host/native/hybrid-control.lisp — source-corpus-2-2026-09-26.md "2026-09-26, source-corpus-3" — WAITS ON COORDINATOR
- [ ] PKT-248 (harness-repair-2) What the harness repair left: seven native modules have a test over the 20 s budget on hbox (bp_node 21 s, native_checkpoint 37 s, control 126 s, history_required 30 s, peer_pull 122 s, served_crash_model 132 s, state_checkpoint 79 s; split or examine, never raise the budget); the reverse sweep ran on bbf52159 images with a tree 85 commits newer (sweep again on an image of the tree to separate skew from behaviour); production-image C10 selector cases (bp_service 7, peer_invite 1) fail instead of being structurally developer-only; make check's remaining cost is outside the tree analysis (session_depth, harness_check, teeth_check, cite_check) — planning/evidence/harness-repair-2-2026-09-26.md — OWNED:harness-repair-2
- [ ] PKT-244 (width-producers-2) The remaining width migration, found by following the transaction ID and the stamp past 2^32 - 1 (mandate §5.5, §12.6). R needs no change (PRF-126: a record with a u32 charge is within the ceiling at any other width) and the frontier frame reads u64 (format 3), but the allocator's successor stays at 2^32 - 1 and the stamp producer at 2^32 s because their readers are u32: (a) the file machine `fn-sf-statep` / `*fn-sf-max-uint*` and the K0 recovery books that carry `(fn-record-uint32p f)`, (b) the observed open `fn-sn-open-observed` and `fn-sn-observed-historyp` (books/store-observed.lisp), `store-open-bridge`, (c) the checkpoint: `fn-checkpointp`, `fn-checkpoint-capture`/`-restore` refuse a frontier past u32, and the codec's TREE naturals (`fn-cpc-treep`, `*fn-cbor-max-uint*`) cannot carry a txid or a stamp past it, so a store past 2^32 could not be checkpointed or reopened from its checkpoint, (d) the consumer candidate bound `fn-csi-candidate-sequence-below-max` (count below 2^32 from a u32 frontier), (e) compaction `fn-cverb-pack-decision-capture-succeeds`. Order: checkpoint codec u64 naturals (a schema bump with a translation keystone, as records schema 2), then the model (a)-(b)-(d)-(e) with the frontier u64, then widen `fn-bs-frontier-next` and `fn-record-stamp-of-observation` (the stamp needs a local floor lemma, arithmetic-5 scoped) and run the developer-twin frontier at 2^32 - 2 natively. Also: the signed composite (kind 4) and peer-carried producers are not charged `fn-sbud-article-figure` before reservation (they check R on their bytes; host/native/owner.lisp `fn-owner-signed-event-boundary`, `fn-owner-peer-carried-event`); `host/bp-ingress-host.lisp` `fn-bpi-host-article-verdict` re-composes the ingress parse and group map in host Lisp and should be one ACL2 function with a theorem equating its counts to the record `fn-bpi-ingress-prepare` stages; `tools/run_bp_receive.py` keeps two Python size checks (65 538, `max_payload_bytes`) — books/store-files.lisp, books/store-observed.lisp, books/checkpoint.lisp, books/checkpoint-codec.lisp, books/consumer-store-invariants.lisp, books/records-stamp.lisp — planning/evidence/width-producers-2-2026-09-26.md "Not done" — GOAL-UNOWNED (§5.5)

## Sweep 4 (wave-2 deputy, 2026-09-26 ~00:55 UTC): dev abcb21d2..51808446

Sources: 57 dev commits, 17 on the first-parent line (six lane merges: width-boundary 03be0a78, source-corpus 6cfe702e, reader-daily b6c6e19a, consumer-e2 4aa3ab61, harness-repair 5c520e61, mission-four-node b007bb82; certification batches 1 to 3; the sweep-3 commit; PKT-245), the records bounds-p6 (its width-boundary sections), source-corpus, reader-daily, consumer-e2, harness-repair and mission-four-node (all 2026-09-25), and the six LANEDUMP copies in build/coordinator/. spike/mega is unchanged at f6d947d0. Each continuation's headline is its reserved packet and is not repeated: width-producers-2 PKT-244 (frontier-3, the stamp past 2106, the kind-4 and peer-carried producers, the served gate natively, the Python BP and owner clients), source-corpus-2 PKT-246 (BP carriage of the corpus, the relay-projection theorem, the conflict word; the PKT-245 docs sentence), mission-signed PKT-247 (the refusal reason logged, the signed mission, PKT-170 as the custody theorem PRF-128), harness-repair-2 PKT-248 (the C1 listener, the native reverse-order sweep, the deploy-gate overlay, the cross-process tree cache). PKT-165 (a retaining consumer mode, consumer-e2) and PKT-175 (a served `:fn-enrollment` HDR item, reader-daily) are ember's, on file in their records, and not repeated. Ticked above: PKT-011, PKT-151, PKT-180, PKT-232 (03be0a78); PKT-125 (4aa3ab61); PKT-222 (b6c6e19a); PKT-092, PKT-219 (5c520e61); PKT-201 (b007bb82).

Ownership moved by the window's merges (the lines above keep their original mark): PKT-178's native reverse-order sweep and PKT-179's cross-process cache are OWNED:harness-repair-2, as is PKT-200's C1 (a connect with no socket is `:failed` exit 0 since 78992f89; the harness, not the node); PKT-181 (charge past 2^32-1 and the retention ledger codecs) and PKT-218's remainder (live_reconfiguration's C3 and control's C4 still fail one test each on hbox; hybrid_author, consumer_project_bounds and the kill harness were not rerun) are GOAL-UNOWNED, since width-boundary and harness-repair merged and no continuation brief names them; PKT-210 (two-node control cases) is GOAL-UNOWNED, since source-corpus ran cancel order on one node; PKT-204 (the mixed NNTP/BP return receipt, contacts as a modelled plan) is GOAL-UNOWNED beside PKT-202; PKT-203 (enrolling an author on a BP store) is OWNED:mission-signed if the signed refusal turns out to be enrolment. The 400-group packet-1 witness cannot be built natively because the initial configuration record holds at most 64 deltas: that is PKT-013's `*fn-cfg-max-deltas*`, not a new packet.

- [ ] PKT-249 The article gate prevents over-commitment and does not repair it: a store an older image already committed past H stays unopenable ("transaction recovery input exceeds configured bound", exit 4), and no verb recovers it. The next qualification must show the live node's store copy sits below H under `fn-sbud-article-figure` accounting before any deploy is proposed — the qualification lane, /tank/fn/node's copy — bounds-p6 "Packet 1 implemented", merge 03be0a78 "For the next qualification" — GOAL-UNOWNED (§15)
- [ ] PKT-250 PRF-123's composition `fn-post-admitted-article-record-is-within-r` is over two host calls in order (the POST boundary, then the prepare), not one ACL2 function the host calls, and `fn-post-admitted-article-keeps-history` lives in books/record-width-producers.lisp, outside the image closure; the owner keystone takes narrowness as a hypothesis through `fn-opc-prepare-equals-owner-event-under-relation`. Owed: a named composed step the host calls (or a theorem equating the two-call sequence to it) inside the image closure — books/record-width-producers.lisp, store-budget-article.lisp; host/native/io.lisp `fnn-command-post`, host/owner-host.lisp `fn-owner-prepare` — bounds-p6 "Assurance chain", "Packet 1 implemented" — GOAL-UNOWNED (§6)
- [ ] PKT-251 source-routes' tombstone theorems (`fn-sr-the-tombstone-keeps-the-source`: a retry after reclamation is `:duplicate`, a changed source `:conflict`) are unreachable-in-composition until a program writes tombstones; the corpus retry after reclamation has no native case — books/source-routes.lisp, tests/test_native_source_corpus.py — source-corpus "Not done" (Reclamation) — OWNED:reclaim-lifecycle (the release verb; the native case follows it, with PKT-237)
- [ ] PKT-252 Reader remainder: the group window does not say a card's parent was withdrawn (only its conversation does); tin logs in without TLS; the web reader has not run on a non-loopback deployment; no human usability session (the walk is the script's observations) — tools/fn_web.py, tools/spike_tin_drive.sh — reader-daily "Not done"; LANEDUMP Open — GOAL-UNOWNED (§11; the human session is ember's, OUT-OF-GOAL)
- [ ] PKT-253 No client verifies signatures independently: the web reader's fifth provenance fact says "not performed", and tools/fn_consumer.py trusts fn's historical verdict principal. tools/fn_verify.py is the repository's independent verifier (outside the host by test_anchor's rule) and could back both — tools/fn_web.py, tools/fn_consumer.py, tools/fn_verify.py — reader-daily "Not done"; consumer-e2 "Remaining open" — GOAL-UNOWNED (§10, §11)
- [ ] PKT-254 The oversize-article poll contract: an article above the poll reply ceiling has no retrieval contract, so a poll at it never progresses (a page that can never fit must be answered, not retried forever; D27 bounds work, not data) — books/consumer-owner-local-progress.lisp `fn-col-poll`, the poll reply ceiling — consumer-e2 "Remaining open"; merge 4aa3ab61 "Not done" — GOAL-UNOWNED (§5.2, §10)
- [ ] PKT-255 Consumer scope beyond one local group: the native remote and multi-group profile with an authenticated version contract, view rebasing, the cursor mapping across restore and compaction (with PKT-228), and unregister then re-register with a stale ack across the exchange (covered only by `test_durable_scope_ack_and_unrelated_article`) — books/consumer-*, tools/fn_consumer.py — consumer-e2 "Remaining open" — GOAL-UNOWNED (§5.2, §10)
- [ ] PKT-256 Consumer evidence gaps: the six cuts ran on the developer image only (no production-image process-kill variant), and the noop conjunct's frontier premise has no corrupted-state must-fail — tests/test_native_consumer_exchange.py, tests/acl2/consumer-owner-local-progress-tests.lisp — consumer-e2 "Remaining open" — GOAL-UNOWNED (§6, §15)
- [ ] PKT-257 The consumer's unavailable gap (content reclaimed before a poll) is unexercised: no native code calls `fn-rcl-reclaimable` or `fn-rcl-verdict`, so the E2 contract's gap case has no native run — books/store-reclaim, consumer-progress — consumer-e2 "Step 5", "Remaining open" — OWNED:reclaim-lifecycle (the release verb; the consumer case follows it)
- [ ] PKT-258 tests/test_acl2_launchers.py's AST rule misses an ACL2 program held in a variable not spelled `acl2`; a launcher written that way escapes the pool and the heap cap — tests/test_acl2_launchers.py, tools/acl2_slots.py — harness-repair "What was wrong" 4 (blind spot) — GOAL-UNOWNED (§14)
- [ ] PKT-259 BP attempt tokens are not proved monotone across every served event and a restart (within a process a fresh token is above every earlier record's; a death ends earlier callbacks, stated only), and "a non-base event never proposes a `:finished` base record" is not a theorem (only the base-event statement is) — books/bp-node-job-offer.lisp `fn-bpnj-attempt-token`, `fn-bpnj-step` — mission-four-node "Not proved, and why" — GOAL-UNOWNED (§6, §9)
- [ ] PKT-260 `fn-bpnp-contact-next` and its PRF-103 keystones stay in the tree though the host now calls `fn-bpnj-contact-next` (51808446 moved PRF-103's statement): mark the old question unreachable-in-composition (`reach_check --strict`) or delete it with its theorems — books/bp-node-progress*.lisp — mission-four-node "Not proved"; LANEDUMP Open — GOAL-UNOWNED (§6)
- [ ] PKT-261 An fn relay forwards held transit only toward its configured PEER-ID (`fn-bpnp-has-forward-pendingp`), so a relay serves one direction at a time and the mission restarts X and Y with the other peer when traffic turns: a multi-peer relay (held transit routed per destination by `fn-bprt-outbound-choice`) is owed — books/bp-node-progress.lisp, host/native/bp-node.lisp, tests/bp-dtn7/run_mission_four_node.py — mission-four-node "Other findings" — GOAL-UNOWNED (§9)
- [ ] PKT-262 A consumer registered after a Store post still polls that post first: classify it against CNS-002 (the registration's scope start) as intended or a defect, and state it in specs/consumer-progress.md — books/consumer-*, tests/bp-dtn7/run_mission_four_node.py — mission-four-node "Other findings" — GOAL-UNOWNED (§10)

- [x] PKT-247 (mission-signed) — closed on lane/mission-signed-2 by PRF-132 `fn-bpaj-dispatch-never-resubmits-a-stored-article` and `fn-bpaj-dispatch-binds-the-stores-own-record` (books/bp-signed-binding.lisp; host `fn-bprj-request-action` for `fnn-bpapp-action`); the multi-listener half moved to PKT-291 (planning/evidence/mission-signed-2-2026-09-26.md). The signed mission still fails at its second layer: a signed article is committed as a kind-4 acceptance (`fn-stxa-p`), and the BP receiver's Store binding reads only plain `fn-record-p` records (`fn-bpaj-record-for-msgid`, `fn-bpr-store-record-acceptedp`, `fn-bpaj-store-record-accepted-fast`), so after the durable commit the dispatcher submits again and the owner answers `(:have :history)`: `BP node delivery refused result=refused reason=history` at B (report) and A (reply). Bind a kind-4 acceptance's article record as the plain record is bound, with the replay/context/receipt theorems; then rerun `run_mission_four_node.py --report signed`. Also: `bp-node serve` listens on one port, so a relay hears one boundary at a time (the mission turns relay listeners with the direction of travel); a multi-listener serve is open — books/bp-native-app, bp-native-app-fast, bp-receipt; host/native/bp-node.lisp — planning/evidence/mission-signed-2026-09-26.md
- [x] PKT-170 Custody from a refused channel (mission-four-node): an fn relay took kind-5 custody of transit from a channel whose admission it refused (`ambiguous-peer`). Not a decision (the coordinator, D23) — closed on lane/mission-signed by PRF-128 `fn-bpaj-refused-channel-takes-no-custody` (books/bp-channel-ingress.lisp; host `fnn-bps-receive`): refused at reception with the admission's reason, no custody (planning/evidence/mission-signed-2026-09-26.md)
## mission-signed (lane, 2026-09-26)
## Sweep 5 (wave-2 deputy, 2026-09-26 ~01:45 UTC): dev 51808446..54cf01c2

Sources: 44 dev commits, 15 on the first-parent line (five lane merges: peering-tls-pull 3bb735fa, bp-lifecycle 0f9d3b35, reclaim-lifecycle 8a9fa487, operator-health b5eecbb5, width-producers-2 4fce0568; certification batches 4 to 7; the sweep-4 commit 77b78ee6; the coordinator's statement of decisions 878ef61e, planning/decisions-2026-09-26-coordinator-statement.md, which adds no packet: its decisions are PKT-166, 169, 170, 173 a/b/c and the §5.2 join, and its "sent to ember" items are PKT-164, 165, 175, 228, 229), the records peering-tls-pull-2026-09-26, bp-lifecycle-2026-09-25 (with its bp-lifecycle-2 continuation), reclaim-lifecycle-2026-09-25, operator-health-2026-09-25 and width-producers-2-2026-09-26, their LANEDUMP copies in build/coordinator/, and LANEDUMP-source-corpus-2.md with its record in build/lanes/source-corpus-2/ (the lane is HELD unmerged: B refuses every relayed corpus article and logs no reason). spike/mega is unchanged at f6d947d0. Each continuation's headline is its reserved packet and is not repeated: reclaim-lifecycle-2 PKT-263 (the F2 reservation as PKT-169 decided, the retention charge released on reclaim, holders from feed/FNBS/BP, a per-article release verb, the reclaimed record's replay proof, the pack book's guards, N=5,000 after chained packs, and first the tight-store refusal count rechecked under dev's per-article charge), operator-walk PKT-264 (the walk with its four defects: `init --profile` under a mission, fault exit 4 on a never-initialized store, the endless MODE STREAM re-dial after 501, docs/operator.md's duplicated block; the upgrade rehearsal with the rollback-loss sentence; the packaging wrapper; PKT-174's `bp-node health`), and PKT-236 (peering-tls-pull's "Not done": the v2 invitation, the TLS replay bound observed natively, the lab-exception native case, a pull-only credential slot), which is unchanged and stays GOAL-UNOWNED. Ticked above: PKT-234 (3bb735fa).

Ownership moved by the window's merges (the lines above keep their original mark): PKT-242 is NOT closed by 3bb735fa (task 3 was not started) and is PKT-236 (a), GOAL-UNOWNED now that peering-tls-pull merged. PKT-195, PKT-194's replay step, PKT-193's archive release and replay commute, and PKT-237, PKT-251 and PKT-257 (the per-article release verb and the holders, then their native cases) are OWNED:reclaim-lifecycle-2; PKT-194's authorship-verdict articles (refused rather than reclaimed) and PKT-193's stub identity, stub codec round trip, the D25 keystone under `A-DIGEST-COLLISION-FREE` and the `FN-Reclaimed:` shape still storable by `store post` are GOAL-UNOWNED. PKT-185: the reassembler half is closed by 0f9d3b35 (`fn-bpfw-reassemble-is-spec`, uncapped, equal to the index-wise reference); the recognizers' 65,538 x 64, the 1 MiB bundle decoder, the ADU record and the held image are PKT-171's P5, OWNED:bp-lifecycle-3 (queued), together with PKT-004's BP rows and PKT-014's/PKT-139's `*fn-bpn-machine-max-jobs*`. PKT-198 (held-transit fragmentation) and PKT-199 (the N16 retire remainder) were not reached by bp-lifecycle and no queued brief names them: GOAL-UNOWNED. PKT-200: its C1 is harness-repair-2's; the rest (qual C2's exit 1, the exit-code classes) is OWNED:bp-lifecycle-3 item 4 (PKT-268). PKT-220: the health states read receipt debt and stranded transfer live from the NNTP owner at b5eecbb5 (FNLS kind 6); the BP-node sources are operator-walk's item 5; `bp-obligation status` and `store retention` themselves stay offline, GOAL-UNOWNED. PKT-098: the verdict landed at b5eecbb5 (eight states, PRF-112); the doctor and alert wiring rides operator-walk's wrapper (item 3), and no state carries a refusal rate, so the packet stays open. PKT-244: its ordered migration (the checkpoint codec's naturals to u64, the model's five u32 readers, the allocator's successor and the stamp producer, the frontier 2^32-2 twin run) is OWNED:width-3 (queued); its "Also" items (kind 4 and peer-carried producers not charged `fn-sbud-article-figure` before reservation; `fn-bpi-host-article-verdict` composing counts in host Lisp; tools/run_bp_receive.py's two Python size checks) are GOAL-UNOWNED. PKT-181 (a charge past 2^32-1) stays GOAL-UNOWNED: PRF-126 covers records whose charge fits u32. PKT-245's docs sentence is written on the held lane/source-corpus-2 (docs/agents.md) and lands with it.

- [ ] PKT-265 The fragment jobs relation (`fn-bpnf-family-apply-conserves-jobs`, `fn-bpnp-step-family-events-keep-jobs-agreeing`) is established at a cold start only: its establishment by recovery replay and its preservation by the dispatch, forward, delivery and deletion arms are unproved (they need a lemma that every issued record names a non-fragment row); the wider job-table relation over kind-8 attempts, re-offer after a crash, receiver duplicates, durable retry counts, stranded obligations and resume (`fn-bpnp-job-table-coherent` over `fn-bpnp-step`, preserved by every `fn-bpnp-host-eventp` event) is untouched — books/bp-node-fragment-jobs.lisp, bp-node-progress — bp-lifecycle "Not done" item 3 (both sessions) — GOAL-UNOWNED (§6, §9)
- [ ] PKT-266 No theorem bounds the work of one reassembly: the sweep is O(n log n + total + the octets held) per completion attempt and the memo allows one attempt per family per host call (`fn-bpnf-family-next-memo-is-aux`), stated in prose only (D27: bound work per step) — books/bp-fragment-sweep.lisp `fn-bpfw-reassemble`, host `fnn-bps-fragment-progress` — bp-lifecycle "Not done" item 2 — GOAL-UNOWNED (§7)
- [ ] PKT-267 SCN-067 (70 fragments across a kill) is BLOCKED and skipped: the receiver refuses the 65th held fragment (XFER_REFUSE No Resources) at `*fn-bpn-machine-max-jobs*` 64; the 10 MiB transfer is blocked by the same row capacity plus `*fn-bpa-max-octets*` 65,538, `*fn-bpb-max-input*` 1 MiB and `*fn-bpnf-max-held-image*`; the kill and EIO at every cut of a large transfer have not run. PKT-171's recommendation is P5 (the ADU admits A, the bundle bound a profile field, the reassembled bundle handed on in N16 §3.6 chunks, never one held image), never raising a constant; `fn-bpn-limits-compose` asserts ADU cap = fragment cap — books/bp-node-machine.lisp:18, bp-adu.lisp:24,31, bp-bundle.lisp:62, bp-limits.lisp; tests/test_bp_fragment_node_native.py — bp-lifecycle-2 "Not done", "PKT-171"; 0f9d3b35 — OWNED:bp-lifecycle-3 (P5, then the native SCN-067 and 10 MiB run)
- [x] PKT-268 Distinct BP exit codes (item 4, not started), with the root cause found: `fnn-tclc-outcome :uncertain` is set both by a Store indeterminate inside the delivery callback and by a transfer failure (`:outbound-failed`, the delivery plan's `:uncertain`), and `fn-bpn-host-run-outcome` makes either the run's `:uncertain`, so `bp send` over a severed contact exits 3 although the sender's state is certain. Design: split into `:uncertain-store` and `:interrupted`; an ACL2 `fn-bpn-host-run-class` (uncertain 3 > interrupted 6 > refused 1 > no-route 7 > busy 8 > expired 9 > accepted 0; clock-domain 10 at `fnn-bps-open`'s fence) with `fn-bpn-host-exit-code` called by `fnn-bp-exit-code` and `fnn-bps-exit-code`; specs/host.md's table, docs/operator.md and six native modules' expectations — host/native/tcpcl.lisp, host/bp-node-host.lisp, host/native/io.lisp — bp-lifecycle-2 "Item 4" — OWNED:bp-lifecycle-3 (after harness-repair-2's C1) — closed 1de05b46 (PRF-131, books/bp-run-class.lisp `fn-bprc-exit-code-separates-the-classes`, host `fnn-bps-exit-code`, `fnn-bp-exit-code`, `fnn-bp-verb`: 0 accepted, 1 refused, 3 fenced, 6 interrupted, 7 not connected; the fence split out as `fnn-tclc-fenced`); the design's expiry, no-route, busy and clock-domain classes are PKT-297, the second meaning of 6 PKT-295
- [ ] PKT-269 The health verdict and its reports are `:verify-guards nil` (as `fn-nls-report`, whose inputs they share); only the leaf predicates, the exit code, the renderer and the reader are guard-verified, although the verdict runs on the served path under the owner mutex (FNLS kind 6) — books/native-health.lisp `fn-nh-verdict`, native-live-status `fn-nls-report` — operator-health "Not done" (Guards) — GOAL-UNOWNED (§6)
- [ ] PKT-270 Health state exhausted (exit 21) has never been produced natively (it needs 2^32-1 committed transactions); the developer-twin frontier at 2^32-2 that width-3 will run reaches it — tests (operator-health native.sh), the width-3 twin run — operator-health "Not done"; b5eecbb5 — GOAL-UNOWNED (§15; after width-3's frontier twin)
- [ ] PKT-271 For the next qualification and deploy: a node at 4fce0568 or later writes format-3 (u64) frontier frames that bbf52159 cannot read, so after the first write the pre-upgrade snapshot is the only rollback and the deploy procedure (build/coordinator/deploy-*.sh shape, bounds-join's upgrade steps, docs/operator.md) must say so; no image was built at width-producers-2's bytes (SCN-072 ran on the a9ed4dc2 developer image), so the qualification's image is the first native run of frontier format 3 — host/store-host.lisp `fn-store-metadata-frontier-frame`, the deploy procedure — width-producers-2 merge 4fce0568 "For the next qualification and deploy"; WAVE-STATE — GOAL-UNOWNED (§15; with PKT-249)
- [ ] PKT-272 The Python owner client's article-verdict path (tools/run_owner.py asking `fn-store-sn-article-verdict`) has not run: its host file does not load on the laptop without certificates (`fn-rcon-ocfg-io` undefined); the BP clients' tests pass with the unaffordable answer mocked, not produced by ACL2 — tools/run_owner.py, run_bp_ingress.py, run_bp_receive.py; tests.test_bp_ingress_host, test_bp_receive — width-producers-2 "Not done" 5 — GOAL-UNOWNED (§14; the Python host's retirement)
- [x] PKT-273 The bp-node delivery path drops the refusal reason: `fnn-bpnode-request-result` returns `:request-refused` without rendering `fn-owner-app-refusal-log` (bp-app's `fnn-bpapp-deliver` does), so B's refusal of every relayed corpus article is unexplained. Render the ACL2 line with `fnn-owner-log` on every refusal — host/native/bp-node.lisp `fnn-bpnode-request-result` — LANEDUMP-source-corpus-2 "Next exact action"; source-corpus-2 record §SCN-073 — OWNED:mission-signed (task 1, the same line) — closed 09ce5e77 (`fn-owner-bp-request-refusal-line`, printed by `fnn-bpnode-request-result`: `BP node delivery refused result=R reason=C`)
- [x] PKT-274 SCN-073 stays red: over dtn7 all seven accepted corpus articles reach B (a kind-8 attempt and a distinct bundle identity each) and B answers request-refused for every one, also after the mid-receive SIGKILL; on the same image B accepted A's raw `store post` records (run 2) while the model plans `:submit` for an `fn-inj-decide` article under a matching configuration, so the host disagrees with the model's witness. Rerun on a dtn-developer image with PKT-273's line, fix what it names on the proved path, and exercise carrier signature verification at B over BP (never reached: B refuses before its store); lane/source-corpus-2 (PRF-127, SCN-073, PKT-246, b7e4dfa6..5b279d34) is held until then — tests/test_native_source_corpus_bp.py, /tank/fn/scratch/source-corpus-2/native.sh — source-corpus-2 "Not done"; LANEDUMP — OWNED:source-corpus-3 — closed 71563785 (source-corpus-3 native-7 55ee81ca on images 6b6378e0/9f38d453: all seven carried elements arrive A -> dtn7 -> B as A's records with B's splice, the signed carrier accepted once after the mid-receive SIGKILL; SCN-073 implemented; NNTP gate native-8 3/3)
- [ ] PKT-275 The FNCT conflict word: a changed source under a held Message-ID over the control socket (D25 `:conflict`) is answered `:refused`, the same word as a bad frame, a closed posting policy or an unusable clock. Default: append `:conflict` last to `*fn-nctrl-statuses*`, classed `:refused` (exit 1), answered by the hybrid-control and operator-post callbacks, with a theorem that it is a refusal and every earlier octet unchanged; rejected: a distinct exit code. An old client decodes the new octet as `:bad` (fault, exit 4), so the client upgrade precedes the node's. The packet has no coordinator answer (the statement 878ef61e lists it as a queued option) — books/native-control.lisp, host/native/hybrid-control.lisp, owner.lisp operator post, tools/fn_client.py — build/lanes/source-corpus-2/planning/evidence/source-corpus-2-2026-09-26.md "The conflict word" — GOAL-UNOWNED (§5.3; the coordinator's answer, then a small lane)

## Sweep 6 (wave-2 deputy, 2026-09-26 ~02:40 UTC): dev 54cf01c2..6c6a6f35

Sources: 17 dev commits, 7 on the first-parent line (two lane merges: harness-repair-2 f2d40879, operator-walk 4a3f88d6; certification batches 8 4b03adc0 and 9 6c6a6f35; the sweep-5 commit 6ba78b10; the ledger regeneration de065c38; the coordinator's lane friction review 363c8014, planning/review-2026-09-26-lane-friction.md), the records planning/evidence/harness-repair-2-2026-09-26.md and operator-walk-2026-09-26.md, and build/coordinator/LANEDUMP-harness-repair-2.md and LANEDUMP-operator-walk.md. spike/mega is unchanged at f6d947d0. PKT-248's line (above, written by the lane) carries harness-repair-2's whole "Not done" list, the seven native modules with a test over 20 s included, and is not repeated; PKT-264 had no line in this file, so it is written below with its headline items (operator-walk's items 1 to 3) and the lane's other findings are packets of their own.

Ownership moved by the window's merges (the lines above keep their original mark): PKT-248 is GOAL-UNOWNED now that harness-repair-2 merged and no continuation brief names it. PKT-200's C1 is closed as a harness defect by b416370e (the outage tests meet an accept-then-close peer; a refused connect keeps its own `:failed`, exit 0, requeued test); its C2 and the distinct exit codes stay with PKT-268, OWNED:bp-lifecycle-3. PKT-218's remainder is confirmed by the reverse sweep (live_reconfiguration's C3 and control's C4 still fail on the bbf52159 images) and stays GOAL-UNOWNED. The friction review's findings 2 (farm.py's hidden preflight cause, local validation of book names and `--affected-by` words), 3 (the verdict block on `farm.py wait`), 5 (the OpenSSL prefix, tools/hbox_native.sh), 6 (`make check-lane`), 7 (the registry merge driver), 8 (tools/wait_for.sh) and 9 (stop every watcher before the final message) are OWNED:friction-tools (PKT-277 to PKT-282) and have no line here; finding 10 (one owner for "B refuses A's signed article") is OWNED:mission-signed through PKT-273 and PKT-274. The operator-walk upgrade rehearsal ran one image; the old image against the new store is PKT-271's for the next qualification.

- [ ] PKT-264 (operator-walk) What the walk left: (1) `bp-node health JOURNAL` in the DTN image feeding `fn-nh-verdict` from the BP node's own report (stranded rows at the retry bound, no route, unavailable neighbour, receipt debt), with the theorem that the DTN verdict is `fn-nh-verdict` over that report (PRF-130 part 2; default PKT-174's, a BP feed-source in books/native-health.lisp and host/native/bp-node.lisp; rejected: opening the DTN journal from the store image); (2) a refused control post names no reason (`refused operator post REFUSED`, no reason in the owner log either); default: the FNCT reply carries ACL2's refusal reason word, printed like the hint line (books/native-control.lisp, the control frame codec; beside PKT-275's conflict word and PKT-209's missing live-reconfiguration reason); (3) the MODE STREAM stop is per owner process (a restart spends one MODE STREAM again) and there is no IHAVE fallback on the same connection (RFC 4644 §2.3); default: a feed record of the peer's refusal replayed at open, the feed's limits opened with streaming off; rejected: rewriting the operator's peer record — books/feed-connection.lisp `fn-fc-mode-stream-refusal-stops-the-dial`, host/owner-host.lisp, planning/evidence/operator-walk-2026-09-26.md "PKT-264" — GOAL-UNOWNED (§11; item 1 is PKT-174's default awaiting a lane)
- [ ] PKT-283 After a crash, `health` answers `fenced store-held` until the restarted owner is listening, so an operator reading health during recovery sees the fault state of a held lock, not "starting". Default: ACL2 distinguishes a lock whose holder has not yet published its control socket (or, weaker, the operator page's fenced row says an owner still starting answers so until it listens) — books/native-health.lisp `fn-nh-verdict`, host/native/operator.lisp — operator-walk "PKT-264" item 4; a828ac47 — GOAL-UNOWNED (§11)
- [x] PKT-284 tools/build_native_host.sh exits 0 after printing `ACL2 Error` in a host file, so an image can be built, installed and tested with a host form that never loaded (operator-walk's first image was); fail the build on any ACL2 error line, with a test — tools/build_native_host.sh (friction-tools edits the same script for the OpenSSL default: coordinate) — operator-walk "PKT-264" item 5; merge 4a3f88d6 "Not done" — GOAL-UNOWNED (§14) — closed d102b9ce (tools/build_native_host.sh fails on `ACL2 Error`, `HARD ACL2 ERROR`, `ABORTING from raw Lisp`, `Uncertified` in its log); the test is PKT-305
- [ ] PKT-285 In-place hbox certification has no farm path: `farm.py submit hbox` with operator-walk's 160 image roots failed its cache preflight three times where the same command by hand succeeded, so the image roots were certified in place by hand (certify-20260926T014439Z-94204, certify-20260926T015203Z-101909) without a farm run record, and the record cited those manifests before they were committed (make check red on dev from 4a3f88d6 until 6c6a6f35 harvested them). Owed: the 160-root failure diagnosed once friction-tools' preflight prints stderr and the exit code, a supported verb that certifies image roots in place and writes and harvests its manifest, and a merge-time check that every manifest a record cites is committed — tools/farm.py, tools/certify_books.py — operator-walk "PKT-264" item 6; merges 4a3f88d6, 6c6a6f35 — GOAL-UNOWNED (§14; the stderr surfacing is OWNED:friction-tools)
- [ ] PKT-286 SCN-076 (the 45-step operator walk) is a one-off script with a 40 s wait, not a tests/ module: split it into tests within test_budget.py's 20 s per test and 180 s per module (waiting on the listener, not a sleep) so every image runs it — the walk script, planning/evidence/operator-walk-2026-09-26/, tests/ — operator-walk "Not done", item 7 — GOAL-UNOWNED (§11, §14)
- [ ] PKT-287 `store rollback-check --snapshot` counts transaction files, so on a compacted store (records in a pack) it undercounts what a restore loses; `fn-native-operator-snapshot-loss-counts-the-suffix` holds over the file-suffix observation only. Default: count through the same observation the open uses for packs, and scope the theorem to the reconstructed history — books/native-operator.lisp, host/native/io.lisp `fnn-command-rollback-snapshot` — operator-walk "PKT-264" item 8 — GOAL-UNOWNED (§5.6, §8; after pack-chain-serve's selection)
- [ ] PKT-288 test_twonode_gate and test_scale_gate keep their pinned reader fallback: tests/deploy_gate_fake has no owner with feeds or peering, so those gates' rehearsals never run an owner, and their owner rows are the reader's (only test_deploy_gate now requires the owner and verdict held). Owed: a fake owner that serves feeds and peering, or the real owner on a scratch store, and the same exit-0, verdict-held requirement — tests/deploy_gate_fake, tests/deploy_gate_owner_fake, tools/*_gate — harness-repair-2 record §3; e669c063 — GOAL-UNOWNED (§14, §15)
## mission-signed-2 (lane, 2026-09-26)
- [ ] PKT-291 (mission-signed-2) Owed from the signed mission: (1) `bp-node serve` listens on one port, so a relay hears one boundary at a time and the mission driver turns relay listeners with the direction of travel and restarts the node whose receipt is owed (steps 4 and 6); design: an ACL2 answer naming one listener per admitted transport-bp boundary row of the live configuration (the channel table; `fn-bpaj-session-principal` already selects the neighbour by listener port), the host binding each and accepting from any, one admission per listener, and a theorem that every bound listener's sessions are admitted under exactly that row; (2) a Message-ID index over Store events for the receiver's binding (today each lookup decodes every kind-4 composite of the history once); (3) books/byte-store-k0-staging measured 15.0 s at 2 jobs on hbox in run-20260926T024334Z-8257 (5.3 to 8.2 s before; unchanged by this lane) — books/bp-node-machine host/native/bp-node.lisp host/native/io.lisp, books/bp-native-app.lisp, tests/bp-dtn7/run_mission_four_node.py — planning/evidence/mission-signed-2-2026-09-26.md
- [x] PKT-289 The Bash tool runs zsh and lanes write bash: 303 NOMATCH failures in 141 of 186 agents (unquoted `--include=*.lisp`, `books/*.acl2`), 13 failed farm submits from an unsplit `$ROOTS`, 14 lanes whose `echo =====` swallowed the second file, and a glob failure inside a `;` chain prints nothing, so an empty result can read as "absent". Default: a "## Shell" section in build/coordinator/BRIEF-COMMON.md (quote globs or use `git grep`; arrays or `${=VAR}`; never a word starting with `=`); alternative, ember's: `setopt no_nomatch sh_word_split; unsetopt equals` for agent shells — BRIEF-COMMON.md — review-2026-09-26-lane-friction §1 — GOAL-UNOWNED (§14; the shell setting is ember's) — closed d102b9ce (build/coordinator/BRIEF-COMMON.md "## Shell", untracked; the zsh options stay ember's)
- [x] PKT-290 Test books cause most red farm runs: 61 of 87 red runs with a failure line failed on a tests/acl2 book (27 on test books only), and only 53 of about 125 submitting agents ever opened a REPL on one. Default: a BRIEF-COMMON Farm line: `proof_repl.py start --through` every changed book and its test book on persvati before each run; submit only when every changed form says ok — build/coordinator/BRIEF-COMMON.md — review-2026-09-26-lane-friction §4 — GOAL-UNOWNED (§14) — closed d102b9ce (build/coordinator/BRIEF-COMMON.md "## Before a farm submit", untracked)

## friction-tools (2026-09-26)
- [x] PKT-277 farm.py submit: a failed cache preflight reports its exit code, first error line and whole stderr (was the last 800 characters of stdout); book, --affected-by and --recertify words are validated locally (BOOK_NAME, whitespace, options, the .lisp exists) before any rsync — tools/farm.py `preflight_detail`, `refuse_bad_book_names` — review-2026-09-26-lane-friction §2 — lane/friction-tools; planning/evidence/friction-tools-2026-09-26.md
- [x] PKT-278 farm.py wait ends with `== verdict RUN`: failing books with their first ACL2 Error/FAILED line and local log path, installed/certified counts, books over 10 s; uncached/unverified lists folded to counts unless --verbose — tools/farm.py `verdict_lines`, `report_lines` — review §3 — lane/friction-tools
- [x] PKT-279 hbox native: build_native_host.sh defaults FN_OPENSSL_PREFIX on hbox and names it on an OpenSSL failure; the ML-DSA refusal names the variable; tools/hbox_native.sh REV MODULE... ships, certifies, builds under swarm-build, runs modules under systemd-run MemoryMax, writes logs and SHA256SUMS — tools/build_native_host.sh, tools/hbox_native.sh, host/native/signatures.lisp:117 — review §5 — lane/friction-tools (DTN images not an --images choice yet)
- [x] PKT-280 `make check-lane`: planning/ledger.json, ledger.md and current.md regenerated into a temp dir and compared there, so a lane never regenerates-and-reverts — Makefile, tools/ledger.py `lane_generated`, tools/current_view.py — review §6 — lane/friction-tools
- [ ] PKT-281 Registry merge driver tools/merge_registry.py (id-keyed three-way; .gitattributes routes the four registries; 150-merge replay: 138 registry conflicts -> 5, all real). Open: register it in the main clone's git config (NIGHT.md step 2a) after the merge, then shrink merge_lane.sh — tools/merge_registry.py, .gitattributes, build/coordinator/NIGHT.md — review §7 — OWNED:coordinator (the git config line)
- [x] PKT-282 tools/wait_for.sh: one background wait (log marker, file, PID, systemd unit, farm run; --host; deadline 124, unobservable 4), no pattern matching of processes; BRIEF-COMMON's Shell section and "stop every watcher before the final message" — tools/wait_for.sh — review §8, §9 — lane/friction-tools

## Sweep 7 (wave-2 deputy, 2026-09-26 ~03:50 UTC): dev 6c6a6f35..081eb72e

Sources: 13 commits on dev's first-parent line: five lane merges (reclaim-lifecycle-2 3ba3f9c6, mission-signed 09ce5e77, friction-tools d102b9ce, rep-wave-d 19a71b2d, bp-lifecycle-3 1de05b46), the reach_check baseline b6c43987 (the payload arena has no host caller yet), certification batches 10 033babf7 and 11 d6c0c5e9 (batch 12 certified nothing new), the sweep-6 commit 1bc54a7f and four ledger regenerations; the records planning/evidence/reclaim-lifecycle-2-2026-09-26.md, mission-signed-2026-09-26.md, friction-tools-2026-09-26.md, rep-wave-d-2026-09-25.md (§1.2, §3, §8) and bp-lifecycle-3-2026-09-26.md, and build/coordinator/LANEDUMP-{reclaim-lifecycle-2,mission-signed,friction-tools,rep-wave-d,bp-lifecycle-3}.md. spike/mega is unchanged at f6d947d0. Running lanes and what their briefs own (build/coordinator/queue/): pack-chain-serve (PKT-168, the 503 over a pack chain), mission-signed-2 (PKT-247 and PKT-291: the receiver's signed record type, then the multi-listener `bp-node serve` if the mission needs it), rep-wave-d-2 (PKT-293: the records-shape freeze, the codec and recovery over the arena, the checkpoint codec and the 64 MiB collection trigger in the operator verbs, then the served reference effect), bp-lifecycle-4 (PKT-294: the codec half of P5, SCN-077, the native journal past its profile), source-corpus-3 (PKT-246, PKT-274, PKT-292; held on mission-signed-2).

Ticked in place above: PKT-195 (Store half, 3ba3f9c6), PKT-268 (1de05b46), PKT-273 (09ce5e77), PKT-284, PKT-289 and PKT-290 (d102b9ce). PKT-169, PKT-171 and PKT-263/PKT-276 had no line in this file and are written below.

Ownership moved by the window's merges (the lines above keep their original mark): PKT-193's archive release and replay commute, PKT-194's replay step, PKT-237, PKT-251 and PKT-257 (the per-article release verb, then their native cases) were OWNED:reclaim-lifecycle(-2); reclaim-lifecycle-2 merged without the verb and no running brief names it, so they wait on PKT-263 and are GOAL-UNOWNED. PKT-187 (the carrier parse over a payload list) was OWNED:rep-wave-d, which merged the arena only; rep-wave-d-2's program does not name it, so it is GOAL-UNOWNED with PKT-302. PKT-029 (the payload arena) has its foundation on dev (PRF-118, books/payload-arena.lisp) and its freeze is OWNED:rep-wave-d-2 with PKT-024 and PKT-033 (the record codec and the recovery decode over the arena, step (ii)). PKT-267's SCN-067 half is closed by 1de05b46 (70 fragments across a SIGKILL under a 128-row profile, none skipped); its 10 MiB transfer is PKT-276's (OWNED:bp-lifecycle-4, one receiver SIGKILL and one rotation mid-family), and the kill and EIO at every cut of a large family stays open under PKT-267, GOAL-UNOWNED. PKT-200 was OWNED:bp-lifecycle; its C2 (no-route exit) folds into PKT-297. PKT-274's first cause (the transit lookup's injecting-node check) is fixed by 09ce5e77 (`fn-bpaj-transit-article-fields`); the rerun stays OWNED:source-corpus-3. PKT-248 is unchanged and GOAL-UNOWNED.

- [x] PKT-169 (reclaim-lifecycle's F2, as the coordinator decided it) A full store can always finish or refuse its own maintenance — closed 3ba3f9c6 (PRF-129: `fn-smr-admission-keeps-the-reserve`, `fn-smr-reserve-admits-the-release`, host/owner-host.lisp and host/store-node-host.lisp; the pack checked against the disk's free octets, refused `temporary-space` with nothing written, host/native/checkpoint.lisp; `status` prints `maintenance-reserve octets=4096 transactions=1 held`; tight3 52172ec1, n300 9450dd9c) — planning/evidence/reclaim-lifecycle-2-2026-09-26.md
- [x] PKT-171 (held-row item) The BP node's held rows and held octets were the constants `*fn-bpn-machine-max-jobs*` 64 and `*fn-bpn-machine-max-octets*` 16 MiB — closed 1de05b46 (books/bp-node-profile.lisp, bp-node-profile-replay.lisp: `bp-node profile JOURNAL NODE ROWS OCTETS`, raise only, read at `fnn-bps-open` through `fn-bpnpf-read`; `fn-bpnpf-replay-past-the-profile-is-refused`, `:held-beyond-profile`, never truncated; SCN-067 native fc37d711); the codec half is PKT-276 — planning/evidence/bp-lifecycle-3-2026-09-26.md "PKT-171's P5 items"
- [ ] PKT-263 (reclaim-lifecycle-2) What the reservation lane left, steps 3 to 7 of its list: (3) holders from feed state, FNBS rows and BP obligations: `fn-rcl-store-holders` reads the Store's consumer projection only, and no bounded offline read of the BP journal (a separate namespace and lock) exists to hand ACL2 its held Message-IDs; (4) a per-article release verb: the retention ledger's `:release` frees `:forward` obligations only (books/store-node-retention.lisp), so an archive release needs its own event kind or the pin's release arm, with `fn-node-statep` relaxed to pins or releases (the reservation it needs is in place); (5) the replay proof that the open of the reclaiming pack equals `fn-rcl-reclaim-state` of the open of the history (PRF-088's open step; the charge release is proved per event, `fn-rclp-freed-charge-account`, and observed at the ledger only); (6) books/store-reclaim-pack is `:verify-guards nil`, called through the `:program` wrapper `fn-store-reclaim-decide`; (7) N=5,000 once chained packs land — books/store-reclaim*, store-node-retention, host/native/checkpoint.lisp — reclaim-lifecycle-2 record §7, LANEDUMP "Owed"; merge 3ba3f9c6 "Not done" — GOAL-UNOWNED (§8; step 7 after pack-chain-serve; PKT-193, 194, 237, 251, 257 follow step 4)
- [x] PKT-276 (bp-lifecycle-3) The codec half of P5: `*fn-bpa-max-octets*` 65,538 (the ADU record), `*fn-bpb-max-input*` 1 MiB (the bundle decoder) and `*fn-bpnf-max-held-image*` 131,072 (one held image) are still constants inside the codec books whose recognizers and round trips name them (bp-adu, bp-bundle, bp-fnbs-codec and its invariants, bp-fnbs-family-codec, bp-node-foundation, bp-node-fragment-plan, bp-node-receive-boundary, bp-limits `fn-bpn-limits-compose`), so SCN-077 (10 MiB through 4 KiB fragments) stays specified; and no native case opens a journal whose rows exceed its profile (the ACL2 teeth witness the verdict on real kind-5 frames) — bp-lifecycle-3 record "PKT-276" items 1 and 6, "Not done"; merge 1de05b46 — OWNED:bp-lifecycle-4 (PKT-294 for its remainder) — closed 9ebf5f0e (PRF-134: the three constants are codec widths at 2^24 and the bounds are the `bp-node profile` ADU, bundle and held octets, refused by name before decode or custody; a journal past its profile fences `held-beyond-profile`, native 89e7545b and a9e8fc11; the remainder is PKT-294 and PKT-309 to PKT-311 below)
- [ ] PKT-295 One operator page, two meanings of exit 6: for the native operator verbs 6 is `refused`, "no store at the configured `[store] path`: run `init`" (HST-008, operator-walk; docs/operator.md's outcome table), and for every BP verb 6 is "a connection lost after it existed; the job stays and is re-offered" (specs/host.md "BP run classes", PRF-131, 1de05b46); the outcome table lists 6 once, with the operator meaning only. Beside it, `bp decode` keeps its article-verdict codes, where 3 means "the clock cannot decide the lifetime", not "recover first" (bp-lifecycle-3 item 5). Default: one fn-wide exit table owned by one ACL2 constant that every verb family's code function reads, the BP interrupted and not-connected classes on codes no other verb uses, docs/operator.md's table carrying every code once with its verb family, and a theorem that the families' codes are disjoint except 0, 1, 3, 4, 5; rejected: per-family tables that reuse a number (a wrapper script reads the number, not the family) — books/bp-run-class.lisp `fn-bprc-run-exit-code`, books/native-operator.lisp (the no-store answer), specs/host.md "CLI exit codes" and "BP run classes", docs/operator.md — the deputy's finding over merges 4a3f88d6 and 1de05b46 — GOAL-UNOWNED (§4, §11; the coordinator's answer on which family moves, then a small lane)
- [ ] PKT-296 The Store profile's `max-bp-rows` (field 10) is read by nothing: the BP node's held rows live in the FNBS journal's `bp-node profile` because `bp-service` and `bp-contact` run with no Store, yet `fn operator init --max-bp-rows N` accepts it and docs/operator.md's profile table advertises it (specs/storage.md says it is not read). Decision: should a `bp-node serve` with a Store take its rows from the Store profile (one number for the operator) or keep the journal's (one number per journal)? Default (the record's): keep the journal's and retire the Store field (reserved, admitted only as today's values, never an input, as PKT-229 does for field 13), removing it from the init usage and the operator table; nothing else waits on it — books/byte-store-frame.lisp `*fn-bs-pf-max-bp-rows*`, store-profile-namespace `fn-bs-profile-max-bp-rows`, native-operator.lisp's init grammar, docs/operator.md — bp-lifecycle-3 record "PKT-276" item 2; merge 1de05b46 "For ember"; PKT-157's BP-rows field — WAITS ON EMBER
- [ ] PKT-297 The BP run class does not yet separate expiry, no-route, busy delivery or a clock-domain change (mandate §9's joins): today expiry and no-route exit 0 with the reason on stdout, a busy delivery 0, a clock-domain fence 3 like any fence. Each is a further evidence word and class in `fn-bprc-*` (the evidence record makes it additive), on codes chosen under PKT-295, with `fn-bprc-exit-code-separates-the-classes` extended and the native expectations moved; PKT-200's C2 (`bp-obligation request` with no route after a durable attempt) and "a no-route request needs a new request once a route exists" are this class's cases — books/bp-run-class.lisp, bp-node-run-class.lisp, host/native/bp-service.lisp `fnn-bps-exit-code`, specs/host.md — bp-lifecycle-3 record "PKT-276" item 3; PKT-268's design; merge 1de05b46 "Not done" — GOAL-UNOWNED (§9, §4)
- [ ] PKT-298 Two older FNBS replay folds, `fn-bpnf-replay-rows` (books/bp-fnbs-replay.lisp) and `fn-bpah-replay-rows` (bp-fnbs-delivery-replay.lisp), still answer `:kind-five-row` at the old bound instead of the named `:held-beyond-profile`; neither is on the served recovery path (`fn-bpnf-family-recover-auto-event` is). Retire them, or mark their theorems `unreachable-in-composition`, or give them the profile verdict — books/bp-fnbs-replay, bp-fnbs-delivery-replay — bp-lifecycle-3 record "PKT-276" item 4 — GOAL-UNOWNED (§9, §14)
- [ ] PKT-299 The BP namespace's maintenance reservation (PKT-195's BP half): its reserve is the FNBS debt cover and `*fn-bpnd-rotation-reserve*` is 0 (books/bp-node-debt.lisp), which says a rotation needs no slot beyond the owed debt; nothing proves that holds for every rotation of a journal filled to its profile. Owed: the theorem that a full BP journal can always rotate and discharge its debt (or a nonzero reserve with admission keeping it, as PRF-129 does for the Store) — books/bp-node-debt.lisp, bp-node-profile — reclaim-lifecycle-2 record §7 — GOAL-UNOWNED (§8, §9)
- [ ] PKT-300 For the next qualification: (a) every profile now admits one transaction fewer than its budget (the reservation holds the last one for a release: the development profile admits 127, not 128), so the qualification's capacity expectations, the operator page and the upgrade rehearsal's counts say so; (b) the run must include a checkpoint publication at the profile's largest admitted article size times K/2 records, because the deployed scale profile auto-publishes at 2,048 records and `store checkpoint` dies by heap exhaustion at N=10,000 x 32 KiB on dev (expected red until rep-wave-d-2's step (iii); a red here blocks a scale-profile deploy, not development) — the qualification brief (qual-c3420013's shape), docs/operator.md — merges 3ba3f9c6 "For the next qualification", 19a71b2d "Consequences" — GOAL-UNOWNED (§15; with PKT-271)
- [ ] PKT-301 Two O(N) terms per operation, from N=1,000 to N=10,000 x 2 KiB: a POST allocates 7.7 MB against 2.3 MB (about 600 bytes per retained record per POST: something on the commit path walks the history or a per-record index), and OVER takes 3.65 ms against 0.62 ms while STAT and ARTICLE do not move (at 32 KiB: 26.3 against 20.8 MB, 10.2 against 1.8 ms). Neither is a representation term; profile both on the baseline's image and load, name the walk, and make the commit and OVER O(1) in N or say why not — host/owner-host.lisp, books/owner*.lisp, group-bucket-article — rep-wave-d record §1.2; merge 19a71b2d — GOAL-UNOWNED (§7, D26; rep-wave-d-2 measures the rows but does not own the cause)
- [ ] PKT-302 The rest of rep-wave-d's program, past rep-wave-d-2's brief: (iv) the ingress span machine (a 32 KiB POST allocates 20.8 MB, 635 bytes per octet, 59% of its CPU in `fn-scar-feed-counted` → `fn-wire-feed-byte` consuming the body a byte at a time; the wire machine's body as a buffer range consumed by span, with the correspondence "feeding the span's bytes one at a time is the span step", which also ingests an article larger than a quantum without a copy per quantum); (v) reclaim's tombstone as a new seal, the feeds, BP and the carrier and header projections reading the arena by handle as their measurements justify (PKT-187, PKT-026, PKT-034, PKT-036) — books/wire.lisp, served-carried.lisp, owner.lisp `fn-owner-chunk`, store-reclaim, bp-native-app*, stx-carrier — rep-wave-d record §3 "The order" (iv), (v) — GOAL-UNOWNED (§7; after rep-wave-d-2's freeze)
- [ ] PKT-303 The arena's five events (`fn-arena-seal-keeps-sealed`, `fn-arena-seals-keep-sealed`, `fn-arena-seal-new-handle`, `fn-arn-store-corr-of-commit`, `fn-arn-store-corr-of-open`) are baselined model-only in planning/reach-baseline.json until a host entry calls `fn-arena`; the baseline shrinks when the record payload becomes a handle. With it, the 64 MiB collection trigger (`+fnn-owner-gc-nursery-octets+`) is set only in `fnn-owner-run`, so the offline verbs (`store checkpoint`) run at 1.6 GB between collections — planning/reach-baseline.json, host/native/owner.lisp and the operator verbs — b6c43987; rep-wave-d record §1.2 — OWNED:rep-wave-d-2 (steps (i) and (iii))
- [ ] PKT-304 The fast transit functions (books/bp-native-app-fast.lisp: `fn-bpaj-store-record-accepted-fast` and the transit lookups the relay runs on every delivered request) are not guard-verified; they run on the served BP path — books/bp-native-app-fast.lisp — mission-signed record "Not done" — GOAL-UNOWNED (§6, §9; mission-signed-2 edits the same book for PKT-247: after it, or there)
- [ ] PKT-305 The tooling tests: `make tooling-test` dies at its 120 s timeout in test_test_budget on the laptop before reaching the new modules, so test_farm, test_merge_registry and test_wait_for are not in it; the ACL2-error refusal tools/build_native_host.sh gained (PKT-284) has no test; the review's optional local reader check (three "unbalanced close parenthesis" submits) was not added — Makefile `tooling-test`, tests/test_test_budget.py, tools/build_native_host.sh, tools/farm.py — friction-tools record "Not done", LANEDUMP "Open"; merge d102b9ce "Not done" — GOAL-UNOWNED (§14)
- [ ] PKT-306 tools/hbox_native.sh builds only host/native/build.lisp images: the DTN images (build-dtn.lisp) are not an `--images` choice, yet the BP lanes run on the DTN developer image (bp-lifecycle-4's brief names hbox_native.sh; bp-lifecycle-3, mission-signed and source-corpus-3 each wrote their own native.sh) — tools/hbox_native.sh — friction-tools record "Not done"; PKT-279's note — GOAL-UNOWNED (§14; small, high reuse)

## Sweep 8 (wave-2 deputy, 2026-09-26 ~04:20 UTC): dev 081eb72e..7f7f980c

Sources: 36 dev commits; on the first-parent line four lane merges (mission-signed-2 67d31fc7, source-corpus-2/-3 71563785, rep-wave-d-2 08a16546, bp-lifecycle-4 9ebf5f0e), the decisions for ember ed25518b and 50d68aa5 (planning/decisions-for-ember-2026-09-26.md), certification batches 13 65a4c514, 14 08c72e9e and 15 6c4cf043 (no batch 16 commit is in the window), the sweep-7 commit 52b26e8c and the ledger regenerations; the records planning/evidence/mission-signed-2-2026-09-26.md, source-corpus-2-2026-09-26.md (its source-corpus-3 sections), rep-wave-d-2-2026-09-26.md (§1, §5, §7, §8) and bp-lifecycle-4-2026-09-26.md ("SCN-077", "PKT-294", "Not done"), and build/coordinator/LANEDUMP-{mission-signed-2,source-corpus-3,rep-wave-d-2,bp-lifecycle-4}.md. spike/mega is unchanged at f6d947d0. Running lanes and what their briefs own (build/coordinator/queue/): pack-chain-serve (PKT-168, the 503 over a pack chain), rep-wave-d-3 (PKT-307, Fable: the checkpoint decoder over the buffer with PRF-135, the named refusal that falls back to the journal, the reopen rows SCN-079, the bulk export if the budget fits), bp-lifecycle-5 (PKT-308: the per-arrival cost, a maintained family index, PRF-136, SCN-077 natively).

Ticked in place above: PKT-274 (71563785) and PKT-276 (9ebf5f0e). PKT-247 was already ticked by its lane and merged at 67d31fc7.

Ownership moved by the window's merges (the lines above keep their original mark): PKT-291 (its line carries no mark) is GOAL-UNOWNED now that mission-signed-2 merged without the multi-listener serve or the Message-ID index. PKT-246 is reduced to the FNCT conflict word and WAITS ON COORDINATOR in the decisions file's "For the coordinator"; PKT-295 is there too (its line stays GOAL-UNOWNED until the coordinator picks the family, then a small lane). PKT-292 was released untaken (source-corpus-3's LANEDUMP). PKT-303's second half is closed by c76038f9 (`+fnn-gc-nursery-octets+` set in `fnn-main` for every entry of the image; its cost on the open path is PKT-316); its first half (the arena's five events baselined model-only) now waits on PKT-293. PKT-300 (b) changed shape: at N=10,000 x 32 KiB the publication now completes and the reopen dies (PKT-313). PKT-004's and PKT-014's `*fn-bpa-max-octets*`, `*fn-bpb-max-input*` and `*fn-bpnf-max-held-image*` are codec widths at 2^24 since 9ebf5f0e; the rest of those lines is unchanged. PKT-304 is unchanged (mission-signed-2's record: "the BP books are still not guard-verified"). PKT-291's item (3), byte-store-k0-staging at 15.0 s under load and 7.4 s alone, stays a watch item.

The decisions file's packets: the existing WAITS ON EMBER lines PKT-164, PKT-228, PKT-229, PKT-235 and PKT-296 are its sections of the same numbers; the lines below add PKT-127, 165, 167, 173, 175 and 293, which had no WAITS ON EMBER line.

- [ ] PKT-127 (decisions pointer) RFC 5536 §3.1.4's five special-purpose group names: the standing default (the local-agreement profile, admitted at init and create, conferring no authority) is implemented; ember confirms (a), or picks (b) refuse all or (c) refuse all but `junk` — planning/decisions-for-ember-2026-09-26.md "PKT-127"; the line above (OUT-OF-GOAL) — WAITS ON EMBER
- [ ] PKT-165 A content-holding consumer mode, separately charged: default (implemented) no implicit pin, a reclaimed article before a poll is an explicit unavailable gap; (b) brief a charged retaining mode; (c) after PKT-228's restore floor — books/store-reclaim holders, consumer-store-events — planning/evidence/consumer-e2-2026-09-25.md "PKT-165"; decisions file "PKT-165" — WAITS ON EMBER
- [ ] PKT-167 Adopt the representation program with the payload in the arena (rep-wave-d §3's order): default (not implemented) adopt with (i) as the coordinator's freeze; rep-wave-d-2 §1 found (i) not closed as briefed, so its shape is PKT-293's question and PKT-167 is the program's go — rep-wave-d-2026-09-25.md §1.2, §8; decisions file "PKT-167" — WAITS ON EMBER (with PKT-293)
- [ ] PKT-173 (decisions pointer) peering-compose's three packets: (a) a declined key statement at reopen, `:recorded` (implemented 4b710f03) or `:current` or `:recorded` plus `keys redecide` (PKT-241); (b) PKT-230, keep refusing unenrolled signed evidence (implemented) or a charged hold; (c) PKT-231, the pull cursor as FNPL files (implemented) or a Store family or files with a power-loss cut test first — decisions file "PKT-173 a/b/c"; the line above (OWNED:peering-compose, merged) — WAITS ON EMBER
- [ ] PKT-175 A wire-visible HDR item `:fn-enrollment` (the verdict principal's current generation and state from the node's keyring view, an ACL2 function on the pinned served step with its theorem): default (not implemented; the coordinator recommends adoption) adopt; (b) authenticated readers only; (c) leave "not available" — planning/evidence/reader-daily-2026-09-25.md "PKT-175"; decisions file "PKT-175" — WAITS ON EMBER
- [ ] PKT-293 (rep-wave-d-2's reserved id) The shape of the records freeze: the briefed freeze (`fn-record-payloadp` to an arena handle) is not closed, because `fn-sn-finish` reads every finished payload for the statement verdict (`fn-stx-verdict-of-octets`, 38 books name it), the reclaim digests the held payload in its step, 40 books of the served machine read `fn-article-payload`/`fn-pending-payload`/`fn-article-body`, and the acceptance state carries a second payload field; default (not implemented) the record's §2 design in order: the checkpoint decoder over the buffer first (PKT-313), then two views of one record shape (wire record keeps its exact octet-list round trip), intern with parsed fields, a payload-parametric transition, the served article as a reference effect (ii); REP-011 registered specified under it — books/store-node.lisp, acceptance.lisp, store-reclaim*, nntp-responses, control-served, records* — rep-wave-d-2 record §1, §2, §7; merge 08a16546; decisions file "PKT-293" (50d68aa5) — WAITS ON EMBER
- [x] PKT-294 (bp-lifecycle-4's reserved id) SCN-077 is blocked by per-arrival work: 2,622 fragments of a 10,486,094-octet ADU, about 45 ms per held row per arrival (7 s per ten arrivals at 20 held, 63 s at 140; about 43 hours projected; stopped after 172 arrivals); inferred, not profiled: `fn-bpnf-heldp` re-encodes every held bundle per call from `fn-bpnf-active-fragmentp` in the selector and the active set, plus the `*1*` guard check per host call, i.e. whole-state revalidation on a served path; item 2, a coverage precheck (sum of extents below the total means not ready) so an early offset zero does not rebuild the canvas per arrival; the whole-family wall time and bytes copied at 10 MiB are not measured. Items 3 to 8 are PKT-309 to PKT-311; item 9 is PKT-296 — books/bp-node-fragment-family, bp-node-fragment-plan, bp-fragment-sweep, host/native/bp-node.lisp — bp-lifecycle-4 record "SCN-077", "PKT-294" items 1 and 2; merge 9ebf5f0e — OWNED:bp-lifecycle-5 (PKT-308, PRF-136) — item 1 closed by bp-lifecycle-5 (profiled: eight re-encodings per held row per session, five in the selector and three in the post-session scans, plus two in the expiry scan; the `*1*` guard checks cost under 1 ms): `fn-bpnf-family-select-is-aux`, `fn-bpnf-family-select-plans-bound`, `fn-bpnf-family-select-steps-bound`, `fn-bpah-local-pendingp-has-a-nonfragment-header`, `fn-bpn-report-observe-held-needs-an-administrative-header`, `fn-bpah-held-expiry-header-of-held` (PRF-136, f72f258d..45e63b0e; a session is about 60 ms at 64 to 256 held rows); item 2 is PKT-308 item 1
- [ ] PKT-308 (bp-lifecycle-5) What PRF-136 leaves: (1) a family whose offset-zero fragment is held is planned on every arrival until complete, so a family arriving in offset order costs its octets per arrival (the coverage precheck: a maintained per-family extent sum, or the lemma that extents summing below the total are not a cover); (2) the receive step still walks every held wire's length (`fn-bpnf-held-octets`, 1.6 to 2 ms at 256 rows of 4 KiB) and compares every held key (`fn-bpnf-find-held`): a maintained octet count; (3) no single counting theorem over a whole `bp-node serve` session (the bound is per scan); (4) SCN-077 is now blocked by the open, not the arrivals: 1,311 held 4 KiB rows open in 51.9 s (three re-encodings per row, `fn-bpb-decode` run unverified at 12 ms a bundle, a quadratic held-octet sum in `fn-bpnf-recovery-heldp` and replay), past the harness's 45 s and 120 s waits; (5) the whole-family wall time and bytes copied at 10 MiB — books/bp-node-fragment-step, bp-node-foundation, host/native/bp-node.lisp — bp-lifecycle-5 record "PKT-308" — GOAL-UNOWNED
- [ ] PKT-309 A node cannot send what it can now receive: the sender machine's job image `*fn-bpn-machine-max-job-octets*` is a plain `:blob` of 131,072, so one job holds about 64 KiB of ADU; `fn-bpfs-plan` can cut a larger bundle but the job cannot hold it, so an fn node cannot originate the 10 MiB article SCN-077 receives (the harness authored it through ACL2 directly). Needs the job image as a profile field at a codec width, or the job as a family of fragment jobs, with the sender's journal replay under the profile — books/bp-node-machine.lisp, bp-node-fragment-plan, bp-fragment-send — bp-lifecycle-4 record "PKT-294" item 3; merge 9ebf5f0e "Not done" — GOAL-UNOWNED (§9, D27; after bp-lifecycle-5)
- [ ] PKT-310 The BP decode is not resumable: a received transfer is decoded in one step under min(profile bundle octets, the transfer MRU), and a held family image is encoded, framed and decoded in one step linear in its octets (up to the profile's held octets, now 2^24), so one scheduling step's work grows with the data; owed: the resumable bundle decoder and family-image decode with explicit resumable state per quantum (mandate §7's last paragraph), and the per-quantum work and bytes-copied figures at 10 MiB — books/bp-bundle, bp-fnbs-family-codec, host/native/bp-node.lisp — bp-lifecycle-4 record "Work per quantum", "PKT-294" item 5, brief item 2 not done — GOAL-UNOWNED (§7, §9, D27)
- [ ] PKT-311 BP profile edges: (a) replay does not re-check the ADU octets (only rows and held octets), so "every held fragment's ADU is within the profile" rests on admission plus raise-only and has no replay check or theorem at open; (b) `bp-node profile` opens the journal, so it cannot raise a profile the journal is already past (the remedy today is restoring the file, docs/operator.md); (c) the profile fields are capped at 2^24 by `fn-bpn-machine-limitp` (PRF-131's representation ceiling), a data ceiling until the machine state's limit widens; (d) tools/run_bp_receive.py still checks a staged request ADU against 65,538 (a Python check of a value ACL2 decides) — books/bp-node-profile, bp-node-profile-replay, bp-node-machine, tools/run_bp_receive.py — bp-lifecycle-4 record "PKT-294" items 4, 6, 7, 8 — GOAL-UNOWNED (§9, D27; (d) small)
- [ ] PKT-312 A specification cites a theorem the tree no longer defines: specs/bp-node-machine.md names the retired `fn-bpn-limits-compose` at lines 2868, 3948, 3953 and 4133 (at 7f7f980c; the D-9 reassembly limit "65538, raised only with fn-bpn-limits-compose"), whose statement (a 65,538-octet ADU fits one 131,072-octet sender job image) is false at the new widths; the replacement is `fn-bpn-limits-compose-at-the-codec-widths` (books/bp-limits.lisp). Tooling gap: no checker flags a spec's citation of a theorem name no book defines (ledger.py checks registry rows, not specs). Fix the four citations and the D-9 row, and add the spec-citation check (every backquoted `fn-...` theorem name in specs/ resolves to a defthm) to `make check` — specs/bp-node-machine.md, tools/ledger.py or a new tools check — the deputy's finding over 9ebf5f0e — GOAL-UNOWNED (§14; small, high reuse)
- [x] PKT-313 THE CHECKPOINT HAZARD: since 08a16546 `store checkpoint` completes at N=10,000 x 32 KiB (154 s, 22.5 GB, a 670,413,555-octet file) and the untouched reader cannot open it: the owner dies "Heap exhausted" in `FNN-STATE-CHECKPOINT-LOAD` after 56 s (segments read as octet lists by `fnn-state-checkpoint-segments`, 10.7 GB of conses, then `fn-scc-decode-tree` beside them), a fatal error and not a refusal, so the corrupt-checkpoint fallback does not run and the store is unopenable until the operator removes store-checkpoint.fnsc (the full replay then opens in 164 s at 22.5 GB). Before the merge the publication died and the store stayed operable from its journal. The deployed scale profile auto-publishes at 2,048 records (about 135 MB at 32 KiB), below the failing size. Owed: the decoder over the buffer (PRF-135 over the host-called `fn-store-sco-decode`), a named refusal that falls back to the journal, `status` naming it; and the next qualification must show a checkpoint at the profile's largest admitted article size times K/2 both publish AND reopen (PKT-300 (b) updated); until then the operator page should say to remove the file — books/store-checkpoint-codec, store-checkpoint-open, host/store-node-host.lisp, host/native/io.lisp, docs/operator.md — rep-wave-d-2 record §5, §8; merge 08a16546 "THE HAZARD" — OWNED:rep-wave-d-3 (the qualification half with PKT-300) — closed 4f386f11 (PRF-135 `fn-sccr-decode-of-plan` over the host-called `fn-store-sco-decode`; a checkpoint past `fn-store-sco-file-read-bound` answers `open=full-replay reason=checkpoint-exceeds-bound` and the journal replays; the N=10,000 x 32 KiB checkpoint reopens in 132.3 s at 19.3 GB peak; the qualification half stays PKT-300 (b): candidate b6759850 predates the repair)
- [ ] PKT-314 The checkpoint file holds every payload twice: `fn-sco-freeze` carries the replayed store (its history with the payloads) and the event index (the records again): 670 MB for 320 MB of payload, which doubles what the reader must hold; decode once into the buffer-held record or name exactly why the copy stays (a format change moves the checkpoint format version and its upgrade) — books/store-checkpoint-open.lisp `fn-sco-freeze`, store-checkpoint-codec — rep-wave-d-2 record §5 "The file holds every payload twice" — OWNED:rep-wave-d-3 (task 1)
- [ ] PKT-315 The checkpoint writer costs about 0.12 us per octet (+8.8 s on the N=1,000 x 32 KiB verb against the same-hour baseline): one guarded, `:protect`ed `fn-octets-append-octet` export call per octet; the fix is a bulk `fn-octets-append-list` export with the concrete `fn-oct-write-list` as its `:exec`, one call per payload, and the buffer book's closure recertified — books/octets-stobj.lisp, store-checkpoint-buffer — rep-wave-d-2 record §5, §8; LANEDUMP item 3 — OWNED:rep-wave-d-3 (task 3, if its budget fits; else GOAL-UNOWNED)
- [ ] PKT-316 The 64 MiB collection trigger's placement: set in `fnn-main` before the open, it halves `store checkpoint`'s residency and slows the checkpoint reopen by 12 s at N=1,000 x 32 KiB (28.1 against 16.0 s, same residency; +2 s at N=10,000 x 2 KiB), because the decode's 7 GB of allocation is collected a hundred times more often against 1.4 GB live; choose: set it after the open, or a larger nursery during recovery, with the record's numbers — host/native/io.lisp `fnn-main` `+fnn-gc-nursery-octets+`, host/native/owner.lisp — rep-wave-d-2 record §5, §8; LANEDUMP item 4 — GOAL-UNOWNED (the coordinator's call; one line, then a matched rerun)
- [ ] PKT-317 Live heap after a full collection is not measured anywhere on the representation path: VmRSS at LISTENING varies with where the collector was (5.01 against 2.73 GB at the same VmHWM order at N=10,000 x 2 KiB), so no residency claim is a live-heap figure; owed: the profiling twin image (with `heap.lisp`) built beside the developer image by the measurement tooling, and live-after-GC rows in tools/rep_measure.py — tools/rep_measure.py, host/native build lists — rep-wave-d-2 record §5, §8 — GOAL-UNOWNED (§7, D26; rep-wave-d-3 uses it "if the profiling twin exists by then")
- [ ] PKT-318 Carrier signature verification at B over BP is not evidenced: source-corpus-2's "Not done" said it was never reached (B refused before its store); native-7 now shows the signed carrier accepted at B after the mid-receive SIGKILL, but the record names no verdict line for the carrier signature at B (PRF-127 is the byte half, PRF-132 the Store binding). Record B's statement verdict for the signed element (or a must-fail: a carrier whose signature does not verify is refused at B over BP) in tests/test_native_source_corpus_bp.py — tests/test_native_source_corpus_bp.py — source-corpus-2 record "Not done", "source-corpus-3 (continued)" — GOAL-UNOWNED (§5.3, §9; small)
- [ ] PKT-319 mission-signed-2's dev-head confirmation image was not built (the acquire failed transiently to load its certificates); the before/after evidence is f1734955 against aa0f6c16, not dev's head. The next qualification's four images and the mission modules cover it; name it there — the qualification brief — mission-signed-2 record "Not done"; LANEDUMP — GOAL-UNOWNED (§15, with PKT-300)
- [ ] PKT-320 hbox scratch /tank/fn/scratch/rep-wave-d-2/ (about 3 GB) can go, but rep-wave-d-3's brief reuses its N=10,000 x 32 KiB store copy for the reopen row: remove it after rep-wave-d-3 merges — /tank/fn/scratch/rep-wave-d-2 — merge 08a16546 "Not done" — OWNED:rep-wave-d-3 (then the deputy)
- [ ] PKT-325 (key-replay-fixture) The explicit re-evaluation of a key statement under today's grants, `operator CONFIG keys redecide MSGID`, is specified and not built: an operator action through the control socket (offline refused, never an effect of open), which ACL2 decides as a new acceptance of the stored statement's source under the grants of the configuration in force at the redecide's own txid (fn-ks-plan over the live rows at that txid), filed as a new durable statement record whose txid is its own, so the next open's recorded reopen reproduces it and PRF-124/PRF-140 carry over (the record's shape, a re-filed kind-4 composite or a redecide record, is the open design question, charged at admission under D27), refusing by name a MSGID that is not a stored key statement or whose statement already acted; the verb and its theorem (the redecided plan is fn-ks-plan under fn-ctl-config-at its txid; a later open is blind to later configuration) and a native case (decline, grant, redecide -> enrol-successor committed; restart -> nothing) — books/key-statements.lisp, books/native-operator.lisp, host/native/owner.lisp — key-replay-fixture record "PKT-325"; supersedes PKT-241 — GOAL-UNOWNED (one lane)

## Sweep 9 (wave-2 deputy, 2026-09-26 ~06:45 UTC): dev 7f7f980c..88ec3ef0

Sources: 28 dev commits (15 on the first-parent line): three lane merges (rep-wave-d-3 4f386f11, Fable; consumer-e2-2 45ab031c; key-replay-fixture fc99c076), the candidate b6759850's closure manifest 145ed138 (940 books, two over 10 s at 4 jobs on a loaded box), certification batch 17 db073533, the proof-cost baseline 04372f7e (bp-node-job-offer 11.6 s admitted with its load named; checkpoint-codec, config-owner-live, native-admin, peer-authored-accept and store-reclaim removed as improved, which retires those five names from PKT-114 and PKT-225), the mandate reconciliation correction 8d4ea42c (the chain 503 was a harness fixture; the real finding is the commit regression), gpt-6's two reviews planning/review-2026-09-26-gpt6-wave2.md (8ca933b1) and planning/review-2026-09-26-gpt6-answers.md (8f5f9d0b), the deputy's questions planning/for-gpt6-2026-09-26.md (b6759850), the decisions file's additions (PKT-173 a answered by implementation; PKT-322, 197b3437), the sweep-8 commit 8ed70574 and the ledger regenerations; the records planning/evidence/rep-wave-d-3-2026-09-26.md, consumer-e2-2-2026-09-26.md and key-replay-fixture-2026-09-26.md, and build/coordinator/LANEDUMP-{rep-wave-d-3,consumer-e2-2,key-replay-fixture,pack-chain-serve}.md. spike/mega is unchanged at f6d947d0.

Running lanes and what their briefs own (build/coordinator/queue/): bp-lifecycle-5 (PKT-308: the per-arrival cost, PRF-136, SCN-077), capacity-vector (PKT-323: gpt-6 wave-2 §3's composed capacity statement and resource vector, SCN-081's fill-release-maintain-recover lifecycle, then PKT-263 steps 3 to 6 as far as its budget reaches), hot-path-scans (PKT-324: wave-2 §4's byte-count cache walk and the BP selector's repeated lookups; PKT-301's commit-path term), rollback-history (PKT-327: answers §7's misstep, with PKT-287's packs), signed-history-index (PKT-330: the signed-entry Message-ID index, the incremental identity prepare, the loaded greeting if its budget reaches: PKT-223, PKT-291 (2), PKT-190), pack-chain-join (PKT-332: lane/bounds-p5 joined with the reclaiming pack and the disk check; pack-chain-serve's finding 4, the compact verb's space budget over one pack of the whole history, rides its obligation 1), commit-regression (the coordinator's: PKT-348 below), qual-b6759850 (PKT-321; the three reframings of answers §4 were sent to it). Queued: rep-wave-d-4 (PKT-328, Fable: answers §1's payload-owning slice with PKT-307's single-payload checkpoint, the execution-memory fallback before fatal allocation, live heap after collection), pack-chain-open (PKT-331: the chain open over the buffer, the 20,000-article claim, the fixture built once), outcome-algebra (PKT-329: answers §5, PKT-295, PKT-246 and PKT-275's CONFLICT word), and ten-second-4 (PKT-326: bp-node-job-offer, bp-node-machine-authorization, owner-invariants; its brief is in the queue though the deputy's roster does not list it).

Ticked in place above: PKT-313 (4f386f11; the qualification half stays PKT-300 (b)) and PKT-241 (fc99c076, superseded by PKT-325). PKT-233 was already ticked at 4b710f03; fc99c076 removes the `:current` switch it pointed to and adds PRF-140 (an accepted statement cut before its key change finishes under its admission context), so PKT-173 a stays WAITS ON EMBER only for ember's confirmation (overruling it is now a Store format version).

Ownership moved by the window's merges (the lines above keep their original mark): PKT-314 (the second payload copy) and PKT-317 (live heap after collection) were OWNED:rep-wave-d-3, which did neither; both are OWNED:rep-wave-d-4 (queued). PKT-315 (the writer's bulk `fn-octets-append-list` export) was not reached by rep-wave-d-3 and rep-wave-d-4's brief does not name it: GOAL-UNOWNED. PKT-316 (the trigger's placement) is unchanged and still the coordinator's call; rep-wave-d-3 notes the open's decode no longer allocates the 7 GB it collected, which is where the 12 s went. PKT-320: rep-wave-d-3 has merged, so /tank/fn/scratch/rep-wave-d-2 is the deputy's to remove, after asking whether rep-wave-d-4's baseline rows reuse its stores. PKT-300 (b): the reopen is repaired on dev at 4f386f11, after the candidate; answers §4 adds that the case is sized by the captured state, not by K/2 (sent to qual-b6759850). PKT-293 and PKT-167: answers §1 gives the design (two views, a shared transition, a state-and-effect relation) and rep-wave-d-4 (queued) implements it; both lines stay WAITS ON EMBER for ember's confirmation. PKT-253: the consumer half is done at 45ab031c (tools/fn_consumer.py runs `fn_verify.py check-article` on every report with its own keyring); the web reader's fifth fact is still "not performed". PKT-265: answers §7 says the cold-start-only fragment/job relation becomes a misstep if an optimization removes the hot checks that rely on it; bp-lifecycle-5 must not drop a check whose preservation is unproved. PKT-304 and PKT-263 (6) (guards on called BP and reclaim books): answers §7 ranks them above the model-only count. PKT-189 (quadratic cold-open validation) and PKT-079/PKT-143 (the publication program) are answers §3's third and fourth targets and stay GOAL-UNOWNED. PKT-291 (1), the multi-listener relay, is rank 4 of the next convergence (answers §4) and stays GOAL-UNOWNED. key-replay-fixture's reachability argument (no transition removes a configuration record; `max-config-generations` refuses, never drops) answers wave-2 §5's retention ask; a future configuration compaction must name the refusal or record the disposition (in the book comment and specs/peering.md; PKT-196's constraint list).

- [ ] PKT-307 (rep-wave-d-3's reserved id) What the checkpoint-reader lane left: the file's second copy of every payload (the fold's node and the event index both hold the records: PKT-314), the writer's bulk export (PKT-315), the seal over a buffer range (PKT-349), live heap after collection (PKT-317); its §7 decision packet (the checkpoint value carries each record once, a schema-3 bump the reader accepts beside 2 for one release) is answered by gpt-6's answers §1 (combine with the arena slice, file-local references translated to handles on load) — books/store-checkpoint-open.lisp `fn-sco-freeze`, store-checkpoint-reader — rep-wave-d-3 record §3, §7, §8; merge 4f386f11 "Not done" — OWNED:rep-wave-d-4 (queued)
- [ ] PKT-322 (consumer-e2-2's reserved id) After a revocation fn refuses the exact saved artifact's resend on enrolment before it checks identity, so a revoked author's uncertain operation settles only through the stored copy served back or `store inspect`: (a) keep the gate order (in force), (b) answer D25 for a source authored before the revocation, (c) a privileged operator query — host/native/hybrid-control.lisp `fnn-hybrid-control-author`, PRF-117, NNT-019, PKT-164 — consumer-e2-2 record "PKT-322"; decisions file "PKT-322" (197b3437) — WAITS ON EMBER
- [ ] PKT-333 The signed two-store application exchange (gpt-6 wave-2 §7, the next major deliverable): A durably authors signed R; R crosses the relay network while B sleeps; B verifies R independently (`fn_verify.py check-article` with B's own keyring, never fn's verdict), commits its transition and immutable reply Q and acknowledges its consumer position; Q crosses back under interruption; A verifies and correlates it; repeated transfers, lost replies and consumer deaths produce no second application transition; conflicting operations in both orders under the application's claims. Record per hop the separate identities (application operation, authored source, signature carrier, hop-local stored projection, bundle, forwarding attempt). consumer-e2-2 built the one-node half and wrote the interface (its record "Interface for the next lane"); the mission's driver still turns relay listeners (PKT-291 (1)) — tools/fn_consumer.py, tests/bp-dtn7/run_mission_four_node.py, tests/test_native_consumer_exchange.py — review-2026-09-26-gpt6-wave2 §7, §8; consumer-e2-2 "Not done"; merge 45ab031c — GOAL-UNOWNED (§10, §9; the convergence's first outcome)
- [ ] PKT-334 A hot-path dependency checker with size provenance (answers §2): seed the dimensions B (new request bytes), K (response), N (retained history), F (held fragments), J (queued jobs) and report paths from host entries to traversals of them (recognizers, encoders, replay folds, `len`, `nthcdr`, membership searches, list conversions), following the executed path (`mbe` :exec, attachments, guard evaluation, host wrappers, callbacks, stobj exports), classifying each as cold, resumable, output-proportional, unexpected retained-state or unresolved, with no approved baseline that hides existing finds; reuse the s-expression reader and `session_depth.py`'s seeded-propagation style. Paired with one-dimension scaling tests (vary N at a fixed request, payload size at a fixed N, fragment count at a fixed ADU) recording primitive visits, bytes copied, allocation, lock-hold, CPU and wall time. Silence of the checker is not a proof — tools/ (new), tools/session_depth.py, tools/rep_measure.py — answers §2 — GOAL-UNOWNED (§6, §7, §14; the method that finds the next PKT-294 before a native run does)
- [ ] PKT-335 A measured v1 service envelope for a named execution profile (answers §3): one Linux/storage/runtime profile, ~32 KiB articles, a stated group distribution, three concurrent readers, latency at a declared sub-saturation load and sustainable throughput separately; rows: warm greeting after the store is loaded, OVER over a fixed 40-row window, unsigned and hybrid-signed POST terminator-to-durable-reply p95, sustained mixed posting (target >= 10 POST/s with no growing queue), checkpoint-assisted and full reopen, at N = 10,000 and 100,000, with retained octets, signed mix, groups per article, pins and suffix length reported beside N. Today's harness measures the greeting before the store loads and never measures a signed POST; publish the weaker measured tier if a storage profile cannot meet the targets, never tmpfs numbers as deployment performance — tools/rep_measure.py, docs/operator.md — answers §3; rep-wave-d record's baseline — GOAL-UNOWNED (§7, D26, §15)
- [ ] PKT-336 The restore-witness correction to decisions-packets packet 2 (PKT-228; wave-2 §6): a sampled per-group high-water is not an upper bound on every number issued (backup through 100, witness 110, the old node serves 111 to 120, the rebase reissues 111), and equal transaction IDs and high-waters do not establish equal history; the success branches need three separate properties: the identity and integrity of the referenced history, coverage of every prior allocation (a final observation from a quiesced writer or a durable allocation bound established before issue), and a floor transformation that preserves existing mappings; without coverage the reuse stays uncertain. Revise the packet before any restore lane implements a success branch — planning/decisions-packets-2026-09-25.md packet 2, books/checkpoint-auxiliary.lisp `fn-cpa-rollover-proposal` — review-2026-09-26-gpt6-wave2 §6 — GOAL-UNOWNED (the coordinator's packet revision; PKT-228 stays WAITS ON EMBER)
- [ ] PKT-337 The field-13 correction to decisions-packets packet 3 (PKT-229; wave-2 §6): do not add a `>= 64` check to profile field 13: the format-8 validator accepts 1 to 2^32-1 today, so saved profiles with 1 to 63 would become invalid for an unread field; mark it reserved, stop advertising an effect it does not have (as PKT-296 proposes for field 10), keep every valid profile readable, and call 64 the current v1 grammar limit, not a justified work limit (a later version may process larger policies resumably; a local resource refusal stays distinct from invalid portable authority) — planning/decisions-packets-2026-09-25.md packet 3, books/byte-store-frame.lisp field 13, docs/operator.md's profile table — review-2026-09-26-gpt6-wave2 §6 — GOAL-UNOWNED (the packet revision, then the operator-page line; PKT-229 stays WAITS ON EMBER)
- [ ] PKT-338 A repair path for a store an older image committed past H (PKT-249's unopenable store): validate the complete history against a proposed larger profile and upgrade to it, never truncate accepted records to fit the old profile; today no verb recovers it ("transaction recovery input exceeds configured bound", exit 4) — books/store-profile-upgrade.lisp, host/native `store upgrade-profile` — answers §4 "an old over-H store remaining refused" — GOAL-UNOWNED (§5.6, §15)
- [ ] PKT-339 The fragment coverage precheck (PKT-294 item 2) must be interval-union coverage, not the sum of fragment extents: overlapping and duplicate fragments make the sum wrong; maintain it incrementally per family and keep the conflict-before-gap order when overlaps disagree — books/bp-fragment-sweep.lisp, bp-node-fragment-family — answers §2 "For fragments" — GOAL-UNOWNED (§7, §9; bp-lifecycle-5 may meet it with its family index)
- [ ] PKT-340 The payload arena grows by doubling (`fn-arena`), which is amortized, not a bound on one scheduling step or on the resize peak; never quote amortized seal cost as a latency guarantee; a paged arena or incremental growth with a per-step work theorem is owed once the arena is on the served path — books/payload-arena.lisp — answers §1 "One caveat" — GOAL-UNOWNED (§7, D27; after rep-wave-d-4)
- [ ] PKT-341 Unprovided necessity witnesses counted as teeth: a `must-fail` whose counter-witness would need 2^64 octets (rep-wave-d-3's length-width hypothesis of `fn-sccr-decode-of-plan`; its header-one-octet-short case "recorded as the prover's refusal"), and similar width hypotheses, show proof-search refusal, not necessity. Owed: teeth_check and the ledger label such entries `unprovided-necessity` (or require a separately justified reduced-width model), and an audit of the existing `must-fail`s marked as prover refusals — tools/teeth_check.py, tools/ledger.py, tests/acl2/store-checkpoint-reader-tests.lisp — answers §7 "Another boundary"; rep-wave-d-3 record §1 — GOAL-UNOWNED (§6, §14)
- [ ] PKT-342 gpt-6's proposed AGENTS.md rule: "A producer's admitted domain is a lifecycle contract: any change to a bound, representation, or summary must show that downstream consumers and recovery preserve its meaning within the declared execution-resource profile. Passing the producer's codec or publication test alone is not completion." (the checkpoint writer/reader mismatch, the sender-job/ADU mismatch, hidden per-arrival work) — AGENTS.md — answers §7 "The one rule" — WAITS ON EMBER (with the coordinator)
- [ ] PKT-343 The first external observation: a second operator on a second physical machine, using the installed production artifact and the operator instructions, with separately generated identities and no shared keys or configuration; they establish peering, exchange signed R and Q, disconnect, restart, inspect retained work, run the consumer on each side, and must tell accepted, pending transport, unresolved acceptance and processed application work apart without developers interpreting state; the operator walk (SCN-076) is regression material, not this evidence; ION stays the next BPA interop target after a stable relay loop — the production image, docs/operator.md — answers §6 — WAITS ON EMBER (it needs a person and a machine)
- [ ] PKT-344 After a SIGKILL the owner's control socket file stays behind, and offline `control` commands are refused until an operator removes it (the PRF-140 native case deletes it before the offline revoke, af119e41). Owed: ACL2 decides a stale socket (no listener, the store lock free) as absent at the offline verbs' entry, or the owner's recovery removes it under the lock, with a native case (SIGKILL, then an offline `control` verb succeeds); beside PKT-283 (health says `fenced store-held` until the restarted owner listens) — host/native/operator.lisp, host/native/owner.lisp control socket setup — key-replay-fixture record "Not done", LANEDUMP Open; merge fc99c076 — GOAL-UNOWNED (§11)
- [ ] PKT-345 tools/native_program_check.py reports an unbalanced paren in a host file as a `StopIteration` from its own reader, which reads like a tokenizer fault (rep-wave-d-3 first took it for an apostrophe in a comment; the developer image build then hit end of file at host/native/io.lisp:1639 `fnn-state-checkpoint-plan`): report "unbalanced: FILE:LINE, form starting at LINE" with a test — tools/native_program_check.py — merge 4f386f11; 389c47e4 — GOAL-UNOWNED (§14; small, with PKT-305's local reader check)
- [ ] PKT-346 persvati's 16 ACL2 slots held by 15 orphan `proof_repl.py` sessions of finished lanes (peer-keys-repl, reclaim-lifecycle-repl, rep-wave-c-repl, status-join-repl, consumer-e2-2-repl, ...; rep-wave-d-3's LANEDUMP still names a live `sccr` session), so `certs.py install-partial` waits indefinitely for a slot (key-replay-fixture waited 24 minutes, consumer-e2-2 and pack-chain-join moved to hbox). Owed: a session belongs to its lane and ends with it (a lane tag and an idle deadline in tools/proof_repl.py, a `proof_repl.py list/reap` the coordinator runs at merge, the slot pool refusing a submit with the holders named instead of waiting silently) — tools/proof_repl.py, tools/acl2_slots.py, tools/certs.py — key-replay-fixture record "Certification"; consumer-e2-2 record; WAVE-STATE — GOAL-UNOWNED (§14; the reap itself is the deputy's now)
- [ ] PKT-347 Build and preserve one source-identified 20,000-article scale fixture (the compacted 13-generation chain store of chain-native-8 at /tank/fn/scratch/bounds-p5) under /tank/fn/scratch/fixtures/chain-20000-<image-rev>/ with its sha256 manifest and origin, so no five-hour build precedes each attempt to refute one compaction property — answers §4 "For the scale gate"; LANEDUMP-pack-chain-serve — OWNED:pack-chain-open (queued; task 3)
- [ ] PKT-348 The commit regression: after dev 483987b1 `store probe 2000 article` took 409.6 s (205 ms per commit) against 9.1 s for 1,000 (9 ms) on the branch's earlier image, about 20x per commit; the unproved suspect is `fnn-subject-id-buffer` passing the payload as an octet list through `fnn-core`; rep-wave-d's 250 to 400 ms ZFS POST may be the same regression misread as durable publish. NOTE: the lane's brief names PKT-283 and PKT-284 for its finding and fix, and both ids are taken above (the health-after-crash state; the build script's ACL2-error refusal): the deputy should give the lane this id — build/coordinator/queue/w2-commit-regression.txt, host/native/io.lisp — 8d4ea42c; LANEDUMP-pack-chain-serve "Separate finding" — OWNED:commit-regression
- [ ] PKT-349 The checkpoint seal over a buffer range without a transient chunk list: `fn-frame-trailer` is closed and constrained, so each segment's seal still conses one list of up to R octets (16 bytes per octet); a range twin needs `sha256-buffer` to take a range and the seal restated over it with the chain's theorems — books/store-checkpoint-reader.lisp, sha256-buffer, frame-fields `fn-frame-trailer` — rep-wave-d-3 record §2, §8 — GOAL-UNOWNED (§7, D27; with PKT-027)
- [ ] PKT-350 The owner's recovery is related to its model by construction (as PRF-098), not by a theorem: `fnn-owner-key-statement-recover` -> `fn-owner-key-statement-rows` = `fn-ks-statement-rows` is a definition, and the host-to-model correspondence of the recovery path has no named theorem — host/owner-host.lisp, books/key-statements.lisp, PRF-098, PRF-140 — key-replay-fixture record "Not done", "Assurance chain" — GOAL-UNOWNED (§6)
- [ ] PKT-351 consumer-e2-2's remainder: one consumer process per database (the in-flight recovery assumes a single process; a second process would read the first's live attempt as unanswered); tests/test_anchor.py's store-command tests fail on hbox for the environment (no ACL2 on the runner's PATH), so the anchor rule runs on the laptop only; the attempt journal is Python, covered by CNS-003 and tests, not a theorem — tools/fn_consumer.py, tests/test_anchor.py — consumer-e2-2 record "Not done", "Step 4" — GOAL-UNOWNED (§10, §14; the single-process rule should be enforced by a database lock, not stated)
- [ ] PKT-352 planning/evidence/mission-four-node-2026-09-25.md's "PKT-170 (for ember)" section still says "Default (kept): a relay may hold and forward transit from an unadmitted channel"; PRF-128 (09ce5e77) reversed it. Add a superseding note naming PRF-128 and `fn-bpaj-refused-channel-takes-no-custody` — planning/evidence/mission-four-node-2026-09-25.md — review-2026-09-26-gpt6-wave2 §7 "For PKT-170" — GOAL-UNOWNED (docs, one paragraph)
