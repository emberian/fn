# Broad capability wave — 2026-09-24

The user asked us to replace serial prerequisite-sized advances with a large
concurrent implementation wave, accepting temporary development breakage and
converging afterwards. This changes sequencing, not the required end state or
the standard for an assurance claim. Work can be checkpointed and integrated
with explicit unqualified obligations; a failed theorem is not silently removed
or replaced with a claim about an easier API.

The current qualification subject is frozen `b074f94e`, independently of the
moving development branch. Its exact detached source passed the combined
closure using matching cached certificates, run `run-20260924T053517Z-1fb9`
on hbox at `/tank/fn/gates/wide-combined-b074f94e-exact-20260924`;
the manifest is `certify-20260924T053544Z-951104.json`. Native image tests are
next. A preceding run copied a transient unresolved cherry-pick from main and
is excluded from revision-level qualification even though its process exited
zero. New qualification snapshots come from an isolated pinned checkout or
immutable export, never the shared integration working tree.

## Complete capability targets

These are cooperating work streams, not exclusive ownership. Agents negotiate
shared changes directly. Root integrates coherent checkpoints and reviews
combined behavior; it does not approve each lemma or require a green whole
tree before independent work proceeds.

| Stream | Outcome pursued in this wave | Cooperation / remaining assurance |
| --- | --- | --- |
| BP engine | Actual native held-bundle dispatch, forwarding attempt/result, session/MRU selection, durable namespace/debt admission and restart. | `bp_foundation` implements with `bp_counterexample_completion` on exact cached debt and reachable N03/N04/N05 traces. Topic replay proof continues alongside; its absence does not stop implementation. |
| Topic authority | Versioned administrator-generation binding through install, anchor, report, exact retry and reopen, while preserving historical v1 bytes. | `topic_metadata` implements; `consumer_contract` owns the cooperating maintained topic/Store/crash/replay proof. No silent downgrade or invented succession policy. |
| Storage publication | Actual served retention preparation through canonical record publication, barrier outcomes, completion and reopen across the program's cut family. | `storage_kernel_completion` joins owner/byte relations; `status_codec_proof` covers root-directory error choices. Related call-entry and physical-platform assumptions remain explicit. |
| Preservation | Safely retire duplicate old packs and regain storage capacity without changing retained event history or obligations. | `retire_old_worktrees` now implements preservation: ACL2 namespace/retire plan, native exclusive owner, interrupted unlink/barrier/reopen, auxiliary topic/consumer/identity comparisons. Active-reader tests share the image campaign. |
| Agent conversation | Two independently configured native Stores, protected peering, durable Mini report processing and cursor ACK, immutable signed reply, restart and lost responses. | `mini_reply_join` and `native_qualification` share existing images and isolated ports. The two previous E2/B3 legs are prerequisites, not the composed result. |
| Author lifecycle | Local operator rotation/revocation with durable history and stable historical signature verdicts, preserving unrelated principals. | `assurance_review_followup` implements ACL2 decisions, native controls, codecs and tests. Enrollment generation is not global revocation of every other principal. Portable delayed acceptance and remote succession are separate policy questions. |
| Human client | Durable outbox and editable drafts, exact frozen submissions, restart discovery and read-only settlement without automatic repost. | `review_branch_inventory`; separate loopback client only, no Python server semantics. Local outbox durability and observed server acceptance remain distinct. |
| Transport extension | Close the concrete ION/LTP observation and EID-mapping obstacles to durable receipt-return exchange. | `stamp_review` is a bounded Astra architecture/adapter lane because the existing thread slot is Astra; substantial ongoing implementation should transfer to Sol when available. ION detention/lifetime and destructive receive assumptions must be explicit. |
| Qualification and efficiency | One source-matched production/developer image campaign; indexed poll costs, interrupted BP fragments, topic/reclaim paths and two-Store exchange. | `native_qualification` shares its images. Proof/cache work and native workflow costs are measured separately. Mini replay optimization remains separately pinned until its complete handoff repeats. |

Eleven Sol agents and one bounded Astra transport lane occupy the available
subagent slots. This is agent parallelism, not permission for twelve simultaneous
proof farms. Shared machine pools, four-job combined certification, at most
two-job scoped runs and the 300-second discovery bound remain. Agents reuse
source/closure/toolchain-matched cache and communicate missing-parent needs.

## Convergence obligations

- A checkpoint names behavior present, source revisions, known failures and
  missing assurance. Runtime activation in development can precede full proof;
  deployment and release claims still require their evidence.
- Preserve the exact-source hybrid profile, indefinite accepted retention until
  authorized release, and separate uncertain/refused/accepted outcomes.
- Keep semantic ownership in executable ACL2 and prove the called paths or their
  named correspondences. Intermediate red books remain visible work, not excuses
  to weaken statements or introduce host twins.
- Qualify a pinned source while development continues. At convergence, combine
  current packets, classify actual failures once, and fix them cooperatively.
- Live `/tank/fn/node` remains protected. New tests use isolated stores, images,
  ports and failure fixtures; the next deployment is a separate action.

Private-group crypto and broader governance remain design work until their
policies/profiles are selected; the request for greater implementation breadth
does not select a cryptosystem. The existing development 9P view is a starting
point for a future native port, not a newly qualified native service in this
wave. Those avenues stay visible rather than being mislabeled complete.
