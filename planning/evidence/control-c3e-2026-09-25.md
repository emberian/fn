# Control C3e: the served withdrawal answers wired (2026-09-25)

Lane `lane/control-c3e`, from `dev` `e747dbcc`. Brief:
`build/coordinator/queue/control-c3e.txt`. Rows: PRF-084, NNT-011, SCN-044.
**Status: step 1 done** (continuation control-c3e-2, below): the wiring
certifies in full (persvati r5, hbox r6 over the image profile), the
developer image serves 423/430/HDR :fn-control natively and `fn_verify
--withdrawal` exits 0. Steps 2 to 4 and the owner-level teeth are not done.

## What changed (step 1)

- `books/group-bucket-index.lisp`: the group pin has a fourth slot,
  `fn-gidx-pin-with-control TRIE BUCKETS CONTROL`; `fn-gidx-pin` is it with
  nil; `fn-gidx-pin-control`.
- `books/control-served.lisp`: now includes `control-authority` (not
  `control-visible`), so `books/nntp.lisp` can include it (adds config,
  control-classify, control-authority, control-served to nntp's closure).
  `fn-ctl-pin W WS`; `fn-ctl-served-held` / `fn-ctl-served-status` (the
  trie twin of the kernel, `fn-ctl-served-status-is-control-status` under
  `fn-midx-correspondencep`); `fn-ctl-subseq-diff` copies the withdrawn
  tail as a true list, so `fn-ctl-refresh-withdrawn-is-withdrawn` and
  `fn-ctl-subseq-diff-of-filter` hold for any RAW (the old true-listp
  must-fail became a witness).
- `books/nntp.lisp`, `fn-nntp-archive-command-pinned` (and its concrete
  twin `fn-pix-archive-command-pinned`, books/peer-offer-indexed.lisp):
  three arms before the fall-through: `423 withdrawn`
  (`fn-nntp-number-withdrawn-p`: W holds the number, the archive does not),
  `430 withdrawn` (`fn-nntp-msgid-withdrawn-p`: W holds the Message-ID, the
  pinned trie misses), `HDR :fn-control <msgid>`
  (`fn-nntp-control-hdr-response`: 225 and `0 ITEM`; 430 for no article; 503
  if the item is not a clean field). Cost: one walk of W per retrieval, then
  the scan/lookup the retrieval already does.
- `books/served.lisp`: the served connection has ten fields
  (`fn-served-conn-control`); `fn-served-conn-pinned-index` builds the pin
  with it. `books/owner.lisp`: the owner connection has twelve fields
  (`fn-own-conn-control`, set from `fn-own-view-control` at open, peer open
  and advance, copied by read, read-step, finish-read, reader-context); the
  view has nine (`fn-own-view-withdrawn`, W, refreshed by
  `fn-ctl-refresh-withdrawn`; start sets `fn-ctl-subseq-diff`).
- K1 restated: every `fn-served-make-conn-group-indexed` /
  `fn-own-conn-make-group-indexed` / `fn-own-view-make-visible` call in the
  books and tests (served, served-carried, nntp-auth-fold,
  nntp-auth-invariants, poster-bytes-invariants, owner, owner-agent,
  owner-tls-prefix, owner-verdict-read, owner-advance-carried,
  owner-invariants, owner-prepare-correspondence, owner-served-carried,
  config-owner-live, config-owner-read-invariants,
  config-owner-advance-reader-invariants, owner-recover-ocl, tests).
- `books/owner-invariants.lisp`: `fn-own-view-okp` carries
  `W = fn-ctl-subseq-diff RAW VISIBLE`; `fn-own-conn-okp` carries
  `fn-own-control-okp` (the archive serves the pinned prefix's visible list
  under the pinned records, W is the rest).

## Theorems (statements in the books)

| Theorem | Book | Status |
| --- | --- | --- |
| `fn-nntp-423-withdrawn-is-a-withdrawn-holder`, `fn-nntp-withdrawn-holder-answers-423-withdrawn` | nntp-control | certified r4 (installed/certified; see manifest) |
| `fn-nntp-430-withdrawn-is-a-withdrawn-article`, `fn-nntp-withdrawn-article-answers-430-withdrawn` | nntp-control | certified r4 |
| `fn-nntp-hdr-fn-control-is-the-status`, `fn-nntp-hdr-fn-control-executed-means-withdrawn` | nntp-control | certified r4 |
| `fn-ctl-served-status-is-control-status`, `fn-ctl-served-held-is-find-held` | control-served | certified r2/r4 |
| `fn-own-read-archive-command-is-the-pinned-dispatcher` (host line: host/owner-host.lisp `fn-owner-chunk` -> fn-ocfg-read-tls-prefix -> fn-own-read) | owner-control-read | **red**: `fn-octl-dispatch-archive-command` (r4, 3 errors) |
| `fn-own-related-conn-control` | owner-control-read | blocked by the above and owner-invariants |

Teeth: `tests/acl2/nntp-control-tests.lisp` (witness through
`fn-nntp-archive-command-pinned` per keystone: ARTICLE 1 / STAT 1 answer
`423 withdrawn`, ARTICLE <t> answers `430 withdrawn`, HDR :fn-control <c>
answers `0 executed withdrawal <t@example.invalid> author`, <d> `owed`, <t>
`none`; a must-fail per substantive hypothesis: the pin's W, the arm's
decision, the archive's miss, the trie correspondence, the keyword, the
argument shape, the selected group). REPL-admitted on persvati; certified in
r4 only if its closure certified (see the manifest). No teeth yet for the
owner-level lift.

Restated elsewhere (honest consequences, not new claims):
`fn-nntp-archive-command-pinned-msgid-arms-are-the-scan` (nntp-pinned-msgid,
PRF-067 text updated) and `fn-served-dispatch-msgid-retrieval` /
`fn-served-step-msgid-retrieval-carries-the-local-number`
(owner-list-counts-read) gain the hypothesis that the pinned view did not
withdraw the number / Message-ID.

## Runs (persvati, ACL2 8.7 w25, 2 jobs, 300 s)

- triage `triage-20260925T200535Z-b64e` (rounds run-20260925T200544Z-5340,
  run-20260925T202049Z-db07) at 322babde: 7 independent reds.
- r2 `run-20260925T204403Z-07a2` at the HDR-effects commit: one root red
  (`fn-nntp-control-hdr-response-effects`), everything above cascaded.
  Manifest `planning/evidence/manifests/certify-20260925T204422Z-1080266.json`.
- triage `run-20260925T204717Z-430c` (r3): 3 reds + 2 timeouts.
- r4 `run-20260925T210939Z-35c4` at 7cf6d625: 271 installed, 239 certified
  attempted; roots red: `books/owner-invariants`
  (`fn-own-open-preserves-relation`, goal
  `(fn-own-control-okp (fn-own-view-control V) ...(fn-own-view-raw V))` with
  view-okp opened: the lemma `fn-own-view-control-okp-raw` has free
  variables), `books/owner-list-counts-read`
  (`fn-served-dispatch-msgid-retrieval`, Subgoal 16.2': the fall-through
  `fn-nntp-archive-command` against `fn-nntp-msgid-retrieval`),
  `books/owner-control-read` (`fn-octl-dispatch-archive-command`). Green in
  r4: nntp, nntp-control, served, owner, nntp-auth-fold, owner-verdict-read,
  nntp-pinned-effects, peer-offer-indexed and the books below them.
  Manifest `planning/evidence/manifests/certify-20260925T211010Z-1315802.json`.
  Logs: persvati `/home/ember/fn-gates/control-c3e-r4/build/acl2/certify-20260925T211010Z-1315802/`.
- After r4, uncertified: `fn-own-view-control-okp-of-conjuncts`
  (owner-invariants), the candidate fix for the open goal.

## Native

Not run: no green image. `tests/test_native_control_filing.py`
`test_signed_withdrawal` now also asserts, after the restart: `LISTGROUP
fn.test; ARTICLE 1; STAT 1` answer `423 withdrawn`, `ARTICLE <target>`
answers `430 withdrawn`, `HDR :fn-control <c3b-cancel>` answers 225 and
`0 executed withdrawal <c3b-target@example.invalid> author`, and
`tools/fn_verify.py <target> --withdrawal <c3b-cancel> --plain` exits 0.

## Not done, and why

- The three red owner books (above), then the full closure, then the image
  and the native run. The lane is at its run budget.
- Step 2 (the relation carries the journal equality and the configuration
  txid fact), step 3 (Message-ID to txid index), step 4 (owner-invariants
  under ten seconds): not started.
- A view whose group index is nil pins no control (its served connection
  has no group pin), so it answers the plain 423/430; that needs a view
  serving no article.
- `make check`: not run to green (generated ledger files).

## Continuation control-c3e-2 (2026-09-25 night)

The three owner-side reds, each fixed event by event in the persvati REPL
(`/home/ember/fn-gates/c3e-repl`) before any run; commit `bba372e9`:

- `books/owner-invariants`: `fn-own-open-preserves-relation` goes through
  with the committed `fn-own-view-control-okp-of-conjuncts`.
  `fn-own-advance-preserves-relation` was the next red (the re-pinned
  connection's control conjunct): new `fn-own-conn-okp-of-view-pin` (the
  connection `fn-own-advance-result` builds from the view is
  `fn-own-conn-okp` when the view is `fn-own-view-okp` and the connection is
  bounded); the advance hint keeps `fn-own-view-okp` and
  `fn-own-view-group-index` closed so it fires.
- `books/owner-list-counts-read`: `fn-served-dispatch-msgid-retrieval`
  keeps `fn-nntp-archive-command-pinned-msgid-arms-are-the-scan` closed, so
  the pinned dispatcher opens to its indexed arm as before (the lemma
  rewrote it to the plain scan dispatcher, which the hint left closed).
- `books/owner-control-read`: the served chain appends the submission
  effect after the dispatcher's, so `fn-octl-dispatch-archive-command`
  needs the reply's effects to be a true list. New
  `fn-octl-archive-command-pinned-effects-true-listp` (unconditional),
  from local per-response lemmas (each callee closed; 0.16 s).
  `fn-own-related-conn-control` opens `fn-own-conn-okp` and keeps
  `fn-own-conn-control` closed.

Runs:
- r5 persvati `run-20260925T213521Z-2db0` at `bba372e9`, `--affected-by`
  every book the lane changed, 2 jobs, 300 s: 414 installed, **96 of 96
  certified**. Manifest
  `planning/evidence/manifests/certify-20260925T213540Z-1548207.json`.
  `owner-invariants` 12.5 s (over the ten-second rule; step 4, open),
  `owner-control-read` 5.3 s, `owner-list-counts-read` 1.6 s.
- Merge of dev `95279f8f`: `c7a0d392` (no conflicts; PRF-084 events
  regenerated by `tools/ledger.py`).
- r6 hbox `run-20260925T214830Z-61bd` at `9ad31f6c` (merged tree), the 156
  roots of `proof_artifacts.py roots --profile default`, w28, 2 jobs: 266
  installed, **97 of 97 certified**. Manifest
  `planning/evidence/manifests/certify-20260925T214919Z-3947867.json`.
  `owner-invariants` 16.3 s there.

## Native (hbox, tree `9ad31f6c`, `/tank/fn/scratch/control-c3e/tree-9ad31f6c`)

Script `control-c3e-2026-09-25/c3e-image.sh` (the c3b script, modules
looped), `systemd-run --user --scope -p MemoryMax=24G`, build under
`swarm-build`, `FN_OPENSSL_PREFIX=/tank/fn/toolchains/openssl-3.5.8`,
`LD_LIBRARY_PATH=$FN_OPENSSL_PREFIX/lib`. `validate --profile default`:
`roots=156 result=loaded`; 0 undefined lines.

- `build/fn-host-developer` `b71b2852dfcb1eccaf0d544118ab20a9f78ff90772b37158d81832da5663bfdd`
- `build/fn-host-developer.core` `19a34a68f9f48f8ccdbea45bbaa086ab0e743a9cbb26a51c75394b1ef9e16212`
- build log `a14b224c9171aa4addea1dfb5dcce5de282ecbf978ed29da0cd50b6c21ef007b` (left on hbox)
- `tests.test_native_control_filing` (`FN_RUN_HYBRID_E2E=1`): 5 tests OK.
  `test_signed_withdrawal` witness: `ARTICLE 1` / `STAT 1` answer
  `423 withdrawn`; `ARTICLE <c3b-target>` answers `430 withdrawn`;
  `HDR :fn-control <c3b-cancel>` answers `225` and
  `0 executed withdrawal <c3b-target@example.invalid> author`;
  `tools/fn_verify.py --withdrawal` exit 0 (outcome verified, node
  executed). Log `control-c3e-2026-09-25/native-control-filing-9ad31f6c.log`
  `1a2e826bc54a2ba9920c7e8a9ed92d5ae718c4fa841f4685d292d0ba5d7ad3f2`.
- `tests.test_native_control_authority`: 1 test OK,
  `native-control-authority-9ad31f6c.log`
  `dcc358b00ec077baebc2367477552d11e160946204d00dadc2907ff989cc599e`.
- `tests.test_fn_verify` with `FN_RUN_VERIFY_E2E=1`: 31 tests OK, 3 skipped
  (the 200 KiB cases need `FN_VERIFY_LARGE=1`), `fn-verify-e2e-9ad31f6c.log`
  `0367e5ff9aad2f4efcef46cf261235608f9eff09c0c71141973903adbeda2862`.
- Not a C3 module, run by mistake: `tests.test_native_control` against the
  developer image timed out at 1800 s (its selector test expects a
  production image). No finding.

## Still not done

- Owner-level teeth for `fn-own-read-archive-command-is-the-pinned-dispatcher`
  (a reachable owner witness and a must-fail per hypothesis).
- `owner-invariants` over ten seconds (12.5 s persvati, 16.3 s hbox): step 4.
- Steps 2 (journal equality and configuration-txid fact in the relation)
  and 3 (Message-ID to txid index).
- `make check`: only `planning/ledger.json` / `ledger.md` stale (generated;
  left for the deputy).
