# Qualification of image 18c91321, 2026-09-24

One lane, `qual-18c91321`, ran four jobs on hbox between 21:13Z and 21:34Z
against the image of source `18c913213863dfd99e9ac763cccd3b003df12dbb`. The
jobs were the image build, the v0 matrix, the cut campaign with the probe,
the differential and the production kill run, and every image-gated native
module. It is a runtime record. It certifies nothing and proves no theorem.
The previous qualification was of `47bdb9a4`, in three records:
[matrix](matrix-47bdb9a4-2026-09-24.md), [campaign](campaign-47bdb9a4-2026-09-24.md)
and [native subsets](native-subsets-47bdb9a4-2026-09-24.md).

**Deploy verdict: deployable.** No row, cut, probe row, kill or native case
failed for an fn reason. Every expectation in the brief was met:
- matrix 188 reached, 0 disagreed, INN 33/33;
- 50/50 cut observations;
- probe 40/40;
- kill run 0 torn, 0 lost-240, 0 reused;
- bp_node 17/17, bp_app 5/5, served_crash_model 3/3, raw_scripts 9/9,
  relocation 2/2, hybrid_author 6/6;
- N08 passes on both developer images.

The failing rows that remain are the known out-of-date or environment rows
listed under "Failures".

## Image

The build used the runbook from the brief:
`sh tools/runbooks/hbox-image-build.sh /tank/fn/gates/qual-18c91321-20260924 18c91321`,
with `FN_OPENSSL_PREFIX` and `LD_LIBRARY_PATH` set to
`/tank/fn/toolchains/openssl-3.5.8`. It ran from 21:13:59Z to 21:15:52Z and
exited 0 ([`image-build.log`](qual-18c91321/image-build.log)).
- Proof artifacts came from the composed cache with 0 rejected. Default:
  289 books, 119 roots loaded. DTN: 280 books, 104 roots loaded.
- The build froze `build/images/18c91321/`. `sha256sum -c image.sha256`
  passed 58 of 58 at the freeze
  ([`image-validation.txt`](qual-18c91321/image-validation.txt)). The
  campaign's check before its run also passed 58 of 58, and so did the
  relocated copy before and after the relocation run.
- The gate tree equals `git archive 18c91321` over `host/ tests/ tools/
  books/ packaging/ Makefile` file by file: 1378 files, 0 differences.
- The gate was not modified except by the runbook.

| artifact (`…/build/images/18c91321/`) | SHA-256 | vs 47bdb9a4 |
| --- | --- | --- |
| `fn-host` | `432622d29a28d59455e01f3e5b426036c5862db21d5f7d1205a9304ab11e3505` | same launcher |
| `fn-host.core` | `e623cfd21725e03f8381e709bc898ff2cf89d6e72be78562dd02f657b4801bd2` | new core |
| `fn-host-developer` | `e4eeefd290450e7497dade96e2c4089e0caacfa88f72c737f51a92b2f3b2ba18` | same launcher |
| `fn-host-developer.core` | `793b937d6b0ac6b05fe1a6da49d720f40aac9ff2a3180eb2a6e670b4d6cea46a` | new core |
| `fn-host-dtn` | `095382bae2b3ff377a2f27b119c0d5f5b247c983f4846d7176fbcd661e1e282c` | same launcher |
| `fn-host-dtn.core` | `f2b07dda51e3d7c431f3b8783e9bc656da4c9abe2c973f1964047bab31893512` | new core |
| `fn-host-dtn-developer` | `4986f14bf4ebc34fccf54f4bfea0e7007222f96c477a9f0519faad6c59c19c88` | same launcher |
| `fn-host-dtn-developer.core` | `3a840c21bde057174e48968c20b6d79e7ebf728aa75d3d92ff5fb88bb500eaa5` | new core |
| `runtime/sbcl` (SBCL 2.6.8) | `b115fe956aadee603459fac401e1ff2cc39321e14848544a0fe438788a2fc6d5` | same |
| `openssl/lib/libcrypto.so.3` / `libssl.so.3` | `14d40ec6…` / `bb2d12be…` | |
| ACL2 `acl2-literal-4g` | `9f73da2a84d664516033fb6e944c55b55d1f7206e9599cac46ca2b208de26aa8` | |

The full list is in [`image.sha256`](qual-18c91321/image.sha256) and
[`image-hashes.txt`](qual-18c91321/image-hashes.txt).

## The matrix

Run `20260924T212040Z` of `tools/v0_matrix.py --backend native-operator`
ran from the laptop worktree at `00d291d0`. Between `18c91321` and
`00d291d0`, `tools/` and `tests/` do not differ. The run took 196 s and
exited 0. It is published with `--publish-current` as `planning/v0-matrix.json`
(`rows_digest` `7832da6f…`). The exact text is in
[`matrix-invocation.txt`](qual-18c91321/matrix-invocation.txt):

```text
I=/tank/fn/gates/qual-18c91321-20260924/build/images/18c91321
S=/tank/fn/scratch/qual-18c91321/matrix/stores
python3 tools/v0_matrix.py HEAD --host hbox --tree native-18c91321 --backend native-operator \
  --native-image $I/fn-host --native-developer-image $I/fn-host-developer \
  --native-config-a $S/a/fn.toml --native-config-b $S/b/fn.toml --native-group fn.letters \
  --native-image-source 18c913213863dfd99e9ac763cccd3b003df12dbb \
  --native-runtime $I/runtime/sbcl --native-openssl-prefix $I/openssl \
  --inn --keep --publish-current
```

The stores were made as `hbox-matrix-provision.sh` makes them, with
`fn-host --fn store <root>/store init fn.letters`, listening on
127.0.0.1:11190 and 11191. `ss -ltn` showed 11190, 11191 and 11290 to 11299
free before the run and again after it. `~/fn-deploy/.locks/` is empty.

| run | accepted | refused | uncertain | not exercised | not built | disagreed | faulted | INN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 47bdb9a4, `20260924T165406Z` | 142 | 44 | 2 | 31 | 1 | 0 | 0 | 33/33 held |
| **18c91321, `20260924T212040Z`** | **142** | **44** | **2** | **31** | **1** | **0** | **0** | **33/33 held** |

**Against 47bdb9a4, no row changed its verdict or its agreement** (220 rows
compared by id). `independent` is 12, as before. The 32 unreached rows are
the same rows with the same reasons: the loopback usage probe, `AUTH-NEW`
not built, no native phase for the crash, BP, STX and media rows, `--scale`
not given, and no slrn. No owner log line and no row answered 436.

The duplicate-POST reason still depends on the clock second, as on
47bdb9a4:
- `V0-POST-DUPLICATE-A` reads `441 … this article is already stored here`.
- `V0-POST-DUPLICATE-B` reads `441 … a different article with this
  Message-ID is stored here`.

Both rows agree. The finding stays with the pending refusal-wording
decision.

Logs:
- [`matrix-driver-run.log`](qual-18c91321/matrix-driver-run.log);
- [`matrix-node-logs.tgz`](qual-18c91321/matrix-node-logs.tgz), the owner
  logs and `fn.toml`s of every node, 21 files;
- the tool's own output in
  [`v0-runs/20260924T212040.766395Z-00d291d-1688717f34f2/`](v0-runs/20260924T212040.766395Z-00d291d-1688717f34f2/).

## The cut campaign, the differential, the probe and the kill run

The runner is [`campaign.sh`](qual-18c91321/campaign.sh). It is the
47bdb9a4 runner with the paths changed and a single campaign and probe run.
- **Tree:** a copy of the gate (`/tank/fn/scratch/qual-18c91321/ctree`).
- **Environment:** `FN_NATIVE_SOURCE_ROOT` = the gate, the OpenSSL 3.5.8
  pair and `PYTHONDONTWRITEBYTECODE=1`.
- **Time:** 21:18:16Z to 21:33:42Z.
- **Judging:** [`judge.py`](qual-18c91321/judge.py) and
  [`faults.py`](qual-18c91321/faults.py) are unchanged from the 47bdb9a4
  record.

### Per cut (operator campaign, one run, 1:31, peak RSS 321 MB)

| cut | table | served: ready / client / owner | store/recover | at death (txns, staging) | recover | candidate | verdict (served, store) |
|---|---|---|---|---|---|---|---|
| `frontier-created` | absent | True / 3 / −9 | −9 | 1, .allocation- | 1 1 0 | absent | PASS, PASS |
| `frontier-written` | absent | True / 3 / −9 | −9 | 1, .allocation- | 1 1 0 | absent | PASS, PASS |
| `frontier-staged-durable` | absent | True / 3 / −9 | −9 | 1, .allocation- | 1 1 0 | absent | PASS, PASS |
| `frontier-replaced` | absent | True / 3 / −9 | −9 | 1, none | 1 1 0 | absent | PASS, PASS |
| `frontier-attempted` | absent | True / 3 / −9 | −9 | 1, none | 1 1 0 | absent | PASS, PASS |
| `frontier-durable` | absent | True / 3 / −9 | −9 | 1, none | 1 1 0 | absent | PASS, PASS |
| `frontier-reserved` | absent | True / 3 / −9 | −9 | 1, none | 1 1 0 | absent | PASS, PASS |
| `record-created` | absent | True / 3 / −9 | −9 | 1, .stage- | 1 1 0 | absent | PASS, PASS |
| `record-written` | absent | True / 3 / −9 | −9 | 1, .stage- | 1 1 0 | absent | PASS, PASS |
| `record-staged-durable` | absent | True / 3 / −9 | −9 | 1, .stage- | 1 1 0 | absent | PASS, PASS |
| `record-linked` | either | True / 3 / −9 | −9 | 2, .stage- | 2 2 0 | present | PASS, PASS |
| `record-attempted` | present | True / 3 / −9 | −9 | 2, .stage- | 2 2 0 | present | PASS, PASS |
| `record-durable` | present | True / 3 / −9 | −9 | 2, .stage- | 2 2 0 | present | PASS, PASS |
| `record-completing` | present | True / 3 / −9 | −9 | 2, .stage- | 2 2 0 | present | PASS, PASS |
| `record-stage-unlinked` | present | True / 3 / −9 | −9 | 2, none | 2 2 0 | present | PASS, PASS |
| `record-staging-cleaned` | present | True / 3 / −9 | −9 | 2, none | 2 2 0 | present | PASS, PASS |
| `finish-consumed` | present | True / 3 / −9 | −9 | 2, none | 2 2 0 | present | PASS, PASS |
| `finish-durable` | present | True / 3 / −9 | −9 | 2, none | 2 2 0 | present | PASS, PASS |
| `recover-replayed` | n/a | False / - / −9 | −9 | 1, .stage- | 1 1 0 | absent | PASS, PASS |
| `recover-barrier-1` to `-5` (5 cuts) | n/a | False / - / −9 | −9 | 1, .stage- | 1 1 0 | absent | PASS, PASS (each) |
| `recovery-stage-unlinked` | n/a | False / - / −9 | −9 | 1, none | 1 1 0 | absent | PASS, PASS |

**50 of 50 observations pass.** The table is cell for cell the 47bdb9a4 table
([`judged.md`](qual-18c91321/judged.md)).
- `record-linked`'s "either" was observed as present in both columns.
- The image-gated `tests.campaign.test_native_operator_campaign` passed 10 of
  10 in 25.6 s.

**Faults and production refusals (F1 to F7).** The fault rows of
[`faults.txt`](qual-18c91321/faults.txt) are those of 47bdb9a4, apart from
pids, times and staging names.
- The developer EIO, control-fault and five test-stop rows give client 3.
- Both cross-entry retry orders give 0, then 1.
- The 65-death allocation-orphan rows pass.
- **Production refuses 30 of 30 selector starts with exit 5.** That is 10
  selectors × {`operator run`, `operator recover`, `store post`}. It was 9
  selectors and 27 starts on 47bdb9a4; `FN_NATIVE_PROFILE_FAULT` from
  m5-profile-upgrade is new and also refused.
- Raw `store post` on production exits 5, and `FN_NATIVE_INIT_FAULT` at
  `operator init` exits 5.

### The ACL2 differential

`FN_NATIVE_CRASH_HOST=<image>/fn-host-developer python3 -m unittest -v
tests.test_native_crash_model` passed **7 of 7** in 76 s, with the bridge on
`acl2-literal-4g` over the gate's certified books
([`crash-model.log`](qual-18c91321/crash-model.log)). It takes all 18 post
cuts, against frames ACL2 derives from the sent article.

### Per cut at the NNTP wire (probe, once, 1:03)

**40 of 40 pass**, and `judgement.failed` is 0
([`probe-rows.txt`](qual-18c91321/probe-rows.txt)). On 47bdb9a4 it was 39 of
40.

| cut | action | arm | reply | owner exit | candidate | verdict |
|---|---|---|---|---|---|---|
| all 18 post cuts | kill | kill | closed, zero octets | −9 | absent at the 10 cuts before `record-linked`, present from it on | pass ×18 |
| `frontier-created`, `-written`, `-staged-durable`, `record-created`, `-written`, `-staged-durable` | eio | refused | `441 posting failed; the store could not write the article, nothing was stored` | 0 | absent; repost 240 | pass ×6 |
| `frontier-replaced`, `-attempted`, `-durable`, `-reserved` | eio | uncertain | `441 posting failed; the outcome is uncertain, do not repost` | 3 | absent | pass ×4 |
| `record-linked`, `-attempted`, `-durable`, `-completing`, `-staging-cleaned` | eio | uncertain | the uncertain line | 3 | present, identical | pass ×5 |
| `finish-consumed`, `finish-durable` | eio | consumed | the uncertain line | 3 | present, identical | pass ×2 |
| **`record-stage-unlinked`** | eio | swallowed | `240 article received OK` | 0 | present, identical | **pass** (was the one failure on 47bdb9a4) |
| controls: dev 240 then SIGKILL; dev no `From`; prod 240 then SIGKILL; prod SIGKILL mid-article | | control | 240 / `441 … From is required` / 240 / none | −9 / 0 / −9 / −9 | present / absent / present / absent | pass ×4 |

At `record-stage-unlinked` the owner now logs the swallowed cleanup error.
The line is
`failed staging cleanup step=directory-barrier sequence=1 name=…/staging/.stage-1919571-… errno=5 error=[Errno 5] Input/output error`.
It is logged before `accepted post`.

A repost of a present article is still answered with either wording,
depending on the `Injection-Date` second:
- `this article is already stored here` at 6 cuts;
- `a different article with this Message-ID is stored here` at 10 cuts.

### The production external-kill run (seed 3, 11:10, peak RSS 1.40 GB)

`native_production_kill run --image <image>/fn-host --seed 3 --kills 80
--concurrent 10` gave **90 SIGKILLs and 100 killed POSTs**
([`pk-judged.md`](qual-18c91321/pk-judged.md)).
- The run used two fresh stores, closed at 116 and 62 transactions. Both are
  under the 128 budget and numbered with no gap.
- 92 owner pids ran, and all were gone at the end.
- The judge lists **0 failures** and exits 0.

| every POST the judge saw (206) | 47bdb9a4 | 18c91321 |
| --- | --- | --- |
| 240-and-identical | 87 | 110 |
| died-absent | 61 | 56 |
| died-present-identical | 35 | 17 |
| refused (441) | 23 | 23 |
| **torn / lost-240 / reused-number** | 0 / 0 / 0 | **0 / 0 / 0** |

| killed POSTs | 240-identical | died-absent | died-present-identical | refused | total |
| --- | --- | --- | --- | --- | --- |
| single | 18 | 44 | 17 | 1 (over-limit; the `441` preceded the kill) | 80 |
| concurrent | 8 | 12 | 0 | 0 | 20 |

- **Replies at death.** Every killed POST that got `240` (26) had its
  article linked, and the owner had logged `accepted post` for it. No kill
  before the link found a `240`. On 47bdb9a4, 3 killed POSTs got a 240
  before the kill.
- **Interpretation, not a measurement.** More kills now land after the
  reply. That fits the faster commit path from commit-path-cost and
  commit-path-2. The schedule is calibrated from medians (`reply` 42 to
  155 ms by size).
- **Client exit codes.** rc 0 came only with `240`. rc 1 came only with a
  `441`. rc 3 came only with no reply.
- **K1 persists.** Of the 73 resubmissions, 51 were answered `240`, 15 `441
  … a different article …`, 5 `441 … not received` (over-limit) and 2 `441
  … already stored here`. So the client that resends its own octets after a
  death is still usually told "a different article". The wording decision
  is pending with ember.

Every owner bound a kernel-assigned 127.0.0.1 port. After the runs, `ss
-ltn` matched the pre-run listing, and no process under the scratch path was
alive.

## Native modules on the four images

The runner is [`run.sh`](qual-18c91321/run.sh), one module at a time, each
as `timeout 3600 python3 -m unittest -v tests.<module>`. It is the 47bdb9a4
runner with these changes:
- There is one tree. The gate copy carries every stale-test fix and the
  kind-8 case, so no `tree-dev` was needed.
- `FN_NATIVE_BP_NODE_HOST` is set: `fn-host-dtn-developer` by default, and
  `fn-host-developer` for the second N08 run.
- `FN_NATIVE_IMAGE_SOURCE_SHA=18c91321`.

The rest of the environment:
- `sbcl` → the image runtime, first on `PATH`, and `SBCL_HOME` = the
  image's `sbcl-home`.
- Every `FN_RUN_*` opt-in flag was set.
- The DTN variables: `FN_NATIVE_DTN_HOST`, `FN_NATIVE_DTN_DEVELOPER_HOST`,
  `FN_NATIVE_BP_HOST` (`fn-host-dtn`, or `fn-host-dtn-developer` for the
  selector modules), and `FN_NATIVE_CONTACT_SENDER` and `_RECEIVER` =
  `fn-host-dtn`.

[`chainA.sh`](qual-18c91321/chainA.sh) (BP/DTN) and
[`chainB.sh`](qual-18c91321/chainB.sh) (the rest, then
[`reloc.sh`](qual-18c91321/reloc.sh), stdin closed) ran side by side from
21:18Z to 21:33:41Z. They ran at the same time as the campaign and the
matrix. Every port was ephemeral. `test_native_crash_model` ran once, in the
campaign above.

| module | tests | pass | fail | skip | s | log SHA-256 (16) | vs 47bdb9a4 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| test_native_auth | 4 | 4 | 0 | 0 | 6.7 | `cf1979c2598729c0` | same |
| test_native_profile | 3 | 3 | 0 | 0 | 0.8 | `a45e958d3f756953` | same |
| test_native_admin | 9 | 9 | 0 | 0 | 3.2 | `ef9f08379589fcc5` | same |
| test_native_starttls | 2 | 2 | 0 | 0 | 0.6 | `8fbca8dd4eb912e4` | same |
| test_native_auth_admin_fidelity | 3 | 3 | 0 | 0 | 12.3 | `f9a126f09f92ccae` | same |
| test_native_owner | 18 | 18 | 0 | 0 | 22.1 | `3b033d57795bb48b` | same |
| test_native_raw_scripts | 9 | **9** | 0 | 0 | 0.4 | `8d49b0dc687ad384` | **was 6/9 (7/9 dev tests)** |
| test_native_live_reconfiguration | 11 | 11 | 0 | 0 | 1.4 | `ad3849bd35e1743d` | same |
| test_native_control | 14 | 14 | 0 | 0 | 20.6 | `206691de900843bd` | was 13/14 (image tests) |
| test_native_operator_cli | 6 | 6 | 0 | 0 | 3.9 | `d34a9a4614010c09` | was 5/6 (image tests) |
| test_native_operator_verbs | 18 | 18 | 0 | 0 | 4.2 | `69011748379ca2af` | same |
| test_native_profile_upgrade | 5 | 5 | 0 | 0 | 8.9 | `bf18079ba34edfae` | **new module, passes** |
| test_native_program_check | 9 | 9 | 0 | 0 | 0.3 | `0eed21c47463522a` | new module, passes |
| test_native_initializer_fidelity | 11 | 11 | 0 | 0 | 1.5 | `0e89b79cf0e1e6ee` | same |
| test_native_recovery | 13 | 13 | 0 | 0 | 12.5 | `f39659f66e2be490` | same |
| test_native_storage_codec | 11 | 11 | 0 | 0 | 63.7 | `2fe0723da7ac4c80` | was 10/11 (image tests) |
| test_native_served_differential | 7 | 7 | 0 | 0 | 1.0 | `94bbaaf19e50c2d0` | same |
| test_native_reader_index | 4 | 4 | 0 | 0 | 5.7 | `beae04a28ec0ec16` | same |
| test_native_newnews_migration | 1 | 0 | 0 | 1 | 0.1 | `fdef87abbaf17853` | same (no pre-T2 image) |
| test_native_stamp_migration | 1 | 0 | 0 | 1 | 0.1 | `8b1af8c6e1ebfec9` | same |
| test_native_hybrid_author | 6 | **6** | 0 | 0 | 14.2 | `64ca6287abff1fed` | **was 5/5; +1 case: signed POST over NNTP gets the transit classification (p8-signed-post)** |
| test_native_topic_local | 4 | 2 | 0 | 2 | 2.7 | `9c40965639efca70` | same (no pre-v2 image) |
| test_native_topic_metadata | 2 | 2 | 0 | 0 | 1.3 | `2fb82515d67e8501` | same |
| test_native_consumer_inspect | 4 | 4 | 0 | 0 | 0.5 | `19dc36b17a672904` | same |
| test_native_consumer_project_bounds | 2 | 2 | 0 | 0 | 0.3 | `8c22ef61d1ea8f97` | same |
| test_native_consumer_e2 | 4 | 2 | 1 | 1 | 3.6 | `e1c16fc663ea19c4` | same (failure 2, (c)) |
| consumer_e2 signed poll, dated source | 1 | 1 | 0 | 0 | 11.5 | `449e33c33abd5629` | same |
| test_native_checkpoint | 23 | 23 | 0 | 0 | 44.8 | `2939f94d241b3b22` | same |
| test_checkpoint | 7 | 7 | 0 | 0 | 485.0 | `f54db166246af948` | same |
| test_native_peering | 5 | 5 | 0 | 0 | 7.8 | `5d2d22ffb57dbdeb` | same |
| test_native_protected_peering | 5 | 5 | 0 | 0 | 28.4 | `ea7c53003e25c159` | same |
| test_native_crash_model (in the campaign) | 7 | 7 | 0 | 0 | 76.3 | `crash-model.log` | same |
| test_native_served_crash_model | 3 (18 cut subtests) | **3** | 0 | 0 | 142.6 | `1cc274a62ef312a6` | **was 18 subtest errors** |
| test_native_tls_transport | 2 | 2 | 0 | 0 | 1.0 | `48c40071a1908245` | same |
| test_native_frozen_relocation (relocated copy, stdin closed) | 2 | **2** | 0 | 0 | 1.6 | `dce9426f1149a6a8` | now asserts exit 5 with stdin closed **and held open**; passes |
| test_fn_web_native | 4 | 4 | 0 | 0 | 6.9 | `afdc28b578b0a7bc` | same |
| test_bp_node_native (`fn-host-dtn-developer`) | 17 | **17** | 0 | 0 | 380.5 | `d2d45b4de5601126` | **was 15/16 (16/17 dev tests, kind-8 failing)** |
| N08 `test_death_after_kind_eight_retries_once_and_peer_holds_one_copy` (`fn-host-developer`) | 1 | **1** | 0 | 0 | 34.8 | `24307edad7e39451` | **was failing (image predated it)** |
| test_bp_app_native | 5 | **5** | 0 | 0 | 74.4 | `8e048a1232888bc1` | **was 1/4 (arity defect); +1 case: unadmitted channel refused with the planner reason** |
| test_bp_obligation_native | 4 | 4 | 0 | 0 | 1.1 | `2cdb51e0d6e26fa1` | same |
| test_bp_fragment_node_native | 1 | 1 | 0 | 0 | 31.1 | `9c13ab97b7368761` | same |
| test_bp_service_native (`fn-host-dtn`) | 16 | 9 | 7 | 0 | 4.3 | `0b786f13563a2a4d` | same (failure 1, (c)) |
| test_bp_service_native (`fn-host-dtn-developer`) | 16 | 16 | 0 | 0 | 14.4 | `8dc3b5af99ead3f6` | same |
| test_bp_contact_native | 2 | 2 | 0 | 0 | 0.5 | `848f48fe3d84b57f` | same |
| test_bp_contact_relay_native | 1 | **1** | 0 | 0 | 2.2 | `363a97d251dc4423` | **was 0/1**: receiver now `bp-node serve` (dtn-node-image) |
| test_bp_receive_integrity_native (`fn-host-dtn`) | 4 | 2 | 2 | 0 | 0.5 | `89a9c2b82a2ef037` | same (failure 1, (c)) |
| test_bp_receive_integrity_native (`fn-host-dtn-developer`) | 4 | 4 | 0 | 0 | 1.0 | `75220bb049dac004` | same |
| test_native_app_journal | 10 | 10 | 0 | 0 | 8.3 | `5bdc0225d485a930` | same |
| test_native_image_profiles | 12 | 12 | 0 | 0 | 1.4 | `45fd18aa583dcf51` | same |

In this table, "was … (image tests)" means that 47bdb9a4's own tests failed
the case and the dev tests passed it. The case passes here with the image's
own tests.

**Kind-8 N08 on both developer images.** Both runs pass:
- `fn-host-dtn-developer`, inside the 17/17 module run;
- `fn-host-developer`, alone, 1/1.

**Diff against 47bdb9a4.**
- **Newly pass:** served_crash_model (18 cuts, 3/3), raw_scripts
  (`owner_bound_commit_raw`, `owner_consumer_raw`, `owner_group_codes_raw`),
  bp_node 17/17 with N08, bp_app 5/5, contact_relay 1/1, relocation with
  stdin held open (exit 5), and control, operator_cli and storage_codec on
  the image's own tests.
- **New modules or cases:** profile_upgrade 5/5, program_check 9/9,
  hybrid_author +1, bp_app +1.
- **Newly fail:** none.
- **Unchanged failures:** bp_service 7 and receive_integrity 2 on the
  production DTN image, and consumer_e2 with an undated source.

## Failures, classified

Classes: (a) fn defect, (b) environment, (c) test out of date or run on the
wrong image.

1. **(c) `test_bp_service_native` 7 and `test_bp_receive_integrity_native`
   2 on `fn-host-dtn`.** Each case sets a developer selector. Examples:
   `FN_BP_SERVICE_TEST_SEND_FAULT` gets exit 5 where the test wants 4, and
   `FN_BP_TEST_DELIVER_FAULT` gets "receiver exited before listen". The
   production DTN image refuses those selectors by design, and both modules
   pass in full on `fn-host-dtn-developer`. Unchanged since 6c0626c5.
2. **(c) `test_native_consumer_e2`,
   `test_signed_composite_poll_and_lost_positive_ack_reply`, undated
   source.** `hybrid-author` exits 1 with empty stderr
   (`tests/test_native_consumer_e2.py:59`). The same case passes with the
   dated source file (`FN_CONSUMER_POLL_SOURCE_FILE`). Unchanged.
3. **(b) Skips.**
   - `consumer_e2` `test_different_local_uid_is_refused_by_owner` needs
     root and setpriv.
   - The newnews and stamp migrations and 2 topic_local cases need a pre-T2
     or pre-v2 image.

No (a). No failure blocks the deploy.

Observations that are not failures:
- the duplicate/conflict wording (K1, and the matrix clock-second
  finding);
- the matrix's 32 unreached rows (unchanged).

## What this shows for P1 to P11 on 18c91321

The sentences appended to the "observed on image" cells of
[the scoreboard](../v0-scoreboard.md) summarise this section: the matrix
rows as on 47bdb9a4, and the native modules above. P2 and P10 gain the probe
40/40, and the production kill run with 0 torn. P11 gains bp_node 17/17 and
bp_app 5/5 with N08 on both developer images.

## Logs and digests

Everything is in [`qual-18c91321/`](qual-18c91321/), and
[`SHA256SUMS`](qual-18c91321/SHA256SUMS) covers every file:
- `native-logs.tgz` holds every module log;
- `owner-logs.tgz` holds the campaign, probe and kill-run owner `.err`
  files, 379 of them;
- `campaign.json.gz`, `nntp-probe.json.gz` and `pk-run.json.gz`, with the
  raw digests in `raw.sha256`;
- the runners and the matrix files.

| raw file (hbox, `/tank/fn/scratch/qual-18c91321/campaign/`) | SHA-256 |
| --- | --- |
| `campaign.json` | `67ef9a8e4056a6b5f0b784c2418a680ea75f3bb0387e0056749988da73a114e0` |
| `nntp-probe.json` | `7e3edbc4fdab76ff252a04baf1ba2299805421704cb554a02171f7880c16a67f` |
| `pk-run.json` | `fc1fa45c05b1bbdf7e4521de0c552472c4132b6453790119d4130e27f5c61aa8` |

These stay on hbox:
- the stores and work trees under `/tank/fn/scratch/qual-18c91321/`
  (`campaign/{work,probe-work,pk-work}`, `matrix/stores`, `tree`, `ctree`,
  `reloc`);
- the matrix deploy tree `~/fn-deploy/native-18c91321-00d291d/`.

The live node `/tank/fn/node` was not touched.

## What this does not establish

This is one run of each campaign on one box, over loopback. It covers
process deaths on ZFS, not power loss. The differential has no case for the
recovery cuts other than `recovery-stage-unlinked`. The developer and
production cores differ (`793b937d…` against `e623cfd2…`). So the per-cut
evidence is coordinate-precise only on the developer twin. On production it
is the kill run's store-state partition. No row here is a theorem or says
which ACL2 subject the host calls.
