# Qualification of candidate 69046a76, 2026-09-26

**Verdict: deployable, as the release the node is reinstalled from (D34).**
No implementation or model/refinement defect was found on the candidate's
bytes. PKT-481 is fixed: a 3 MiB ARTICLE and a 3,954,943-octet OVER are served
whole with the owner alive and a clean exit, on the production and developer
images. Every red module is classified below. None is class (a) or (b), and
no expected answer was changed. The deploy is ember's go through the
coordinator.

**The deploy shape changed during this run.** ember and gpt-6 decided (D34,
planning/review-2026-09-26-gpt6-consolidation.md, relayed by deputy 4) that
69046a76 is the release the node is **reinstalled from, not upgraded to**.
- So item 1 (c) is now "fresh install from the release tarball, plus an import
  of the test store". It was rehearsed and passes (below).
- The in-place upgrade path and the marker two-step had already run before
  the change (`logs/upgrade-live.log`). They pass, but they are recorded as
  **unexercised by design** for this deploy, not as its path.
- 69046a76 has no `store export/import` verb; D34 names one for later. So the
  only import the release has is a `cp -a` of the stopped store placed at the
  new node's `[store]` path. That is what was rehearsed.

**The deploy facts of item 1, answered on this candidate's bytes:**
- **(a) The checkpoint publishes and reopens.** The scale profile's largest
  admitted article is 32,693 octets (32,694 is refused by name), and K/2 is
  2,048. Loading 2,048 × 32,693 took 662 s. `store checkpoint` captured 2,112
  records into 210,493,179 octets in 38.85 s (rc 0). The owner reopened from
  it in 24.9 s, and `status` says `open=checkpoint:2112 suffix=0`. Peak RSS
  was 19.2 GB (dfa810fc: 17.9 GB). The live store's copy wrote a 41,272-octet
  checkpoint, and the owner reopened from it in 1.98 s.
- **(b) The live store's copy opens below H.** It holds 13 of 4,096
  transactions and 10,960 bytes used of H = 805,306,368 (headroom 4,083
  transactions and 805,295,408 octets). bbf52159 and 69046a76 print the same
  accounting numbers.
- **(c) The reinstall from the release, with the test store imported,
  passes** (`logs/reinstall.log` `c545c787…`).
  - The tarball `fn-69046a76798b-linux-x86_64.tar.gz` (`1a2a61e9…`) was
    unpacked into a fresh prefix. Its `.sha256` and `SHA256SUMS` check out.
  - Every `FN_NATIVE_*` variable was removed from the environment, and
    `fn --version` printed `fn 69046a76798b8be6169eeed5a66cc46fbe399e51`.
  - A fresh node: `init`, `run`, POST 240, ARTICLE 220, stop, `recover` rc 0.
  - The imported store (a `cp -a` of the live store) gives `status` rc 0
    below H. The owner listens after 0.18 s. `GROUP fn.agents` answers `211
    12 1 12`, and ARTICLE 1 is served. A new POST gets 240 as #13, and
    `recover` then counts 14 transactions.
  - Earlier, before the change, the in-place upgrade path passed against the
    real bbf52159: `needs-upgrade`, `rollback-check`, and the marker two-step
    on a copy. It is recorded, but it is not the deploy path.

**What a rollback loses.** Under D34 a rollback is a reinstall of the older
release with the store imported. These facts about an older release opening
a store that this one wrote were witnessed on copies, with every
`FN_NATIVE_*` popped and the answering image proven. bbf52159 answers
`--version` with `unknown verb`, which the candidate does not.
1. **An account code, once redeemed (delta codes 15 and 16), or a login
   binding published live (code 17), makes bbf52159 refuse the store**
   (exit 4). The pre-upgrade snapshot restores. docs/operator.md: "Once an
   account code is redeemed on a release with accounts, releases before it
   cannot open the store; roll back only from the pre-upgrade snapshot" and
   "The same rule covers the login-binding rows (delta code 17 …)".
2. **A pull journal holding `:pull-unavailable`** makes the owners of
   bbf52159 and b6759850 exit 4, while their offline `status` and `recover`
   say rc 0. Deleting `pull/` restores the node, with the cursor one day
   back. docs/operator.md: "A release from before 2026-09-26 refuses a pull
   journal that holds the `:pull-unavailable` record … the cursor then
   restarts one day back".
3. **A pre-C1 store holding a signed cancel is refused by name, exit 1**:
   `pre-C1 control record (txid 2, <prec1-cancel@example.invalid>): run store
   repair-control`. `store repair-control` refuses with `repair semantics
   undecided (PKT-444)`. docs/operator.md: "refused by name by every open
   (`recover`, `run`, `health`, `inspect`, `checkpoint`)".
4. **Checkpoint files across releases.** An older image rejects a newer
   checkpoint file and replays in full. bbf52159 on a 69046a76 checkpoint says
   `open=full-replay reason=checkpoint-open-refused`, rc 0
   (`logs/ckptrb2.log` `8c57187e…`). 69046a76 likewise refuses a bbf52159
   checkpoint and replays in full: 0.09 to 0.17 s on the 13-record live copy
   (`logs/ckptrb.log` `f9b72409…`). On the 20,000-record fixture (8cc3cd4c's
   file) it took 96 s to `LISTENING` (dfa810fc: 524 s).
5. **Deputy 4's deploy note (PKT-395):** a node on 69046a76 may move only to
   an image that carries served-path-scale-2's checkpoint-index-shape
   refusal. A post-d0df09ed image without that fix silently misreads a
   69046a76 checkpoint (a wrong Message-ID trie, count 0). 69046a76 itself
   writes and reads one shape.

**The friends result at qualified bytes: complete, same-box.** The tarball's
core `dc3b8919…` is identical to the gate's.
- friends_feed 2/2, friends_accounts 1/1 and auth 5/5 pass on the tarball's
  node.
- A redeemed friend's SIGNED post got 240, `verified` at keyring 3 and
  `active`.
- `principal bind` applied live: an open session keeps its binding.
- `peer keygen` exits 0.
- `peer pull` against a 430-listing peer drops at the bound.
- Gap PKT-481 (b) persists: `principal bind` refuses a redeemed account's
  login with `unknown-login`.
- persvati's two-machine variant was not run.

**Recommended path (ember's go):** stop the bbf52159 node and take a `cp -a`
snapshot of `/tank/fn/node/store`. Install the release tarball into a fresh
prefix, point its `[store]` at the snapshot copy (the import), and start it.
Keep the format-8 marker **unmarked**. A rollback is a reinstall of
bbf52159's release over the pre-deploy snapshot, never over the store this
release has written once any of consequences 1 to 3 has happened.

One lane, `qual-69046a76`, ran this on hbox from 15:05Z to 20:25Z on
2026-09-26 (hbox clock), against source
`69046a76798b8be6169eeed5a66cc46fbe399e51`.
- It is a runtime record. It certifies nothing and proves no theorem.
- Its shape and scripts are qual-dfa810fc's (lane/qual-dfa810fc 88f5cf58),
  with the paths changed. Every item that runs the image was **rerun** here.
- Only the PKT-384 reading and the C2 to C26 classifications **carry**, and
  both were rechecked against this tree.
- Everything is in [`qual-69046a76/`](qual-69046a76/), with `SHA256SUMS`.

## Image and gate

- **Closure:** hbox run-20260926T144730Z-9abc, manifest
  certify-20260926T144826Z-2113131 (dev f0730c16). 965 Makefile roots: 480
  books certified afresh and 509 from the cache at 4 jobs, 0 failed.
- **Image check:** `sha256sum -c image.sha256` gave 58 of 58 OK
  (`image-check.log` `a2d6da08…`).
- **Build logs:** the gate has no `build/image-build.log`. The four
  native-build logs and six freeze files hold 0 lines of PKT-284's patterns
  (`build-log-scan.txt` `38c49658…`).
- **The gate tree equals `git archive 69046a76`.** It is 8,006 files with no
  content differences. The only difference is five tracked `build/acl2`
  manifest directories, which the gate lacks (`gate-vs-archive.txt`).
- **Artifacts:**
  - `fn-host` `432622d2…`
  - `fn-host.core` `dc3b8919…`
  - the throughput gate's unfrozen `build/fn-host-developer.core`
    `271f8297…`
- **multi-peer-relay (8041d5b0) and served-path-scale (c150c506) are in this
  candidate.** They were not in dfa810fc. The brief's "multi-peer-relay is NOT
  in this candidate" is wrong for 69046a76.

## The standing cases (PKT-481), run first

| case | fn-host | fn-host-developer | fn-host-dtn, -dtn-developer |
| --- | --- | --- | --- |
| `LargeReplyTests` (3 MiB ARTICLE and a large OVER, clean exit) | OK, 40 s | OK, 40 s | **n/a**: `operator run` is refused with "needs the nntp-service surface, which this image omits". There is no served ARTICLE on these images, so this is class (e), not a failure |
| `bigart.py` 1, 2 and 3 MiB, 4 MiB profile | 240, then 220 identical; owner alive; exit 0 (3 MiB: POST 4.09 s, ARTICLE 1.11 s) | the same (3 MiB: 3.34 s and 0.99 s) | n/a |
| `bigart.py` 3 MiB, scale profile (T = 1,048,576, H = 4 GiB, A = 4 MiB, R = 32 MiB) | identical, owner alive, exit 0 | the same | n/a |
| `bigover.py` 2,000 articles × 2,000 octets of References (**PKT-491: the time**) | OVER 3,954,943 octets whole in **7.82 s**, STAT after, exit 0 | **7.73 s** | n/a |
| `bigover.py` on the scale profile | **7.16 s**, exit 0 | | n/a |

- Posting the 2,000 articles took 487 to 547 s on ZFS. The fix's own run on
  tmpfs took 10 s.
- `scale-1m` (tools/service_envelope.py) has A = 16 KiB, so it cannot hold a
  3 MiB article. Raising A to 4 MiB on the scale preset without also raising
  R is refused by name: `MAX-RECORD-OCTETS-BELOW-THE-ARTICLE-RECORD`.
- **The 20,000-record fixture** (`bigreply.py`, `logs/bigreply.log`
  `493ee589…`): it opened by full replay in 96 s.
  - `OVER 1-20000` served 1,706,674 octets in 180.3 s (about 9 ms a line).
  - `OVER 1-2000` took 60.2 s, running beside the automatic checkpoint
    (36.6 s, 151,595,027 octets). `OVER 1-10000` took 132.5 s.
  - The owner stayed alive and exited 0.

## 2. Friends at qualified bytes

The runner is `friends.sh`, with the logs under `friends/`.
- **The tarball** was built with `packaging/release-tarball.sh` over the
  gate's frozen image directory (`install.log` `ba1546cc…`).
  - The core `dc3b8919…` and the launcher `432622d2…` equal the gate's.
  - `fn --version` prints 69046a76.
- **friends_feed 2/2** (`6611e3fe…`): invite, accept and confirm, then the
  cancel through the ordinary feed.
- **friends_accounts 1/1** (`59592e34…`):
  - cleartext XREDEEM gets 483;
  - the redeem gets 381, then 281, and the login posts 240;
  - the retry after the crash cut gets 281;
  - `account list` is correct;
  - bbf52159 refuses the redeemed store.
- **The signed post** (`friends_signed.log` `f2754fe7…`):
  - robin redeems (381 and 281) and posts a carrier signed by its account
    principal: 240;
  - `HDR :fn-verified` answers `0 verified 2bf135d1… keyring 3`;
  - `HDR :fn-enrollment` answers `0 active 2bf135d1… keyring 3`.
- **Live rebind** (alice):
  - session A keeps P and gets 240;
  - a new session's P post gets 441 `not bound`, its Q post gets 240, and its
    unsigned post gets 441;
  - auth on the tarball's image is 5/5 (`9c2f1fab…`).
- **PKT-481 (b), unchanged:** `principal bind robin …` gets `refused …
  unknown-login`.
- **`peer keygen KEYDIR`** gives principal `874c9d70…` and exits 0
  (`peer-keygen.log`).
- **`peer pull` against a 430-listing peer:**
  `test_unavailable_id_is_dropped_at_the_bound` OK. Its cut variant is a
  developer selector, which the production tarball image refuses with exit
  5 (C10).
- **Same-box only.** persvati was not taken (PKT-354).

## 3. The section 15 table

| scenario | evidence | result |
| --- | --- | --- |
| Protected ordinary use | tin 2.6.2 (sanding's NNTPS build, `79d78e3a…`) over implicit TLS (`tin-walk/readback.txt` `c0c8c6c1…`); `protected.py` (`7268fc47…`); `node_probe.py` 18/18 (`e7d94c23…`); the web pass | tin logs in, reads, follows up (References set), posts, and files an unsigned cancel in control.cancel; the target stays, since an unsigned cancel carries no authority. fn_client posts, reads, replies, resumes after the watermark and reconciles. Clear AUTHINFO gets 483. Wrong secret gets 481. A bound login's unsigned post gets 441 and its Q-signed post 441 `not bound`, while E-signed gets 240. **Harness note:** `~/.cargo/bin/tin` is built without `--with-nntps` ("Option not enabled"), so runs 1 and 2 are kept, labelled, as harness |
| Retry at a later injection time | qual-harness-c18's C4 in `test_native_control` (prod OK, and **C20 is gone**); `test_native_source_corpus` (C19); the old-client run | a supplied Path is accepted with the prefix; a supplied Xref is refused; a changed byte is `CONFLICT` |
| Lost reply, then withdrawal | `test_native_visibility_join` on dev; on prod it is C10 | as at dfa810fc |
| Cancel order and policy change | `test_native_control_across_peers` OK on prod and dev | both orders, every basis |
| Consumer transaction cuts | `consumer_exchange` and `consumer_exchange_two_nodes` OK, prod and dev | the six cuts, and two nodes |
| Cursor/reclaim boundary | `s15/n300.jsonl` (`5e4a99ca…`); `s15/chain20000.log` (`1fe34229…`); `logs/chaincuts.log` (`1d35220a…`); `logs/chain-eio.log` (`9e6d8d26…`) | N=300: all 18 cuts converge, freeing 192,490 octets. **The kept 20,000-record fixture is served identically after compaction: OK in 1,377 s.** **pack-chain-cut's kill at `pack-chain-link`: OK. EVERY chain publication cut from both entries: OK** (3,124 s; dfa810fc could not finish it). **The 4,500-record EIO campaign at each link publication cut: OK** (1,795 s on dev, and OK inside prod's whole run) |
| Multi-relay outage | `labs.sh`, DTN developer image (`request-labs/rcs`, `missions/rcs` `6c1f60da…`) | request labs 4/4 rc 0. **The four-node mission: unsigned rc 0 (108 s), signed rc 0 (129 s).** The ambiguous peer is refused. multi-peer-relay IS in this candidate |
| Fragment and generation interruption | `test_bp_fragment_node_native` (dtn, dtndev) | SCN-067 OK. **SCN-077 (10 MiB) is unexercised: bp-crc-exec pending** (not on origin/dev when checked). The whole run errored at the test's own timeout on both images, as at dfa810fc |
| Budget exhaustion and recovery | `consumer_profile` OK; `s15/tight.jsonl`; `probe-count.log`; smalldisk | consumers 256, then an upgrade to 300, then the 301st refused (the module). History bytes: the 189th POST refused, the reclaim frees 216,842, and the next POST gets 240. Count: 127 of 128. Small disk: `temporary-space`, 8 files unchanged |
| Pack/reclaim/restore | the compaction cuts; N=300; the chain campaigns; the reinstall rehearsal | compaction 18/22: the 4 `pack-retire-*` rows are unreached on a one-generation store, and they converge in N=300 |
| Recovered success, then another crash | marker cuts 20/20; `test_native_served_crash_model` whole, OK on prod and dev | |
| Long-lived mixed workload | `mixed.py`, one hour, developer image, **under load** (this lane's dev chain and peer-rows run); `logs/mixed.log` `2dc9bb1f…`, `mixed.json.gz` | see below |

**The mixed hour. M1 (PKT-321) is measured again: not met, and correctness
held.**
- The owner stayed alive (exit 0), and `recover` gives rc 0 with
  `open=checkpoint:2048 suffix=385`.
- From 256 s the control socket queues past the client's 10 s deadline:
  - status 45/87 uncertain;
  - group create 24/26 and grant 23/26 uncertain (exit 3);
  - signed reports 73/129 uncertain;
  - consumer wakes 63/68 uncertain;
  - `health` returned 20, 40 times;
  - one grant was refused `BUSY` by name.
- **Reads:** p50 0.5 ms, p95 634 ms, p99 61 s, max 397 s.
- **POST:** p50 128 ms, p95 811 ms, p99 76 s, max 397 s.
- **Resources:** owner CPU 2,786 s in the hour and HWM 1.38 GB. The store
  was 9.4 MB in 2,493 inodes, and the BP journal 323,906 octets.
- **This is not a matched baseline.** dfa810fc's hour ran with less load and
  saw the first timeout at 651 s.
- **Harness:**
  - the consumer's keyring was repaired here, and consumer B now verifies
    (own verdict `verified`). Its dispositions are `conflict/unentitled`
    because the harness configures no entitlement;
  - `bp-queue` exits 7 with no route, because the harness has no BP peer.

## 4. Every image-gated native module, on the four images

- **The runners:** `brun.sh`, `chainM.sh` and `chainB.sh`, through
  tools/test_budget.py under `systemd-run --user -p MemoryMax=24G`.
- **The module list is qual-dfa810fc's**, plus the four modules the merges
  added: checkpoint_generations, control_evidence, control_reply_fit and
  peer_rows_growth.
  - That gives 79 non-BP modules on prod and on dev, and 15 BP modules on dtn
    and dtndev.
  - That is **188 module runs; 145 end OK** (`module-table.md`). The OK
    count includes the 9 whole-module skips listed below, which are not
    evidence.
  - **Whole reruns of the terminated modules:**
    - served_crash_model OK on prod (282 s) and dev (261 s);
    - control on dev stopped at 3,600 s in the production-only selector
      sweep (C10′);
    - pack_chain's whole run stopped at 3,600 s on prod, after the EIO
      campaign and every other test were ok. On dev it was stopped by this
      lane, because every part of it ran separately on dev (section 3);
    - fragment_node's whole run errored (SCN-077) on dtn and dtndev.
  - tests/test_build_native_host.py and tests/test_hbox_native.py are tool
    tests, not image-gated.
- **The environment was set by hand in `env.sh`** (PKT-499 (d)).
  - It was checked against `tools/native_env.py plan`.
  - `FN_NATIVE_TEST_ROOT` was added.
  - The legacy-image variables (topic-v1, pre-T2, T2, T2b) are unset, so
    those cases are unexercised (e).
- **SKIPPED modules are not evidence:**
  - newnews_migration and stamp_migration on every image (legacy images);
  - frozen_relocation without its opt-in. It ran separately: **2/2 OK on a
    `cp -a` copy**, with image.sha256 OK before and after
    (`5a6f5680…`);
  - admin, peering and protected_peering on dev (production-only; all OK on
    prod).
- **GREEN at 69046a76, red at dfa810fc:**
  - bounds_join (PKT-481 fixed);
  - control's C20 (the operator_post Path tail);
  - hybrid_author's C6/PKT-485 (carrier-bound-test).

**Every red, classified** (see `module-table.md`). Per test, compared with
dfa810fc's logs (`reds.py`), every red is dfa810fc's same test except the
three new ones below.

| id | module (images) | what | class |
| --- | --- | --- | --- |
| C10 | prod: key_statements 2, peer_invite 2, peer_pull 4, visibility_join 3, owner; dtn: bp_service 7, bp_receive_integrity 2, bp_node 27 | the production image refuses a developer selector by name (exit 5) | (c), intended; they pass on the developer images |
| C10′ | dev: operator_verbs and outcome_algebra `production_image_refuses_the_selector`; control whole (the selector sweep) | a production-only test on the developer image | (c) |
| C2 | bp_obligation `kill_between_attempt_and_outcome` (dtn, dtndev) | `no-route` exit 1 | (c), unchanged |
| C7 | consumer_project_bounds 1/2 | `codec` vs `limit` | (c), unchanged |
| C19 | source_corpus `signed_route_retry`; campaign module 8/10 | `REFUSED` expected where outcome-algebra prints `CONFLICT` | (c) |
| C21, C22 | live_reconfiguration 3, state_checkpoint 1 | moved call/text (`fn-sopc-classified-open`) | (c) |
| C23, C24 | owner 2; auth_admin_fidelity 2 | raw-SBCL structure harnesses | (c) |
| C25 | native_checkpoint `clone_reopens…` | `retired pack-generations=1` under chained packs | (c); the rest of that test is still not reached |
| C26 | v0_matrix `historical_schema_one` | planned rows missing from the historical file | (c) |
| **C27, new** | test_checkpoint `corrupt_selected_generation_is_reported_not_rolled_back` (prod, dev) | tools/checkpoint.py, the Python development tool, not the native path. The test calls `generations()` on an unopened `Store` whose `.config` is None. caps-to-profile (e10e4530) made the namespace limit read the opened profile. The CLI publish and select in the same test pass | (c) |
| **C28, new** | history_required `the_host_calls_acl2s_decisions`; profile_upgrade `the_verb_is_acl2s…` (prod, dev) | source-text: profile-open-refusal (b13af68e) moved the body of `fnn-command-upgrade-profile` into `fnn-command-upgrade-profile-open` (host/native/io.lisp:2640). `'fn-hmr-upgrade-verdict` and `(fnn-open-live-store root t (fnn-profile-test-fault))` are there, one call down | (c) |
| **C29, new** | operator_verbs `peer_list_is_a_query…` (prod, dev) | source-text: keys-and-accounts-3 (a50312ef) made the query report call `fn-native-admin-peer-budget-report` (books/native-admin-peer-budget.lisp) | (c) |
| C9 | the production kill run: 6 `died-absent-resubmit-failed` | the known over-limit band | (c) |
| SCN-077 | fragment_node 10 MiB | bp-crc-exec pending | (e) |
| — | peer_rows_growth (new): over its budget, and its whole rerun stopped at 3,600 s on prod (and was stopped on dev) | 1,100 offline `peer carries`, each replaying every configuration record: O(N²), caps-to-profile §7, which measured 8,335.9 s | **not red**: a dedicated developer-image run is **OK, 1,100 requests in 6,440.7 s** (`logs/peer-rows-growth.log` `759cd452…`) |

**Over budget (PKT-248's kind):** native_checkpoint, state_checkpoint,
capacity_vector, consumer_profile, public_exposure, peer_pull,
served_crash_model, pack_chain, peer_rows_growth, bp_app, bp_node (dtndev),
source_corpus_bp and fragment_node.

## 5. Old clients (PKT-372)

`oldclient.py` wrote `logs/oldclient.log` (`d1face93…`). The client runs
against one 69046a76 node:

| client | CONFLICT | UNKNOWN-GROUP |
| --- | --- | --- |
| b6759850 image, `operator post` | `uncertain operator post UNCERTAIN`, **exit 3** | `refused operator post REFUSED`, 1 |
| b6759850 image, `hybrid-author` | `uncertain`, **exit 3** | `uncertain`, **exit 3** |
| 69046a76 image, `operator post` | `refused operator post CONFLICT`, 1 | `refused … REFUSED unknown-group`, 1 |
| 69046a76 image, `hybrid-author` | `refused hybrid-author CONFLICT`, 1 | `refused hybrid-author UNKNOWN-GROUP`, 1 |
| b6759850 `tools/fn_client.py` (NNTP) | `refused 441 …`, 1 | `refused 441 …`, 1 |
| 69046a76 `tools/fn_client.py` | `refused CONFLICT 441 …`, 1 | `refused 441 …`, 1 |

- docs/agents.md says: "Upgrade the client with the node: the `CONFLICT` word
  is new in the control reply, and an `fn` image from before it cannot decode
  it, so its `operator post` or `hybrid-author` answers `uncertain` (exit
  3) …".
- `test_native_outcome_algebra`, with `FN_OLD_NATIVE_HOST` set to the
  b6759850 image, is 9/9, including
  `test_an_old_client_reads_conflict_as_uncertain` (`05e7eee3…`).

## 6. The throughput gate

- **The run:** `tools/throughput_gate.py run --image
  /tank/fn/gates/qual-69046a76-20260926/build/fn-host-developer --revision
  69046a76… --label qual-69046a76 --wait-quiet 120 --under-load`.
- **It ran under load:** this lane's chain and peer-rows units, plus another
  lane's b6759850 scope.
- **`check` passes, rc 0.** It compared 1 of 12 metrics, the deterministic
  counter (PKT-477 (1)).
- **The JSON:** `qual-69046a76/throughput/69046a76798b-qual-69046a76-20260926T174825Z.json`,
  for the deputy (it writes the baseline with `--release` = this run).

| metric | bbf52159 (release) | dfa810fc (under load) | **69046a76 (under load)** |
| --- | --- | --- | --- |
| probe CPU s (1,000 commits) | 68.675 | 1.775 | **1.25** |
| bytes consed per commit | 103,341,504 | 1,692,140 | **1,406,484** |
| POST owner CPU ms | 1.6 | 2.2 | **1.7** |
| POST median ms | 1.545 | 2.03 | 1.617 |
| ARTICLE median ms | 2.609 | 2.878 | 2.437 |
| reopen s | 0.145 | 0.163 | 0.145 |
| checkpoint publish ms | 350 | 810 | 643 |

## 7. docs_check, spec citations, PKT-384

- **`python3 tools/docs_check.py` on the candidate's tree:** rc 0, 0
  failures, 13 templates (`docs-check.log`).
- **`tools/spec_cite_check.py`:** 5,229 citations, 322 known stale (232
  names), 0 unlisted. This is **fewer than 349**, so it has not grown
  (`spec-cite.log`).
- **PKT-384, carried and rechecked:** `CHECKPOINT_CUTS` still names
  `fn-cpp-publication-step` for the three candidate-* cuts, and no book
  defines it.
  - The cuts are `fn-jpub-step`'s phases (books/journal-publish.lisp:59),
    reached through `fnn-immutable-publish-effect` with
    `fnn-checkpoint-candidate-observer`.
  - They cover both the checkpoint generation publication and each pack
    link's publication.
  - caps-to-profile changed host/native/checkpoint.lisp only by passing the
    store's config. So the cut map lies about the program's name only.

## 8. The web reader, the operator and the probe

- **Web** (`webnode.py`, production image, STARTTLS with `[auth]` required;
  `tools/fn_web.py` at 69046a76 on the laptop through an ssh tunnel, as
  guest, TLSv1.3 verified, `--keyring` pinning E; `web-walk.tgz` `b63de6c1…`):
  - **The five facts, apart, on the signed root:**
    - historical `verified by principal eeee… under keyring generation 1`;
    - current `active … is enrolled now at keyring generation 1`;
    - independent `verified-here`.
  - **guest's Q carrier:** `verified … generation 2` and `active` at the
    node. The reader's own check is `not-performed: principal 5151… is not
    in the keyring`.
  - **tin's reply:** evidence none, historical absent.
  - `retired` and `revoked` pass in `test_fn_web_native` (prod and dev OK).
  - **Search:** `XPAT Subject 1-10 *notes*` gives #4. #2 shows `423
    withdrawn`, and the conversation shows `430 withdrawn`.
  - **Double-click:** one article, the range going from 1–10 to 1–11. Back
    and refresh show "This form was already posted", and the draft is saved.
  - **A lost post with the tunnel down:** "Outcome: uncertain … could not
    connect".
    - After a client restart the outbox lists it as "needs settling".
    - The re-send gets 240, and the outbox then says "settled by re-sending:
      accepted".
    - It became #12.
- **NNTP probe:** `tools/node_probe.py` gave 18/18 held, exit 0, over TLSv1.3.
- **The operator walk:** 63 lines. All match except 04 to 06: never-initialized
  `status`, `health` and `run` exit 1 `NO-STORE` where the walk script
  expects 6. That is outcome-algebra's documented move (c).
  - `health` answered 19 offline on the live copy.
  - The `starting` and stale-socket cases are in `test_native_control` (OK on
    prod).
- **The v0 matrix** was driven from the candidate's detached tree at
  69046a76, with `--inn`. Its stores were provisioned
  `init --profile development fn.letters control.cancel`.
  - Run `20260926T153732.941529Z-69046a7-c7ea98d3f8c5`: 225 rows, 147
    accepted, 44 refused, 2 uncertain, 31 not exercised and 1 not built.
    **0 disagreed, 0 faulted.**
  - `V0-INN-INTEROP` held (34 held).
  - **0 verdict changes** from dfa810fc's run over all 225 rows. The run is
    copied under `v0-run/`.
  - Run 1 is kept under `v0-run1-provisioning/`. Its stores lacked
    fn.letters and control.cancel, which gave 2 disagreements (411 no such
    group, and a cancel `control-not-filed`). That was harness provisioning.
- **The campaign** (`campaign.sh`):
  - cut observations 60/60 (`eb16ec63…`);
  - compaction 18/22 (`59c75905…`);
  - marker cuts 20/20;
  - NNTP post probe 50/50;
  - `test_native_crash_model` 7/7 (51 s);
  - the D25 matrix 21/21 (`38a66283…`);
  - **the production kill run** (seed 3, 2:58, peak RSS 519 MB): **0 torn,
    0 lost-240, 0 reused-number**. 120 were 240-identical, 53 died-absent,
    27 died-present-identical, and 6 were C9 (`acd7ef9a…`).

## Not established

- It is one run of each case, on one busy box, over loopback. It covers
  process deaths, not power loss. The friends result is same-box.
- SCN-077 is unexercised (bp-crc-exec pending).
- The legacy-image migration modules are unexercised.
- The mixed hour ran under load and has no matched baseline.
- `store export/import` does not exist at 69046a76, so the "import" is a
  `cp -a` of a stopped store.
- No row here is a theorem.

## For the deputy: the current-view.json sidecar

- **`images["69046a76"]`:**
  - `source` 69046a76798b8be6169eeed5a66cc46fbe399e51
  - `qualification` planning/evidence/qual-69046a76-2026-09-26.md
  - `closure_manifest` certify-20260926T144826Z-2113131
- **`tested.image = 69046a76`** for these capabilities, whose native modules
  ran on this candidate's frozen images:
  - **P1:** starttls, implicit_tls, auth, protected_peering, the web pass
    and the probe.
  - **P2:** owner, the campaign cuts and the kill run.
  - **P3:** reader_index, visibility_join and the matrix.
  - **P4:** the D25 matrix 21/21 and source_corpus.
  - **P5:** owner, block_fault_ownership and served_crash_model.
  - **P6:** live_reconfiguration and control.
  - **P7:** peering, protected_peering, and the matrix with INN.
  - **P8:** fn_verify, hybrid_author and fn_web_native.
  - **P9:** consumer_profile, the tight store and the operator verbs.
  - **P10:** crash_model, served_crash_model, the marker and chain cuts, and
    crash_correspondence.
  - **P11:** the BP modules on dtn and dtndev, and the missions.
  - **M4:** bp_app, the request labs and the missions.
  - **M5:** the compaction campaign, N=300 and pack_chain.
  - **M6:** reader_index (its `LIST COUNTS` cases; OK on prod and dev),
    and the web and tin walks.
  - **T17:** reader_index, the served path and served_differential.
  - The deputy checks each against the capability's own module list before
    pinning.
