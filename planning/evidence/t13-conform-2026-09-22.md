# T13-conform: four conformance defects from the first native node, decided in books

Lane T13-conform, branch `t13/conform` from `dev` `7c5d2e6e` (merged `dev`
`bb0affe6` before certifying). The findings are the INN lab of 2026-09-22
([inn-lab-dabebb84](inn-lab-dabebb84-2026-09-22.md), its three fn findings)
and the agents run of the same day
([agents-on-hbox](agents-on-hbox-2026-09-22.md), "What the node did that
deserves a look"). Each defect is decided in a book; the host only carries
octets.

## The defects, their causes, and what the node does now

| # | Finding | Cause (at `7c5d2e6e`) | Now |
| --- | --- | --- | --- |
| 1 | `fn operator CONFIG post` fed INN an article with no Path; `437 Missing "Path" header field` | `books/owner.lisp:776` `fn-own-control-decision` "Preserve those octets exactly" (line 785): the operator's control submission queued the payload as read; `host/native/owner.lisp:960` stepped `fn-owner-control-submit` for it | the owner event `(:operator-submit ...)`, `fn-own-operator-submit` (`books/owner.lisp`), injects through `fn-inj-decide` under the owner's posting configuration and clock; `host/native/owner.lisp` `fnn-owner-control-submit-serialized` takes one clock reading and steps `fn-owner-operator-submit` (`host/owner-host.lisp`), and stores the octets ACL2 injected (`fnn-owner-complete-bound-submission` with `:injected`) |
| 2 | a transit article was served with the sender's Path, no `fnA.hbox.test` | `books/peer-inbound.lisp:336` `fn-peer-injection-arguments` staged the received `octets`; `host/owner-host.lisp:458` `fn-owner-take` put `fn-peer-submission-octets` in `fn-owner-submit-octets`, which `host/native/owner.lisp:834` stored | both stage `fn-peer-relayed-octets` (`books/path-update.lisp` `fn-pu-relay-article`): this node's identity and the RFC 5537 3.2.1 diagnostic prepended to Path, once, at acceptance; the native drain compares the two before the store attempt |
| 3 | the sender's `Xref: inn.hbox.test fn.letters:15` was served | as 2 | the same walk removes every Xref with its continuation lines |
| 4 | `From: yue` was answered 240 and served | `books/injection.lisp:565-566`: `:from-missing` and `:from-duplicate`, presence only | `books/mailbox.lisp` `fn-mbx-mailbox-listp`, a bounded RFC 5322 mailbox-list recognizer under RFC 5536 2.1/2.2; `fn-inj-mandatory-reason` refuses `:from-invalid`, `441 posting failed; From is not a valid mailbox list` |

Where 2 and 3 are decided, and why there: specs/peering.md 2.3. fn's relaying
and serving agents are one news server over one store, so RFC 5537 3.2.1's
first sentence lets the update be made once; the stored octets are the served
octets and the octets the outbound feed offers. Serving-time rewriting would
put a transformation on every served read and make stored and served bytes
differ.

## The theorems, their subjects and the host lines

**PRF-056 (defect 1)**, `books/owner-invariants.lisp`, subject
`fn-own-operator-submit`, stepped by `host/owner-host.lisp`
`fn-owner-operator-submit`, called from `host/native/owner.lisp`
`fnn-owner-control-submit-serialized` for `fn operator CONFIG post`:

- `fn-own-operator-submission-is-an-injection-of-the-payload`: a
  `:submitted` answer queues exactly one control submission, injected,
  carrying the Message-ID and groups the command named, whose octets are
  `fn-inj-reinjectionp` of the payload (this agent's Path line, an
  Injection-Date, its Injection-Info, optionally generated Message-ID/Date,
  then the payload unchanged).
- `fn-own-operator-decision-is-an-injection-of-the-payload`: the same of the
  decision for any configuration, clock and node.
- `fn-own-operator-submit-without-a-clock-refuses-and-changes-nothing` (D10-a).
- `fn-own-operator-retry-resubmits-the-stored-injection`: a retry under any
  later usable clock is the stored injection, so the store answers duplicate.
- All rest on `fn-inj-injected-article-is-a-reinjection-of-its-source`
  (`books/injection-invariants.lisp`, over `fn-inj-decide`).

**PRF-057 (defect 4)**: `fn-mbx-mailbox-list-names-an-address`
(`books/mailbox.lisp`) and `fn-inj-injected-proto-article-has-a-mailbox-list-from`
(`books/injection-invariants.lisp`), over `fn-inj-decide`, which
`books/nntp-post.lisp` `fn-nntp-post-step` calls on the served POST path and
`fn-own-operator-decision` on the operator's. Refusal word `:from-invalid`,
line `441 posting failed; From is not a valid mailbox list`.

**PRF-058 (defects 2, 3)**, `books/peer-inbound-invariants.lisp`, subject
`fn-peer-relayed-octets`, called at `host/owner-host.lisp` `fn-owner-take`
(fills `fn-owner-submit-octets`, which `host/native/owner.lisp`
`fnn-owner-drain-one` digests and stores); `fn-peer-injection-arguments`
(called at `fn-owner-transit-decide`) stages the same, by
`fn-peer-injection-arguments-payload-unfolds`, and the drain faults if they
differ:
`fn-peer-relayed-octets-change-only-path-and-xref` (no hypothesis),
`-are-idempotent` (none), `-carry-no-xref` (none),
`-name-this-node-in-every-path` (the node has a path identity); each the
`fn-pu-relay-article-*` keystone of `books/path-update.lisp`. Stored equals
served: `fn-durable-completion-installs-exact-pending-article` (PRF-002).

Teeth, from the labs' own octets: `tests/acl2/owner-operator-tests.lisp`
(new), `tests/acl2/injection-tests.lisp` and `tests/acl2/peer-inbound-tests.lisp`
(last sections): a reachable witness each and one `must-fail` per hypothesis
(for a statement with none, the stronger statement refuted by the lab value).

Certification: `run-20260922T225657Z-1589` on persvati (manifest
`planning/evidence/manifests/certify-20260922T225718Z-866045.json`, git
`80f4080f` plus the uncommitted test edits of that moment): 100 of 110
passed, including mailbox, injection, injection-invariants, nntp-post,
path-update, peer-inbound, peer-inbound-invariants, injection-tests,
nntp-post-tests, nntp-auth, served. Failed: `books/store-node-resolution`
timed out at 1800 s (it takes 1748 s on dev, manifest
certify-20260922T221439Z-474314; recertified because its closure reaches
books/injection), which cascaded to store-observed, owner, owner-invariants,
native-admin, native-operator, owner-fault, owner-tests and
owner-operator-tests; and `tests/acl2/peer-inbound-tests` failed on an
`mv-nth` over `fn-peer-transfer`, fixed in `1bf64ce0`. The owner-level books
and both test books were loaded to their last form in local proof sessions
(not a certificate). Provisional wave `run-20260922T234646Z-df2e`
(persvati, remote root /home/ember/fn-gates/t13-conform-triage, budget 800 s)
over the owner cluster and peer-inbound-tests (triage
`triage-20260922T234645Z-ca93`, 108 books) finished: every proof in books/owner,
owner-invariants, owner-config, owner-fault, owner-prepare-correspondence,
native-operator, native-admin, served, nntp-auth, peer-inbound-invariants,
tests/acl2/owner-operator-tests, owner-tests and peer-inbound-tests succeeded
("proved, not certified"). The two reds are timeouts under the 800 s budget:
books/store-node-resolution (1748 s on dev) and books/peer-inbound (584 to
770 s on dev's recent runs, 875 s passing in this lane's closure run). No
other red. The owner cluster is proved, not certified, until
store-node-resolution certifies under a budget that holds it.


## What is not verified

- No image carries any of this. The native tests that would run it
  (`tests/test_native_control.py`, the injected prefix and the
  `From: yue` / supplied-Path / mismatched-Message-ID refusals through the
  operator verb) skip without an image and have not run. The INN lab rows
  V0-INN-OPERATOR-POST and V0-INN-SERVING-AGENT and the new
  V0-POST-FROM-MAILBOX are measured by root's next run on a frozen image.
- `fn store ROOT post`, the offline store verb, still writes exact octets:
  it is not the operator's post and nothing here changes it.
- An article taken by transit with no Path field is stored with none; RFC
  5537 3.7 step 1's rejection of it is open (the v0 matrix offers such
  articles).
- The recognizer's completeness (every conformant mailbox-list accepted) is
  witnessed, not proved.
- The operator's control reply carries the status, not the refusal word:
  `:from-invalid`, `:control-mismatch` and the rest reach the operator as
  `refused`.
