# Ten-second books, third pass, 2026-09-25

This lane changed hints in seven books and changed no theorem statement,
name, rule class or exported rule. Each changed hint names the rules that
backchained without use in the slow event. Every book in the brief now
measures under ten seconds at two jobs on persvati, except `owner-invariants`.
`native-admin`'s post-merge repair is admitted in the REPL only; no farm run
certifies it yet.
No new PRF.

## Method

The per-event `Time` and `Rules:` come from the certify logs of
`certify-20260925T090038Z-2952748` (persvati) and
`certify-20260925T093339Z-3045962` (hbox). The slow events were then run in
`tools/proof_repl.py` sessions under `(accumulated-persistence t)`:
- persvati: `/home/ember/fn-gates/toolchains/w25/acl2-literal`, cache
  `/home/ember/fn-certcache`.
- hbox: `/tank/fn/toolchains/w28/acl2-literal-4g`, cache `/tank/fn/certcache`,
  with a dev-bytes copy for the BP closure.

`store-node-invariants` and `owner-invariants` also got one whole-book
instrumented session each. Their book-wide candidates were A/B-tested as
alternating whole-book REPL loads.

## Variance first (native-admin-peer 25.7 s on hbox, native-admin 8.3 to 15.1 s)

The run records the process wall time, and ACL2's summary `Time` is CPU time.
- `native-admin-peer`, same bytes: CERTIFY-BOOK took 10.80 s CPU on hbox
  (093339Z) and 10.89 s on persvati (090038Z). The hbox wall time was 25.7 s,
  so the process ran on about 42 % of a core. That is contention, not proof work.
- `native-admin`: 7.20 s CPU on hbox and 7.07 s on persvati.
- The load also raises CPU time on persvati. At the same bytes,
  `native-admin-peer` took 6.88 s in 101629Z and 9.13 s in 110029Z (load 7.4).
  Its CERTIFY-BOOK CPU figure rose by the same proportion.
- Compare figures only when the host, the load and the job count match.

## What was changed

| Book | Slow event and cause | Fix | Before, 2 jobs | After, 2 jobs |
| --- | --- | --- | ---: | ---: |
| `peer-inbound` | `fn-peer-step-pinned-effects-well-formed`, 3.35 s. 1.1 M useless frames on `fn-nntp-article-idp-is-consp`, `fn-nntp-article-idp`, `fn-nntp-message-id-tokenp`, `fn-nntp-response-text-true-listp`, `fn-af-message-idp` and `fn-peer-message-id-len` | Those rules are disabled in the hint. REPL 5.0 → 0.3 s | 11.09 persvati, 25.1 hbox (contended) | 6.43 (101629Z), 5.79 (183809Z) |
| `native-admin-peer` | Guard proofs of `fn-native-admin-peer-plan-base` (0.95 s) and `fn-native-admin-bp-boundary-base-plan` (1.98 s) opened the decimal recognizer and its value to learn `stringp`. The second guard proof also opened `fn-native-admin-bp-boundary-split`. `fn-native-admin-peer-plan-base-kind` (2.36 s) simplified every branch test | A local forward-chaining lemma says a decimal word is a string, and the guard hints keep the decimal functions and the split closed (REPL 3.8 → 0.1 s). The kind theorem is proved in `minimal-theory` plus the definition, `kind-of-result` and `(:e member-equal)`, since every branch has a literal kind (REPL hbox 10.5 → 0.9 s) | 10.99 persvati, 13.19 hbox | 6.88, 6.58 |
| `native-admin` | Guard proof of `fn-native-admin-plan` (0.82 s), same decimal opening | The same local lemma and guard hint. It sits above dev's new `fn-native-admin-retention-days` (merge 177eccf0) | 7.18 persvati, 11.24 hbox | 7.18, 6.48 |
| `bp-node-forward-lower-guards` | `verify-guards fn-bpnp-forward-image` (2.09 s) opened the fragment and true-list chain (the ten-second-2 chain) and `fn-bpah-held-expiry`. `fn-bpnp-forward-blocks` (1.55 s) opened the per-block step | These are disabled in the two guard hints. REPL hbox 2.7 → 0.18 s and 2.3 → 0.15 s | 11.43 persvati, 13.64 hbox | 6.83, 6.73 |
| `nntp-list-counts-tests` | The must-fail `nlc-t-work-false-with-repeats` nested inductions until the 200,000-step limit (3.71 s) | `:do-not-induct t` added inside the must-fail. The statement is unchanged, and it is still refuted by the ground witness just above it (`assert-event` on four repeats of fn.letters) | 11.04 | 6.03, 4.87 |
| `store-node-invariants` | Flat profile. The whole-book accumulated persistence shows `fn-cp-idp-true-listp` (0.2 M useless frames), `fn-prov-structured-is-not-a-string`, and `fn-sn-verdict-listp` with `member-equal` (0.13 M and 0.15 M, mostly useless) | Book-local disable of those four rules. `fn-sn-finish-preserves-state` also keeps them closed in its hint (1.78 → 1.15 s). `fn-sn-actual-durable-completion-installs-record` keeps the three freshness facts closed (2.22 → 1.82 s). A/B whole-book REPL load: 41.9 and 44.9 s before, 34.0 and 30.3 s after | 12.84 persvati, 20.55 hbox | 10.13 (first two edits only), 8.09 (183809Z) |

`nntp-auth` was not edited. Its proof is 2.9 s of 8.5 s, and its 11.24 s on
hbox was contention (6.45 s CPU). It measures 5.87 and 5.42 s after
`peer-inbound`'s fix. The fix does not change the include of `peer-inbound`.
The two measurements differ in load.

## Runs (ACL2 8.7, 2 jobs, --timeout-seconds 300, no --closure)

- `run-20260925T101600Z-5cad`: persvati, source 5eddc188, `--affected-by`
  the five changed roots. 312 certified, 0 failed. Load average about 5 to 7.
  [manifest](manifests/certify-20260925T101629Z-3766562.json)
- `run-20260925T110006Z-e8e9`: persvati, source 6527a87b. This is the
  deputy's quiet re-measure (`--recertify` with `--affected-by`) of
  bp-request-plan, byte-store-scan, checkpoint-codec, consumer-store-projection,
  native-admin, native-admin-peer, store-node-traces, store-node-traces-prepare
  and owner-invariants, plus `--affected-by` store-node-invariants. 394
  certified, 0 failed. Load average 7.4, so the run was not quiet. Results:
  bp-request-plan 8.58, byte-store-scan 9.73, checkpoint-codec 9.58,
  consumer-store-projection 7.48, store-node-traces 8.93,
  store-node-traces-prepare 10.34, owner-invariants 17.25.
  [manifest](manifests/certify-20260925T110029Z-50297.json)
- `run-20260925T183738Z-fc1b`: persvati, source 8df249ea, `--recertify`
  with `--affected-by` for every changed book, plus nntp-auth and
  store-node-invariants. 312 certified, 0 failed. Load average 1.0 at
  submission and 6.9 mid-run.
  [manifest](manifests/certify-20260925T183809Z-4084815.json)
- `run-20260925T185402Z-a0d9`: hbox (w28 `acl2-literal-4g`), merged source
  177eccf0 (dev da9d538f merged in), the same roots plus `store-node-invariants`.
  518 certified and 2 failed. Load average 12 to 13 during the run.
  [manifest](manifests/certify-20260925T185444Z-3591801.json)
  - Results: peer-inbound 7.18, native-admin-peer 7.13, nntp-auth 6.08,
    nntp-list-counts-tests 4.88, bp-node-forward-lower-guards 7.58,
    store-node-invariants 12.00 (8.62 s CPU), native-admin 10.49 (10.33 s CPU).
  - The two failures are `books/poster-bytes-buffer`
    (`fn-pbb-source-index-is-inj-source-of`) and
    `tests/acl2/octets-stobj-tests`. Neither closure contains a book this lane
    changed; `poster-bytes-buffer` changed on dev in 4e1b2a71 (pbb-d32). They
    belong to that lane.

## After the merge: native-admin (41186266, REPL only)

On the merged bytes, dev's new retention and peer-extend arms made
`fn-native-admin-plan-group-name-is-a-group-name` cost 3.46 s on hbox r4. The
profile shows the map `fn-native-admin-words` expanded 16,000 times. Four rules
now relate the words to the argv: car, cdr, consp and len of the words. They are
local to the theorem's encapsulate. The theorem's hint also keeps the words, the
carries helpers, `subsetp-equal` and `fn-native-admin-retention-days` closed.
REPL on hbox under accumulated persistence: 6.38 → 1.47 s, and the whole book
reloads (65 forms). **No farm run certifies this commit**: r4 certified the
merged bytes before it, and the lane is at its run budget. The deputy's next
merge certification (`--affected-by books/native-admin`) owes it. `native-admin`
stays in the baseline until that run exists.

## Baseline

`planning/proof-cost-baseline.json` is edited by hand for the books this lane
fixed. Four books are removed:
- `peer-inbound`: 7.18 hbox r4, 5.79 persvati.
- `native-admin-peer`: 7.13, 6.58.
- `nntp-auth`: 6.08, 5.42.
- `bp-node-forward-lower-guards`: 7.58, 6.73.

`nntp-list-counts-tests` is already absent from dev's baseline.
`store-node-invariants` is lowered to its worst matching measurement at the
current closure: 20.55 → 12.00, from hbox r4 under load 12. It measured 8.09 s
on persvati before the merge.

`tools/proof_cost.py --write-baseline` would also drop 23 books this lane did
not work on. Its re-measures on r4 fall under ten seconds. That shrink belongs
to the deputy's re-measure, not to proof work here, so this lane leaves those
entries alone. The same tool reports two books over the threshold that are not
in the baseline: `bp-node-busy-delivery` 11.99 s and `bp-route-step` 11.54 s,
both measured in hbox r4 at load 12. This lane did not add them.

`make check` in this worktree stops at "ledger stale" (planning/ledger.*). The
ledger is generated, and the deputy regenerates it on merge.

## Not done

- `owner-invariants`: 11.12 s (101629Z), 17.25 s (110029Z, load 7.4) and
  10.73 s (183809Z) on persvati, at dev bytes before the control-c3b merge.
  The whole-book session was run at control-c3b's a541e90f:
  - Proof time is 9.7 s over 168 events, and the include of `owner` is 3.5 to 4.5 s.
  - The largest events are `fn-own-refresh-preserves-relation` (2.15 s REPL)
    and `fn-own-durable-reply-names-a-durable-record` (1.1 s).
  - Disabling the six book-wide rules that were never useful
    (`fn-ctl-authorize-execute-is-nonempty`, the two
    `fn-wire-next-*-needs-input`, `fn-snrt-new-success-is-actual-matching-durable-completion`,
    `fn-article-parse`, `fn-article-parse-lines`) changed nothing: A/B 15.06
    and 14.41 s before, 14.96 and 14.89 s after.
  - For the refresh theorem, the midx build is needed. Disabling the midx
    branch helpers and `fn-snt-ready-or-recovered-node-is-exact-replay` saves
    only 0.4 s.
  - This book needs a structural change. One option is a lemma that states the
    midx and gidx correspondence of the refresh result, so the relation proof
    stops rebuilding the index. A split does not reduce total work, because
    every half pays the 4 s include of `owner`.
  - It stays in the baseline. By the deputy's sequencing decision, its next
    editor is control-c3d, which restates K1 in the same book.
- The farm's cache still has no `.fasl`, so includes load uncompiled. This is
  the ten-second-2 finding. It is 2.5 to 6.6 s of every book above.
- Other books over 10 s in these runs, not in this brief: config-owner-live
  12.3, consumer-store-invariants 12.1, bp-node-forwarding-teeth-tests 11.9 and
  topic-history-store-invariants 10.4 (183809Z, persvati, load up to 6.9).

These runs certify books at these bytes. They do not certify an integrated
image.
