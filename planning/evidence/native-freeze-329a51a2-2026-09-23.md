# Native qualification batch 329a51a2 — in progress

The source cut is `329a51a2`; later lane packets are excluded from this image.
`make check` passed before the cut. This batch joins T2 acceptance stamps and
NEWNEWS, pre-stamp selected checkpoint migration, the ACL2-owned duplicate
decision, Store composite index preservation, portable hybrid carrier CLI
commands, and the release-aware native workflow constructors/replay.

Root submitted `run-20260923T175826Z-ffee` on hbox, isolated at
`/tank/fn/gates/takeover-v0-image-329a51a2`, from detached local tree
`build/lanes/image-v0-329a51a2`. This is one explicit fresh all-root
qualification at four jobs and a 300-second per-book limit, through the
qualified w28 ACL2 executable and existing shared slot pool. The purpose is
to qualify the low-level FNWF frame extension across its dependents and obtain
a coherent certificate set after measured mixed-origin include failures.
It is not the default lane iteration policy.

Invocation: `tools/farm.py submit hbox <tools/ledger.py makefile_roots()>
--closure --jobs 4 --timeout-seconds 300
--remote-root /tank/fn/gates/takeover-v0-image-329a51a2`.
The build will use `tools/runbooks/hbox-image-build.sh`, which now preserves
acquisition/validation failures instead of masking them through `tee`.

Certification passed for all 466 books in the selected 456-root closure in
186.051 seconds at four jobs, with no book failures; the source audit found
no forbidden facilities. The manifest is
[`certify-20260923T175847Z-4039310.json`](manifests/certify-20260923T175847Z-4039310.json).
The remote staged tree has no Git metadata; its source digests identify the
frozen input. Both image profiles acquired the resulting coherent artifact
sets successfully. Image build and runtime results remain pending. Planned runtime
checks cover old checkpoint/pack migration, stamped NEWNEWS, real dual-suite
carrier verification, native workflow and receipt paths, admin/checkpoint and
protected peering, and served crash observations. A separate hbox/persvati
protected exchange uses the source/hash-pinned frozen-launcher gate. No live
node upgrade or network exposure is part of this batch.

The newer BP guard/load packet and FNBS recovery machine integration, T8b
configured recovery/live adoption, and the strengthened E2 consumer kernel
continue in their lanes for the following cycle. No result here qualifies
those excluded changes.
