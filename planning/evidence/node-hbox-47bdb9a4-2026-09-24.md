# The hbox node upgraded to the 47bdb9a4 image — 2026-09-24

The fourth image built from `dev` and the second upgrade of the running node
in place: the store (7 transactions of its 128 budget), the TLS pair, the
credentials and `fn.toml` from the 2026-09-22 deploy stay; the previous
releases (`fn-da5fd8cb`, `fn-dabebb84`) stay under `/tank/fn/node` for a
rollback that is one `ExecStart` line. Deployed 17:30 UTC; the node
listened on 192.168.50.39:1119 seven seconds after restart.

## The image

Sources at `dev` `47bdb9a4`, frozen at `/tank/fn/gates/qual-47bdb9a4-20260924`
on hbox, images under `build/images/47bdb9a4/` with `image.sha256` (58
files). Combined closure of all 690 roots green (`certify-20260924T164607Z-1635771`,
3 certified and 702 installed at exact bytes). Production launcher `fn-host`
`432622d2…` with core `73ad25bc…`; developer `e4eeefd2…` / `b81bdaf6…`;
DTN `095382ba…` / `3c829019…`; DTN developer `4986f14b…` / `65da00ed…`.
Installed by `packaging/install-native.sh` into `/tank/fn/node/fn-47bdb9a4`
(the frozen digest check passed; the refusal probe exits 5).

What this image carries that da5fd8cb did not, all landed 2026-09-24:
receiver-local signed ingress shared by NNTP and BP; the three NNTP wire
repairs (an uncertain POST answers `441 ... do not repost` before the
recovery stop, a durable article is never reported refused, a refusal names
its reason); the completion names the submission by the octets the owner
stores (the transit 436 regression fixed before this image); reconfiguration
and its delta constructor in ACL2; retention refusal and keep-until-release
over the called prepare and finish; the transaction budget decided in ACL2
from the store profile with headroom in `operator status`; group names per
RFC 5536 §3.1.4 and the reserved names refused; the BP recovery arm
clearing sessions; the BP restart transaction id from the recovered state;
the whole-store checks removed from every read (STAT flat in the archive
size); one owner-log line for every peer transfer, every peer reply and
every BP application refusal; the local author lifecycle, ION binding,
retention publication and pack retirement of the gpt-6 shift.

## The qualification on this image

- [Matrix](matrix-47bdb9a4-2026-09-24.md): 188 rows reached, 0 disagreed,
  0 faulted, INN 33/33 (142 accepted, 44 refused, 2 uncertain).
- [Campaign](campaign-47bdb9a4-2026-09-24.md): 25 cuts through both entries
  twice, 50/50; differential 7/7; probe 39/40 (the miss is the cleanup log
  line that landed after the cut); production external-kill campaign, 90
  SIGKILLs over 206 POSTs, 0 torn, 0 lost 240, 0 reused numbers.
- [Native modules](native-subsets-47bdb9a4-2026-09-24.md): every module on
  the four images; bp_node 15/16, peering 5/5 with a Path identity,
  app_journal 10/10, operator_verbs 18/18, hybrid_author 5/5.

## The probe from a Mac on the LAN

`tools/node_probe.py 192.168.50.39 1119 --cafile <the node's cert> --group
fn.agents` as `ember` ([probe.json](node-hbox-47bdb9a4-2026-09-24/probe.json),
SHA-256 `e193daf5e597f72b…`): every assertion held on both connections: 483 before TLS,
TLS 1.3 (`TLS_AES_256_GCM_SHA384`) against the pinned certificate, STARTTLS
withdrawn and AUTHINFO offered after it, 281, POST offered only after the
login, `211 6 1 6 fn.agents` (the six articles posted on the earlier images
served), 240, the article read back on a fresh connection. Exit 0.

## Which of P1 to P11 hold on it

The [scoreboard](../v0-scoreboard.md) is the row-by-row statement. On this
image: P1 (protected channel) observed by the probe and the matrix rows and
proved over `fn-auth-step-pinned`; P2 crash half at all 25 cuts and 90
production kills, wire half on the probe's 39 rows; P3 pinned view and never-
reused numbers proved, matrix rows observed; P4 duplicate-vs-conflict decided
in ACL2 and observed; P5 fault theorems with teeth, the shared-owner fault
stops the service by design (ember's wording pending); P6 live
reconfiguration over the called path, observed; P7 two-node exchange both
ways with Path identities, loop refused, exactly one copy after a kill; P8
the verdict recorded and served by `HDR :fn-verified`, verification only
through fn's binary; P9 refusal at the budget observed (the 129th POST) and
proved; P10 K0 at 18 of 25 cuts, campaign and kills on this image; P11
served step bridged to the machine, N08 retry policy pending ember, the DTN
profile not deployed on this node.

## What this record does not claim

No durability past process death on this platform (D14, T16); no signature
security; no peers configured on the deployed node; no DTN on it. The node
is reachable on the LAN only. The kind-8 retry policy, the swallowed cleanup
log line and the BP application receive fixes postdate this image.
