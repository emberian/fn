# Store identity-sequence proof on the topic/index union

The frozen `8c61c098` hbox gate failed at `FN-SN-REFUSE-RESERVATION-PRESERVES-IDENTITY-NEXT`: its old hint expanded the new 14-field Store constructor before the `identity-next` selector could rewrite. The identity book now has a local v6 selector fact and keeps that constructor closed in its proof world. The later enabled-completion proof also keeps the topic recognizer closed, allowing the certified topic/identity disjointness facts to eliminate an impossible topic-and-STXE branch. No executable Store transition, event codec, identity allocation, or consumer cursor behavior changed.

Selected hbox run `run-20260924T034328Z-3c29`, original manifest `certify-20260924T034331Z-811799.json`, passed `books/store-identity-sequence-invariants` and `tests/acl2/store-identity-sequence-invariants-tests` at the changed bytes with ACL2 w28 and the shared certificate cache. `make check` passed after regenerating the ledger. `green_check --changed-since 8c61c098` reports the changed book and test green and seven dependent roots stale; those dependents require root's next frozen combined run. This is a proof-source repair only, not an independent whole-tree qualification or native image result.
