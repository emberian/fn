# Derived consumer index foundation

`fn-cei-build` derives a four-octet radix index from the authoritative committed Store event list. `fn-cei-get-of-build-is-committed-event` and `fn-col-poll-index-window-is-committed-prefix` prove that indexed lookup and a bounded window return exactly the events at the corresponding dense journal sequences, under the explicit correspondence and uint32 history bounds. The tests include a three-event bootstrap/register/ack witness at nonzero position and a stale-index counterexample. Each indexed lookup traverses four octet levels; a builder-produced branch has at most 256 octet keys plus one terminal key. Build scans retained history once. These functions are not yet called by the served owner poll, so the current poll still walks the acknowledged prefix.

The exact root-based source `e657b1cf` plus this four-file packet passed scoped ACL2 certification on hbox: run `run-20260924T004750Z-8578`, original manifest `certify-20260924T004752Z-635105.json`, with three roots reused at identical bytes and the fourth certified. No native image or whole Store closure was built for this packet. Store slot 13 maintenance, reachable all-transition correspondence, and the actual owner poll call are separate pending joins on the coherent topic branch.
