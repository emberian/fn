# Current-source preservation and clone packet

The preservation packet was ported from its finite E2 branch onto coherent integrated Store commit `632653a86e21b30efbd1dca54d9e78d9828b7640`. That base already has the E2 durable publisher (`fnn-owner-consumer-commit`) and the T10 exact-source accepted-event/verdict projection. The port keeps both the newer local consumer control functions and the checkpoint clone-phase/bootstrap helpers in `host/owner-host.lisp`; generated ledger conflicts were resolved by regeneration. Its six commits are `e0e98e41`, `6a822ae1`, `3b4c0e00`, `1215d96a`, `8638d087`, and `90e3f2a7`.

`fn-cpa-store-auxiliary-agrees` compares the recovered live consumer projection, dense identity sequence, historical verdicts and enrolled keyring snapshots against exact completed Store history before a selected checkpoint can report differential equality. The node summary alone is not treated as complete state. A cold writable clone copies exact selected-pack bytes while the source Store is locked, publishes a durable fence before the copied directory, and uses an ACL2-constructed consumer rollover event before serving. A refused or uncertain rollover keeps the destination fenced. Recovery after the publisher closes independently checks the event in the authoritative journal before removing the fence. The source-owned copy depth, entry and byte limits are enforced by the host while walking files; the host filesystem and fsync calls remain the physical trust boundary.

On persvati, `python3 tools/farm.py submit persvati --root /Users/ember/dev/fn/build/lanes/preservation-current --remote-root /home/ember/fn-lanes/preservation-current --jobs 2 books/checkpoint-auxiliary tests/acl2/checkpoint-auxiliary-tests` passed in [the archived manifest](manifests/certify-20260923T221444Z-853557.json): ACL2 8.7, toolchain identity `1b4169e9c5825a4e1fc827767f00470ceafc459619e0a4fd522c48f1ba964286`, proof-book SHA-256 `fd5a276e80ab701cbd74bc2bb61c9ccd00c617ab9c9eea1e9fb09a7965c12906`, teeth SHA-256 `dc4833c45a2273e7be11b0533dd700b4c407ec3505db6272a092a18f8f8ca713`. `make check` and `python3 -m py_compile tests/test_native_checkpoint.py` passed locally. Other root branches had current-digest proof obligations outside these scoped roots at that time.

The opt-in saved-image test `tests/test_native_checkpoint.py` exercises production consumer bootstrap, registration, position and zero-position ACK, then posts another accepted article, polls its exact Store report and ACKs the advanced cursor before selected-pack clone. It covers nonempty-destination refusal, process death at fence durability, publication, rollover durability and fence removal, then independent reopen/resume. It checks that the copied advanced cursor and registration are refused after rollover. This advancing-ACK portion requires the finite read-only poll caller (`57907b8e`) and remains prepared source, not runtime evidence. A second test constructs a real schema-1 Ed25519 plus ML-DSA65 article, rotates the enrolled key generation, clones the selected pack, reopens the target, independently verifies its portable carrier and queries its historical `HDR :fn-verified` value. These native tests are prepared but **not yet run on a source-matched combined developer image**; no runtime verdict follows from ACL2 certification or Python compilation alone. The production clone command mints 32 incarnation octets from OS entropy rather than accepting an operator-selected ID; ACL2 refuses history/current-ID equality. Distinct sibling clones rely on the OS CSPRNG's probabilistic uniqueness, not a global uniqueness proof.

The production-bootstrap and entropy revision ports `41fb2b86` and removes the duplicate checkpoint bootstrap fixture. The two scoped roots passed on persvati in [manifest `certify-20260923T223055Z-1028045.json`](manifests/certify-20260923T223055Z-1028045.json), run `run-20260923T223052Z-ea03`, ACL2 8.7 and toolchain identity `1b4169e9c5825a4e1fc827767f00470ceafc459619e0a4fd522c48f1ba964286`. It verified guards for the five actual cold recovery/clone helpers plus the last-record selector. Two earlier scoped attempts failed on the guard of `fn-cpa-clone-phase`; the final version uses `nfix` in its completed-branch sequence arithmetic and passed. Native caller guard execution remains governed by the saved-image build, which has not yet been run at these bytes.

Integrated-source review at `a8e4b17e` found two narrow contract gaps. Clone path arguments had no source-owned width before `truename`, and a short parent alias could expand past the native Store path policy; the follow-up adds an ACL2 512-octet absolute/NUL-free preflight, then checks canonical source/target, staging, fence, and joined copy paths before filesystem traversal. The opt-in native fixture adds oversized, relative, and canonical-parent-alias refusals, pending a source-matched image. The scoped path-policy book and test both passed on persvati in [manifest `certify-20260923T225103Z-1229338.json`](manifests/certify-20260923T225103Z-1229338.json), run `run-20260923T225059Z-2979`, ACL2 8.7 with the same qualified toolchain identity as above. The prior prose also overstated uncertainty after a durably confirmed rollover: if fence unlink succeeds but its directory barrier fails, the command reports uncertainty while ordinary opens may already be safe because independent reopen confirmed the new incarnation. This is now stated separately from pre-confirmation uncertainty, which leaves the destination fenced.
