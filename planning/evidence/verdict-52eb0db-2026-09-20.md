# Verdict: 52eb0db across every gate

One commit through the box gate (`make certify`, the Python suite,
`tools/gate_publish.sh`), then every harness this tree has, then this
table.  Produced by `tools/verdict.py`; the per-harness evidence files
cited below are the primary records and this one adds nothing to them.

## What ran

| fact | value |
| --- | --- |
| commit | `52eb0db` (52eb0db623ceab4a36371876e34f55681e3acc6a) |
| gate host | `persvati` |
| INN host | `hbox` |
| started | 2026-09-20T18:46:02Z |
| wall time | 2193 s (0.6 h) |
| gate directory | `$HOME/fn-gates/dev-52eb0db` |
| gate reused | no |
| certify manifest | `-` |
| manifest status | - |
| acl2 | - |
| platform | - |
| certify wall | - s at - jobs |
| certificates written | - |
| python suite | - of 0 tests, failures=0 errors=0 skipped=0 |

## Fibers

| fiber | host | tool | state | rc | steps | failed | not run | s | evidence |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| gate | `persvati` | `make certify + unittest + gate_publish.sh` | fail | 124 | - | 0 | 0 | 394 | - |
| deploy | `persvati` | `tools/deploy_gate.py` | fail | 2 | 19 | 0 | 1 | 365 | `planning/evidence/deploy-52eb0db-2026-09-20.md` |
| twonode | `persvati` | `tools/twonode_gate.py` | fail | 2 | 33 | 0 | 0 | 649 | `planning/evidence/twonode-52eb0db-2026-09-20.md` |
| inn | `hbox` | `tools/inn_lab.py` | fail | 2 | 48 | 0 | 0 | 676 | `planning/evidence/inn-lab-52eb0db-2026-09-20.md` |
| scale | `persvati` | `tools/scale_gate.py` | fail | 2 | 4 | 1 | 0 | 56 | `planning/evidence/scale-52eb0db-2026-09-20.md` |

Per fiber, in its own words:

- **gate** (fail): launch failed: timed out after 300s
- **deploy** (fail): steps 18 ok, 0 failed, 1 not exercised
  - `/opt/homebrew/opt/python@3.14/bin/python3.14 tools/deploy_gate.py 52eb0db623ceab4a36371876e34f55681e3acc6a --host persvati --tree dev`
- **twonode** (fail): steps 33 ok, 0 failed, 0 not exercised
  - `/opt/homebrew/opt/python@3.14/bin/python3.14 tools/twonode_gate.py 52eb0db623ceab4a36371876e34f55681e3acc6a --host persvati --tree dev`
- **inn** (fail): steps 48 ok, 0 failed, 0 not exercised
  - `/opt/homebrew/opt/python@3.14/bin/python3.14 tools/inn_lab.py 52eb0db623ceab4a36371876e34f55681e3acc6a --host hbox --tree dev --jobs 10`
- **scale** (fail): steps 3 ok, 1 failed, 0 not exercised
  - FAILED rc=1 reuse the deployed tree: NO-TREE /home/ember/fn-deploy/52eb0db
  - `/opt/homebrew/opt/python@3.14/bin/python3.14 tools/scale_gate.py 52eb0db623ceab4a36371876e34f55681e3acc6a --host persvati --tree dev --reuse`

## Certify roots by owner

| owner | roots failing | which |
| --- | --- | --- |
| - | 0 | every requested root exited 0 |

Owners are the rows of `planning/deputies/CLUSTERS.md`, matched by
book-name prefix in `tools/verdict.py`; a root matching no row is
reported as `unassigned` rather than silently dropped.

## What this run does NOT establish

- A certificate records that ACL2 read a book and admitted its events.
  It is not a claim that the theorem in that book is the property its
  name suggests, nor that the host calls the function it is about.
- The harness rows are live runs against real sockets and real kill
  signals. None of them is an RFC conformance audit, none qualifies
  storage hardware, and a SIGKILL is not a power loss.
- Fibers marked `not run` establish nothing at all; they are in the
  table so their absence is visible in the claim.
- This file is generated. Its numbers come from the gate manifest and
  from each harness's own stdout summary, never from typing.

## The claim

On this commit 0 of 0 Makefile roots certified under ACL2 ? and the Python suite ran 0 tests (unknown); it claims nothing about the gate, deploy, twonode, inn, scale harnesses reported failing steps.

## Reading this run (added by the lane, after it finished)

This run is a **defect report about `tools/verdict.py`, not a verdict about
`52eb0db`**, and it is committed for that reason rather than discarded.

The gate fiber reports `rc=124 launch failed: timed out after 300s`. The gate
was not failing: `gate.sh` was already certifying in
`$HOME/fn-gates/dev-52eb0db` and went on running for another hour. The launch
is fire-and-forget, and the ssh that starts it did not close inside the 300 s
this run allowed. Because the gate fiber was recorded failed, the four
harnesses each ran against a gate directory whose certificates were still
being written, and each stopped early with `rc=2` -- their own evidence files
(`deploy-52eb0db`, `twonode-52eb0db`, `inn-lab-52eb0db`, `scale-52eb0db`,
all 2026-09-20) record where.

Fixed in `46a6035`: the launch gets 900 s, and a launch whose ssh does not
return is checked against the box -- if a `certify-*` run exists under the
gate, the gate is followed rather than failed, and the evidence says so.

**So the claim line of this run is the empty one, and it is correct.** Zero of
zero roots certified *in this run*, because this run never read a finished
manifest. It is not a statement that no root certifies on `52eb0db`. The next
run, `python3 tools/verdict.py 52eb0db --host persvati --reuse-gate`, takes the
finished gate this one abandoned and produces the number.
