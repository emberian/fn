# Bounded BP fragment-family replacement proposal

`books/bp-node-fragment-plan.lisp` consumes the certified C2 active set from the actual FNBS held list. On `(:ok bytes)` from the certified fast/reference-equal reassembler, it selects the offset-zero held row as the primary and extension-block source, replaces the payload block's data while retaining its type/number/flags/CRC choice, and returns `(:ready whole exact-wire consumed-ids zero-row)` only if the reconstructed bundle is valid and the post-replacement held count, image size and total wire-octet budget fit. Consumed identifiers contain admitted principal, RFC bundle identity and retained arrival. Missing, conflict, invalid and capacity remain distinct. This is a read-only proposal; it neither retires fragments nor authorizes Store dispatch.

`fn-bpnf-family-ready-has-valid-whole` proves a ready result contains a valid bounded whole bundle, has a successful C2 query and an offset-zero source. The test book uses C2's same-principal overlapping cover whose offset-zero row arrived later, plus cross-principal and incoherent rows, and exercises complete, conflict, missing, invalid and capacity outcomes. It checks exact eight-octet ADU and consumed row identities. This does not prove the pending kind-18 atomic replacement, arrival non-reuse, physical publication, or native caller. `fn-bpnf-family-whole-bundle` and `fn-bpnf-family-plan` have admitted definitions but unverified guards; no served call to them is claimed yet.

Focused ACL2 8.7/SBCL certification on hbox passed both book and test in `run-20260923T212635Z-4f52`, manifest `certify-20260923T212641Z-194496.json`. The earlier guard attempt was interrupted at its expensive `fn-bpnf-family-whole-bundle` guard goal and supplies no guard evidence.
