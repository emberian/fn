# Indexed owner poll correspondence: reduced premises

The host-called `fn-col-poll` in `books/consumer-owner-local.lisp` selects a bounded window from the derived Store event index and feeds the exact events to the existing ACL2 scanner. The proof-only list reference now uses the same `fn-col-scope-entry` as the caller. `fn-col-poll-index-window-is-committed-prefix` no longer requires the consumer frontier to end inside the retained list: total lookup returns `nil` on either side past the list end. The new narrow non-uint, past-end and `nth` lemmas keep the radix codec closed in the main induction.

`fn-col-poll-agrees-with-committed-list-under-index-relation` now requires the carried index relation, a non-replaying/non-fault phase, and `fn-sf-statep` for the file state. It no longer assumes a numeric frontier, `frontier <= len(records)`, `true-listp(records)` or an independent `len(records) <= 2^32` bound. A successful shared scope lookup itself implies a numeric frontier; both callers return the same refusal otherwise. A new inductive record-count theorem derives `len(records) <= Store frontier` from the Store's ordered txids, and the file-state recognizer supplies a uint32 frontier and true-list records. No served command runs these full-state proof predicates.

The positive test asserts the entire antecedent on a committed bootstrap/register/article owner and compares the actual poll bytes/report with the list reference. A stale derived index changes the actual answer while all other premises hold. In `:fault`, the index relation is deliberately vacuous; a stale index again changes the answer while file state remains valid. Those two negative witnesses passed. A separate must-fail witness for the file-state premise is still open: its relevant failure would require an out-of-profile history beyond the uint32 sequence domain, and this packet does not allocate such a history or count the theorem as fully teethed. The file-state premise is maintained by the Store model and is not checked anew on poll.

ACL2 w28 on hbox with the shared cache passed lower book/test in run `run-20260924T034838Z-48b0`, manifest `certify-20260924T034844Z-819397.json`, and outer equality book/test plus actual owner book/test in run `run-20260924T034957Z-0921`, manifest `certify-20260924T035001Z-823734.json`. Earlier bounded attempts found a radix unfolding timeout and then an explicit past-end `nth` goal; they are diagnostic attempts, not passing evidence. This source has no source-matched native indexed-poll cost measurement yet. The next measurement uses native_qualification's single combined image after its frozen source gate passes; this lane does not build another image.
