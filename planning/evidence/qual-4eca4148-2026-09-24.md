# Qualification of image 4eca4148, 2026-09-24

One lane, `qual-4eca4148`, ran the qualification of the image of source
`4eca4148c59df75af916aa9c4be51647e7dc21b6` on hbox between 22:59Z and 23:18Z:
the v0 matrix (twice, see below), the cut campaign with its checkpoint cuts,
the ACL2 differential, the NNTP probe, the production kill run, every
image-gated native module, and the web reader by hand. It is a runtime record.
It certifies nothing and proves no theorem. The previous qualification is
[18c91321](qual-18c91321-2026-09-24.md).

**Deploy verdict: deployable.** No row, cut, checkpoint cut, probe row, kill
or native case failed for an fn reason. What failed is either a test that
predates D25 (c), the production DTN image refusing developer selectors (c,
as before), or the environment (b): the INN lab's innfeed race in the first
matrix run, and no ML-DSA library for `tools/fn_verify.py` on hbox. With
`dilithium-py` on `PYTHONPATH`, the D23 cases pass with the verifier.
- matrix run 2 (published): 142/44/2/31/1, 0 disagreed, 0 faulted, INN 33/33;
  no row changed against 18c91321. Run 1: 1 disagreed (`V0-INN-INTEROP`,
  innfeed never reached fn; (b)).
- D25: both duplicate-POST rows answer `this article is already stored here`
  (at 23:00:41 and 23:00:46 in run 1, and again in run 2).
- cut campaign 50/50; checkpoint cuts 20/20 (10 cuts through both entries);
  differential 7/7; probe 40/40; kill run 0 torn, 0 lost-240, 0 reused.
- bp_node 19/19 (N07 and N11 included), bp_app 5/5, hybrid_author 8/8 with
  the verifier (sink exit 0, relay exit 3), reader_index 5/5,
  profile_upgrade 5/5, the production compaction test passes, web 4/4.

## Image

The images were built and frozen before this lane under
`/tank/fn/gates/qual-4eca4148-20260924/build/images/4eca4148/`
([`image-build.log.gz`](qual-4eca4148/image-build.log.gz)). The closure
manifest is [certify-20260924T224341Z-2081666](manifests/certify-20260924T224341Z-2081666.json)
(464 certified, 0 failed).
- `sha256sum -c image.sha256` passed 58 of 58 before any run
  ([`image-check.log`](qual-4eca4148/image-check.log)), and again on the
  relocated copy before and after the relocation run.
- The gate tree equals `git archive 4eca4148` over `host/ tests/ tools/
  books/ packaging/ Makefile`: 1407 files, 0 content differences. The only
  extra files in the gate are build products (`.cert`, `.fasl`, `.port`, ...).
- dev (`be2b815b`, the lane's base) and `4eca4148` do not differ in those
  paths, so the matrix ran from the lane worktree against the same tools.

| artifact (`…/build/images/4eca4148/`) | SHA-256 | vs 18c91321 |
| --- | --- | --- |
| `fn-host` | `432622d29a28d59455e01f3e5b426036c5862db21d5f7d1205a9304ab11e3505` | same launcher |
| `fn-host.core` | `3411e52b435ae8c31e68a3d64ab84687a04071885b9840ee2c46a989403375ba` | new core |
| `fn-host-developer` | `e4eeefd290450e7497dade96e2c4089e0caacfa88f72c737f51a92b2f3b2ba18` | same launcher |
| `fn-host-developer.core` | `b94f959e610c1b96559c32467b05400290d13873d813cd2b4fec9c6d14b04445` | new core |
| `fn-host-dtn` | `095382bae2b3ff377a2f27b119c0d5f5b247c983f4846d7176fbcd661e1e282c` | same launcher |
| `fn-host-dtn.core` | `e2786d1cb8059acce3424d6b746694aed47e08e3a7741f03b5d92564e72ea25d` | new core |
| `fn-host-dtn-developer` | `4986f14bf4ebc34fccf54f4bfea0e7007222f96c477a9f0519faad6c59c19c88` | same launcher |
| `fn-host-dtn-developer.core` | `2dcdffa47140934423f2c4a036af74f059eef7ddacffbfb6c2c945f39f06dd30` | new core |
| `runtime/sbcl` (SBCL 2.6.8) | `b115fe956aadee603459fac401e1ff2cc39321e14848544a0fe438788a2fc6d5` | same |
| `openssl/lib/libcrypto.so.3` / `libssl.so.3` | `14d40ec6…` / `bb2d12be…` | same |
| ACL2 `acl2-literal-4g` (w28) | `9f73da2a84d664516033fb6e944c55b55d1f7206e9599cac46ca2b208de26aa8` | same |

The full list is [`image-full.sha256`](qual-4eca4148/image-full.sha256).

## The matrix

`tools/v0_matrix.py --backend native-operator` ran from the laptop worktree
at `be2b815b`, with the exact text in
[`matrix-invocation.txt`](qual-4eca4148/matrix-invocation.txt) (the
18c91321 invocation with the paths and source changed). The stores were made
as `hbox-matrix-provision.sh` makes them, fresh for each run, under
`/tank/fn/scratch/qual-4eca4148/matrix/stores{-run1,}`, on 127.0.0.1:11190 and
11191.

| run | accepted | refused | uncertain | not exercised | not built | disagreed | faulted | independent | INN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 18c91321, `20260924T212040Z` | 142 | 44 | 2 | 31 | 1 | 0 | 0 | 12 | 33/33 held |
| 4eca4148 run 1, `20260924T225948Z`, 265 s, rc 1 | 138 | 44 | 2 | 35 | 1 | **1** | 0 | 8 | violated: 27 held, 1 violated, 5 not exercised |
| **4eca4148 run 2, `20260924T231407Z`, 197 s, rc 0 (published)** | **142** | **44** | **2** | **31** | **1** | **0** | **0** | **12** | **33/33 held** |

**Run 2 against 18c91321: no row changed its verdict, its agreement or its
independence** (220 rows compared by id). It is published with
`--publish-current` as `planning/v0-matrix.json` (`rows_digest`
`38dd3741…`). Run 1's five changed rows are all INN rows and all follow from
one lab assertion (failure 1 below): `V0-INN-INTEROP` disagreed, and
`V0-INN-FEED-IN`, `-DUPLICATE-FN`, `-RESTART` and `-SERVING-AGENT` were not
exercised. Run 1 was repeated once, unchanged, to tell a race from a
regression; both runs' outputs are kept
([run 1](v0-runs/20260924T225948.416733Z-be2b815-455e5a9d50c3/),
[run 2](v0-runs/20260924T231407.797120Z-be2b815-28e5aef76a94/)). No other
matrix row differed between the two runs.

**D25 on the rows.**
- `V0-POST-DUPLICATE-A` and `-B` both read `441 posting failed; this
  article is already stored here`, in both runs. On 18c91321, -B read "a
  different article" by the clock second. Run 1's DATE after the duplicate
  was `111 20260924230041` and `…230046`; the answer no longer depends on it.
- **Transit duplicates with a different Path (D25 finding 2).** The rows
  answer as duplicates: `V0-TRANSIT-DUPLICATE-AB/BA` `435 duplicate`,
  `CHECK-DUP` `438`, `TAKETHIS-DUP` `439`. What the rows can and cannot say:
  - The second `IHAVE` gets 435 at the command, before any octet is sent.
    So it does not reach the byte comparison.
  - `TAKETHIS` does send octets: A's served copy, whose Path
    (`a.gate.example.invalid!not-for-mail`) differs from B's stored copy
    (`b.gate.example.invalid!!a.gate.example.invalid!not-for-mail`, the
    `V0-TRANSIT-REREAD` row). B logs
    `refused transit … message-id=<alpha@a.example.invalid> code=439 decision=have reason=history detail=none`.
    That line says duplicate, not conflict. It does not show which test
    decided it.
- No row and no owner log line answered 436.

Logs: [`matrix-run1-driver.log`](qual-4eca4148/matrix-run1-driver.log),
[`matrix-run2-driver.log`](qual-4eca4148/matrix-run2-driver.log), and
[`matrix-node-logs.tgz`](qual-4eca4148/matrix-node-logs.tgz) (run 2's node
logs and `fn.toml`s, 18 files).

## The cut campaign, the checkpoint cuts, the differential, the probe and the kill run

The runner is [`campaign.sh`](qual-4eca4148/campaign.sh): the 18c91321
runner with the paths changed. The tree is a copy of the gate
(`/tank/fn/scratch/qual-4eca4148/ctree`). `FN_NATIVE_SOURCE_ROOT` is set to
the gate, with the OpenSSL 3.5.8 pair and `PYTHONDONTWRITEBYTECODE=1`. The run
lasted from 23:00:28Z to 23:17:54Z. `native_operator_campaign` without
`--checkpoint-only` runs the 10 `CHECKPOINT_CUTS` first, then the 25 cuts and
the faults, in one run (2:23, peak RSS 326 MB). Judging used
[`judge.py`](qual-4eca4148/judge.py) and [`faults.py`](qual-4eca4148/faults.py),
unchanged from 18c91321, and the new
[`checkpoint_judge.py`](qual-4eca4148/checkpoint_judge.py), which only
tabulates the campaign's own `pass`.

### Per cut

**50 of 50 observations pass** ([`judged.md`](qual-4eca4148/judged.md)).
The table is 18c91321's cell for cell in the table, served, txns/staging,
recover and candidate columns:
- the 7 frontier and 3 early record cuts are absent;
- `record-linked` ("either") is present in both columns;
- from `record-attempted` on, the candidate is present;
- the 7 recovery cuts are 1 1 0 and absent.

**The resubmit column changed with D25.**
- Every present cut's resubmit through the operator entry is `accepted
  operator post DUPLICATE rc 0`.
- Through the store entry it is `duplicate rc 0`.
- Absent cuts commit, as before.

### Checkpoint cuts through both entries (developer image)

**20 of 20 pass** ([`checkpoint-judged.md`](qual-4eca4148/checkpoint-judged.md)).
The compact-verb record ran the same set
([m5-compact-verb](m5-compact-verb-2026-09-24.md), 20 of 20 on its own image).

For each cut and entry, the run did the following:
- stopped the entry at the cut and killed it;
- ran `recover`, which exited 0;
- reread both articles byte-identical;
- resumed through the same entry;
- checked that a further `store compact` was refused `already-compact` (1);
- reread both articles again;
- posted a third article, which went to GROUP high+1 (2 → 3).

| cut | developer: step killed / resume rcs | operator: resume rc | both entries |
| --- | --- | --- | --- |
| `candidate-file`, `candidate-link`, `candidate-directory` | pack (0) / 0,0,0 | 0 | PASS ×6 |
| `selection-file`, `selection-replace`, `selection-directory` | pack (0) / 0,0,0 | 0 | PASS ×6 |
| `pack-reclaim-unlink`, `pack-reclaim-directory` | reclaim (1) / 0,0 | 0 | PASS ×4 |
| `pack-retire-unlink`, `pack-retire-directory` | retire (2) / 0 | 1 (`already-compact`: the kill landed after the retire's effect) | PASS ×4 |

### Faults and production refusals

The details are in [`faults.txt`](qual-4eca4148/faults.txt).
- The developer EIO, control-fault and five test-stop rows give client 3 and
  recover 0, as before. Their resubmits are now `duplicate`/`DUPLICATE` rc 0.
- The 65-death allocation-orphan rows pass.
- **Production refuses 30 of 30 selector starts with exit 5** (10 selectors ×
  {`operator run`, `operator recover`, `store post`}). The store is
  unchanged. Raw `store post` on production exits 5, and so does
  `FN_NATIVE_INIT_FAULT` at `operator init`.
- **Cross-entry retry changed with D25.** One payload through `store post`,
  then `operator post` (and the reverse order), now gives 0, then 0
  (`accepted operator post DUPLICATE` / `duplicate`). The transaction count is
  unchanged (2 → 2). On 18c91321 the second post gave 1 (conflict). The
  image-gated `tests.campaign.test_native_operator_campaign` therefore failed
  9 of 10 (failure 3, (c)).

### The ACL2 differential

`FN_NATIVE_CRASH_HOST=<image>/fn-host-developer python3 -m unittest -v
tests.test_native_crash_model` passed **7 of 7** in 72 s (peak RSS 469 MB)
on `acl2-literal-4g` ([`crash-model.log`](qual-4eca4148/crash-model.log)).

### Per cut at the NNTP wire (probe, once, 0:52)

**40 of 40 pass**, and `judgement.failed` is 0. The table is in
[`judged.md`](qual-4eca4148/judged.md) below the cut table. The judge now
requires the duplicate line for a present repost, and it gets it.

| rows | reply | owner exit | candidate | repost | verdict |
| --- | --- | --- | --- | --- | --- |
| all 18 post cuts, kill | closed, zero octets | −9 | absent before `record-linked`, present from it on | `240` absent / `441 … already stored here` present | pass ×18 |
| 6 early cuts, eio | `441 … could not write the article, nothing was stored` | 0 | absent | 240 | pass ×6 |
| `frontier-replaced`, `-attempted`, `-durable`, `-reserved`, eio | `441 … the outcome is uncertain, do not repost` | 3 | absent | 240 | pass ×4 |
| `record-linked` to `finish-durable` except `record-stage-unlinked`, eio | the uncertain line | 3 | present, identical | `441 … already stored here` | pass ×7 |
| `record-stage-unlinked`, eio | `240 article received OK` | 0 | present, identical | `441 … already stored here` | pass |
| controls: dev 240 then SIGKILL; dev no `From`; prod 240 then SIGKILL; prod SIGKILL mid-article | 240 / `441 … From is required` / 240 / none | −9 / 0 / −9 / −9 | present / absent / present / absent | already stored / From is required / already stored / − | pass ×4 |

**The repost of a present article is `this article is already stored here`
at all 18 present rows.** On 18c91321 it was that line at 6 and "a different
article" at 10, depending on the `Injection-Date` second. That was D25's
target, and it is met on the wire.

### The production external-kill run (seed 3, 12:32, peak RSS 1.41 GB)

`native_production_kill run --image <image>/fn-host --seed 3 --kills 80
--concurrent 10` gave 90 iterations (80 single, 10 concurrent), 100 killed
POSTs and 206 POSTs in the ledger
([`pk-judged.md`](qual-4eca4148/pk-judged.md)).
- Two fresh stores closed at 116 and 62 articles, numbered 1..116 and 1..62
  with no gap, under the 128 budget.
- 92 owner pids ran; all were gone at the end.
- **The judge lists 0 failures** and exits 0.

| every POST the judge saw (206) | 18c91321 | 4eca4148 |
| --- | --- | --- |
| 240-and-identical | 110 | 98 |
| died-absent | 56 | 68 |
| died-present-identical | 17 | 17 |
| refused (441) | 23 | 23 |
| **torn / lost-240 / reused-number** | 0 / 0 / 0 | **0 / 0 / 0** |

| killed POSTs | 240-identical | died-absent | died-present-identical | refused | total |
| --- | --- | --- | --- | --- | --- |
| single | 9 | 54 | 16 | 1 (over-limit) | 80 |
| concurrent | 5 | 14 | 1 | 0 | 20 |

- **Client exit codes.** rc 0 came only with `240`. rc 1 came only with
  `441 … not received` or `441 … already stored here`. rc 3 came only with no
  reply.
- **K1 is gone.** The 85 resubmissions after a death were answered `240` (63),
  `441 … already stored here` (17) and `441 … not received` (5, over-limit).
  None was told "a different article". On 18c91321, 15 of 73 were.
- Every killed POST that got a `240` (14) was linked. No kill before the link
  found a 240.

## Native modules on the four images

The runner is [`run.sh`](qual-4eca4148/run.sh), one module at a time, each
as `timeout 3600 python3 -m unittest -v tests.<module>`. It is the 18c91321
runner with these changes:
- the paths and `FN_NATIVE_IMAGE_SOURCE_SHA=4eca4148`;
- `FN_D23_VERIFY=1`;
- `FN_TEST_OPENSSL` = the OpenSSL 3.5.8 wrapper;
- `FN_RUN_VERIFY_E2E=1` added to the opt-in flags.

The rest is as before:
- the image's `sbcl` is first on `PATH`, with `SBCL_HOME` set;
- every `FN_RUN_*` flag is set;
- the DTN variables are `FN_NATIVE_DTN_HOST` and
  `FN_NATIVE_DTN_DEVELOPER_HOST`; `FN_NATIVE_BP_HOST` is `fn-host-dtn`, or
  `-dtn-developer` for the selector modules; `FN_NATIVE_BP_NODE_HOST` is
  `fn-host-dtn-developer`, or `fn-host-developer` for the second N08 run;
  `FN_NATIVE_CONTACT_SENDER` and `_RECEIVER` are set.

[`chainA.sh`](qual-4eca4148/chainA.sh) (BP/DTN) and
[`chainB.sh`](qual-4eca4148/chainB.sh) (the rest, then
[`reloc.sh`](qual-4eca4148/reloc.sh), a `cp -a` copy with stdin closed) ran
side by side with the campaign and matrix run 1, from 23:00Z to 23:17:19Z.
Every port was ephemeral. The dtn7 labs were skipped (another lane's).

**Rerun with an ML-DSA library.** `test_native_hybrid_author` and
`test_fn_verify` were run a second time with
`PYTHONPATH=/tank/fn/scratch/qual-4eca4148/pylib`. That directory holds
`dilithium_py-1.4.0-py3-none-any.whl` (SHA-256 `dda3ae43…c33ff8`, pure
Python, no dependencies), unpacked. hbox's Python has `cryptography` 42.0.5,
which has no ML-DSA, and no pip. Nothing in the tree was changed.

| module | tests | pass | fail | skip | s | log SHA-256 (16) | vs 18c91321 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| test_native_auth | 4 | 4 | 0 | 0 | 6.4 | `a279493e4bbe44a7` | same |
| test_native_profile | 3 | 3 | 0 | 0 | 0.8 | `37307fda32b3299b` | same |
| test_native_admin | 9 | 9 | 0 | 0 | 3.3 | `c92996fa719675eb` | same |
| test_native_starttls | 2 | 2 | 0 | 0 | 0.6 | `ae9ebdae13aa8904` | same |
| test_native_auth_admin_fidelity | 3 | 3 | 0 | 0 | 12.4 | `54dcb995de0b07f8` | same |
| test_native_owner | 18 | 18 | 0 | 0 | 23.5 | `2536fbcde062b38e` | same |
| test_native_raw_scripts | 9 | 9 | 0 | 0 | 0.4 | `25e1820c5030ff66` | same |
| test_native_live_reconfiguration | 11 | 11 | 0 | 0 | 1.5 | `b4726eb4c84f5bf4` | same |
| test_native_control | 14 | 14 | 0 | 0 | 20.9 | `5ded685c07e97090` | same |
| test_native_operator_cli | 6 | 6 | 0 | 0 | 4.0 | `558ac067c7bf90f7` | same |
| test_native_operator_verbs | 18 | 17 | **1** | 0 | 3.5 | `42e9cce883bcf536` | **was 18/18; failure 2, (c) D25** |
| test_native_profile_upgrade | 5 | 5 | 0 | 0 | 9.4 | `15f0ab54d60f5282` | same |
| test_native_program_check | 9 | 9 | 0 | 0 | 0.3 | `40de9db86e155781` | same |
| test_native_initializer_fidelity | 11 | 11 | 0 | 0 | 1.5 | `ce235f9b46521ce3` | same |
| test_native_recovery | 13 | 13 | 0 | 0 | 13.5 | `58e66a83e0061492` | same |
| test_native_storage_codec | 11 | 11 | 0 | 0 | 66.7 | `1ce705ff5f726b60` | same |
| test_native_served_differential | 7 | 7 | 0 | 0 | 1.0 | `6619c4af1b0e5e54` | same |
| test_native_reader_index | 5 | **5** | 0 | 0 | 5.8 | `b13db698adebc804` | **+1: `test_list_counts_and_numbered_msgid_lookup` passes** |
| test_native_newnews_migration | 1 | 0 | 0 | 1 | 0.0 | `df51ee4d81ae0ee9` | same (no pre-T2 image) |
| test_native_stamp_migration | 1 | 0 | 0 | 1 | 0.0 | `9d812b8167fd9aca` | same |
| test_native_hybrid_author (no ML-DSA lib) | 8 | 7 | 1 | 0 | 17.8 | `5bd9ba67546fb629` | failure 4, (b) |
| **test_native_hybrid_author (`dilithium-py`)** | 8 | **8** | 0 | 0 | 20.0 | `3bf651510ee7c675` | **+2 D23 cases: relay `0 carried`, sink `0 verified … keyring 1`, `fn_verify` sink 0 / relay 3 ("carried this article"); unlisted relay 439 logged `detail=local-enrollment`** |
| test_fn_verify (no ML-DSA lib) | 17 | 4 | 0 | 13 | 0.2 | `59deb07ba5499ef1` | new module; 13 skip `pip install cryptography dilithium-py` |
| **test_fn_verify (`dilithium-py`)** | 17 | **17** | 0 | 0 | 4.7 | `363a1de2ebe4caa9` | new module, passes |
| test_native_topic_local | 4 | 2 | 0 | 2 | 2.7 | `acb99de81a6c367a` | same (no pre-v2 image) |
| test_native_topic_metadata | 2 | 2 | 0 | 0 | 1.2 | `821e0eef6106a04c` | same |
| test_native_consumer_inspect | 4 | 4 | 0 | 0 | 0.5 | `deda4f55dd8d1caf` | same |
| test_native_consumer_project_bounds | 2 | 2 | 0 | 0 | 0.2 | `6be86017cceee7c5` | same |
| test_native_consumer_e2 | 4 | 2 | 1 | 1 | 3.4 | `5a6287768cf601c2` | same (failure 6, (c)) |
| consumer_e2 signed poll, dated source | 1 | 1 | 0 | 0 | 11.9 | `7c296885bc3a03ae` | same |
| test_native_checkpoint | 26 | **26** | 0 | 0 | 59.4 | `6434e46ae4cc92c8` | **+3 incl. `NativeProductionCompactTests.test_operator_compact_keeps_every_article_and_next_number` on production** |
| test_native_compaction_crash_map | 4 | 4 | 0 | 0 | 0.0 | `1e5b7d8925c3d53b` | not run on 18c91321 |
| test_native_cut_map | 3 | 3 | 0 | 0 | 0.0 | `1cc34d7dfc2bf409` | not run on 18c91321 |
| test_checkpoint | 7 | 7 | 0 | 0 | 558.7 | `473cf9576fdbcbd0` | same |
| test_native_peering | 5 | 5 | 0 | 0 | 7.3 | `13f4e115776de278` | same |
| test_native_protected_peering | 5 | 5 | 0 | 0 | 26.7 | `791d87dda015c7a5` | same |
| test_native_crash_model (in the campaign) | 7 | 7 | 0 | 0 | 72.0 | `crash-model.log` | same |
| test_native_served_crash_model | 3 (18 cut subtests) | 3 | 0 | 0 | 132.0 | `6296d0e09a3ad9b4` | same |
| test_native_tls_transport | 2 | 2 | 0 | 0 | 0.8 | `bb388a719cc0cfcf` | same |
| test_native_frozen_relocation (relocated copy, stdin closed) | 2 | 2 | 0 | 0 | – | `6a493246a99e6a51` | same; `image.sha256` OK before and after |
| test_fn_web_native | 4 | 4 | 0 | 0 | 7.3 | `58f4ac6da31c1205` | same |
| test_bp_node_native (`fn-host-dtn-developer`) | 19 | **19** | 0 | 0 | 433.4 | `4fec3561743be1ad` | **+2: N07 (durable boot domain names another boot) and N11 (a second reception naming a held identity) pass** |
| N08 kind-8 case (`fn-host-developer`) | 1 | 1 | 0 | 0 | 35.0 | `081568fc2088307c` | same |
| test_bp_app_native | 5 | 5 | 0 | 0 | 76.1 | `68c0076fb471e71a` | same |
| test_bp_obligation_native | 4 | 4 | 0 | 0 | 1.1 | `e5912d943cf69e4b` | same |
| test_bp_fragment_node_native | 1 | 1 | 0 | 0 | 31.1 | `b7ebcc99a34690c2` | same |
| test_bp_service_native (`fn-host-dtn`) | 16 | 9 | 7 | 0 | 4.3 | `2c60c26bce51d28f` | same (failure 5, (c)) |
| test_bp_service_native (`fn-host-dtn-developer`) | 16 | 16 | 0 | 0 | 14.5 | `4239c9974297ad00` | same |
| test_bp_contact_native | 2 | 2 | 0 | 0 | 0.5 | `42d1d05616f1f986` | same |
| test_bp_contact_relay_native | 1 | 1 | 0 | 0 | 2.3 | `554ee8fa7687e477` | same |
| test_bp_receive_integrity_native (`fn-host-dtn`) | 4 | 2 | 2 | 0 | 0.6 | `069c11967955e81f` | same (failure 5, (c)) |
| test_bp_receive_integrity_native (`fn-host-dtn-developer`) | 4 | 4 | 0 | 0 | 1.0 | `22104749b30ae5ee` | same |
| test_native_app_journal | 10 | 10 | 0 | 0 | 8.8 | `e5ad9af7b7589b7b` | same |
| test_native_image_profiles | 12 | 12 | 0 | 0 | 1.5 | `e3cadb3897200922` | same |
| campaign `test_native_operator_campaign` (image-gated) | 10 | 9 | 1 | 0 | 26.2 | `image-gated-test.log` | **was 10/10; failure 3, (c) D25** |

**Diff against 18c91321.**
- **Newly pass / new cases:** hybrid_author +2 (D23 with the verifier),
  reader_index +1 (LIST COUNTS and the numbered lookup), checkpoint +3 (the
  production compaction test), bp_node +2 (N07, N11), test_fn_verify 17/17
  (new), compaction_crash_map 4/4 and cut_map 3/3 (first run here).
- **Newly fail:** operator_verbs 1 and the campaign module 1. Both assert the
  pre-D25 conflict answer for a resend of the same poster bytes (class (c)).
- **Unchanged failures:** bp_service 7 and receive_integrity 2 on the
  production DTN image, and consumer_e2 with an undated source.

## The web reader by hand (production image, TLS, login required)

The script is [`web-manual.py`](qual-4eca4148/web-manual.py). Its log and every
page are in [`web-manual.tgz`](qual-4eca4148/web-manual.tgz). The steps:
1. A scratch store `fn.agents` on `fn-host`, with `fn.toml` carrying
   `tls_cert`/`tls_key` (a fresh self-signed cert for 127.0.0.1) and `[auth]
   required = true, protected_only = true`.
2. `bin/fn principal set-password reader --posting` (rc 0; it needs
   `FN_ACL2` to derive the verifier).
3. `fn-host --fn operator fn.toml run` (LISTENING 45831).
4. `python3 tools/fn_web.py --node 127.0.0.1:45831 --tls-cert cert.pem
   --credentials cred --port 47927`, a separate process, driven over HTTP with
   the page's own csrf and submission tokens.

| page | what it showed |
| --- | --- |
| `/` before any post | `fn.agents` "nothing unread", "No local articles yet · 0 articles · posting allowed"; header "TLSv1.3, certificate verified" |
| `POST /post` (root) → `/result` | 303, then "Post accepted … The node answered that it accepted this article … The node's answer: 240 article received OK", the Message-ID and the exact article sent |
| reply (`/compose?reply=1`) and a second root | the same 240 page each; the reply carries `References:` to the root |
| `/` after three posts | `fn.agents` "**3 unread**", "Local article numbers 1–3 · 3 articles". The node's own `LIST COUNTS` over STARTTLS and AUTHINFO: `fn.agents 3 1 3 y` |
| `/g?name=fn.agents` (thread) | three rows: #1 "qual root" at indent 0, #2 "Re: qual root" at indent 18 px (under #1), #3 "second thread" at indent 0; each badged `absent` (unsigned) |
| `/a?group=fn.agents&number=1` | "node verdict: **absent**", badge class `badge absent`, "Server report of historical verification verdict: absent: no-field", "No FN-Statement / no FN-Authorship carrier recorded", Path `fn.example.invalid!not-for-mail`, the body |
| `POST /mark` through 1, then `/` | "**2 unread**", and a resume link at local #1 |

The unread counts are the node's numbers minus this client's local marks.
They matched `LIST COUNTS` exactly. The owner exited 0 on SIGTERM, and
`fn_web` was terminated (−15). Both were PIDs this script started.

## Failures, classified

Classes: (a) fn defect, (b) environment, (c) test out of date or run on the
wrong image.

1. **(b) Matrix run 1, `V0-INN-INTEROP` disagreed: innfeed never offered to
   fn within the lab's 92 s.** The syslog shows the order of events:
   - innfeed started at 23:02:15Z and tried the relay at :17 (connection
     refused; the relay was not up yet).
   - At :22 it got "can't read banner": the relay was up, the owner not yet.
   - It went to sleep with the lab's article spooled to its backlog
     ("spooled 1, sleeping 1"). Once connected at :32, it offered nothing for
     89 s.
   - After the lab's innd restart, the new innfeed read the backlog and fn
     accepted the article: "offered 1 accepted 1 … unspooled 1" at 23:04:07Z.

   This is a start-order race in the lab (innfeed's reconnect backoff against
   the relay and owner start), not fn. Run 2, unchanged, held 33/33, with
   innfeed's CHECK/TAKETHIS answered 238/239. An owner start before the relay,
   or a backlog flush in `tools/inn_lab.py`, would remove the race; this lane
   did not change it.
2. **(c) `test_native_operator_verbs`
   `test_the_default_profile_budget_is_reported_and_refused_by_name`**
   (`tests/test_native_operator_verbs.py:510`). The test expects the
   same-bytes re-POST at the budget to be a conflict ("the owner injected
   headers into the stored copy"). Under D25 the key is the poster's bytes,
   so the image answers `this article is already stored here`. The
   compact-verb record reported the same failure.
3. **(c) `tests.campaign.test_native_operator_campaign`
   `test_served_owner_cuts_stop_and_production_refusals`**
   (`tests/campaign/test_native_operator_campaign.py:171`). The test asserts
   that "one payload through the two entries is a conflict either way round"
   (second rc 1). Under D25 both orders give `DUPLICATE`/`duplicate` rc 0
   with no new transaction.
4. **(b) `test_native_hybrid_author` D23 sink verify, `3 != 0`
   (`tests/test_native_hybrid_author.py:791`), without an ML-DSA library.**
   `tools/fn_verify.py` finds no ML-DSA-65 implementation on hbox and cannot
   decide. It passes 8/8 with `dilithium-py` on `PYTHONPATH`. The same cause
   gives `test_fn_verify` its 13 skips. hbox needs `dilithium-py` (or a
   `cryptography` with ML-DSA) for the verifier to run there.
5. **(c) `test_bp_service_native` 7 and `test_bp_receive_integrity_native` 2
   on `fn-host-dtn`.** These cases set developer selectors, which the
   production DTN image refuses by design. Both modules pass in full on
   `fn-host-dtn-developer`. Unchanged.
6. **(c) `test_native_consumer_e2` signed poll with the undated source**
   (`tests/test_native_consumer_e2.py:59`, `hybrid-author` exits 1). It
   passes with the dated source. Unchanged.
7. **(b) Skips.**
   - consumer_e2's different-uid case needs root.
   - The newnews and stamp migrations and 2 topic_local cases need a pre-T2
     or pre-v2 image.

No (a). No failure blocks the deploy. Tests 2 and 3 should be brought up to
D25 by the owner of those tests. This lane did not modify them.

## Logs and digests

Everything is in [`qual-4eca4148/`](qual-4eca4148/), and
[`SHA256SUMS`](qual-4eca4148/SHA256SUMS) covers every file:
- `native-logs.tgz` holds every module log, including the `.pylib` reruns;
- `owner-logs.tgz` holds the campaign, probe and kill-run owner `.err`
  files, 439 of them;
- `matrix-node-logs.tgz`, `web-manual.tgz`;
- `campaign.json.gz`, `nntp-probe.json.gz` and `pk-run.json.gz`, with the
  raw digests in [`raw.sha256`](qual-4eca4148/raw.sha256):
  `campaign.json` `3a259e53…`, `nntp-probe.json` `dbabdd3c…`,
  `pk-run.json` `9b87d99a…`.

On hbox, under `/tank/fn/scratch/qual-4eca4148/`, are the stores and work
trees: `campaign/{work,probe-work,pk-work}`, `matrix/stores{,-run1}`, `tree`,
`ctree`, `reloc`, `web`, `pylib`. The matrix deploy tree is
`~/fn-deploy/native-4eca4148-be2b815/`.

After the runs:
- no process under the scratch path was alive;
- 11190, 11191, 11290 to 11299, 11417 to 11420 and 11490 were free;
- `~/fn-deploy/.locks/` was empty.

The live node `/tank/fn/node` was not touched.

## What this does not establish

This is one run of each campaign on one box, over loopback. The matrix ran
twice because of the INN race. The runs cover process deaths on ZFS, not
power loss. The per-cut and checkpoint-cut evidence is coordinate-precise
only on the developer twin (`b94f959e…`, against production `3411e52b…`); on
production the evidence is the kill run's store-state partition and the
production compaction test. The web pass was by script over HTTP, not a
browser. The transit rows show that a Path-different offer is answered as a
duplicate, not which test decided it. No row here is a theorem.
