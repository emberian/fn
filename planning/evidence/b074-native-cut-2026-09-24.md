# b074 native cut: exact image and bounded runtime evidence

The immutable source is `b074f94ed3e612a6e71c18859a3af3c9112c3708`. The clean hbox source gate is `/tank/fn/gates/wide-combined-b074f94e-exact-20260924`; a tracked-file checksum comparison against `git archive b074f94e` found zero differences. The earlier `run-20260924T053322Z-8ba2` copied main during a concurrent cherry-pick and is excluded from this source claim. Its apparent success is not b074 evidence.

The one ordinary four-job, 300-second/book default+dtn+ACL2-test qualification was `run-20260924T053517Z-1fb9`, raw manifest `certify-20260924T053544Z-951104.json` SHA-256 `667198f8b410cf6bfd027f4573e01c25ff4fad4834d942677cbd7a5ac6f21709`. It passed 617/617 roots from 631 matching cached certificates, with zero new certifications and no source-audit findings. Root's canonical archive in `7a15190c` has distinct JSON serialization/SHA; the raw gate manifest remains intact. `proof_artifacts acquire --profile default` accepted 261 books and rejected zero; default validation loaded 101 roots. This is inherited matching-source certificate evidence, not 617 fresh proofs.

The single source-matched production/developer pair used w28 ACL2 and `/tank/fn/toolchains/openssl-3.5.8` via `tools/build_native_host.sh`. Production launcher/core SHA-256 are `21a21df1379f3d395ad9d0d760797aab654abedf5509d55e0fc3968150180076` / `65a9448cbd4b5a289a1aef4711ba8cf3e2f3d21aad3d25d9b525f564065e2390`; developer launcher/core are `c901bdf5d366fa1982a8035a0db4178d543d8df17aadfb3f1798611a9aa0fbba` / `b6dcd8c31860962032290671e62189999c6076f1ea6358c0f970ef8678499f0b`. Build logs are `build/freeze/native-build-production.log` SHA `145b4cdd0022f40be62202620185b745e154d8c4c76e9e13bee810aa557c38ad` and `native-build-developer.log` SHA `da6d3500dc0c519d554956b743d2f42d3426bf3ddcd9c1bdfd66b8b0dcfe882d`. The live `/tank/fn/node` service was untouched.

Source-matched selected native cases passed: topic local (`topic-index-topic-local.log`, SHA `d790828513c783513a3e1dec4a4ba79eb12ddd43fd957e362b5b7c95782dce7e`), topic metadata (`topic-index-topic-metadata.log`, SHA `082478f0c371f3bfb084711313f9e0c6706d821e5c3ff314e87ce90e97e86f8c`), consumer inspect (`topic-index-consumer-inspect.log`, SHA `a525904e544abf789fcfd3bbc01b560be1f1a2e019271243514458c257a9707e`), consumer project bounds (`topic-index-consumer-project-bounds.log`, SHA `f34e989bca8c5a7977d03881c3c7247c232d01f9a89508058ed548e8e1eb8247`), indexed OVER/XOVER reader (`topic-index-reader-over-index.log`, SHA `c52a51d18eea4314541a2eca28bbe5d746c6b7e1882f5e25c69770d94cbc3633`), and BP deletion-report D1b (`topic-index-bp-deletion-report.log`, SHA `14d98a6e28be48d9b19275b8dacc63ad73e3cff6b6c4fc5f3d50aa59ee982081`). All named logs are relative to the gate's `build/freeze/` directory. These are selected real-owner tests, not a full runtime suite.

Two test-only overlays passed against the unchanged developer image. The active-reader pack-reclaim case from `97978480` (overlay SHA `ae848fb37f2b98639ccbaeb34a8eadbe3e219f4f6bf1a593bb459fed001f9ccd`) passed in `checkpoint-reader-overlay.log` SHA `b6c25a0ec9926034060eb4d3c609e679a71dc32db12863ef48d248aa28d52f63`. The newer Python human web client case from `13e7032d`, with separate client-source overlay in `build/human-client-overlay`, passed its modeled `record-attempted:kill` draft/lost-reply/restart case in `human-web-durable-outbox-v2.log` SHA `fde491673f6b863724ca5e4f62c6d674ebaed66daf0c5fc0fb71bf97c01ce6b7`; this does not make that newer Python source part of the b074 image. Its first `postpublish:kill` attempt was a fixture refusal because b074's served owner accepts modeled point `record-attempted`, not the older CLI alias.

The real-owner poll-cost fixture SHA `249092aea3652348c184d59ffc1b0b619455f6c031f35afdef7707798e7b9eb2` passed after two setup-only failures and kept `consumer-poll-cost-v4.json` SHA `79f4e81571e31ed6b4847ca04bee11162f4d92a495ab7c60c5d2df7af3906e3d`. At acknowledged positions 0 and 96 on the same frontier 118, twenty interleaved CLI/socket polls each had medians 58.253 and 58.390 ms; the observed high-position tail was 22 events. These samples include process and transport overhead and do not establish asymptotic complexity. Earlier setup attempts stopped on unregister refusal and at the Store's 128-event consumer journal cap; neither yielded timing evidence.

The consumer E2 lost-positive-ACK case is **red** on b074. Its original run intermittently received a non-boolean ACL2 bridge reply at the exact-source assertion; targeted reruns returned `T\nACL2 !>` there, then exposed a separate repeatable post-reopen failure: durable position equals the ACK cursor and status is ACK 4/frontier 5/distance 1, but first reopened `consumer poll` returns exit 3, stdout `consumer uncertain`, empty stderr while the owner process remains alive. The targeted CLI trace is `e2-consumer-cli-diagnostic.jsonl` SHA `815970f9bb90b412c6deacf164b16d49df8bb8e7cff245f7a8df68cd093eb453` and test log `e2-cli-diagnostic-v2.log` SHA `c332f89b7b43b7aab4f50ebfba913a545537ff87f9be0d4c17e2c490ed7f6d43`. The source-only configured-open index omission was found separately and awaits a new source-matched image; b074 cannot claim this E2 pass.

The interrupted BP fragment case is also **red**. A test-only trust/clock overlay now admits the sender and retains the first nonzero fragment without an article; the second contact logs `BP fragment family durable` and accepted xfer, then exits 4 before application handoff on native `#(54 54 ...) is not a string designator`. Raw `bp-fragment-trust-clock-overlay.log` SHA `beeb47daded710885f0d63edca6ba10f5769fd38215b0d01e5c8dd54fae888f0`. The same host byte/vector boundary affected the N03 route-wait case. The repair `ca68b5a1` is outside b074 and needs a newly qualified, matching image. The original fragment attempt first refused `no-trust-profile`/`lifetime-uncertain`; that was fixture setup, not evidence of assembly.

The gate, raw manifests and logs are retained. No current-source topic v2, author lifecycle, pack-retire or BP host-fix success is inferred from this older cut.
