# Repaired BP union: combined source qualification

Input snapshot: clean fn `e657b1cf8f1307f36e26bd9038e46da6c6560c18`.
The snapshot was submitted before subsequent documentation edits. It combines
the `48425e4e` BP union with its replay-frontier and fragment/report guard
repairs, the consumer-project overbound refusal, and current test/tool changes.

Invocation from the Mac checkout:

```sh
python3 tools/farm.py submit hbox --jobs 4 --timeout-seconds 180 \
  --cache /tank/fn/certcache \
  --acl2 /tank/fn/toolchains/w28/acl2-literal-4g \
  --remote-root /tank/fn/gates/bp-repaired-e657-20260924
python3 tools/farm.py wait hbox run-20260924T001850Z-bd79 --wait-seconds 3
make check
```

No `--closure` was used. The default Makefile roots were requested. The
original [manifest](manifests/certify-20260924T001919Z-568673.json) reports
`passed`, no book failures, 575 compatible installed certificates, ten new
book results, and 14.452 seconds of certification at four jobs. This is not
an uncached build or a full edit-to-verdict measurement. Its start/finish
timestamps are 00:19:19/00:19:54 UTC; cache/transport and orchestration costs
are distinct from the certification field. `make check` also exited zero;
it is a static gate, not runtime evidence.

The ACL2 8.7/SBCL toolchain identity is
`d5f2b9f0d2cf68c6074ea7046f4bd2e560d2984fe22d7e03f93045975ac889f0`.
The manifest retains executable/runtime hashes, individual source and
certificate digests, cache origins, commands and versions. Its remote
`git_revision` is null because the farm snapshot excludes Git metadata;
the submitted revision is recorded above and its source bytes are bound by
the manifest digests, not by an invented remote Git field.

This closes the source-composition failures recorded for
`certify-20260923T232613Z-475686`. It does **not** qualify the new union as a
running native image, prove physical durability, or establish native fragment
delivery. The native receive path still rejects fragments before kind-5
custody; the admission/expiry-safe assembly work is on the active BP branch.
The previously qualified consumer/reader/clone image is `1d26e01f`; the live
node remains `da5fd8cb`.
