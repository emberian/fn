# Native indexed consumer poll sample, 2026-09-24

Frozen fn source `b074f94ed3e612a6e71c18859a3af3c9112c3708` passed the shared ACL2 full gate (`certify-20260924T053544Z-951104.json`) before its developer image was built. The run used `/tank/fn/gates/wide-combined-b074f94e-exact-20260924/build/fn-host-developer` on hbox, Linux 6.11.0-29-generic x86_64, Python 3.12.7, ACL2 toolchain w28. Launcher SHA-256: `c901bdf5d366fa1982a8035a0db4178d543d8df17aadfb3f1798611a9aa0fbba`; core SHA-256: `b6dcd8c31860962032290671e62189999c6076f1ea6358c0f970ef8678499f0b`.

Fixture `tests/perf/native_consumer_poll_cost.py` at commit `383e29a2`, SHA-256 `249092aea3652348c184d59ffc1b0b619455f6c031f35afdef7707798e7b9eb2`, ran from the frozen gate with `FN_NATIVE_DEVELOPER_HOST` naming that image. It used the actual owner socket and ACL2 commands to bootstrap, register two consumers, append 109 distinct neutral registrations, and advance one consumer with six actual poll/ACK cycles. The test Store's 128-event journal capacity bounded the setup. Before measurement, ACL2 status reported `(ACK, frontier, distance)` of `(0, 118, 118)` and `(96, 118, 22)`; after the samples both statuses were unchanged. Each of 20 interleaved polls at each position returned an empty report and an ACL2 cursor.

The 40 CLI-inclusive samples in [raw JSON](b074-v4.json) have medians 58.2527 ms at ACK 0 and 58.3901 ms at ACK 96, and maxima 63.7144 and 60.9149 ms. The [stderr file](b074-v4.stderr) is empty; exit status was 0. Raw JSON SHA-256: `79f4e81571e31ed6b4847ca04bee11162f4d92a495ab7c60c5d2df7af3906e3d`.

This is a bounded real-owner sample, not an asymptotic benchmark: it includes process startup, socket and reply-file I/O, and only two positions within a 128-event Store. The ACL2 indexed-poll correspondence proof, not these timings, establishes that the derived index selects the committed event. Earlier fixture attempts stopped before timings at an unregister refusal and then the journal cap; they are not performance data. A separate signed-report positive-ACK end-to-end test currently finds an uncertain poll after process-death recovery and is under investigation.
