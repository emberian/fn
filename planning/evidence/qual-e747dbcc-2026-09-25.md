# Qualification of image e747dbcc, 2026-09-25

**Verdict: not deployable as cut. The defect is A1: a POST at the store's
transaction budget stops the owner** (production image, exit 4, `owner
core/store fault; process stopped: unexpected ACL2 action: :UNAFFORDABLE`,
the client gets no reply). It was fixed on dev after the cut, at `8a381541`
(merged at `fbbb6622`, lane status-join). The same merge fixes A2: `status`
prints the history-marker word as `0` and the history bound as `0`. On
e747dbcc the operator cannot confirm the marker step with `status`.

**Recommended path.**
1. Cut again at dev `d78ee77d` or later. That tree has both fixes, and
   peer-invite, which e747dbcc lacks.
2. Requalify the delta: A1, A2, A3, the upgrade rehearsal, and the modules
   named below.
3. Deploy with the two-step migration. Tonight, go only to format 8,
   unmarked, keeping `config.json`. Go to `--history-marker required` once
   `status` can show it.

**If the deputy deploys e747dbcc anyway**, A1 is not reachable soon on the
live store. It has 11 of 4096 transactions and 22 of 1,048,576 charge, so
about 4085 more commits are needed first. After that, every POST that
reaches the budget stops the node. systemd restarts it, and
`StartLimitBurst=5` then gives up.

One lane, `qual-e747dbcc`, ran this on hbox between 20:02Z and 20:41Z on
2026-09-25. The source is `e747dbcc7f9f4ba6d86e0dc3ec8856a06f1994e6`. It ran:
- the v0 matrix with the INN lab;
- the cut campaign, the compaction cuts, and the marker cuts through both
  entries;
- the ACL2 differential and the served crash model;
- the NNTP probe, the production kill run, and the D25 review matrix;
- every image-gated native module on the four images, plus the nine
  modules new since c3420013;
- the request verb's four labs and the web reader pass;
- an upgrade rehearsal on copies of the live node's store and `fn.toml`.

This is a runtime record. It certifies nothing and proves no theorem. The
previous qualification is [c3420013](qual-c3420013-2026-09-25.md).

## Summary

- **Matrix `20260925T201026` (published):** 142 accepted, 44 refused, 2
  uncertain, 31 not exercised, 1 not built, **0 disagreed**, 0 faulted,
  12 independent. INN: 34 held, 0 violated.
  - Against c3420013, the only change is that `V0-INN-OPERATOR-POST` and
    `V0-INN-INTEROP` now agree. F2 is closed by the lab update.
  - Two matrix-tool defects stopped the first two runs. Both are fixed on
    this branch (C12).
- **Campaign:** cut observations **60/60** (the 25 cuts plus the five
  marker cuts, both entries); compaction cuts 20/20; marker cuts through both
  entries 20/20; probe **50/50**; D25 review matrix 21/21.
- **F1 is closed.** ACL2 differential 7/7, served crash model 3/3 with the
  bridge fixed on this branch (C8), and the image-gated campaign module 10/10
  (F3's test was updated).
- **Production kill run** (seed 3, 90 iterations, 206 POSTs): **0 torn,
  0 lost-240, 0 reused numbers**. The judge also lists 6
  `died-absent-resubmit-failed`, all in the over-limit band. The harness's
  "over" article (33,600 octets) is under the 16 MiB article bound of the
  profile the harness initializes (C9).
- **Upgrade rehearsal on a copy of the live store** (format 7, 11
  transactions, marker present). Every step answered as the procedure says,
  with one exception: restoring the kept format-7 `config.json` over a
  *required* store lets c3420013 open it and silently drops the requirement
  (U1).
  - The steps: the new image opens the store; 7 to 8 with `config.json`
    kept; `needs-upgrade`; `rollback-check`; c3420013 back on the kept
    config; the marker two-step; the downgrade refused; the snapshot restored
    under c3420013; the one-step variant; a lost newest file and a missing
    marker refused by name.
  - The served surface on the migrated copy: a signed POST, tin's supplied
    Path, XPAT, a cancel by the author, and a key succession.
- **Native defects (class a):**
  - A1: a POST at the budget stops the owner.
  - A2: status renders 0.
  - A3: `control list` prints no grant rows although the grants are durable
    and in force.
  - A4: an ACL2 invariant-risk warning appears on the owner's stdout at the
    first POST.
  - Every other red module is a test or lab older than the behaviour (C1 to
    C11), or one transient (B1).
- **Invite, accept and confirm between two copies were not rehearsed.** The
  peer-invite merge (`e8009a3c`) landed after the cut; e747dbcc has no
  `peer invite` verb.

## Image

The gate is `/tank/fn/gates/qual-e747dbcc-20260925`, with the images under
`build/images/e747dbcc7f9f4ba6d86e0dc3ec8856a06f1994e6/`. The build log is
`build/image-build.log`, sha256 `99b2a914…`. The closure is
certify-20260925T194254Z-3661260: 602 certified over 828 roots, 0 failed,
committed on dev at `d78ee77d`.

- `sha256sum -c image.sha256`: 58 of 58 passed
  ([`image-check.log`](qual-e747dbcc/image-check.log)). It passed again on
  the relocated copy, before and after the relocation run, and inside
  `packaging/install-native.sh` for the rehearsal install.
- The gate tree equals `git archive e747dbcc` over `host/ tests/ tools/
  books/ packaging/ Makefile`: 1545 files, 0 differences (`cmp` of each file
  on hbox).
- The matrix ran from this lane's worktree (dev `d78ee77d`). Its `tools/`
  equals e747dbcc's except `tools/build_lists_check.py`, which the matrix
  does not use, and the two C12 fixes. Every other runner ran from copies of
  the gate (`tree`, `ctree`). The one bridge-fixed rerun used `treefix`,
  which is `tree` plus the C8 line.

| artifact | SHA-256 | vs c3420013 |
| --- | --- | --- |
| `fn-host` | `432622d29a28d594…` | same launcher |
| `fn-host.core` | `0c96e554fae5ed7345b6ac2be89a9975430e879afae0ba5ccb072720dbd38d08` | new core |
| `fn-host-developer` | `e4eeefd290450e74…` | same launcher |
| `fn-host-developer.core` | `5889c6fb30c2589e05c2b293769bb1a11734e7ae5710cb49dc2d1a0dc78fc109` | new core |
| `fn-host-dtn` | `095382bae2b3ff37…` | same launcher |
| `fn-host-dtn.core` | `440067011ea5c681cc4a197c61c18024854d94d7e01137f208daecb4bb92403a` | new core |
| `fn-host-dtn-developer` | `4986f14bf4ebc34f…` | same launcher |
| `fn-host-dtn-developer.core` | `268640b88e776f3270e0570748558e9aa199b5a845f8d397fe7fc2b81c0003e3` | new core |
| `runtime/sbcl` (SBCL 2.6.8) | `b115fe956aadee60…` | same |
| `openssl/lib/libcrypto.so.3` / `libssl.so.3` | `14d40ec6…` / `bb2d12be…` | same |

The full list is [`image-full.sha256`](qual-e747dbcc/image-full.sha256).

## The matrix

The invocation is in [`matrix-invocation.txt`](qual-e747dbcc/matrix-invocation.txt).
The stores were fresh, on 127.0.0.1:11190 and 11191. The matrix ran alone,
before anything else was started.

| run | result |
| --- | --- |
| `20260925T200259` | stopped early: `could not install …/gate-run/tin-home/.newsrc: No such file or directory`. The tin client phase (added by reader-surface) pushes files into a directory nobody creates. [`matrix-driver-run1.log`](qual-e747dbcc/matrix-driver-run1.log) |
| `20260925T200459` | 220 steps; the 10 INN rows not exercised: `KeyError: 'fn-post-supplied-path-240'`. D32's lab scenario checks an assertion `tools/inn_lab.py` never declares. [`matrix-driver-run2.log`](qual-e747dbcc/matrix-driver-run2.log) |
| **`20260925T201026` (published)** | **142 / 44 / 2 / 31 / 1, 0 disagreed, 0 faulted, 12 independent; INN 34 held.** [`matrix-driver.log`](qual-e747dbcc/matrix-driver.log), [`v0-runs/20260925T201026.327335Z-d78ee77-397dd54da3d1/`](v0-runs/20260925T201026.327335Z-d78ee77-397dd54da3d1/), `rows_digest` `d5e4849c…` |

Against c3420013's `20260925T004159`, by id over 220 rows, exactly two rows
changed: `V0-INN-OPERATOR-POST` and `V0-INN-INTEROP`, refused/NO →
accepted/yes. The injected feed carries no Injection-Date, which the lab
now accepts. innfeed reached fn (`CHECK 238 / TAKETHIS 239`). The four tin
rows stay in `PENDING_TIN_PLAN`: the tin phase ran (its wire log is in
[`matrix-node-logs.tgz`](qual-e747dbcc/matrix-node-logs.tgz)), but no row
is published for it. It is published with `--publish-current` as
`planning/v0-matrix.json`.

## Campaign, cuts, differential, probe, D25 matrix, kill run

The runner is [`campaign.sh`](qual-e747dbcc/campaign.sh), c3420013's with
the paths changed. The judges are unchanged. It ran from 20:15Z to 20:28Z,
beside the three module chains.

- **Image-gated `test_native_operator_campaign`: 10/10**
  ([`image-gated-test.log`](qual-e747dbcc/image-gated-test.log)). The
  cross-entry retry still answers 0 then 1 in both orders, as in F3
  ([`faults.txt`](qual-e747dbcc/faults.txt)). The test now expects that.
- **Cuts: 60 of 60 observations pass** ([`judged.md`](qual-e747dbcc/judged.md)).
  `tests/campaign/native_cuts.py` now lists the five `fn-bs-marker-program`
  cuts, and each is present with the resubmission answered `DUPLICATE` /
  `duplicate` rc 0. The other 25 rows are c3420013's cell for cell.
- **Compaction cuts 20/20** ([`checkpoint-judged.md`](qual-e747dbcc/checkpoint-judged.md)),
  the same rows as before.
- **Marker cuts through both entries 20/20**
  ([`marker-cuts.log`](qual-e747dbcc/marker-cuts.log)): the record is
  durable, the resubmit is a duplicate, the third post commits, and a lost
  newest file is refused.
- **ACL2 differential: 7/7** ([`crash-model.log`](qual-e747dbcc/crash-model.log),
  94 s, peak RSS 450 MB). `finish-consumed` and `finish-durable` now agree.
  The byte model knows the marker. **F1 is closed.**
- **NNTP probe 50/50** ([`nntp-probe.log`](qual-e747dbcc/nntp-probe.log)).
  There are 10 more rows (the marker cuts). A present article reposted is
  `this article is already stored here` at every present row.
- **D25 review matrix 21/21** ([`d25-matrix.log`](qual-e747dbcc/d25-matrix.log)),
  with the same answers as on c3420013.
- **Production kill run** (seed 3, 80 kills, 10 concurrent, 4:16, peak RSS
  613 MB; [`pk-judged.md`](qual-e747dbcc/pk-judged.md)).
  - 115 are 240-and-identical, 58 died-absent, and 27
    died-present-identical.
  - **0 torn / 0 lost-240 / 0 reused-number.**
  - 6 are `died-absent-resubmit-failed`: C9, not a store fault.
  - rc 0 came only with 240, rc 1 only with "already stored here", and rc 3
    only with no reply.

## Native modules on the four images

Each module runs alone as `timeout 3600 python3 -m unittest -v tests.<m>`.
The runner is [`run.sh`](qual-e747dbcc/run.sh): c3420013's, plus `NS_HOST`
(the image for `FN_NATIVE_HOST`), `FN_FORMAT7_IMAGE` and `FN_OLD_NATIVE_HOST`
(both c3420013's `fn-host`), and `NS_TREE`. There were three chains under
`systemd-run --user -p MemoryMax=24G`:
- [`chainA.sh`](qual-e747dbcc/chainA.sh): BP/DTN, then the request labs;
- [`chainB.sh`](qual-e747dbcc/chainB.sh): the c3420013 list, then
  relocation;
- [`chainC.sh`](qual-e747dbcc/chainC.sh): the nine new modules, with
  `test_native_key_statements` and `test_native_operator_verdicts` on the
  developer image their docstrings name.

The full table with log SHA-256 (16 hex) is
[`module-table.md`](qual-e747dbcc/module-table.md). All logs are in
[`native-logs.tgz`](qual-e747dbcc/native-logs.tgz).

| module (image) | tests | pass | fail | skip | log (16) | vs c3420013 / class |
| --- | --- | --- | --- | --- | --- | --- |
| auth, profile, admin, starttls, auth_admin_fidelity, owner, raw_scripts, operator_cli, program_check, initializer_fidelity, recovery, storage_codec, served_differential, reader_index, topic_metadata, consumer_inspect, checkpoint (26), compaction_crash_map, cut_map, peering, protected_peering, tls_transport, fn_web_native, app_journal, image_profiles, bp_contact_relay, bp_node (26, `fn-host-dtn-developer`), N08 kind-8 (`fn-host-developer`), bp_app, bp_fragment_node, test_checkpoint (7, 844 s) | all | all | 0 | 0 | module-table | same |
| history_marker | 6 | 6 | 0 | 0 | `9e3622c38343c962` | same |
| test_fn_verify | 31 | 28 | 0 | 3 | `78e73bc47e7b7116` | +14 tests |
| newnews_migration, stamp_migration, topic_local | 1/1/4 | 0/0/2 | 0 | 1/1/2 | module-table | same (no pre-T2 or pre-v2 image) |
| frozen_relocation (relocated copy, stdin closed) | 2 | 2 | 0 | 0 | `bdb96072b8d7ef97` | same; `image.sha256` OK before and after |
| **bounds_blob** | 1 | 1 | 0 | 0 | `a5ea97699017c0d7` | new |
| **bounds_join** (format-7 image = c3420013) | 2 | 2 | 0 | 0 | `fea9b64918790553` | new |
| **control_filing** (`FN_RUN_HYBRID_E2E=1`) | 5 | 5 | 0 | 0 | `c7ef2d8990cd855e` | new: C1 filing, C3 withdrawal, the two-node authority case |
| **profile_namespace** | 1 | 1 | 0 | 0 | `e5d428dbde6a894f` | new |
| **state_checkpoint** | 5 | 5 | 0 | 0 | `57f8794e61d3a739` | new |
| **crash_correspondence** | 8 | 8 | 0 | 0 | `7e3e3fe14038a269` | first run in a qualification |
| **operator_verdicts** (developer) | 6 | 6 | 0 | 0 | `f1d7a18e99922720` | new |
| **served_crash_model** (bridge fixed, C8) | 3 (18 cut subtests) | **3** | 0 | 0 | `b81a9a7fa8e0f673` | was 2/3 (F1); **F1 closed**. As shipped: 1/3, 26 errors (C8), `78e9f5dde71792ed` |
| **history_required** | 6 | 4 | 2 (21 subtests) | 0 | `c0ec4be5e934200c` | new; **A2** |
| **operator_verbs** | 22 | 20 | 2 | 0 | `44d218a083846c52` | **A1** and **A2** |
| **profile_upgrade** | 11 | 10 | 1 | 0 | `10371993111051a5` | **A2** |
| **control_authority** | 1 | 0 | 1 | 0 | `0889d1bd54828220` | new; **A3** |
| **control** | 14 | 11 | 3 | 0 | `fe1c4517bed7cfac` | 2 × **A4**, 1 × C4 |
| live_reconfiguration | 11 | 9 | 2 | 0 | `d5f95f773d5f1435` | C3 |
| key_statements (developer) | 2 | 1 | 1 | 0 | `3ccf157933c2f2fb` | new; C5 |
| hybrid_author (`FN_D23_VERIFY=1`) | 10 | 9 | 1 | 0 | `c21568c40c750ff5` | C6 |
| consumer_project_bounds | 2 | 1 | 1 | 0 | `2115f92685bfe628` | C7 |
| consumer_e2 | 4 | 2 | 1 | 1 | `74aa3e553ad4e5c1` | same (F6 = C11) |
| consumer_e2 signed poll, dated source | 1 | 0 | 0 (1 error) | 0 | `3383404065b55cd7` | B1; passed alone, `8496f424b9045876` |
| bp_service (`fn-host-dtn`) | 16 | 2 | 14 | 0 | `168cfea10fc9dc40` | 7 × F5 (C10, as before), 9 × C1 |
| bp_service (`fn-host-dtn-developer`) | 16 | 6 | 10 (12 subtests) | 0 | `66e63bfabb53563e` | was 16/16; C1 |
| bp_contact | 2 | 1 | 1 | 0 | `c156ebd4e31572b8` | was 2/2; C1 |
| bp_receive_integrity (`fn-host-dtn` / developer) | 4 / 4 | 2 / 4 | 2 / 0 | 0 | `94ecfd7e…` / `6d7014f6…` | same (F5) |
| bp_obligation | 5 | 4 | 1 | 0 | `2bce77436297d0a4` | was 4/4; C2 |

**The request verb's labs 4/4 exit 0** ([`request-labs.tgz`](qual-e747dbcc/request-labs.tgz)),
with c3420013's outcomes.
- direct, one dtn7 relay, and two relays: `request-accepted`,
  `receipt-accepted`, and A's obligation `receipted pinned=no`;
- unauthorized: `refused reason=source-not-carried` and `request-refused`,
  and A stays `restart-observed pinned=yes`;
- the control obligation stays `outstanding pinned=yes` everywhere;
- the kill cases are `restart-observed pinned=yes`.
- The labs now log `BP queued job routed peer=… expected=…` (PRF-103).

**The web reader** ([`web-manual.log`](qual-e747dbcc/web-manual.log),
[`web-manual.tgz`](qual-e747dbcc/web-manual.tgz)) gave c3420013's pages. It
ran on the production image with TLSv1.3 and the certificate verified. It
showed "nothing unread", then three posts, each "240 article received OK",
then "3 unread". The group threaded the reply under its root. Article 1 was
"node verdict: absent". After a mark, "2 unread". The node's own `LIST
COUNTS` gave `fn.agents 3 1 3 y`.

## The upgrade rehearsal (copies of the live node)

The script is [`upgrade_live.py`](qual-e747dbcc/upgrade_live.py) and the log
is [`upgrade-live.log`](qual-e747dbcc/upgrade-live.log) (runs 1 to 3 were
script defects, kept). It ran under `systemd-run -p MemoryMax=24G`.
- `/tank/fn/node/store`, `fn.toml`, `tls/` and the `fn-c3420013` install
  were copied with `cp -a` at 20:26Z. The live directory was only read, and
  its digest was unchanged at the end.
- The config copy moves every path into the scratch area and listens on
  127.0.0.1. `[auth] required`, `protected_only` and TLS are kept.
- The candidate was installed as a deploy installs it
  (`packaging/install-native.sh` into `upgrade-live/fn-e747dbcc`, rc 0), and
  both images were run through their installed `bin/fn`.
- The copied store's file digests are in
  [`pristine-store.sha256`](qual-e747dbcc/pristine-store.sha256). It holds
  11 transactions, `config.json` `bf6e6df9…` (format 7, scale), and the
  marker `e4f49e80…`.

| step | result |
| --- | --- |
| c3420013 `status` | rc 0, transactions=11 |
| e747dbcc `status` offline | rc 0, `profile format=7 max-transactions=4096 … history-marker=0` (A2: the word is missing) |
| `store needs-upgrade` (format 7) | rc 0 `needs-upgrade` |
| e747dbcc owner on the format-7 store (deploy step 1) | LISTENING; the marker is unchanged (`e4f49e80…`; it already covered 11); `GROUP` without login → 480 (auth kept) |
| **status live vs offline** | both rc 0, same lines (`pins=11 reserved=22 connections=0`) |
| snapshot (`cp -a` of the stopped store) | taken |
| `cp -p config.json config.json.format-7`; `store upgrade-profile` | rc 0 `upgraded … previous-budget=4096`, `format=8 … history-marker=unmarked` |
| `store needs-upgrade` (format 8) | rc 0 `current` |
| `store rollback-check config.json.format-7` | rc 0 `rollback sound transactions=11` |
| **rollback of 7→8** (copy): c3420013 on format 8 | rc 4 `ACL2 rejected durable configuration frame` |
| same, kept `config.json` restored | c3420013 `status` rc 0; owner LISTENING |
| `store upgrade-profile --history-marker required` | rc 0 `history-marker=required` (and `status` says `history-marker=0`, A2) |
| `needs-upgrade` / `rollback-check` after required | `current` / **`rollback sound`** (U1) |
| c3420013 on the required store | rc 4 `ACL2 rejected durable configuration frame` |
| downgrade `--history-marker unmarked` | rc 1 `not-an-upgrade history-marker` |
| **U1**: kept format-7 `config.json` restored over the *required* store | c3420013 `status` and `recover` rc 0; e747dbcc reads `format=7`; marker deleted → `recover` rc 0 (legacy admit). **The requirement is gone.** |
| **rollback by the snapshot**: c3420013 `recover`, `status`, owner | rc 0 / rc 0 / LISTENING; the transactions equal the pristine copy's |
| one-step variant (format 7 → required in one command) | rc 0 `history-marker=required` |
| one-step, newest `.txn` removed | `recover` rc 4 `history-short-of-marker marker=11 records=10` |
| one-step, marker removed | `recover` rc 4 `marker-missing records=11` |
| one-step, restored | `recover` rc 0 |

**The served surface on the migrated copy** (format 8, required, production
owner, STARTTLS and AUTHINFO as login `qual`, bound with `--principal P`):
- `principal set-password` rc 0, `effective-at-next-start`.
- `group create control.cancel` and `fn.keys`: configured generations 6
  and 7. The live store has neither group. **A cancel needs
  `control.cancel` configured**; without it the cancel is refused
  `control-not-filed`, as `test_native_control_filing` shows.
- `hybrid-enroll` P over the control socket: rc 0 on the production image.
- **tin-shaped POST with `Path: not-for-mail`**: 240. It is served as
  `Path: hbox.ember.software!not-for-mail`, and the resend is 441 "already
  stored here".
- **Signed POST** (a `hybrid-sign-carrier` carrier): 240. `HDR
  :fn-verified` gives `0 verified 5555…5555 keyring 1`.
- **XPAT**: `XPAT Subject 1- *qual rehearsal*` gives 221 with 11 and 12;
  `XPAT Message-ID <…> *` gives 221 with the id.
- **Cancel by the author** (a signed `Control: cancel <signed>` from P): 240.
  A fresh reader then gets `ARTICLE <signed>` **430**, and `LISTGROUP
  control.cancel` is `211 1 1 1`.
- **Key statement**: `control grant P keys fn.keys` rc 0.
  - A succession-v1 carrier under the old keys (key material in the body):
    240, and the log says `key-statement enrol-successor committed`.
  - An old-key article is refused 441 `local-enrollment`; a new-key article
    is 240.
  - `hybrid-key-history` (owner stopped): `generation=2 state=active`,
    `generation=1 state=retired`.
- Restart: `GROUP fn.agents` `211 12 1 13`. The tin article is 220, the
  cancelled one 430, the cancel 220 (in control.cancel), and the new-key
  article 220. `recover` rc 0, transactions=18 articles=16.
- **Invite, accept and confirm: not in e747dbcc** (see above).

### Migrate to required tonight, or stay unmarked

- **Stay unmarked (format 8, `config.json.format-7` kept).**
  - Rollback is the kept config under c3420013, rehearsed rc 0 and served.
  - The marker is still written at every commit and caught up at open.
    While present, a lost newest record is refused.
  - The risk: a store whose marker is deleted together with the files it
    covers is admitted (the stated limit).
- **Required tonight.**
  - A missing marker or a lost newest record is refused at open, rehearsed
    on the copy. The price is availability: a node that meets either stays
    down (exit 4) until an operator acts.
  - On e747dbcc `status` cannot show that the step took (A2). Only the
    upgrade command's own line says `required`.
  - Rollback needs the pre-migration snapshot, or the kept config. U1: the
    kept config silently drops the requirement, and `rollback-check` still
    says `sound`.
- **This lane recommends unmarked tonight, required with the image that
  carries status-join.**

## Failures, classified

(a) is an fn runtime defect, (b) is the environment or a flake, and (c) is a
test or lab assertion older than the behaviour. No red module is waved
through.

1. **A1 (a): a POST at the transaction budget stops the owner.**
   - Reproduced on the production image
     ([`probe-budget.log`](qual-e747dbcc/probe-budget.log),
     [`probe_budget.py`](qual-e747dbcc/probe_budget.py)). With the
     development profile (budget 128), POSTs 1 to 128 get 240. POST 129
     gets 340 and then no reply, and the owner exits 4 with `owner
     core/store fault; process stopped: unexpected ACL2 action:
     :UNAFFORDABLE`.
   - `test_native_operator_verbs`
     `test_the_development_profile_budget_is_reported_and_refused_by_name`
     fails for this reason.
   - The cause is known: since `219fd392` the owner's buffer calls lost the
     keyword check. It is fixed at `8a381541` (status-join), after the cut.
2. **A2 (a): `status` prints the history-marker word as `0`, and a large
   history bound as `0`.**
   - Examples: `max-history-octets=0` for 2^40, and `history-bound=0` in the
     kill-run store.
   - It fails `test_native_history_required` (2 tests, 21 subtests),
     `test_native_operator_verbs`
     `test_init_writes_the_operators_fields_and_status_prints_them`, and
     `test_native_profile_upgrade`
     `test_a_raise_of_any_field_and_a_shrink_refused_by_name`.
   - The upgrade itself prints `history-marker=required`. Fixed at
     `fbbb6622` (status-join), after the cut.
3. **A3 (a): `control list` prints nothing, live or offline, while the
   grant is durable and in force.**
   - `store/config/00000002.cfg` holds the grant. After an owner restart
     `control revoke` is accepted, and a second revoke is refused
     ([`probe-control-list.log`](qual-e747dbcc/probe-control-list.log)).
   - It fails `test_native_control_authority`.
   - The report path is `fnn-admin-query` → `fn-native-admin-host-query-report`
     → `fn-native-admin-control-report` over `fn-cfg-authorities`. The peer
     report over the same replay works (operator_verbs' peer-list case
     passes).
   - Not fixed on dev as far as this lane can see: no commit after the cut
     touches the report.
4. **A4 (a): an ACL2 `Invariant-risk` warning on the owner's stdout.** It
   appears at the first POST, on both images: `…in
   FN-OWNER-SUBJECT-ID-BUFFER … possibly leading to an ill-guarded call of
   FN-SHS-W-SET`.
   - `fn-owner-subject-id-buffer` (host/owner-host.lisp:1831) is a
     `:program` wrapper around the stobj digest `fn-shb-subject-id`.
   - It breaks two `test_native_control` cases, which read `OWNER-CLEANUP` /
     `CONTROL-SUBMITTED` from that stdout and find a blank line first.
   - It costs nothing measurable: a 30 KB served POST takes 0.14 to 0.18 s
     on e747dbcc against 0.27 to 0.37 s on c3420013
     ([`probe_cost.py`](qual-e747dbcc/probe_cost.py)).
   - Still present on dev.
5. **B1 (b): consumer_e2's dated-source case raised `ACL2 returned a
   non-boolean`** under the concurrent chains. Alone it passes, and the
   bridge answers `T`. That is the Python bridge (PKT-162) under load.
6. **C1 (c): a connect that never made a socket is now `:failed`**
   (`78992f89`, bp-budgets). `bp-service run` to a closed port prints
   `BP forwarding retained reason=failed` and exits 0. The tests expect 3.
   This covers 9 `bp_service` cases on each image and one `bp_contact`
   case.
7. **C2 (c), with a D13 note: `bp_obligation` request with no route row**
   (PRF-103). It gives `decision=no-route (not queued; the obligation
   stays)` and exit 1, after a durable attempt record. The test expects 0 or
   3, and predates routing. Whether exit 1 is the right outcome after a
   durable attempt is the BP owner's question.
8. **C3 (c): `live_reconfiguration`.**
   - `peer list` with an owner running is now answered (exit 0; live
     status). The test expects the lock refusal.
   - A source test expects `fn-native-admin-plan-deltas`; PRF-099 made it
     `-over`.
9. **C4 (c): `test_native_control`** expects `operator post` to refuse a
   supplied Path. D32 accepts it.
10. **C5 (c): `key_statements`' carried IHAVE is refused 437
    `carried-budget-unset`** ([probe log](qual-e747dbcc/native-logs.tgz),
    `_qe_probe_ks…`). The test's peer has no opaque-carriage budget
    (PRF-099, merged beside peer-keys).
11. **C6 (c): `hybrid_author` expects `hybrid-sign-carrier` to refuse
    source+26,000 octets** as over the article cap. D27 removed the
    constant.
12. **C7 (c): `consumer_project_bounds` expects `refused-v1 limit`** for a
    196,609-octet event. D27 made the limit a codec ceiling, so the answer
    is `codec`.
13. **C8 (c): the served crash model's bridge setup lds
    `host/store-node-host.lisp` without `books/records-concrete-owner`.**
    That is what `host/native/build.lisp` includes first, so
    `fn-store-decode-records` fails to translate (`FN-RCON-STORE-EVENT-P`
    undefined): 26 errors.
    - Fixed on this branch: one `include-book` line in
      `tests/test_native_served_crash_model.py`. The rerun passes 3/3.
14. **C9 (c): the kill harness's over-limit band.** It uses 33,600 octets
    under a profile it initializes with `max-article-octets=16777216`, so
    the resubmission is accepted. The harness predates D27.
15. **C10 (c): F5**, the production DTN image refusing developer
    selectors: 7 in `bp_service` and 2 in `receive_integrity`. Unchanged.
16. **C11 (c): F6**, consumer_e2 with the undated source. Unchanged.
17. **C12 (c): the two matrix-tool defects** (tin home directory,
    undeclared `fn-post-supplied-path-240`). Fixed on this branch:
    `tools/v0_matrix.py` +1 line, `tools/inn_lab.py` +4 lines;
    `tests.test_inn_lab` 30/30 and `tests.test_v0_matrix` 50/50.
18. **U1 (design, for the D31 owner): the kept format-7 `config.json`
    undoes `required`.** `rollback-check` answers `sound` without saying
    so, and the marker-required procedure calls the snapshot the only
    rollback. The requirement is protected against the verb, not against a
    file copy the documented format-8 rollback itself uses.

## Logs and digests

Everything is in [`qual-e747dbcc/`](qual-e747dbcc/).
[`SHA256SUMS`](qual-e747dbcc/SHA256SUMS) covers every file, and
[`raw.sha256`](qual-e747dbcc/raw.sha256) the uncompressed JSON. The live
store's copies, `auth.toml`, and the TLS and web keys stay on hbox and are
not committed.

On hbox, `/tank/fn/scratch/qual-e747dbcc/` holds `tree`, `ctree`,
`treefix`, `campaign/{work,probe-work,pk-work,marker-work,d25-work}`,
`matrix/stores*`, `reloc`, `web`, `request-labs`, `upgrade-live/`
(pristine copy, the working copy, the snapshot, the rollback copies,
one-step) and the probes. The matrix deploy tree is
`~/fn-deploy/native-e747dbcc-d78ee77/`.

After the runs no process under the scratch path was alive
([`ps-final.txt`](qual-e747dbcc/ps-final.txt) is empty), 11190, 11191 and
11396 were free, and every `qe747-*` unit was stopped. **`/tank/fn/node` was
only read.**

## What this does not establish

This is one run of each campaign on one box, over loopback. It covers
process deaths on ZFS, not power loss. The per-cut evidence is
coordinate-precise only on the developer twin (`5889c6fb…`, against
production `0c96e554…`). The rehearsal used a copy of an 11-transaction
store; it was not the live node's own upgrade. The web pass was by script.
The key-statement rehearsal did not revoke. No row here is a theorem.
