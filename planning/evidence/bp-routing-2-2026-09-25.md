# bp-routing-2 (2026-09-25): routed queued jobs, once per contact, the gated session (PRF-103)

Lane `lane/bp-routing-2` from dev `534a68d3`. Brief:
build/coordinator/queue/bp-routing-2.txt. The spike record
(`spike/mega:planning/evidence/spike-bp-2026-09-25.md` §1, §7.1, §8) was the
specification; nothing was taken from the spike's host code except the
`fnn-bps-read-route-table` helper and the lab's contact-row hunks.

## 1. Routed queued jobs (PKT-055; bp-routing finding 1; spike deferral 1)

**Queue time.** `books/bp-route-jobs.lisp` `fn-bprt-job-route DEST TABLE NODE
KEEPALIVE SEGMENT TRANSFER` answers the loopback contact route of the boundary
`fn-bprt-outbound-choice` names, with this node's session parameters, or nil.
The host (`fnn-bps-queue-route`, host/native/bp-service.lisp) calls it at the
three queue sites:
- `fnn-bpnode-queue-outbox` (receipts) and `fnn-bpnode-queue-report`
  (reports), host/native/bp-node.lisp, over the owner's table
  (`fnn-bpnode-route-by-owner`, read each pass);
- `fnn-command-bpo-owner-request` (the request carrier),
  host/native/bp-obligation.lisp, over the Store's table.

No route: nothing is queued, no sequence is reserved, and the host logs
`BP queue route destination=… decision=… (not queued; the obligation stays)`.
The receipt stays owed in FNRJ and a later pass queues it; the report intent
stays; the request's durable attempt keeps the pin and the verb exits 2
(`BP obligation request carrier no-route`). The route a job is queued with is
its durable route (the `:queued` record).

**Contact time.** `books/bp-node-contact-driver.lisp` `fn-bpnp-contact-next
ST PEER ROUTING OFFERED` is the one question the host asks before each offer
(`fnn-bpc-drive-contact`, now in bp-service.lisp; every base contact goes
through it: `fnn-bps-attempt-ready` for `bp-service run/resume` and
`bp-obligation request`, `bp-contact tick`, and `fnn-bpnode-send-receipts` in
`bp-node serve`/dispatch). It answers `(:offer (:base (:contact PEER t))
OFFERED' EID)`, `(:held KEY :no-route | :no-live-hop | :route-changed)` or
`(:close)`. The host passes EID to the TCPCL session machine as the expected
node ID (`fnn-bps-send-effect`; before, the expected peer was nil).

**The durable contact-time route (spike deferral 1a).** The deferral names a
record that fixes the address an attempt was made on. Here the address is the
durable `:queued` record's route, and the driver offers a job only when the
contact-time decision names exactly that route; a table change after queueing
holds the job `:route-changed`, reported each contact, until the table routes
it back. No attempt is ever made to an address a durable record does not
hold. What stays open is a durable **re-route** record (a lower-machine
record kind, with codec and replay arm) so that such a job moves to the new
hop instead of waiting; that touches `bp-node-machine` and every BP book
above it, beyond this lane's run budget.

## 2. Once per contact (PKT-054, PKT-140)

The loop's decision is ACL2's. The host threads the OFFERED the driver
answers and stops at the first answer that is not an offer; it no longer
reads the transfer outcome to decide.

## 3. The gated session (PKT-056; bp-routing findings 2 and 3)

- `fn-bpnp-start-one` takes ORDERED, the rows it may offer.
- `fn-bpnp-routed-start` scans `fn-bpnp-routed-rows` of the held rows (those
  `fn-bprt-offer-decision` offers on this session). When no routed row is
  ready and the scan over every row is, it reports that row's decision; every
  row stays held.
- A `:session` open without VIA installs the session and offers nothing.
  `:resume` takes the same optional VIA field (`fn-bpnp-resume-via`) and goes
  through `fn-bpnp-routed-start`; without VIA it offers nothing. The host
  sends neither ungated form.

## 4. PKT-064: the with-issued waits slot is still open (narrower than filed)

`fn-bpnp-with-issued` (books/bp-node-progress.lisp:86) rebuilds the
foundation state through `fn-bpnf-with-issued` → `fn-bpnf-state-with-arrival`,
an 11-element list (slots 0 to 10), then writes slots 12 to 15 back with
`update-nth`, which pads slot 11 with nil. Slot 11 is `fn-bpnp-waits`: the
per-key volatile `:mru`, `:credit` and BP-R17 `(:busy m budget)` waits. So
`fn-bpnp-with-issued` clears every row's waits.

What is already right: every durable-success arm rebuilds with
`fn-bpnp-with-waits … (fn-bpnp-waits st)` (attempt persist :867, forward-result
persist :987, busy delivery and deferral :1229, :1345), and the fence and
recovery paths clear volatile state by design. What is still open: the
`:refused` persist results of the attempt (:879) and forward-result (:996)
arms answer `fn-bpnp-with-issued st nil` and drop every wait, so after a
refused kind-8 or kind-9 publication a busy-deferred local row can be offered
before its backoff reading (the durable busy count still bounds its
retries; MRU and credit waits are re-derived by the next scan). Not closed
here: the repair is `fn-bpnp-with-waits` around those two answers, and the
keep lemmas of both arms in bp-node-progress-premises, -guards, -bridge and
bp-node-machine-gaps (`bpgap-with-issued-fields`) open that shape; it needs
its own step with the witness (a busy-deferred row, then a refused kind-8
publication, then a progress tick before the backoff reading, which must not
deliver).

## 5. PKT-070: a control article through BP transit

`tests/test_bp_node_native.py`
`test_control_article_through_bp_transit_is_filed_not_executed`: the
receiver's Store has `control.cancel` (`operator … group create`); the
sender's request carries an article with `Control: cancel
<bp-node-a3@example.invalid>` and `Newsgroups: fn.test`; the receiver's
`bp-node serve` delivers it (`request-accepted`) through bp-app's transit,
which calls `fnn-owner-attempt-transit` (host/native/owner.lisp), whose first
decision is `fn-pa-filing-plan`. It is filed once in `control.cancel`;
`fn.test`'s next article number is unchanged; one article in the Store.

## 6. Theorems (subject: the function the host calls)

| theorem | book | host line |
| --- | --- | --- |
| `fn-bpnp-contact-offer-is-the-routed-hop` | bp-node-contact-driver | `fn-bpnp-step` via `fnn-bps-foundation-step` (bp-service.lisp:1067), on the event `fn-bpnp-contact-next` answered at bp-service.lisp:1058 |
| `fn-bpnp-contact-holds-an-unrouted-job` | bp-node-contact-driver | bp-service.lisp:1058 |
| `fn-bpnp-contact-offers-each-job-at-most-once` | bp-node-contact-driver | the loop of `fnn-bpc-drive-contact` (bp-service.lisp:1056 to 1076) |
| `fn-bpnp-contact-closes-only-when-nothing-owed-remains` | bp-node-contact-driver | bp-service.lisp:1058 |
| `fn-bpn-apply-record-keeps-forwarded` | bp-node-contact-driver | `fn-bpn-apply-record`, under the step's `:persist-result` arm |
| `fn-bpnp-step-session-offer-is-the-scan-choice` (restated) | bp-node-forward-retry | `fn-bpnp-step` on the routed `:session` from `fnn-bpnode-forward-session` (bp-node.lisp) |
| `fn-bpnp-routed-rows-skip-an-unrouted-row` | bp-route-step | same |
| `fn-bpnp-step-offers-only-the-routed-hop`, `fn-bpnp-step-unrouted-bundle-stays-held-and-is-reported` (restated over the filtered scan) | bp-route-step | same |

Full statements are in planning/proofs.json PRF-103 and the books.

**Teeth.** tests/acl2/bp-node-contact-driver-tests: the reachable witness is a
queued base job routed to port 4556 (`fn-bprt-job-route`), offered with the
relay's EID; must-fail per hypothesis of the routing keystone (route removed,
no contact row, table moved to another hop, token at the bound, malformed
base); a reachable contact (offer, durable `:attempting`, `:uncertain`
transfer, durable `:requeued`) that the threaded driver closes and the
unthreaded one would offer again (must-fail); one must-fail per gate of the
close keystone (issued, delivery uncertain, fenced, pending, string peer);
the `:forwarded` job kept under a `:requeued` record, must-fail on the
`:attempting` job. tests/acl2/bp-route-tests: the VIA-less `:session` open
and `:resume` answer no effect (must-fail that they propose), the routed
`:resume` proposes; two rows under one key, older to dtn://elsewhere/, younger
to dtn://bp-dest/: the unfiltered scan picks the older (must-fail that it
picks the younger), the routed session proposes the younger. Six BP test
books open routed sessions (a `*…-via*` constant each) instead of the
6-field form.

## 7. Certification

Farm, hbox, w28 `acl2-literal-4g`, cache `/tank/fn/certcache`, 2 jobs, 300 s,
`--affected-by books/bp-node-progress --affected-by books/bp-route-jobs`,
gate `/tank/fn/gates/bp-routing-2-r1`, at c671e9e9 (books unchanged since):

| run | result | manifest |
| --- | --- | --- |
| run-20260925T100459Z-1422 | **74 of 74 certified, 156 installed from the cache, 31 roots, all passed** | `manifests/certify-20260925T100537Z-3113587.json` (sha256/16 `2e9b5ae863fa6a36`) |

`green_check --changed-since dev`: every changed book and dependent green.

Book times in that run are **not a D26 verdict**: hbox's load average was 19
and this lane's own DTN-closure certification ran beside it. Unchanged books
ran 1.5 to 2 times their recorded figures (bp-report-guards 16.9 s,
bp-node-debt-cache-invariants 17.6 s), and `(include-book
"bp-node-progress")` alone took 12 s in premises and forward-retry. The new
books: bp-node-contact-driver 8.0 s, its tests 8.2 s, bp-route-jobs 4.0 s.
Changed books: bp-node-progress-premises 29.4 s, bp-node-forward-retry 18.5 s,
bp-route-step 17.8 s, bp-node-progress 12.6 s, bridge 12.3 s, guards 10.1 s,
bp-route-tests 10.8 s. In the scratch run of the same bytes under less load
(4 jobs), premises was 10.0 s, forward-retry 10.0 s, route-step 9.1 s,
guards 8.7 s. The costliest proof events in route-step and forward-retry
are the unchanged `…-with-runtime-keeps-selection-inputs` lemmas (5.1 s
each, `(enable fn-bpn-nth)`). **Owed:** one matched-conditions timing run
(quiet hbox, 2 jobs) before these figures are compared with the baseline.

The DTN closure (126 roots, incl. the two new books) certified in the native
tree with `certify_books.py --incremental --jobs 2` under `swarm-build`
(263 from the cache, 66 certified, all passed; `proof_artifacts validate
--profile dtn`: roots=126 result=loaded).

## 8. Native (hbox, tree `/tank/fn/scratch/bp-routing-2/tree` at ebde18df)

Image `fn-host-dtn-developer`, `host/native/build-dtn.lisp` under
`swarm-build`, 0 undefined lines: launcher `14b8eba8870e3c8f`, core
`a30d8d25573b8aee`, build log `973551b11abbe2b0`. Script:
[`native.sh`](bp-routing-2-2026-09-25/native.sh); all SHA-256s in
[`SHA256SUMS`](bp-routing-2-2026-09-25/SHA256SUMS).

- **`tests.test_bp_node_native`: 26 tests, the 25 existing ones OK** (845 s,
  log `43d678cdce0ed957`). No frame-count assertion changed: every receipt in
  the module is queued on the route the table names, which is the relay port
  the old command line gave. The one error was the new PKT-070 case, whose
  first Store read used `run_store.group_codes`, which refuses this Store
  format (the Python store tool is behind format 8). The case now reads the
  groups through a reader port.
- **PKT-070 case alone, at the committed test file** (sha256
  `e38336b140b27ce6`): OK, 26 s, log `a630c3996acf9363`. The DTN image omits
  the NNTP surface, so GROUP is read through `operator run` on a default
  developer image (`FN_NATIVE_READER_HOST`; this run used the checkpoint-cost
  lane's dev-based image, launcher `7f2e815bc5c0bf58`, core
  `10bfaceeb9bfcdb3`). `control.cancel` answers `211 1`, `fn.test` `211 0`.
- **dtn7 app-receipt lab, `--relays 1 --native --routes present`**
  (dtn7-rs at /tank/fn/dtn7/repo; report `c7bf42ca8f896a8b`): setup 23 × rc 0;
  A's request logs `BP queue route destination=dtn://receiver/ port=34253`
  and `BP queued job routed peer=dtn://receiver/ expected=dtn://dtn7-r1/`
  (a-1-send `1030595bef2c9d0c`), uncertain at the held first hop; resume
  accepted (`0706e8aabace77b5`); B `request-accepted` (`8dcc8dadf77d492c`); A
  `receipt-accepted` (a-serve `949b581f68c92208`); `work-bp-node receipted
  pinned=no`.
- **`--relays 1 --native --routes removed`** (report `a6c2f967ccfedcd6`):
  setup 24 × rc 0. **Pin stays, no-route logged:** a-1-send
  (`b29f210b90b81c0c`) logs `BP queue route destination=dtn://receiver/
  decision=no-route (not queued; the obligation stays)` and `BP obligation
  request carrier no-route`, exit 2 (refused); B has no verdict and no
  receipt; `work-bp-node` ends `restart-observed pinned=yes`; the control
  obligation stays `outstanding pinned=yes`.
- **`--relays 1 --no-contact-tick`** (report `e18a4b9a27ca588d`): B's `serve`
  sends the receipt through the driver; A `receipt-accepted`, `receipted
  pinned=no`.

## 9. Not done, and deferrals

- A durable re-route record (§1): a table change after queueing holds the job
  `:route-changed`; because the lower machine offers the first queued job for
  a peer, such a job also holds younger jobs to that peer on that contact.
- PKT-064 (§4): confirmed open (refused kind-8/kind-9 publications drop the
  waits); not closed.
- Verbs without a Store keep the queued address (`bp-service run`, `resume`
  and `bp-contact tick` without STORE); the request carrier with no route
  exits 2 and needs a new `bp-obligation request` once a route exists.
- Queue-time routing is host-sequenced (the host calls `fn-bprt-job-route`
  and queues its answer); the contact-time theorem covers what is offered.
- Timing at matched conditions (§7).
- The Python store tool's format check (`run_store.group_codes`) refuses the
  current Store format; not this lane's.

## 10. Whole-module rerun (the deputy's merge gate)

The full `tests.test_bp_node_native` module, 26 tests, on the same DTN
developer image (launcher `14b8eba8870e3c8f`, core `a30d8d25573b8aee`), with
`FN_NATIVE_READER_HOST` a default developer image built from this lane's tree
in `/tank/fn/scratch/bp-routing-2/tree` (`host/native/build.lisp` under
`swarm-build`, default profile 140 roots validated, 0 undefined lines;
launcher `5cea9992ccc1a2b4`, core `cdbe7179b03b983e`, build log
`339ac942c70b9942`). The test file is the committed one (`e38336b140b27ce6`).
**26 of 26 OK**, 928 s, log sha256
`5038214f764d9c9df254972413775c0258795f14eea13c3d235831929b46f319`. Script:
[`native2.sh`](bp-routing-2-2026-09-25/native2.sh); hashes:
[`SHA256SUMS-rerun`](bp-routing-2-2026-09-25/SHA256SUMS-rerun). This
supersedes §8's split result (25 in the module plus the PKT-070 case alone on
another lane's image).

Harvested by assurance-triage (2026-09-26) as the scenario catalog's evidence log: [`test_bp_node_native.log`](bp-routing-2-2026-09-25/test_bp_node_native.log), the 26-of-26 log, copied from hbox /tank/fn/scratch/bp-routing-2/logs2/, sha256 `5038214f764d9c9df254972413775c0258795f14eea13c3d235831929b46f319`.
