# The hbox node upgraded to 18c91321, with the store's profile upgraded to scale — 2026-09-24

The sixth image built from `dev` today and the third upgrade of the running
node in place, at about 21:50 UTC: the store, the TLS pair, the credentials
and `fn.toml` stay; `fn-47bdb9a4` and `fn-da5fd8cb` stay beside the new
release for a one-line rollback. In the same stop window the store's
profile was upgraded from development (128 transactions) to scale (4096)
with `operator CONFIG store upgrade-profile scale`: 8 used before and
after, `config.json` `dcbd90d3…` -> `bf6e6df9…`, the development frame
copied aside as `config.json.development-<stamp>` under `/tank/fn/node`
(rollback: put it back with the owner stopped, while used is at most 128).

## The image

Sources at `dev` `18c91321`, frozen at `/tank/fn/gates/qual-18c91321-20260924`
(closure `certify-20260924T210828Z-1892761`, 0 failed; the gate matches
`git archive 18c91321` file for file). Launchers byte-identical to
47bdb9a4's; cores: production `e623cfd2…`, developer `793b937d…`, DTN
`f2b07dda…`, DTN developer `3a840c21…`. Qualified by one lane:
[qual-18c91321](qual-18c91321-2026-09-24.md): matrix 142/44/2 with 0
disagreed and INN 33/33; 25 cuts both entries 50/50; differential 7/7;
probe 40/40; 90 production SIGKILLs with 0 torn, 0 lost 240, 0 reused
numbers; every native module at or above the previous image.

Since 47bdb9a4 this image carries: the kind-8 retry policy (default,
pending ember), signed POST verdicts through the transit classification,
the DTN image as a BP node, the staging-cleanup log line, the BP
application receive fixes and the raw-arity check, a refused start exiting
5, K0 over all 25 cut coordinates, the commit path's whole-history search
and the advance's node check carried, the transaction file name and the
payload bound decided by ACL2, and the profile-upgrade verb itself.

## The probe from a Mac on the LAN

`tools/node_probe.py 192.168.50.39 1119 --cafile <cert> --group fn.agents`
as `ember` ([probe.json](node-hbox-18c91321-2026-09-24/probe.json), SHA-256
`e4858edba4078d4e…`): every assertion held on both connections, exit 0; the group now
serves the articles posted on the earlier images plus this probe's.

## Not claimed

No durability past process death on this platform (D14, T16); no signature
security; no peers configured on the deployed node; the node is LAN-only;
the DTN profile is not deployed on it. The independent verifier
(`tools/fn_verify.py`) and the spec prose for the signed bytes landed after
this cut and are not on the image (they are client-side).
