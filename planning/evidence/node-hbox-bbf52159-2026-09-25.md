# The hbox node upgraded to bbf52159 — 2026-09-25

The sixth in-place upgrade of the running node in this cycle, at about 21:55
UTC, by the night deputy. The store, the TLS pair, the credentials and
`fn.toml` stay. `fn-c3420013`, `fn-dabebb84`, `fn-4eca4148`, `fn-18c91321`,
`fn-47bdb9a4` and `fn-da5fd8cb` stay beside the new release. The store moved
from format 7 to format 8 **unmarked**. `config.json.format-7` is kept, and a
full copy of the format-7 store is kept at
`/tank/fn/node/store.snapshot-pre-bbf52159-format8`.

## The image

- Sources: `dev` `bbf52159dcab19228bd6cd0b855b99dd6d68758d`, frozen at
  `/tank/fn/gates/qual-bbf52159-20260925`.
- Closure: [certify-20260925T211549Z-3867068](manifests/certify-20260925T211549Z-3867068.json),
  204 certified at these bytes over 850 roots, 0 failed.
- Cores: production `6e569af1…`, developer `e6640102…`, DTN `10af88d9…`, DTN
  developer `a144a645…`. `sha256sum -c image.sha256` passed before the
  install.
- Qualified by [qual-bbf52159](qual-bbf52159-2026-09-25.md): **deployable**.
  - The five findings of [qual-e747dbcc](qual-e747dbcc-2026-09-25.md) are
    confirmed fixed.
  - Matrix 142/44/2/31/1, 0 disagreed, INN 34 held.
  - Cuts 60/60, marker cuts 20/20, ACL2 differential 7/7, served crash model
    3/3.
  - Kill run: 0 torn, 0 lost-240, 0 reused.
  - The upgrade path was rehearsed on copies of this store.

This image carries everything merged on 2026-09-25 up to `bbf52159`
([now.md](../now.md), the night report): control C2 and C3 (the served
withdrawal), marker-required (not engaged: the store stays unmarked), the
format-8 profile with u64 widths, the owner opening from its checkpoint,
peering (invite/accept, key statements, the carriage budget, the pull feed),
reclamation part one, D32 (a supplied Path) and login binding, XPAT, live
status, BP routing and fragmentation, and the deploy fixes.

## The procedure (build/coordinator/deploy-bbf52159.sh, in this record's directory)

**Phase 1** ([deploy-p1.log](node-hbox-bbf52159-2026-09-25/deploy-p1.log)):
1. The image digests verified.
2. Installed at `/tank/fn/node/fn-bbf52159`.
3. The production image refuses a developer selector: `fn --version` under
   `FN_STORE_TEST_STOP` exits 5.
   - The old probe (`status` under the selector) exits 1 here. With the
     previous owner running, `status` answers "refused" before the selector
     is read, so the script now probes `--version`.
4. The unit was backed up as `fn-node.service.pre-bbf52159`, stopped,
   repointed and started. It was active and listening on 1119 on the format-7
   store: 11 transactions, 11 articles.

**Phase 2** ([deploy-p2.log](node-hbox-bbf52159-2026-09-25/deploy-p2.log)):
1. Stopped, and the store copied (`cp -a`) to the snapshot.
2. `cp -p config.json config.json.format-7`.
3. `store upgrade-profile` gave `format=8 … history-marker=unmarked`, with
   every field unchanged.
4. `store needs-upgrade` answers `current`, and `store rollback-check
   config.json.format-7` is accepted.
5. Restarted: `OWNER-OPEN open=full-replay reason=absent`, `LISTENING 1119`.
   `status` through the running owner shows format 8.

## The probe from the laptop

`tools/node_probe.py 192.168.50.39 1119 --cafile <cert> --group fn.agents` as
`ember` ([probe.json](node-hbox-bbf52159-2026-09-25/probe.json), SHA-256
`fd6a2ba41adedfbc…`), exit 0, every assertion held:
- `201` greeting, STARTTLS offered;
- AUTHINFO before TLS refused `483`; `382`, TLS 1.3;
- after login `281` POST is offered;
- `211 11 1 11 fn.agents`; the post `240`;
- a fresh connection logs in again and rereads the article, `220 12`, with
  the body marker found.

The unauthenticated checks before and after phase 2 are in
[p1](node-hbox-bbf52159-2026-09-25/probe-anon-p1.txt) and
[p2](node-hbox-bbf52159-2026-09-25/probe-anon-p2.txt).

## Rollback

- **To c3420013:** stop, restore `config.json.format-7` over `config.json`
  (the store is unmarked, so nothing is dropped), repoint the unit to
  `fn-c3420013` (its unit file is `fn-node.service.pre-bbf52159`), start.
  Rehearsed on a copy by qual-bbf52159.
- **The snapshot** is the fallback if the kept config does not open.

## Next

- Take `store upgrade-profile --history-marker required` after a day of
  service, with a fresh snapshot. After that, c3420013 cannot open the store
  and the snapshot is the only rollback.
- Gap U1b: a kept config copied by hand over a required store still opens
  under c3420013 and drops the requirement. `rollback-check` refuses that
  case.
