# Qualification of image c3420013, 2026-09-25

One lane, `qual-c3420013`, ran the qualification of the image of source
`c34200132b2b0c725a93672fc4ba93a528817e18` on hbox between 00:41Z and 01:15Z
on 2026-09-25: the v0 matrix with the INN lab, the cut campaign with its ten
compaction cuts, the five committed-history marker cuts through both entries,
the ACL2 differential, the NNTP probe, the production kill run (one seed), the
D25 review matrix, every image-gated native module on the four images, the
native request verb's four labs, the web reader by hand, and an upgrade
rehearsal from a 4eca4148 store. It is a runtime record. It certifies nothing
and proves no theorem. The previous qualification is
[4eca4148](qual-4eca4148-2026-09-24.md).

**Deploy verdict: deployable.** No runtime row, cut, marker cut, compaction
cut, probe row, kill, lab or upgrade step failed for an fn reason. One class
(a) finding (F1) is a model-fidelity defect, not a runtime one: the
byte-level crash model does not know the committed-history marker, so the
ACL2 differential fails at the two finish cuts. F1 does not change what the
node does. It does mean **P10 ("every cut is a model crash point") must not be
cited for this image** until the model expresses the marker or the claim is
narrowed. The other failures are tests or lab assertions that predate the
injection inverse (c), and the production DTN image refusing developer
selectors (c, as before).

- Matrix `20260925T004159Z` (published): 140/46/2/31/1, **2 disagreed**, 0
  faulted, INN 32 held, 1 violated. The two disagreeing rows are the INN lab
  asserting an `Injection-Date` that RFC 5537 §3.5 item 11 now forbids fn to
  add (F2, (c)). No other row changed against 4eca4148, and the innfeed race
  did not recur.
- Cut campaign 50/50; compaction cuts 20/20 (10 cuts, both entries); marker
  cuts **20/20** (5 cuts × kill/EIO × both entries); probe 40/40; kill run 0
  torn, 0 lost-240, 0 reused; D25 review matrix 21/21.
- ACL2 differential 6/7 tests (23 of 25 cut subtests); served crash model 2/3
  tests (16 of 18 cut subtests): F1.
- history_marker 6/6, operator_verbs 19/19 (the peer-list case included),
  hybrid_author 8/8 with `FN_D23_VERIFY=1`, fn_verify 17/17, bp_obligation
  4/4, the request verb's labs 4/4 as recorded by m4-native-request, web 4/4
  and the manual pass.
- Upgrade rehearsal: a copy of a 4eca4148 store (no marker) opens on c3420013,
  gets its marker at the first commit, the next open admits it, and a deleted
  newest transaction file is refused by name, with and without a pack.

## Image

The gate is `/tank/fn/gates/qual-c3420013-20260925`, images under
`build/images/c3420013/`. The closure manifest is
[certify-20260925T001652Z-2229194](manifests/certify-20260925T001652Z-2229194.json)
(430 certified, 0 failed).
- `sha256sum -c image.sha256` passed 58 of 58 before any run
  ([`image-check.log`](qual-c3420013/image-check.log)), and on the relocated
  copy before and after the relocation run.
- The gate tree equals `git archive c3420013` over `host/ tests/ tools/
  books/ packaging/ Makefile`: 1416 files, 0 content differences (`cmp` of
  each file on hbox).
- The matrix ran from the lane worktree at dev `e8f4faa5`, whose `tools/`
  equals c3420013's. The host/books/tests differences (BP-R17, merged after
  the cut) do not reach the matrix, which runs the image. Every other runner
  ran from copies of the gate (`tree`, `ctree`).

| artifact (`…/build/images/c3420013/`) | SHA-256 | vs 4eca4148 |
| --- | --- | --- |
| `fn-host` | `432622d29a28d59455e01f3e5b426036c5862db21d5f7d1205a9304ab11e3505` | same launcher |
| `fn-host.core` | `7a124c6f70e072dbd0a8ade9b29b756bbdad8b821dc801c1f234826b242a9ec8` | new core |
| `fn-host-developer` | `e4eeefd290450e7497dade96e2c4089e0caacfa88f72c737f51a92b2f3b2ba18` | same launcher |
| `fn-host-developer.core` | `b22251d8db73ba46ca77ef8c075ad1d83985ab544183265f413a1faa8039e41e` | new core |
| `fn-host-dtn` | `095382bae2b3ff377a2f27b119c0d5f5b247c983f4846d7176fbcd661e1e282c` | same launcher |
| `fn-host-dtn.core` | `f5b7bb360a0749c891b2bde606138e790c7a0762687efd4401a431acebc69709` | new core |
| `fn-host-dtn-developer` | `4986f14bf4ebc34fccf54f4bfea0e7007222f96c477a9f0519faad6c59c19c88` | same launcher |
| `fn-host-dtn-developer.core` | `345357d27213b32ebaffc400aaf464de4056c33251d1ef048e8b0f5fb0405a30` | new core |
| `runtime/sbcl` (SBCL 2.6.8) | `b115fe956aadee603459fac401e1ff2cc39321e14848544a0fe438788a2fc6d5` | same |
| `openssl/lib/libcrypto.so.3` / `libssl.so.3` | `14d40ec6…` / `bb2d12be…` | same |

The full list is [`image-full.sha256`](qual-c3420013/image-full.sha256).
hbox's Python has `dilithium_py` and `cryptography` 42.0.5 in the user
site-packages ([`pylib.txt`](qual-c3420013/pylib.txt)), so no `PYTHONPATH`
was needed this time.

## The matrix

`tools/v0_matrix.py --backend native-operator … --inn --keep
--publish-current`, exact text in
[`matrix-invocation.txt`](qual-c3420013/matrix-invocation.txt). Stores were
made as `hbox-matrix-provision.sh` makes them, fresh, under
`/tank/fn/scratch/qual-c3420013/matrix/stores/{a,b}` on 127.0.0.1:11190 and
11191. It ran alone, before anything else was started on hbox, from 00:41:59Z
to 00:45:35Z (rc 1).

| run | accepted | refused | uncertain | not exercised | not built | disagreed | faulted | independent | INN |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 4eca4148 run 2, `20260924T231407Z` | 142 | 44 | 2 | 31 | 1 | 0 | 0 | 12 | 33/33 held |
| **c3420013, `20260925T004159Z` (published)** | **140** | **46** | **2** | **31** | **1** | **2** | **0** | **12** | **32 held, 1 violated** |

Against 4eca4148 run 2, by id over 220 rows, exactly two rows changed:
`V0-INN-OPERATOR-POST` and `V0-INN-INTEROP`, accepted → refused, agreement
yes → NO. Both follow from one lab assertion, `operator-post-feeds-inn` (F2).
Everything else, verdict, agreement and independence, is unchanged. It is
published with `--publish-current` as `planning/v0-matrix.json` (`rows_digest`
`d49424a0…`); the run is
[`v0-runs/20260925T004159.810142Z-e8f4faa-0d8859e35924/`](v0-runs/20260925T004159.810142Z-e8f4faa-0d8859e35924/).

**The INN race did not recur.** innfeed reached fn: `V0-INN-FEED-IN` is
accepted, `CHECK 238 / TAKETHIS 239`. The lab's start order is unchanged
(innd first, then the owner). The lab owns that order and has no option to
change it, so this lane did not change it. Running the matrix alone on the
box is the only difference from 4eca4148's run 1.

**D25 on the rows.**
- `V0-POST-DUPLICATE-A` and `-B`: `441 posting failed; this article is
  already stored here`, as on 4eca4148.
- Transit: `TRANSIT-DUPLICATE-AB/BA` 435, `CHECK-DUP` 438, `TAKETHIS-DUP` 439.
  B logs `refused transit … <alpha@a.example.invalid> code=439 decision=have
  reason=history detail=none`, as before: the history decides before any
  byte comparison. So these rows cannot show the injection-inverse record's
  "transit is back to octet equality", and no row answered 436.
- The operator-post feed into innd now carries Path and Injection-Info and
  **no Injection-Date** ("only in the second: injection-info, path"): the
  lab's article supplies Date and Message-ID. That is recipe v2, and it is
  what RFC 5537 §3.5 item 11 requires.

Logs: [`matrix-driver.log`](qual-c3420013/matrix-driver.log) and
[`matrix-node-logs.tgz`](qual-c3420013/matrix-node-logs.tgz) (node logs and
`fn.toml`s of the deploy tree `~/fn-deploy/native-c3420013-e8f4faa`).

## Campaign, compaction cuts, marker cuts, differential, probe, D25 matrix, kill run

The runner is [`campaign.sh`](qual-c3420013/campaign.sh): 4eca4148's with the
paths changed, plus the marker cuts and the D25 review matrix. The tree is a
copy of the gate (`ctree`). `FN_NATIVE_SOURCE_ROOT` is the gate, with the
OpenSSL 3.5.8 pair and `PYTHONDONTWRITEBYTECODE=1`. It ran from 00:45:48Z to
01:03:51Z, beside the two module chains. Judging used 4eca4148's
[`judge.py`](qual-c3420013/judge.py), [`faults.py`](qual-c3420013/faults.py)
and [`checkpoint_judge.py`](qual-c3420013/checkpoint_judge.py), unchanged.

### Per cut (developer image, both entries)

**50 of 50 observations pass** ([`judged.md`](qual-c3420013/judged.md)). The
table is 4eca4148's cell for cell:
- 10 absent cuts: the resubmission commits (`ACCEPTED` / `committed`).
- 7 recovery cuts: n/a, absent, the resubmission commits.
- `record-linked` ("either"): present, resubmission `DUPLICATE` / `duplicate`.
- 7 present cuts: `accepted operator post DUPLICATE rc 0` / `duplicate rc 0`.

`native_operator_campaign` ran in 3:08, peak RSS 327 MB.

### Compaction cuts through both entries

**20 of 20 pass** ([`checkpoint-judged.md`](qual-c3420013/checkpoint-judged.md)),
identical to 4eca4148 row for row: the six candidate/selection cuts die at
the pack step and resume 0,0,0; the reclaim cuts die at step 1; the retire
cuts at step 2; the operator entry resumes 0, except at the retire cuts
where it gets 1 `already-compact` because the kill landed after the retire's
effect. Every row: recover 0, both articles reread identical, a further
compact refused `already-compact`, GROUP 2 → 3 after a third post.

### The five committed-history marker cuts

`tests/campaign/native_cuts.py` does not list the marker cuts. Its `CUTS`
are the fn-bs frontier/record/finish programs and the compaction cuts. The
five cuts are ACL2's `fn-hm-marker-cut-names`
(`books/store-history-marker.lisp`), selected with
`FN_NATIVE_POST_FAULT=<cut>:kill|eio` as the
[history-marker record](m5-history-lifetimes-2026-09-24.md) says. Two runs
cover them:
- `tests.test_native_history_marker`
  `test_every_marker_cut_recovers_and_keeps_detecting`, through `store ROOT
  post`: 10 subtests pass (module 6/6, below).
- This lane's [`marker_cuts.py`](qual-c3420013/marker_cuts.py) adds the
  served entry (`operator CFG post` to an owner started with the selector,
  i.e. `fnn-owner-publish-prepared`), reusing the campaign's `Node`/`seed`
  helpers unchanged. For each cut, action and entry it seeds one article,
  then posts the candidate under the selector, recovers, resubmits the
  candidate through the same entry, posts a third article through the store
  entry, deletes the newest transaction file and opens.

**20 of 20 pass** ([`marker-cuts.log`](qual-c3420013/marker-cuts.log),
[`marker-cuts.json.gz`](qual-c3420013/marker-cuts.json.gz)):

| cut | entry | fault rc (kill / EIO) | txns after fault | recover | resubmit | third post | newest file lost |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `marker-created`, `-written`, `-staged-durable`, `-replaced`, `-durable` | store | −9 / 3 | 2 | 0 | `duplicate` rc 0 | committed | refused rc 4, `history-short-of-marker marker=3 records=2` |
| same five | operator | 3 / 3 (the client's; the owner died or fenced) | 2 | 0 | `DUPLICATE` rc 0 | committed | refused rc 4, same |

The record is durable at every marker cut. A death between the record and
the marker leaves the marker behind by one, and the next commit catches up.

### Faults and production refusals

Details in [`faults.txt`](qual-c3420013/faults.txt).
- The developer EIO, control-fault and five test-stop rows: client 3, recover
  0, both articles reread identical, resubmission `DUPLICATE`/`duplicate`
  rc 0, as on 4eca4148.
- The 65-death allocation-orphan rows: recover 0, the next post `ACCEPTED`.
- **Production refuses 30 of 30 selector starts with exit 5** (10 selectors
  × {`operator run`, `operator recover`, `store post`}), and the store is
  unchanged. Raw `store post` on production exits 5, and so does
  `FN_NATIVE_INIT_FAULT` at `operator init`.
- **Cross-entry retry changed back (F3).** One payload through `store post`
  and then `operator post` gives 0, then **1** (`refused operator post
  REFUSED`, `refused post path=control`). The reverse order gives 0, then 1
  (`store: conflicting immutable Message-ID`). The changed-byte third
  submission is 1 in both orders, and the transaction count stays 2 → 2 → 2.
  4eca4148 gave 0/`DUPLICATE`. The image-gated
  `tests.campaign.test_native_operator_campaign` therefore fails 9/10 again.
  The same shape appears in `dev-allocation-orphans-store-post`, where the
  operator post after a `store post` commit is `REFUSED`.

### The ACL2 differential

`FN_NATIVE_CRASH_HOST=<image>/fn-host-developer python3 -m unittest -v
tests.test_native_crash_model` on `acl2-literal-4g`: **6 of 7 tests**. In
`test_all_native_post_process_death_cuts`, 23 of the 25 cut subtests pass, and
`finish-consumed` and `finish-durable` fail with
`(T T T NIL T T T T (NIL T T NIL NIL))`
([`crash-model.log`](qual-c3420013/crash-model.log), 73 s, peak RSS 445 MB).
That is F1.

### The NNTP probe (once, 1:08)

**40 of 40 pass** ([`nntp-probe.log`](qual-c3420013/nntp-probe.log)). The
table is 4eca4148's: 18 post cuts killed (absent before `record-linked`,
present from it on), the EIO rows with their three replies (`nothing was
stored` / `outcome is uncertain` / `240` at `record-stage-unlinked`), and the
four controls. **The repost of a present article is `this article is already
stored here` at all 18 present rows.**

### The D25 review matrix

The injection-inverse lane's
[`d25b_matrix.py`](qual-c3420013/d25b_matrix.py) (unchanged), with the
c3420013 developer image and the d25-dup (recipe v1) image
`/tank/fn/scratch/d25-dup/img/build/fn-host-developer`, on port 11396:
**21 of 21** ([`d25-matrix.log`](qual-c3420013/d25-matrix.log)). The rows
answer as they did in the lane's record:
- U1/U2/S1/R1/R2/R3/V1 resends are "already stored here".
- U3/U4/U5/S3 (a changed authored byte or Date) and I1/I2 (another path
  identity) are "a different article".
- V2 (a v1 record without Date) is "a different article" (ambiguous, compared
  exactly).
- U6 (a new Message-ID) is 240.
- S2 is refused by `hybrid-sign-carrier`.

This is the module-free "D25 review matrix". There is no
`tests/test_native_poster_bytes*.py`.

### The production external-kill run (seed 3, 11:17, peak RSS 1.41 GB)

`native_production_kill run --image <image>/fn-host --seed 3 --kills 80
--concurrent 10`: 90 iterations, 206 POSTs in the ledger, 178 final articles
over two stores (closed and swept), 92 owner pids all gone
([`pk-judged.md`](qual-c3420013/pk-judged.md)). **The judge lists 0
failures.**

| every POST the judge saw (206) | 4eca4148 | c3420013 |
| --- | --- | --- |
| 240-and-identical | 98 | 102 |
| died-absent | 68 | 48 |
| died-present-identical | 17 | 33 |
| refused (441) | 23 | 23 |
| **torn / lost-240 / reused-number** | 0 / 0 / 0 | **0 / 0 / 0** |

| killed POSTs | 240-identical | died-absent | died-present-identical | refused | total |
| --- | --- | --- | --- | --- | --- |
| single | 14 | 36 | 29 | 1 (over-limit) | 80 |
| concurrent | 4 | 12 | 4 | 0 | 20 |

The shift from died-absent to died-present is in the kill instants against
the store state: more kills landed after the link. That the marker's extra
fsyncs lengthen the window is likely but was not measured here. rc 0 came only with `240`; rc 1 only with `441 … not
received` or `441 … already stored here`; rc 3 only with no reply. The run
has "a different article" 0 times.

## Native modules on the four images

The runner is [`run.sh`](qual-c3420013/run.sh), 4eca4148's with the paths and
`FN_NATIVE_IMAGE_SOURCE_SHA=c3420013`. It is one module at a time, each as
`timeout 3600 python3 -m unittest -v tests.<module>`, with:
- the image's `sbcl` first on `PATH` and `SBCL_HOME` set;
- every `FN_RUN_*` flag, `FN_D23_VERIFY=1`, and `FN_TEST_OPENSSL` = the
  OpenSSL 3.5.8 wrapper;
- the DTN variables as before (`FN_NATIVE_DTN_HOST`,
  `FN_NATIVE_DTN_DEVELOPER_HOST`, `FN_NATIVE_BP_HOST`,
  `FN_NATIVE_BP_NODE_HOST`, contact sender/receiver).

[`chainA.sh`](qual-c3420013/chainA.sh) (BP/DTN, then the request labs) and
[`chainB.sh`](qual-c3420013/chainB.sh) (the rest, then
[`reloc.sh`](qual-c3420013/reloc.sh)) ran beside the campaign from 00:45:49Z.
Every port was ephemeral.

| module | tests | pass | fail | skip | s | log SHA-256 (16) | vs 4eca4148 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| test_native_auth | 4 | 4 | 0 | 0 | 6.6 | `380a2e0a4f1210cb` | same |
| test_native_profile | 3 | 3 | 0 | 0 | 0.8 | `02aee40f6d6f8b19` | same |
| test_native_admin | 9 | 9 | 0 | 0 | 3.4 | `76c58543a7f62521` | same |
| test_native_starttls | 2 | 2 | 0 | 0 | 0.7 | `050ab35c0eb1f4ac` | same |
| test_native_auth_admin_fidelity | 3 | 3 | 0 | 0 | 12.8 | `22cbee75b093f97d` | same |
| test_native_owner | 18 | 18 | 0 | 0 | 23.6 | `85896120ab239917` | same |
| test_native_raw_scripts | 9 | 9 | 0 | 0 | 0.4 | `c2497a8c820c34f4` | same |
| test_native_live_reconfiguration | 11 | 11 | 0 | 0 | 1.5 | `cd6691692ac3c2cf` | same |
| test_native_control | 14 | 14 | 0 | 0 | 21.2 | `37676c98b52f29b2` | same |
| test_native_operator_cli | 6 | 6 | 0 | 0 | 4.0 | `fcaa3207e9f89092` | same |
| **test_native_operator_verbs** | 19 | **19** | 0 | 0 | 4.7 | `6ad8ec47abcdb22f` | **was 17/18: +1 case (the D23 peer-list rows, `test_the_d23_rows_are_listed_one_word_per_row`), and the D25 budget case now passes** |
| test_native_profile_upgrade | 5 | 5 | 0 | 0 | 9.2 | `ea76ca6a0d7c36e0` | same |
| test_native_program_check | 9 | 9 | 0 | 0 | 0.3 | `c935ab195e1ab48b` | same |
| test_native_initializer_fidelity | 11 | 11 | 0 | 0 | 1.6 | `02f18d15f57532cd` | same |
| test_native_recovery | 13 | 13 | 0 | 0 | 13.5 | `67136c68a5c94a13` | same |
| test_native_storage_codec | 11 | 11 | 0 | 0 | 67.2 | `0427fea48bda8bdb` | same |
| test_native_served_differential | 7 | 7 | 0 | 0 | 1.1 | `4a296873469824f8` | same |
| test_native_reader_index | 5 | 5 | 0 | 0 | 6.0 | `d529272596447a4a` | same |
| test_native_newnews_migration | 1 | 0 | 0 | 1 | 0.0 | `ef05ad3db74ef1ad` | same (no pre-T2 image) |
| test_native_stamp_migration | 1 | 0 | 0 | 1 | 0.0 | `1e610954f77e3ba3` | same |
| **test_native_history_marker** | 6 | **6** | 0 | 0 | 5.3 | `c77072fa102d0ee5` | **new; the five marker cuts × kill/EIO through `store post`** |
| test_native_hybrid_author (`FN_D23_VERIFY=1`) | 8 | 8 | 0 | 0 | 18.0 | `1c01ddcd424caf1a` | same (with the verifier, no `PYTHONPATH` now) |
| test_fn_verify | 17 | 17 | 0 | 0 | 3.9 | `6841fb26f43b9a30` | same |
| test_native_topic_local | 4 | 2 | 0 | 2 | 2.4 | `dc3c8b91a67fc798` | same (no pre-v2 image) |
| test_native_topic_metadata | 2 | 2 | 0 | 0 | 1.2 | `1da0617d7bd2a77c` | same |
| test_native_consumer_inspect | 4 | 4 | 0 | 0 | 0.5 | `0c927c58634469aa` | same |
| test_native_consumer_project_bounds | 2 | 2 | 0 | 0 | 0.2 | `91ddf7f15c8e0203` | same |
| test_native_consumer_e2 | 4 | 2 | 1 | 1 | 3.3 | `8ddf5adf392737c5` | same (F6, (c)) |
| consumer_e2 signed poll, dated source | 1 | 1 | 0 | 0 | 11.7 | `5c261d493209bf5a` | same |
| test_native_checkpoint | 26 | 26 | 0 | 0 | 56.7 | `5cc51b17fc53f62e` | same (the production compaction test passes) |
| test_native_compaction_crash_map | 4 | 4 | 0 | 0 | 0.0 | `255539519c469b33` | same |
| test_native_cut_map | 3 | 3 | 0 | 0 | 0.0 | `a395a800f677f785` | same |
| test_checkpoint | 7 | 7 | 0 | 0 | 503.5 | `df1b02f4042b4ef3` | same |
| test_native_peering | 5 | 5 | 0 | 0 | 7.4 | `ebd178afc008c3af` | same |
| test_native_protected_peering | 5 | 5 | 0 | 0 | 27.2 | `a13b79c72cd19e24` | same |
| test_native_crash_model (in the campaign) | 7 | **6** | **1** | 0 | 73.3 | `crash-model.log` | **was 7/7; F1, (a)** |
| **test_native_served_crash_model** | 3 (18 cut subtests) | **2** | **1 (2 subtests)** | 0 | 128.4 | `c16ef515b8f741a5` | **was 3/3; F1, (a)** |
| test_native_tls_transport | 2 | 2 | 0 | 0 | 0.8 | `cbde8fe34397ad7f` | same |
| test_native_frozen_relocation (relocated copy, stdin closed) | 2 | 2 | 0 | 0 | – | `a218bfb5f113715b` | same; `image.sha256` OK before and after |
| test_fn_web_native | 4 | 4 | 0 | 0 | 7.0 | `ead438a703ed953a` | same |
| test_bp_node_native (`fn-host-dtn-developer`) | 19 | 19 | 0 | 0 | 426.0 | `983f60e565e32702` | same |
| N08 kind-8 case (`fn-host-developer`) | 1 | 1 | 0 | 0 | 35.4 | `345f0bd06b627c7a` | same |
| test_bp_app_native | 5 | 5 | 0 | 0 | 75.5 | `72bed21afb1c2192` | same |
| test_bp_obligation_native | 4 | 4 | 0 | 0 | 1.1 | `1a6e8902b906fda5` | same |
| test_bp_fragment_node_native | 1 | 1 | 0 | 0 | 30.3 | `81bda70bd41eb0d6` | same |
| test_bp_service_native (`fn-host-dtn`) | 16 | 9 | 7 | 0 | 4.5 | `21385984102c6209` | same (F5, (c)) |
| test_bp_service_native (`fn-host-dtn-developer`) | 16 | 16 | 0 | 0 | 14.6 | `2e93634e185eb1ea` | same |
| test_bp_contact_native | 2 | 2 | 0 | 0 | 0.5 | `9cbbb0fad9eb578a` | same |
| test_bp_contact_relay_native | 1 | 1 | 0 | 0 | 2.3 | `28605d653d6b155b` | same |
| test_bp_receive_integrity_native (`fn-host-dtn`) | 4 | 2 | 2 | 0 | 0.5 | `72d9df767095f6cd` | same (F5, (c)) |
| test_bp_receive_integrity_native (`fn-host-dtn-developer`) | 4 | 4 | 0 | 0 | 1.0 | `06a88138d86d0f39` | same |
| test_native_app_journal | 10 | 10 | 0 | 0 | 9.0 | `f0f496e0ea9b6f92` | same |
| test_native_image_profiles | 12 | 12 | 0 | 0 | 1.4 | `4aba5bd10421c250` | same |
| campaign `test_native_operator_campaign` (image-gated) | 10 | 9 | 1 | 0 | 26 | `image-gated-test.log` | **was 9/10 for the opposite reason; now F3, (c)** |

**The native request verb's cases.** m4-native-request's `labs.sh`, with
only the image (`fn-host-dtn-developer` of this gate) and the paths changed
([`labs.sh`](qual-c3420013/labs.sh), outputs in
[`request-labs.tgz`](qual-c3420013/request-labs.tgz)), runs `bp-obligation
request` direct, through one dtn7 relay, through two, and through one relay
where B's boundary carries nothing. **4 of 4 exit 0, with that record's
outcomes:**
- the three carried runs are `request-accepted`, `receipt-accepted` at A,
  and A's requested obligation `receipted pinned=no`;
- the unauthorized run is `refused reason=source-not-carried`,
  `request-refused`, `no-receipt`, and A stays `restart-observed
  pinned=yes`;
- the never-requested control obligation stays `outstanding pinned=yes` in
  every run;
- a SIGKILL after `:submit` leaves `restart-observed pinned=yes`, and the
  re-request after a kill before the outcome is `refused`.

**Diff against 4eca4148.**
- **Newly pass or new:** operator_verbs 19/19 (+1 case; the D25 budget case
  now passes), history_marker 6/6, the marker cuts through the served entry
  20/20, the request labs 4/4, and the D25 review matrix 21/21.
- **Newly fail:** crash_model 1 test (2 cuts) and served_crash_model 1 test
  (2 cuts), F1; the campaign module's cross-entry assertion, F3; the INN lab's
  `operator-post-feeds-inn`, F2.
- **Unchanged failures:** bp_service 7 and receive_integrity 2 on the
  production DTN image; consumer_e2 with an undated source.

## The web reader by hand (production image, TLS, login required)

[`web-manual.py`](qual-c3420013/web-manual.py), 4eca4148's with the paths
changed; the log and every page are in
[`web-manual.tgz`](qual-c3420013/web-manual.tgz) and
[`web-manual.log`](qual-c3420013/web-manual.log). A scratch `fn.agents`
store on `fn-host` with a fresh self-signed certificate and `[auth] required
= true, protected_only = true`; `bin/fn principal set-password reader
--posting` rc 0; the owner LISTENING on 51011; `tools/fn_web.py` on 44837.

| page | what it showed |
| --- | --- |
| `/` before any post | "nothing unread", "No local articles yet · 0 articles · posting allowed"; "TLSv1.3, certificate verified" |
| root post, reply, second root | 303 → "Post accepted … The node's answer: 240 article received OK" each, with the Message-ID |
| `/` after three posts | "**3 unread**", "Local article numbers 1–3 · 3 articles"; the node's own `LIST COUNTS` over STARTTLS + AUTHINFO: `fn.agents 3 1 3 y` |
| `/g?name=fn.agents` | three rows threaded by References, the reply under its root |
| `/a?…number=1` | "node verdict: **absent**" (unsigned), the article |
| `POST /mark` through 1, then `/` | "**2 unread**", resume link at local #1 |

The owner exited 0 on SIGTERM and `fn_web` −15, both started by the script.

## Upgrade rehearsal: a 4eca4148 store opened by c3420013

[`upgrade.sh`](qual-c3420013/upgrade.sh), log
[`upgrade.log`](qual-c3420013/upgrade.log), stores in
[`upgrade.tgz`](qual-c3420013/upgrade.tgz). The live node was not used; the
store was made for this with the 4eca4148 production image
(`fn-host.core` `3411e52b…`) and copied with `cp -a`.

| step | result |
| --- | --- |
| 4eca4148: `store init`, owner, three `operator post` | 3 × `ACCEPTED`; 3 transactions; **no `committed-history.json`** |
| copy, first open on c3420013 (`store recover`) | rc 0, `recovered transactions=3 articles=3`; **still no marker**; the open writes nothing |
| c3420013 owner started | LISTENING; still no marker |
| one `operator post` | `ACCEPTED`, 4 transactions, **the marker appears**: 43 octets, `FNSM` frame, SHA-256 `652649b9…`, count 4 (it counts the three pre-upgrade records too) |
| next open (`store recover`), then the owner | rc 0, `transactions=4 articles=4`; owner up, `GROUP fn.letters` → `211 4 1 4` |
| delete `transactions/00000000000000000003.txn`, open | `store recover` **rc 4**, `committed history refused at open: history-short-of-marker marker=4 records=3`; `operator run` **exits 4** with the same line and never listens |
| the original 4eca4148 store | untouched, no marker |

The same run over a **packed** store ([`upgrade-packed.sh`](qual-c3420013/upgrade-packed.sh),
[`upgrade-packed.log`](qual-c3420013/upgrade-packed.log)) did `operator store
compact` on the 4eca4148 store first (`records=3 … reclaimed=3`, 0
transaction files, a `packs/` directory). It gives the same rows: first open
rc 0 without a marker; the first post writes a marker with count 4 (3 pack
events plus 1 file); the next open admits it; and a lost newest file is
refused `marker=4 records=3`.

**What the marker does on the live node's first open after deploy.** The
store has no marker, so it is admitted and nothing is written at open. The
first acknowledged commit writes the marker with the whole committed count.
From then on a lost newest record is refused at open. Between the deploy and
the first commit there is no detection, which is the stated limit of "a store
without one is admitted". The rehearsal used the default profile; the live
store runs the `scale` profile, which the marker does not depend on (it is
written in every profile, per the history-marker record). The marker is a
binary `FNSM` frame although its name ends `.json`.

## Failures, classified

Classes: (a) fn defect, (b) environment, (c) test or lab out of date, or run
on the wrong image.

1. **(a) F1: the byte-level crash model does not express the
   committed-history marker.** `test_native_crash_model`
   `test_all_native_post_process_death_cuts` and `test_native_served_crash_model`
   `test_served_prepare_publish_finish_cuts` fail at `finish-consumed` and
   `finish-durable` only, with `(T T T NIL T T T T (NIL T T NIL NIL))`
   (`tests/test_native_crash_model.py:285`).
   - What holds: the choices are admissible, the observed scan is ok, the
     store opens, the transaction names agree, the model scan equals the
     observed scan, and both open alike.
   - What fails: `fn-bso-served-image-agree` and the root-directory
     agreement. The transaction and staging directories agree.
   - Why: `fnn-mark-committed` writes `committed-history.json` into the root
     after the record is durable and before `fnn-finish`
     (`host/native/io.lisp`, called at all three publish sites). The finish
     cuts are after it. The marker's program exists only in
     `books/store-history-marker.lisp` (`fn-hm-*`). `fn-bs-finish-program`,
     whose `fn-bs-crash` image the differential compares with the real
     directory, has no such file, and `tests/campaign/native_cuts.py` lists
     none of the five marker cuts.
   - The history-marker lane ran `test_native_crash_correspondence` and
     `test_native_cut_map`, which pass, and not these two modules.
   - By the rule "every process-death cut is a model crash point", the host
     now has five cuts, and a root file, that the composed byte model cannot
     express. Either the fn-bs finish program gains the marker transition
     (and native_cuts the five cuts), or the P10 claim is narrowed to exclude
     them.
   - Runtime behaviour at those cuts is fine: marker cuts 20/20, campaign
     50/50, probe 40/40, the kill run 0 torn.
2. **(c) F2: INN lab `operator-post-feeds-inn`
   (`tools/inn_lab.py:1440`) requires `Injection-Date` on the fed article.**
   The lab's article supplies Date and Message-ID, so under recipe v2 fn adds
   Path and Injection-Info and no Injection-Date. RFC 5537 §3.5 item 11 says
   an Injection-Date "MUST NOT be added" in that case. The assertion predates
   the injection inverse (which the d25-injection-inverse lane ran without the
   INN lab). It makes `V0-INN-OPERATOR-POST` and `V0-INN-INTEROP` disagree.
   innd took the article (`IHAVE 335/235`).
3. **(c), with a contract question, F3: cross-entry retry.** The post-d25-tests
   lane updated `tests/campaign/test_native_operator_campaign.py:176` to
   expect `DUPLICATE`/`duplicate` rc 0 for one payload through the two
   entries, which is what the d25-dup four-field projection gave. The
   injection inverse, merged after it, compares sources only "when both
   articles give back a source … otherwise the payloads are compared octet
   for octet". A `store ROOT post` record has no injection block, so it gives
   no source, and the owner's injected copy then differs: conflict, rc 1,
   in both orders, with nothing written.
   - This follows the inverse's stated design. The entry is developer-only:
     production refuses raw `store post`.
   - Which answer is the contract is for the D25 owner, not this lane. The
     test and the design disagree.
4. **(c) F5: `test_bp_service_native` 7 and `test_bp_receive_integrity_native`
   2 on `fn-host-dtn`.** Developer selectors, refused by the production DTN
   image with exit 5 by design; both modules pass in full on
   `fn-host-dtn-developer`. Unchanged.
5. **(c) F6: `test_native_consumer_e2` signed poll with the undated source**
   (`tests/test_native_consumer_e2.py:298`). Passes with the dated source.
   Unchanged.
6. **(b) Skips.** consumer_e2's different-uid case needs root; the newnews and
   stamp migrations and 2 topic_local cases need a pre-T2 or pre-v2 image.

No failure stops the node from running correctly. F1 stops P10 from being
claimed on this image. F2 and F3 need their owners, not this lane; this lane
modified no node, campaign, probe, lab or test.

## Logs and digests

Everything is in [`qual-c3420013/`](qual-c3420013/), and
[`SHA256SUMS`](qual-c3420013/SHA256SUMS) covers every file:
- `native-logs.tgz` holds every module log;
- `owner-logs.tgz` holds the campaign, marker-cut, probe, D25-matrix and
  kill-run owner `.err` files;
- `matrix-node-logs.tgz`, `request-labs.tgz`, `web-manual.tgz`,
  `upgrade.tgz`, `upgrade-packed.tgz`;
- `campaign.json.gz`, `nntp-probe.json.gz`, `pk-run.json.gz`,
  `marker-cuts.json.gz`, `d25-rows.json.gz`, with the raw digests in
  [`raw.sha256`](qual-c3420013/raw.sha256).

On hbox, under `/tank/fn/scratch/qual-c3420013/`, are the work trees and
stores: `campaign/{work,probe-work,pk-work,marker-work,d25-work}`,
`matrix/stores`, `tree`, `ctree`, `reloc`, `web`, `request-labs`, `upgrade`,
`upgrade-packed`, `harvest`. The matrix deploy tree is
`~/fn-deploy/native-c3420013-e8f4faa/`.

After the runs:
- no process under the scratch path was alive;
- 11190, 11191, 11396, 11397, 11417 to 11420 and 11490 were free;
- `~/fn-deploy/.locks/` was empty ([`ss-final.txt`](qual-c3420013/ss-final.txt)).

The live node `/tank/fn/node` was not touched.

## What this does not establish

This is one run of each campaign on one box, over loopback, covering process
deaths on ZFS, not power loss. The per-cut, marker-cut and compaction-cut
evidence is coordinate-precise only on the developer twin (`b22251d8…`, against
production `7a124c6f…`). On production the evidence is the kill run's
store-state partition, the production compaction test and the upgrade
rehearsal. The upgrade rehearsal used a fresh 4eca4148 store of three
articles in the default profile, not a copy of the live store. The web pass
was by script over HTTP, not a browser. The transit rows show that the
history answers a Path-different offer before any byte comparison. No row
here is a theorem.
