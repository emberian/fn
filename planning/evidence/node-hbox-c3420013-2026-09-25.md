# The hbox node upgraded to c3420013 — 2026-09-25

The wave-three image and the fifth in-place upgrade of the running node in
this cycle, at about 01:15 UTC: the store (scale profile), the TLS pair, the
credentials and `fn.toml` stay; `fn-4eca4148`, `fn-18c91321`, `fn-47bdb9a4`
and `fn-da5fd8cb` stay beside the new release for a one-line rollback. The
node listened seven seconds after restart.

## The image

Sources at `dev` `c3420013`, frozen at `/tank/fn/gates/qual-c3420013-20260925`
(closure `certify-20260925T001652Z-2229194`, 430 certified, 0 failed).
Production core `7a124c6f…`, developer `b22251d8…`, DTN `f5b7bb36…`, DTN
developer `345357d2…`. Qualified by one lane:
[qual-c3420013](qual-c3420013-2026-09-25.md): matrix 140/46/2 (two INN rows
changed because the lab requires an Injection-Date fn now correctly omits
when Date and Message-ID were supplied), 25 cuts 50/50, compaction cuts
20/20, the five committed-history marker cuts 20/20, probe 40/40, the D25
review matrix 21/21, 206 killed production POSTs with 0 torn, 0 lost 240,
0 reused; the upgrade rehearsal on a copy of a 4eca4148 store.

Since 4eca4148 this image carries: the receipt-issuer authority separated
from carriage (a carried receipt releases only with a `releases-for` row),
the native `bp-obligation request` verb, the injection inverse (recipe v2;
no Injection-Date when Date and Message-ID were supplied; resends duplicates
at any clock), the committed-history marker, P5's fault domain, TCPCL
Completed on the sender, special-purpose group names by pattern, the
generated current view, the post-D25 tests and the peer list rows.

## The marker on the deployed store

The store had no `committed-history.json` before this deploy (every
earlier image predates it). The image admits such a store and writes the
marker at the first commit; the probe's POST below was that commit. After
it: 43 bytes

## The probe from a Mac on the LAN

`tools/node_probe.py 192.168.50.39 1119 --cafile <cert> --group fn.agents`
as `ember` ([probe.json](node-hbox-c3420013-2026-09-25/probe.json), SHA-256
`256fd247db74041d…`): exit 0.

## Not claimed

P10 ("every cut is a model crash point") is not cited for this image: the
byte crash model's finish program does not yet include the marker file,
so the differential fails at two finish cuts although every native cut
passed; a lane is adding the transition. No durability past process death
on this platform; no signature security; no peers configured; LAN-only;
the DTN profile is not deployed on it. The BP resume verbs, BP-R17 busy
delivery, the EID grammar and the stale-comment batch landed after this cut.
