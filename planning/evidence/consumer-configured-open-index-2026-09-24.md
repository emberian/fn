# Configured owner reopen restores consumer poll index

The actual native owner recovery call at `host/owner-host.lisp:176` invokes `fn-cpo-open-observed`, not the lower-level `fn-sn-open-observed`. On frozen source `b074f94ed3e612a6e71c18859a3af3c9112c3708`, the former reconstructed the exact event list, consumer projection and topic projection but left the derived event index empty. In the source-matched native E2 test, an ACK survived a killed owner (reopened position 4, frontier 5), then the next `consumer poll` returned `uncertain` while the owner stayed alive. The lower-level observed-open index theorem did not cover this caller.

`fn-cpo-open-observed` now installs `fn-cei-build events` in the recovered Store state. Its existing `fn-cpo-open-success-exact-image` theorem includes equality between the carried index and that ACL2 rebuild for every successful configured open. The configured-open test checks both the equality and indexed retrieval of the second exact event in a nonempty physical history. The derived index is rebuilt from the authoritative event directory; it is not persistent authority and no served poll scans the prefix to repair it.

ACL2 8.7, hbox w28 toolchain identity `d5f2b9f0d2cf68c6074ea7046f4bd2e560d2984fe22d7e03f93045975ac889f0`, scoped incremental run `run-20260924T060338Z-7255`, [manifest](manifests/certify-20260924T060348Z-986903.json): `books/config-observed` and `tests/acl2/config-observed-tests` both passed. This establishes the logical configured-open index projection and a concrete two-event witness. The repaired native image and post-reopen poll rerun remain pending; the old b074 image is known to fail that call. Static `make check` on this isolated older base is blocked by four unrelated checkpoint-preservation evidence manifests cited but absent locally; root has the newer integration source.
