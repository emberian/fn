# Ten-second books, fifth pass: the quiet re-measure, 2026-09-26

PKT-371 (retired), PKT-326 (a) (closed), PKT-478 (filed). No PRF, no
statement, name, rule class or exported rule changed; only `:hints` and
`:guard-hints` in `books/native-admin.lisp` and `books/native-operator.lisp`.

## The host, and why persvati

hbox's load average was 7.5 to 9.4 from 12:27 UTC (the b6759850
qualification at 2 h 43 min, native modules, a throughput gate). The
baseline's nine widened figures are all persvati measurements, and
`tools/proof_cost.py` keeps the newest measurement per book, host and
toolchain and takes the worst over hosts: a quiet hbox figure cannot
retire a loaded persvati one at unchanged bytes. The measurement is
therefore matched on persvati (w25 `acl2-literal`, cache
`/home/ember/fn-certcache`, 2 jobs, 300 s), started when its 1- and
5-minute load averages were both under 3.

- r1 `run-20260926T123916Z-b14e`, manifest
  [certify-20260926T123922Z-1828132](manifests/certify-20260926T123922Z-1828132.json),
  the dev bytes (5c6825b2) of every listed book, each a root with
  `--recertify`: 82 certified, 0 failed, 212 installed. Load before: 1.90 /
  1.93 / 2.37. During (1-minute): 2.7, 4.6, 4.9, 5.4, two of it this run's
  own jobs and the rest one other lane's 2-job certification that started
  at 12:38. No book was installed rather than certified.
- r2 `run-20260926T125338Z-e625`, manifest
  [certify-20260926T125425Z-1983585](manifests/certify-20260926T125425Z-1983585.json),
  the repaired bytes (f2eba85f), `--affected-by` native-admin and
  native-operator plus `--recertify books/anchor-replace`: 20 certified
  (the 17 roots of both books' includers and what lies between), 0
  failed, no book over 10 s. Load during: 1.4 to 2.5.

## The quiet table (PKT-371)

| Book | Quiet, 2 jobs (r1) | Loaded figure it replaces |
| --- | ---: | --- |
| books/native-admin | 11.54 s; 5.19 s repaired (r2) | 15.9 s (112155Z, baseline) |
| books/native-operator | 8.89 s; 6.08 s repaired (r2) | 11.8 s (112155Z, baseline) |
| books/owner-invariants | 11.19 s | 16.2 s (110838Z, baseline) |
| books/peer-authored-accept | 7.99 s | 11.5 s (110838Z, baseline) |
| books/peer-pull | 8.39 s | 10.3 s (112155Z, baseline) |
| books/peer-pull-session | 8.08 s | 11.7 s (112155Z, baseline) |
| books/public-exposure | 9.33 s | 10.6 s (110838Z, baseline); 12.4 s |
| books/topic-history-store-invariants | 7.94 s | 11.4 s (110838Z, baseline) |
| tests/acl2/source-routes-tests | 8.58 s | 10.2 s (110838Z, baseline) |
| books/store-node-invariants | 8.03 s | 11.29 s (deb2bd16) |
| books/byte-store-k0-step-bridge | 7.53 s | 11.4 s |
| books/config-owner-live | 7.98 s | 11.1 s (103616Z) |
| books/bp-node-job-offer | 3.42 s | 11.65 s (044558Z) |
| tests/acl2/login-binding-live-tests | 2.47 s | watch |
| books/owner-control-read | 6.58 s | 10.34 s (ten-second-4's run) |
| tests/acl2/native-admin-tests | 2.12 s; 1.87 s (r2) | watch |
| tests/acl2/native-operator-tests | 3.27 s; 2.27 s (r2) | watch |

Fifteen of the seventeen are under ten seconds quiet at unchanged bytes:
their loaded figures were load. Two were over: native-admin (bytes grown
by three lanes' grammar rows; repaired below) and owner-invariants (bytes
unchanged since ten-second-4; PKT-478).

## The baseline

`python3 tools/proof_cost.py --write-baseline` at r1 and r2 (no
`--allow-regression`): 9 books to 1. `books/owner-invariants` stays at its
quiet figure, 11.185 s, run certify-20260926T123922Z-1828132, persvati, 2
jobs. `proof_cost`: baseline 1 book, 0 failing.

## The repairs (examined in `tools/proof_repl.py` on persvati)

Per-event times from REPL loads of the whole book, persvati at load 2 to
3; profiles under `(accumulated-persistence t)`.

books/native-admin.lisp, event total 11.96 → 5.84 s; certify 11.54 → 5.19 s:
- `fn-native-admin-plan`'s guard proof took 2.95 s, 2.17 of it in the tau
  system (344,251 steps with and without it). `(tau-system)` is off in its
  `:guard-hints`: 3.11 → 0.75 s.
- The four theorems that open the plan (`fn-native-admin-plan-group-name-
  is-a-group-name`, local `plan-of-set-bp-boundary`,
  `fn-native-admin-plan-refuses-a-reserved-group-create`,
  `fn-native-admin-plan-create-ignores-special-purpose`) now also close tau,
  `fn-record-octets-string`, `fn-cbor-octet-listp` and
  `fn-digest-octetsp-implies-octet-listp` (56 k, 37 k and 61 k useless
  frames): 1.55 → 0.62, 1.33 → 0.55, 1.73 → 0.80 s. The plan is still
  opened about 390 times in the group-name proof (once per subgoal); a
  kind lemma for `fn-native-admin-peer-extend-plan` took 0.12 s more off
  and was not kept.

books/native-operator.lisp, event total 6.63 → 4.94 s; certify 8.89 → 6.08 s:
- the four local `fn-nop-parse-store-*-words` lemmas close
  `fn-nop-parse-profile-flags` (69 k useless frames): 0.33 → 0.04 s each;
- the four local `fn-nop-parse-command-*-words` lemmas close
  `fn-nop-profile-decimal`: 0.32 → 0.18 s (closing `fn-nop-parse-account`
  or `fn-nop-parse-peering` as well fails the proof: their results' command
  is needed);
- the four keystones `fn-native-operator-run-*-action-is-only-store-*`
  close seven rules tried uselessly (`fn-nntp-response-text-true-listp`,
  `fn-cp-idp`, `true-listp`, `fn-nntp-article-idp-is-consp`,
  `fn-nntp-response-textp`, `fn-cp-id-length-bound`,
  `fn-nntp-clean-line-is-response-text`): 0.31 → 0.18 s each;
- part of the certify drop is the include of `anchor-replace` now compiled
  (below).

No split, no new book, no timeout change, no theorem weakened. r2
certified every includer (native-mission, native-live-status,
native-health, host/native-operator-host and their tests) at the new
bytes.

## PKT-366 (the uncompiled include)

`defrecord` now installs with its compiled file: in r1,
`bp-node-job-offer`'s include of `bp-node-contact-driver` took 1.37 s
(4.18 s uncompiled in 044558Z) and the book 3.42 s. The fasl cache
(2026-09-25) is the fix; no tool change is needed for it. The remaining
class is different: entries published BEFORE the fasl cache carry no
`book.fasl`, and a book not recertified since installs uncompiled for
every includer. r1 had one (`anchor-replace`: all 17 cached entries
fasl-less), r2 one (`membership-epochs-invariants`). r2 recertified
`anchor-replace`, which published it with its fasl. The self-healing
rule (install-partial counting a fasl-less entry as a miss once) changes
every lane's run plan and is not a one-liner to take blind: PKT-478 (2).

## Not done (PKT-478)

1. owner-invariants 11.19 s quiet: PKT-326 (b)'s flat per-theorem tail.
   Not this lane's book to repair (its bytes did not change).
2. The fasl-less pre-cache entries (above).
