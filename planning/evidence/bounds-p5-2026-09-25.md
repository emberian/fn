# P5 (bounds): chained packs, 2026-09-25

Lane bounds-p5 (Opus 5.5). Commits e4c693cb, 361524a8, a4c1f1c9 and this one.
Design: planning/design-2026-09-25-bounds.md, packet P5.

## Status

- **Chained packs (task part 2): proved and certified.** books/checkpoint-pack-chain.lisp,
  the host walk and extension (host/native/checkpoint.lisp `fnn-pack-walk`,
  `fnn-pack-extend-chain`). `store compact` no longer refuses above 4096 transactions.
  The statements are in PRF-085 and specs/storage.md "Chained packs".
- **Constants into the profile (task part 1): not done.** The only change is that
  `*fn-cc-max-events*` and `*fn-cc-max-octets*` now bound one link, a scheduling quantum,
  and no longer bound the history. The ADU article bound, the pre-reservation figure 65,538,
  the frame `:blob` width, the namespace counts and the BP widths are unchanged. The
  LANEDUMP table gives each constant's old value, its intended home and its accessor.
- **Native (task part 3): not achieved.** The images were built on hbox. The N = 20,000 and
  N = 9,000 scenarios (tests/test_native_pack_chain.py) did not run: `store probe` of
  20,000 commits exceeds the test helper's subprocess timeout. They need a longer timeout
  (tmpfs), or a fixture copied from one probe run. The control-socket 1 MiB article
  depends on the blob width, which was not done.

## Certification

- persvati run-20260925T091418Z-19a2: `--affected-by books/checkpoint-compaction`, 2 jobs,
  w25 toolchain. Manifest
  planning/evidence/manifests/certify-20260925T091457Z-3109471.json. 12 roots passed; the
  largest book is checkpoint-pack-chain at 5.38 s. proof_cost: no book exceeds 10 s.
  `green_check --changed-since e366c633`: 6 changed and 6 dependent books, 0 not green.
- hbox image closure: w28 `acl2-literal-4g`, 8 jobs, incremental, default roots. 7
  certified, 317 from the cache (scratch evidence certify-20260925T091733Z-3012132).

## Native logs (hbox, images built from a4c1f1c9 plus the uncommitted test and host fixes of this commit)

| Log | sha256 |
| --- | --- |
| fn-host.core | 23774954eabd16b580871a8961eacba6a179fd294a5400fb2d3ed69eed9f4345 |
| fn-host-developer.core | d1b444e0633329b36680cce9f3fe15f59ff8f9d1d3b8d8a420b85b8738589515 |
| bounds-p5-2026-09-25/native-chain.log | 6424e17a599ca7370f73feec2bce53bbd37a2adafc66e781f301c8a52653bbd3 |
| bounds-p5-2026-09-25/native-checkpoint.log | 2972779eb861e5755d985f0c71dc84d14f851272a8fbb900bfbdaffff214e165 |

The developer build reports uncertified books-records-attach-concrete, hybrid-store-injected
and hybrid-lifecycle (developer-only includes, not this lane's). The core is written anyway.

Results of `tests.test_native_checkpoint` + `tests.test_native_compaction_crash_map`,
filtered to compact, pack, reclaim and retire (16 run):
- Passing: operator compact (a second compaction adds one link and retires nothing), the
  pack-retire death cuts, reclaim cuts, selection cuts.
- `test_retiring_old_pack_generations_keeps_exact_retained_sources` is **red**. Its last
  step runs `checkpoint pack` after a chain already covers the history. The capture then
  refuses, because there is nothing uncovered. The test still encodes the pre-chain
  semantics.
- `test_native_and_python_frames_cross_open_byte_identically` is red for an environmental
  reason: `acl2` is not on PATH for the Python store.

## Findings

1. Pack generation names stay below 4096 (`fn-cprt-next-generation`). A chain holds its
   names, so a store can be compacted at most 4096 times.
2. The link codec round trip is executed, not proved (as for the version-0 pack).
3. The chain publication crash keystone is stated under the two facts the pack program's
   keystones give. It is not composed with them by a theorem, and there is no K0 over the
   chain cuts.
4. The compact verb's space budget still counts one pack of the whole history, and the
   per-link header is not in `fn-cverb-capture-within-pack-octets`.
5. `checkpoint pack` with nothing uncovered is refused (exit 1) rather than reported as a
   no-op.
