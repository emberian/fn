# Cold FNBS replay arrival frontier

The combined `48425e4e` qualification found that `fn-bpnf-recover-ready-bytes-install-held` assumed a decoded `:ready` held list would always pass the new 64-bit arrival-frontier check. The old replay result did not state that bound, so the theorem was false for an arbitrary `:ready` logical value. `fn-bpnf-replay-rows` now returns `(:fault :arrival-frontier)` if its cold-folded held list exceeds `1 + *fn-frame-max-nat*`; the recovery step already refuses such a value. The new `fn-bpnf-replay-ready-frontier-bounded` theorem derives the bound from the exact byte replay function, and the existing installation theorem uses that fact without a new premise.

The test reaches `:ready` from two canonical kind-5 byte rows at an exact two-row capacity, then exercises the direct recovery event at the largest permitted logical arrival and one above it. The latter is a malformed logical replay value, not a witnessed canonical byte stream. In the current codec and sequential-arrival fold, the overflow-fault branch is marked `unreachable-in-composition`; this packet does not claim a separate proof of that unreachability. The frontier scan runs only during cold replay, outside the served event path.

The focused hbox run `run-20260923T233907Z-0b37` certified `books/bp-fnbs-replay-invariants` and `tests/acl2/bp-fnbs-replay-tests` with three books newly certified against matching cached dependencies. Its [manifest](manifests/certify-20260923T233915Z-508715.json) records ACL2 8.7 and toolchain identity `d5f2b9f0d2cf68c6074ea7046f4bd2e560d2984fe22d7e03f93045975ac889f0`. The broader BP image and native campaign remain pending the combined proof repair.
