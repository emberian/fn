# Maintained topic projection across Store publication and recovery

The new `fn-sti-livep` relates the carried topic projection to the exact committed Store prefix at `fn-sn-identity-next`, checks a staged topic candidate before publication, and checks the physical last event during `:completed`. These are proof-side scans, not served validation. `fn-thr-*` selector lemmas show that actual Store crash erases the topic projection and successful recovery or observed open rebuilds it from the physical history. The prefix book supplies append and rollback facts used by that relation.

Earlier scoped ACL2 8.7 hbox w28 evidence is split by source: [recovery selector manifest](manifests/certify-20260924T053007Z-944061.json) passed before the v2 topic change; [Store invariant manifest](manifests/certify-20260924T055537Z-975087.json) passed the v2 book through `fn-sti-io-preserves-live` before `fn-sti-finish-preserves-live` was added; [fresh v2 mixed crash witness manifest](manifests/certify-20260924T060055Z-981183.json) passed a bootstrap/register, authored source, generation-bound anchor/report/admission, actual Store crash/recover and observed-open test. The latter checks the consumer ACK remains unchanged and a wrong install generation refuses. The manifests contain exact source digests, toolchain identity, and root results; they do not certify this combined checkpoint.

The [current-source scoped manifest](manifests/certify-20260924T062919Z-1013298.json) passed `books/topic-history-store-invariants` and its test on hbox w28, with 74 compatible cached dependencies. It covers `fn-sti-finish-preserves-live`, actual retention/consumer/identity preparations, and reservation refusal as individually preserved live relations. Its test is a structural negative and the separate mixed v2 trace above; the new branch lemmas do not yet have independent hypothesis teeth and are not registry keystones.

The actual `fn-snrt-step` dispatcher still needs the `:known-abort` live step, the full crash/recover relation, and arbitrary mixed-trace preservation. A bounded REPL attempt for known abort exposed the precise missing obligation: when it enters `:completing`, the completion-record selector must be shown to denote the staged candidate whose topic prefix step is valid. The attempted general theorem was removed from the certified checkpoint. There is no maintained full-relation or crash-safe topic service claim from these component facts and the witness alone.
