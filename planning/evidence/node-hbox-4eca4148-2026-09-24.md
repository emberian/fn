# The hbox node upgraded to 4eca4148 — 2026-09-24

The wave-two image of the evening cycle and the fourth in-place upgrade of
the running node today, at about 23:45 UTC: the store (scale profile, 4096
budget), the TLS pair, the credentials and `fn.toml` stay; `fn-18c91321`,
`fn-47bdb9a4` and `fn-da5fd8cb` stay beside the new release for a one-line
rollback. The node listened seven seconds after restart.

## The image

Sources at `dev` `4eca4148`, frozen at `/tank/fn/gates/qual-4eca4148-20260924`
(closure `certify-20260924T224341Z-2081666`, 464 certified, 0 failed, at
4 jobs). Launchers byte-identical to the earlier images; production core
`3411e52b…`, developer `b94f959e…`, DTN `e2786d1c…`. Qualified by one lane:
[qual-4eca4148](qual-4eca4148-2026-09-24.md): matrix 142/44/2 with 0
disagreed and INN 33/33; 25 cuts 50/50 and the ten compaction cuts 20/20;
differential 7/7; probe 40/40 with every repost a duplicate; 100 killed
production POSTs with 0 torn, 0 lost 240, 0 reused; the D23 three-node
chain (relay `carried`, receiver `verified`, the independent verifier 0 and
3); web reader by hand with exact unread counts.

Since 18c91321 this image carries: D23 on both sides (the NNTP carried
verdict; the BP carried-source list, still with the collapsed receipt trust
that the issuer-authority merge after this cut replaces), D24 (no peer
event evaluates the whole-node recognizer), D25 (duplicate versus conflict
on the poster's bytes), LIST COUNTS and the numbered Message-ID answer,
`operator CONFIG store compact`, the ION replay fence, N07 and N11, K0 over
all 25 cut coordinates, the owner relation proved at recovery, the advance
projection carried, the ratchet at two jobs.

## The probe from a Mac on the LAN

`tools/node_probe.py 192.168.50.39 1119 --cafile <cert> --group fn.agents`
as `ember` ([probe.json](node-hbox-4eca4148-2026-09-24/probe.json), SHA-256
`b05eaf6621eef7d1…`): every step decided and held, exit 0.

## Not claimed

No durability past process death on this platform; no signature security;
no peers configured on the deployed node; the node is LAN-only; the DTN
profile is not deployed on it. The receipt-issuer authority, the native
request verb, the history marker and the standing-defaults precisions
landed after this cut and are not on the image.
