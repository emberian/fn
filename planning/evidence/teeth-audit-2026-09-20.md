# Teeth audit of `tests/acl2/`, 2026-09-20

Generated by `python3 tools/teeth_check.py --report`, which is
`--evaluate` (one ACL2 per test book, the book's own source with a `cw`
probe spliced in after the top-level form that holds each assertion, run
in the book's directory) followed by the static pass. Do not hand-edit:
regenerate it.

- tree: `w10/teeth-audit` at `7cd1d0b77434fcbdac4d5f9845c060beef2b0fab`, merged with dev `99a348c`
- ACL2 8.7, `/opt/homebrew/Cellar/acl2/8.7_6/bin/acl2`, this laptop,
  through `tools/acl2` (the machine-wide slot pool), one process at a time
- certificates: `python3 tools/certs.py install` into the worktree; a book
  whose dependency has no certificate is INCLUDED UNCERTIFIED, which loads
  its source with proofs skipped. Values are still the book's own; proofs
  are not evidence from this run and none is claimed.

5194 `assert-event`s in 96 test books: 525 guard-world audits and 4669 witnesses.

72 keystones in `planning/proofs.json` have two or more hypotheses and a test book names 12 of them, so AGENTS.md's one-must-fail-per-hypothesis rule is unchecked for 60.

| check | count |
| --- | --- |
| assertion-false | 3 |
| book-does-not-run | 3 |
| both-sides-degenerate | 4 |
| empty-collection | 6 |
| hypotheses-without-teeth | 1 |
| keystone-without-witness | 19 |
| no-subject | 8 |
| predicate-never-anchored | 37 |
| recogniser-never-true | 37 |
| unevaluable | 141 |

## assertion-false (3)

- `tests/acl2/bp-bundle-tests.lisp:185` the body evaluates to NIL in this world: (equal (fn-cbor-result-value (fn-bpb-decode-block (list 133 1 1 0 0 26 255 255 255 255))) :limit)
- `tests/acl2/bp-bundle-tests.lisp:196` the body evaluates to NIL in this world: (equal (fn-bpb-decode (list 159 255) *fn-bpb-max-input*) (fn-cbor-error :primary-block-refused))
- `tests/acl2/checkpoint-codec-tests.lisp:204` the body evaluates to NIL in this world: (not (fn-cpc-validp *cpc-value* *cpc-groups* 10 (list *cpc-r0-bad-generation*)))

## book-does-not-run (3)

- `tests/acl2/bp-bundle-tests.lisp:0` the survey saw ACL2 refuse 2 form(s); the first is an error in ASSERT-EVENT. Everything a refused form defines is MISSING below it, so the assertions after it are not weak, they are INERT (2x ASSERT-EVENT)
- `tests/acl2/checkpoint-codec-tests.lisp:0` the survey saw ACL2 refuse 1 form(s); the first is an error in ASSERT-EVENT. Everything a refused form defines is MISSING below it, so the assertions after it are not weak, they are INERT (1x ASSERT-EVENT)
- `tests/acl2/owner-tests.lisp:0` the survey saw ACL2 refuse 264 form(s); the first is an error in DEFTHEORY. Everything a refused form defines is MISSING below it, so the assertions after it are not weak, they are INERT (151x ASSERT-EVENT, 108x DEFCONST, 2x DEFTHEORY, 2x DEFUN)

## both-sides-degenerate (4)

- `tests/acl2/index-tests.lisp:71` both sides of the equality are NIL; the claim distinguishes nothing: (equal (fn-index-query-range *index-built* "missing" 1 9) (fn-index-reference-range "missing" 1 9 *i
- `tests/acl2/store-node-teeth-tests.lisp:113` both sides of the equality are NIL; the claim distinguishes nothing: (equal (fn-sf-successes (fn-sn-files (fn-sn-finish *snt-prepared*))) (fn-sf-successes (fn-sn-files *
- `tests/acl2/stx-transit-tests.lisp:303` both sides of the equality are NIL; the claim distinguishes nothing: (equal (fn-pol-current (fn-stx-lace-of-store (fn-stx-accept-batch nil *stxt-hostile-batch*) *stxt-ke
- `tests/acl2/stx-transit-tests.lisp:407` both sides of the equality are NIL; the claim distinguishes nothing: (equal (fn-me-chain (fn-me-site-merge *stxt-site* (fn-stx-commits-of-batch *stxt-commit-batch* *stxt

## empty-collection (6)

- `tests/acl2/container-tests.lisp:229` `member-equal` is applied to an EMPTY collection, so the claim holds of nothing: (member-equal *ct-id-c* (fn-ct-article-deps *ct-s-provider*))
- `tests/acl2/container-tests.lisp:244` `member-equal` is applied to an EMPTY collection, so the claim holds of nothing: (member-equal *ct-a* (fn-ct-conflict-evidence (list *ct-b*) (list *ct-a* *ct-a-prime*)))
- `tests/acl2/membership-epochs-tests.lisp:269` `member-equal` is applied to an EMPTY collection, so the claim holds of nothing: (member-equal *me-dave-3* (fn-me-held (fn-me-receive *me-site-a* *me-dave-3*)))
- `tests/acl2/membership-epochs-tests.lisp:341` `member-equal` is applied to an EMPTY collection, so the claim holds of nothing: (member-equal (fn-me-message "alice" 1 "x") (fn-me-held (fn-me-receive *me-site-a* (fn-me-message "a
- `tests/acl2/nntp-auth-tests.lisp:348` `member-equal` is applied to an EMPTY collection, so the claim holds of nothing: (member-equal (fn-auth-starttls-effect) (au-reply (fn-post-result-session *au-starttls*) "STARTTLS")
- `tests/acl2/scheduler-tests.lisp:447` `member-equal` is applied to an EMPTY collection, so the claim holds of nothing: (member-equal "work-big" (fn-sched-aged *sched-ss*))

## hypotheses-without-teeth (1)

- `tests/acl2/bp-receiver-evolving-tests.lisp:352` `fn-bprv-evolving-invariant-survives-observed-reopen` (PRF-007) has 2 hypotheses and the section citing it carries 1 assert-event(s); AGENTS.md wants one violating value per hypothesis. Section boundaries are the `; <name>` convention, so this is a floor, not a verdict

## keystone-without-witness (19)

- `planning/proofs.json:0` PRF-001 (in-progress): 28 of 28 events are named in no test book: fn-bp-initial-state-is-binding-state, fn-bp-step-preserves-binding-state, fn-bp-trace-preserves-binding-state, fn-bprv-actual-finite-replay-preserves-invariant, fn-bprv-actual-record-preserves-invariant, fn-bprv-apply-record-preserves-evolving-invariant ...
- `planning/proofs.json:0` PRF-002 (in-progress): 2 of 5 events are named in no test book: fn-durable-completion-installs-exact-pending-article, fn-state-has-fresh-local-numbers
- `planning/proofs.json:0` PRF-003 (in-progress): 3 of 3 events are named in no test book: fn-complete-preserves-existing-message-id-binding, fn-duplicate-accepted-prepare-is-no-op, fn-recover-preserves-existing-message-id-binding
- `planning/proofs.json:0` PRF-004 (in-progress): 3 of 6 events are named in no test book: fn-retain-admit-preserves-statep, fn-retain-release-preserves-known-identity, fn-retain-release-preserves-statep
- `planning/proofs.json:0` PRF-005 (in-progress): 7 of 12 events are named in no test book: fn-bpa-round-trip, fn-bpa-success-is-canonical, fn-cbor-byte-encoding-are-octets, fn-cbor-byte-encoding-fits-input-bound, fn-cbor-byte-string-round-trip, fn-cbor-decode-reencode-prefix ...
- `planning/proofs.json:0` PRF-006 (in-progress): 9 of 12 events are named in no test book: fn-served-feed-of-append, fn-served-reply-stream-is-partition-independent, fn-served-step-partition-independence, fn-wire-feed-byte-preserves-statep, fn-wire-feed-proper-append, fn-wire-next-closed-noop ...
- `planning/proofs.json:0` PRF-007 (in-progress): 31 of 40 events are named in no test book: fn-bpr-live-receipt-regenerated-after-restart, fn-bprv-replay-no-receipt-before-committed-decision, fn-bprv-replay-rest-preserves-receipt-adu, fn-bprv-successful-replay-has-invariant, fn-replay-advance-preserves-node-statep, fn-replay-apply-record-non-nil-is-node-state ...
- `planning/proofs.json:0` PRF-008 (in-progress): 3 of 3 events are named in no test book: fn-checkpoint-admissible-capture-is-exact, fn-checkpoint-admissible-capture-value-is-valid, fn-checkpoint-plus-suffix-equals-full-replay
- `planning/proofs.json:0` PRF-010 (in-progress): 2 of 2 events are named in no test book: fn-index-range-query-correct, fn-index-rebuild-correspondence
- `planning/proofs.json:0` PRF-011 (in-progress): 11 of 12 events are named in no test book: fn-exchange-admitted-order-member, fn-exchange-merge-commutative-member, fn-exchange-merge-idempotent, fn-exchange-policy-ingest-trace-preserves-facts, fn-exchange-policy-ingest-trace-preserves-statep, fn-transfer-add-chunk-preserves-statep ...
- `planning/proofs.json:0` PRF-012 (in-progress): 20 of 24 events are named in no test book: fn-bp-initial-state-is-binding-state, fn-bp-step-preserves-binding-state, fn-bp-step-preserves-node, fn-bp-step-preserves-state, fn-bp-trace-preserves-binding-state, fn-bp-trace-preserves-state ...
- `planning/proofs.json:0` PRF-014 (in-progress): 1 of 5 events are named in no test book: fn-sn-extended-history-equals-live-completion
- `planning/proofs.json:0` PRF-015 (in-progress): 9 of 14 events are named in no test book: fn-nntp-closed-step-has-no-effects, fn-nntp-command-effects-well-formed, fn-nntp-finite-trace-preserves-consistent-session, fn-nntp-listgroup-unknown-preserves-session, fn-nntp-msgid-preserves-session, fn-nntp-opened-finite-trace-is-consistent ...
- `planning/proofs.json:0` PRF-016 (in-progress): 23 of 25 events are named in no test book: fn-article-parse-work-input-bound, fn-article-parse-work-profile-bound, fn-article-parse-work-value, fn-article-successful-parse-component-bounds, fn-article-successful-parse-syntax-p, fn-cbor-byte-encoding-fits-input-bound ...
- `planning/proofs.json:0` PRF-019 (planned): 2 of 4 events are named in no test book: fn-stx-detached-accepted-input-is-canonical, fn-stx-detached-round-trip
- `planning/proofs.json:0` PRF-020 (planned): 4 of 4 events are named in no test book: fn-stx-verdict-is-typed, fn-stx-verdict-records-its-keyring-generation, fn-stx-verified-implies-signature-over-own-octets, fn-stx-verified-is-the-statement-the-article-carries
- `planning/proofs.json:0` PRF-023 (in-progress): 4 of 4 events are named in no test book: fn-stx-index-bindings-agree, fn-stx-index-equivocators-agree, fn-stx-index-invariant-preserved-by-accept, fn-stx-index-slots-agree
- `planning/proofs.json:0` PRF-025 (in-progress): 2 of 2 events are named in no test book: fn-stx-commit-decode-is-a-commit, fn-stx-commits-of-batch-are-verified-and-well-formed
- `planning/proofs.json:0` PRF-030 (in-progress): 9 of 9 events are named in no test book: fn-prov-of-wire-of-a-durable-wire, fn-prov-of-wire-of-a-plain-string, fn-prov-of-wire-of-wire, fn-prov-render-of-bp, fn-prov-render-of-legacy, fn-prov-render-of-local ...

## no-subject (8)

- `tests/acl2/assumptions-tests.lisp:38` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (member-equal 1 (append '(1 2) nil))
- `tests/acl2/assumptions-tests.lisp:65` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (member-equal :unit-a (list :unit-a :unit-b))
- `tests/acl2/assumptions-tests.lisp:93` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (equal (nth 0 '(:a :b)) :a)
- `tests/acl2/assumptions-tests.lisp:122` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (not (consp nil))
- `tests/acl2/assumptions-tests.lisp:141` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (member-equal '(:origin 1 7) '((:origin 1 7)))
- `tests/acl2/assumptions-tests.lisp:158` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (not (consp nil))
- `tests/acl2/assumptions-tests.lisp:181` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (natp 0)
- `tests/acl2/identity-tests.lisp:175` the body names nothing this tree defines and the next assertion does not use it, so it exercises ACL2 and not fn: (natp 32768)

## predicate-never-anchored (37)

- `tests/acl2/nntp-auth-tests.lisp:445` `fn-auth-config-requiredp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/nntp-auth-tests.lisp:445)
- `tests/acl2/bp-primary-tests.lisp:324` `fn-bpc-shapep` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/bp-primary-tests.lisp:324, tests/acl2/bp-primary-tests.lisp:567)
- `tests/acl2/bp-primary-tests.lisp:74` `fn-bpc-valuep` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/bp-primary-tests.lisp:74)
- `tests/acl2/bp-fragment-tests.lisp:124` `fn-bpf-fragmentp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/bp-fragment-tests.lisp:124)
- `tests/acl2/bp-fragment-tests.lisp:173` `fn-bpp-identifiablep` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/bp-fragment-tests.lisp:173, tests/acl2/bp-primary-tests.lisp:186)
- `tests/acl2/byte-store-tests.lisp:166` `fn-bs-dir-quietp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/byte-store-tests.lisp:166)
- `tests/acl2/config-tests.lisp:300` `fn-cfg-entry-livep` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/config-tests.lisp:300)
- `tests/acl2/clock-tests.lisp:47` `fn-clock-may-drop-local-copyp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/clock-tests.lisp:47)
- `tests/acl2/checkpoint-codec-tests.lisp:117` `fn-cpc-result-okp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/checkpoint-codec-tests.lisp:117)
- `tests/acl2/container-tests.lisp:135` `fn-ct-article-shapep` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/container-tests.lisp:135)
- `tests/acl2/container-tests.lisp:291` `fn-ct-unknowns-okp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/container-tests.lisp:291)
- `tests/acl2/crypto-seam-tests.lisp:173` `fn-digest-tagp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/crypto-seam-tests.lisp:173)
- `tests/acl2/exchange-tests.lisp:118` `fn-exchange-authorizedp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/exchange-tests.lisp:118)
- `tests/acl2/exchange-tests.lisp:125` `fn-exchange-batch-validp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/exchange-tests.lisp:125)
- `tests/acl2/exchange-tests.lisp:110` `fn-exchange-factp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/exchange-tests.lisp:110)
- `tests/acl2/exchange-tests.lisp:111` `fn-exchange-policyp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/exchange-tests.lisp:111)
- `tests/acl2/peer-feed-tests.lisp:197` `fn-feed-takethis-linep` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/peer-feed-tests.lisp:197)
- `tests/acl2/index-tests.lisp:81` `fn-index-completep` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/index-tests.lisp:108, tests/acl2/index-tests.lisp:81)
- `tests/acl2/nntp-tests.lisp:825` `fn-nntp-block-textp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/nntp-tests.lisp:825)
- `tests/acl2/nntp-auth-tests.lisp:464` `fn-nntp-command-inputp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/nntp-auth-tests.lisp:464)
- `tests/acl2/nntp-tests.lisp:802` `fn-nntp-range-okp` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/nntp-tests.lisp:802, tests/acl2/nntp-tests.lisp:803)
- `tests/acl2/node-tests.lisp:179` `fn-node-binding-listp` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/node-tests.lisp:179, tests/acl2/node-tests.lisp:183)
- `tests/acl2/node-tests.lisp:325` `fn-node-eventp` is asserted FALSE 3 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/node-tests.lisp:325, tests/acl2/node-tests.lisp:326, tests/acl2/node-tests.lisp:327)
- `tests/acl2/owner-feed-tests.lisp:71` `fn-own-feed-boundp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/owner-feed-tests.lisp:71)
- `tests/acl2/records-tests.lisp:26` `fn-record-ascii-stringp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/records-tests.lisp:26)
- `tests/acl2/relay-tests.lisp:202` `fn-relay-kindp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/relay-tests.lisp:202)
- `tests/acl2/replay-tests.lisp:51` `fn-replay-advance-okp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/replay-tests.lisp:51)
- `tests/acl2/retention-tests.lisp:245` `fn-retain-known-idp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/retention-tests.lisp:245)
- `tests/acl2/scheduler-tests.lisp:218` `fn-sched-drive-okp` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/scheduler-tests.lisp:218, tests/acl2/scheduler-tests.lisp:432)
- `tests/acl2/scheduler-tests.lisp:404` `fn-sched-submit-for-idp` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/scheduler-tests.lisp:404, tests/acl2/scheduler-tests.lisp:448)
- `tests/acl2/store-observed-tests.lisp:52` `fn-sf-history-recoverablep` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/store-observed-tests.lisp:52, tests/acl2/store-observed-traces-tests.lisp:29)
- `tests/acl2/bp-receiver-evolving-tests.lisp:197` `fn-sf-prefixp` is asserted FALSE 3 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/bp-receiver-evolving-tests.lisp:197, tests/acl2/store-files-teeth-tests.lisp:133, tests/acl2/store-files-teeth-tests.lisp:136)
- `tests/acl2/store-node-guards-tests.lisp:114` `fn-sn-known-abort-enabledp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/store-node-guards-tests.lisp:114)
- `tests/acl2/store-node-guards-tests.lisp:112` `fn-sn-refuse-reservation-enabledp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/store-node-guards-tests.lisp:112)
- `tests/acl2/transfer-tests.lisp:301` `fn-transfer-present-atp` is asserted FALSE 1 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/transfer-tests.lisp:301)
- `tests/acl2/store-files-exploration-tests.lisp:399` `sfe-crash-changes-frontier-at-phasep` is asserted FALSE 2 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/store-files-exploration-tests.lisp:399, tests/acl2/store-files-exploration-tests.lisp:401)
- `tests/acl2/store-files-exploration-tests.lisp:409` `sfe-crash-changes-records-at-phasep` is asserted FALSE 4 times across the corpus and never asserted TRUE anywhere, so a definition that is constantly false satisfies every one of them (tests/acl2/store-files-exploration-tests.lisp:409, tests/acl2/store-files-exploration-tests.lisp:411, tests/acl2/store-files-exploration-tests.lisp:413)

## recogniser-never-true (37)

- `tests/acl2/nntp-auth-tests.lisp:445` `fn-auth-config-requiredp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-auth-config-requiredp *au-open*)
- `tests/acl2/bp-primary-tests.lisp:324` `fn-bpc-shapep` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-bpc-shapep :item 7)
- `tests/acl2/bp-primary-tests.lisp:74` `fn-bpc-valuep` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-bpc-valuep (cons :uint 18446744073709551616))
- `tests/acl2/bp-fragment-tests.lisp:124` `fn-bpf-fragmentp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-bpf-fragmentp '(:fn-bp-fragment 6 (70 80 90) 8))
- `tests/acl2/bp-fragment-tests.lisp:173` `fn-bpp-identifiablep` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-bpp-identifiablep *bpf-anon*)
- `tests/acl2/byte-store-tests.lisp:166` `fn-bs-dir-quietp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-bs-dir-quietp *bst-linked* :transactions)
- `tests/acl2/config-tests.lisp:300` `fn-cfg-entry-livep` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-cfg-entry-livep (fn-cfg-group-find *cfg-t-late-entry* "fn.late") 0)
- `tests/acl2/clock-tests.lisp:47` `fn-clock-may-drop-local-copyp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-clock-may-drop-local-copyp (fn-clock-expiry-decision 1600000000000 2000 nil *clock-obs
- `tests/acl2/checkpoint-codec-tests.lisp:117` `fn-cpc-result-okp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-cpc-result-okp (fn-cpc-decode (take (- (len *cpc-octets*) 1) *cpc-octets*) *cpc-groups
- `tests/acl2/container-tests.lisp:135` `fn-ct-article-shapep` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-ct-article-shapep *ct-oversize* *ct-profile*)
- `tests/acl2/container-tests.lisp:291` `fn-ct-unknowns-okp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-ct-unknowns-okp (list *ct-big-unknown*) *ct-profile*)
- `tests/acl2/crypto-seam-tests.lisp:173` `fn-digest-tagp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-digest-tagp '(300))
- `tests/acl2/exchange-tests.lisp:118` `fn-exchange-authorizedp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-exchange-authorizedp "auth" 7)
- `tests/acl2/exchange-tests.lisp:125` `fn-exchange-batch-validp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-exchange-batch-validp '(7 . tail) 7)
- `tests/acl2/exchange-tests.lisp:110` `fn-exchange-factp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-exchange-factp '(1 :object . tail))
- `tests/acl2/exchange-tests.lisp:111` `fn-exchange-policyp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-exchange-policyp '((1 . tail) ("auth")))
- `tests/acl2/peer-feed-tests.lisp:197` `fn-feed-takethis-linep` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-feed-takethis-linep (fn-frame-item 2 (car *ff-restart-fx*)) *ff-b*)
- `tests/acl2/index-tests.lisp:81` `fn-index-completep` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-index-completep *index-omitted* *index-articles*)
- `tests/acl2/nntp-tests.lisp:825` `fn-nntp-block-textp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-nntp-block-textp (list (fn-nntp-hdr-line '(65 13 66) (fn-nntp-hdr-octets (fn-nntp-hdr-
- `tests/acl2/nntp-auth-tests.lisp:464` `fn-nntp-command-inputp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-nntp-command-inputp *au-nul-line*)
- `tests/acl2/nntp-tests.lisp:802` `fn-nntp-range-okp` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-nntp-range-okp (fn-nntp-parse-range '(45 49)))
- `tests/acl2/node-tests.lisp:179` `fn-node-binding-listp` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-node-binding-listp (list (fn-node-make-binding "<same>" "content-a" "archive-a") (fn-n
- `tests/acl2/node-tests.lisp:325` `fn-node-eventp` came back NIL at all 3 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-node-eventp '(:unknown 1 2 3))
- `tests/acl2/owner-feed-tests.lisp:71` `fn-own-feed-boundp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-own-feed-boundp "nodeD" *oft-tbl*)
- `tests/acl2/records-tests.lisp:26` `fn-record-ascii-stringp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-record-ascii-stringp (coerce (list (code-char 128)) 'string))
- `tests/acl2/relay-tests.lisp:202` `fn-relay-kindp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-relay-kindp :destination)
- `tests/acl2/replay-tests.lisp:51` `fn-replay-advance-okp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-replay-advance-okp '(a . b) 0)
- `tests/acl2/retention-tests.lisp:245` `fn-retain-known-idp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-retain-known-idp "archive-2" (fn-retain-pins *ret-teeth-two-pins*) (fn-retain-releases
- `tests/acl2/scheduler-tests.lisp:218` `fn-sched-drive-okp` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-sched-drive-okp *sched-ss* *sched-wf-fenced* "attempt:0")
- `tests/acl2/scheduler-tests.lisp:404` `fn-sched-submit-for-idp` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-sched-submit-for-idp "work-big" (fn-sched-result-effects (fn-sched-tick-step *sched-pr
- `tests/acl2/store-observed-tests.lisp:52` `fn-sf-history-recoverablep` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-sf-history-recoverablep *fn-so-groups* 10 (list *fn-so-alien*) 1)
- `tests/acl2/bp-receiver-evolving-tests.lisp:197` `fn-sf-prefixp` came back NIL at all 3 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-sf-prefixp (list *bpr-record*) (list *bpre-record2*))
- `tests/acl2/store-node-guards-tests.lisp:114` `fn-sn-known-abort-enabledp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-sn-known-abort-enabledp 7)
- `tests/acl2/store-node-guards-tests.lisp:112` `fn-sn-refuse-reservation-enabledp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-sn-refuse-reservation-enabledp 7 #c (1 2))
- `tests/acl2/transfer-tests.lisp:301` `fn-transfer-present-atp` came back NIL at all 1 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (fn-transfer-present-atp 3 (fn-transfer-entry-chunks (fn-transfer-find-entry *fn-transfer-
- `tests/acl2/store-files-exploration-tests.lisp:399` `sfe-crash-changes-frontier-at-phasep` came back NIL at all 2 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (sfe-crash-changes-frontier-at-phasep :frontier-staged (sfe-result-edges *sfe-result*))
- `tests/acl2/store-files-exploration-tests.lisp:409` `sfe-crash-changes-records-at-phasep` came back NIL at all 4 evaluated claims in the corpus and non-NIL at none, so a definition that is constantly false passes every one: (sfe-crash-changes-records-at-phasep :record-staged (sfe-result-edges *sfe-result*))

## unevaluable (141)

- `tests/acl2/owner-tests.lisp:271` the body did not evaluate: (equal (fn-own-step *own-clocked* (list :observe *own-obs-backwards*)) *own-clocked*)
- `tests/acl2/owner-tests.lisp:274` the body did not evaluate: (equal (fn-own-replay-facts (fn-own-facts *own-declared*)) '("fn.new"))
- `tests/acl2/owner-tests.lisp:275` the body did not evaluate: (equal (fn-own-group-fact-stamp (car (fn-own-facts *own-declared*))) *own-obs*)
- `tests/acl2/owner-tests.lisp:276` the body did not evaluate: (fn-own-relation (fn-own-step *own-declared* (list :observe *own-obs-later*)))
- `tests/acl2/owner-tests.lisp:313` the body did not evaluate: (equal (fn-own-take 4 (fn-served-reply-octets (car *own-440*))) (fn-nntp-string-octets "440 "))
- `tests/acl2/owner-tests.lisp:315` the body did not evaluate: (null (fn-own-queue (cdr *own-440*)))
- `tests/acl2/owner-tests.lisp:320` the body did not evaluate: (fn-own-relation *own-p0*)
- `tests/acl2/owner-tests.lisp:322` the body did not evaluate: (equal (fn-own-conn-observation (fn-own-find-conn 4 (fn-own-conns *own-p1*))) *own-post-obs*)
- `tests/acl2/owner-tests.lisp:324` the body did not evaluate: (equal (fn-own-conn-config (fn-own-find-conn 4 (fn-own-conns *own-p1*))) *own-config*)
- `tests/acl2/owner-tests.lisp:326` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 3 (fn-own-conns *own-p1*))) 2)
- `tests/acl2/owner-tests.lisp:331` the body did not evaluate: (equal (fn-own-take 4 (fn-served-reply-octets (car *own-offer*))) (fn-nntp-string-octets "340 "))
- `tests/acl2/owner-tests.lisp:334` the body did not evaluate: (null (fn-served-reply-octets (car *own-submitted*)))
- `tests/acl2/owner-tests.lisp:335` the body did not evaluate: (fn-inj-injectedp (fn-served-submission (car *own-submitted*)))
- `tests/acl2/owner-tests.lisp:337` the body did not evaluate: (fn-own-relation *own-q*)
- `tests/acl2/owner-tests.lisp:338` the body did not evaluate: (equal (len (fn-own-queue *own-q*)) 1)
- `tests/acl2/owner-tests.lisp:339` the body did not evaluate: (equal (fn-own-sub-id (car (fn-own-queue *own-q*))) 4)
- `tests/acl2/owner-tests.lisp:340` the body did not evaluate: (equal (fn-own-sub-version (car (fn-own-queue *own-q*))) 2)
- `tests/acl2/owner-tests.lisp:341` the body did not evaluate: (null (fn-own-inflight *own-q*))
- `tests/acl2/owner-tests.lisp:342` the body did not evaluate: (null (car (fn-own-outcome *own-q* 4 :durable)))
- `tests/acl2/owner-tests.lisp:346` the body did not evaluate: (null (fn-own-queue *own-taken*))
- `tests/acl2/owner-tests.lisp:347` the body did not evaluate: (equal (fn-own-sub-id (fn-own-inflight *own-taken*)) 4)
- `tests/acl2/owner-tests.lisp:348` the body did not evaluate: (equal (fn-own-sub-mark (fn-own-inflight *own-taken*)) 2)
- `tests/acl2/owner-tests.lisp:349` the body did not evaluate: (equal (fn-own-pending *own-taken*) 4)
- `tests/acl2/owner-tests.lisp:351` the body did not evaluate: (equal (fn-own-take-submission (fn-own-enqueue *own-taken* (fn-own-sub-make 3 2 nil nil))) (fn-own-e
- `tests/acl2/owner-tests.lisp:356` the body did not evaluate: (equal (fn-own-outcome-completion *own-taken* :durable) :uncertain)
- `tests/acl2/owner-tests.lisp:357` the body did not evaluate: (equal (fn-own-take 4 (fn-served-reply-octets (car *own-early*))) (fn-nntp-string-octets "441 "))
- `tests/acl2/owner-tests.lisp:363` the body did not evaluate: (fn-own-relation *own-p-done*)
- `tests/acl2/owner-tests.lisp:364` the body did not evaluate: (equal (len (fn-own-ledger *own-p-done*)) 3)
- `tests/acl2/owner-tests.lisp:365` the body did not evaluate: (equal (fn-own-view-version (fn-own-view *own-p-done*)) 3)
- `tests/acl2/owner-tests.lisp:366` the body did not evaluate: (equal (fn-own-outcome-completion *own-p-done* :durable) :durable)
- `tests/acl2/owner-tests.lisp:368` the body did not evaluate: (equal (fn-served-reply-octets (car *own-240*)) (append (fn-nntp-string-octets "240 article received
- `tests/acl2/owner-tests.lisp:370` the body did not evaluate: (null (fn-own-inflight (cdr *own-240*)))
- `tests/acl2/owner-tests.lisp:371` the body did not evaluate: (null (fn-own-pending (cdr *own-240*)))
- `tests/acl2/owner-tests.lisp:372` the body did not evaluate: (fn-own-relation (cdr *own-240*))
- `tests/acl2/owner-tests.lisp:380` the body did not evaluate: (fn-own-conn-boundedp (fn-own-find-conn 4 (fn-own-conns *own-p-done*)) (fn-sn-groups (fn-own-store *
- `tests/acl2/owner-tests.lisp:384` the body did not evaluate: (not (equal (fn-own-conns (cdr *own-240*)) (fn-own-conns *own-p-done*)))
- `tests/acl2/owner-tests.lisp:386` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 4 (fn-own-conns (cdr *own-240*)))) (fn-own-view-versio
- `tests/acl2/owner-tests.lisp:389` the body did not evaluate: (equal (fn-own-conn-archive (fn-own-find-conn 4 (fn-own-conns (cdr *own-240*)))) (fn-own-view-archiv
- `tests/acl2/owner-tests.lisp:393` the body did not evaluate: (equal (fn-own-find-conn 0 (fn-own-conns (cdr *own-240*))) (fn-own-find-conn 0 (fn-own-conns *own-p-
- `tests/acl2/owner-tests.lisp:395` the body did not evaluate: (null (car (fn-own-outcome *own-p-done* 3 :durable)))
- `tests/acl2/owner-tests.lisp:397` the body did not evaluate: (equal (fn-served-reply-octets (car (fn-own-outcome *own-p-done* 4 :refused))) (append (fn-nntp-stri
- `tests/acl2/owner-tests.lisp:400` the body did not evaluate: (not (equal (car (fn-own-outcome *own-p-done* 4 :refused)) (car (fn-own-outcome *own-p-done* 4 :unce
- `tests/acl2/owner-tests.lisp:402` the body did not evaluate: (not (equal (car (fn-own-outcome *own-p-done* 4 :uncertain)) (car *own-240*)))
- `tests/acl2/owner-tests.lisp:403` the body did not evaluate: (equal (car (fn-own-outcome *own-p-done* 4 :fault)) (car (fn-own-outcome *own-p-done* 4 :uncertain))
- `tests/acl2/owner-tests.lisp:409` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 3 (fn-own-conns *own-after-post*))) 2)
- `tests/acl2/owner-tests.lisp:410` the body did not evaluate: (equal (fn-served-reply-octets (car (fn-own-read *own-after-post* 3 *own-group-octets*))) (append (f
- `tests/acl2/owner-tests.lisp:413` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 5 (fn-own-conns *own-late*))) 3)
- `tests/acl2/owner-tests.lisp:414` the body did not evaluate: (equal (fn-served-reply-octets (car (fn-own-read *own-late* 5 *own-group-octets*))) (append (fn-nntp
- `tests/acl2/owner-tests.lisp:416` the body did not evaluate: (equal (fn-own-take 2 (fn-sf-records (fn-sn-files (fn-own-store *own-late*)))) (fn-own-take 2 (fn-sf
- `tests/acl2/owner-tests.lisp:423` the body did not evaluate: (equal (fn-served-reply-octets (car *own-bad*)) (append (fn-nntp-string-octets "441 posting failed; 
- `tests/acl2/owner-tests.lisp:426` the body did not evaluate: (null (fn-own-queue (cdr *own-bad*)))
- `tests/acl2/owner-tests.lisp:429` the body did not evaluate: (equal (len (fn-own-queue *own-queued-again*)) 1)
- `tests/acl2/owner-tests.lisp:430` the body did not evaluate: (null (fn-own-queue (fn-own-step *own-queued-again* '(:close 4))))
- `tests/acl2/owner-tests.lisp:448` the body did not evaluate: (not (fn-own-relation *own-bogus*))
- `tests/acl2/owner-tests.lisp:452` the body did not evaluate: (let* ((o *own-bogus*) (conn (fn-own-find-conn 0 (fn-own-conns o))) (s (fn-own-store o))) (not (equa
- `tests/acl2/owner-tests.lisp:471` the body did not evaluate: (let* ((o *own-after*) (s (fn-own-store o)) (sconn (fn-served-result-conn (fn-served-open (fn-own-vi
- `tests/acl2/owner-tests.lisp:491` the body did not evaluate: (let* ((o *own-bogus*) (conn (fn-own-find-conn 0 (fn-own-conns o))) (s (fn-own-store o))) (not (equa
- `tests/acl2/owner-tests.lisp:509` the body did not evaluate: (let* ((o *own-after*) (s (fn-own-store o))) (not (equal (car (fn-own-read-step o 99 *own-group-comm
- `tests/acl2/owner-tests.lisp:77` the body did not evaluate: (fn-own-relation *own-0*)
- `tests/acl2/owner-tests.lisp:524` the body did not evaluate: (not (fn-own-relation (fn-own-run *own-bogus* nil)))
- `tests/acl2/owner-tests.lisp:525` the body did not evaluate: (null (fn-own-find-conn 99 (fn-own-conns (fn-own-run *own-after* nil))))
- `tests/acl2/owner-tests.lisp:533` the body did not evaluate: (not (equal (fn-own-take 7 (fn-sf-records (fn-sn-files (fn-own-store (fn-own-run *own-bogus-pin* *ow
- `tests/acl2/owner-tests.lisp:542` the body did not evaluate: (with-guard-checking :none (not (<= (fn-own-reclaim-floor *own-after*) (fn-own-conn-version (fn-own-
- `tests/acl2/owner-tests.lisp:547` the body did not evaluate: (with-guard-checking :none (let ((o (fn-own-make (fn-own-store *own-0*) (fn-own-view-make 3 0 nil) (
- `tests/acl2/owner-tests.lisp:565` the body did not evaluate: (not (<= (len (fn-own-conns (fn-own-run *own-over* nil))) (fn-own-max-conns *own-over*)))
- `tests/acl2/owner-tests.lisp:567` the body did not evaluate: (not (fn-own-conns-boundedp (fn-own-conns (fn-own-run *own-over* nil)) *own-groups*))
- `tests/acl2/owner-tests.lisp:78` the body did not evaluate: (equal (fn-own-view-version (fn-own-view *own-0*)) 0)
- `tests/acl2/owner-tests.lisp:574` the body did not evaluate: (not (fn-sf-record-has-pairp (cons 0 0) (fn-sf-records (fn-sn-files (fn-own-store (fn-own-run *own-f
- `tests/acl2/owner-tests.lisp:579` the body did not evaluate: (not (fn-sf-record-has-pairp (cons 9 9) (fn-sf-records (fn-sn-files (fn-own-store (fn-own-run *own-a
- `tests/acl2/owner-tests.lisp:587` the body did not evaluate: (not (fn-own-relation (fn-own-run *own-bogus* nil)))
- `tests/acl2/owner-tests.lisp:588` the body did not evaluate: (with-guard-checking :none (not (fn-snt-relation (fn-own-store (fn-own-run (fn-own-make nil nil nil 
- `tests/acl2/owner-tests.lisp:595` the body did not evaluate: (equal (fn-sn-open-kind (fn-sn-open-observed *own-groups* 10 "frontier" *own-image-records*)) :error
- `tests/acl2/owner-tests.lisp:598` the body did not evaluate: (with-guard-checking :none (not (fn-own-relation (fn-own-start (fn-sn-open-state (fn-sn-open-observe
- `tests/acl2/owner-tests.lisp:605` the body did not evaluate: (not (fn-own-relation (fn-own-start (fn-sn-open-state (fn-sn-open-observed *own-groups* 10 *own-imag
- `tests/acl2/owner-tests.lisp:614` the body did not evaluate: (not (member-equal "fn.new" (fn-own-replay-facts (fn-own-facts (fn-own-declare-group *own-after* "fn
- `tests/acl2/owner-tests.lisp:616` the body did not evaluate: (not (member-equal 42 (fn-own-replay-facts (fn-own-facts (fn-own-declare-group *own-clocked* 42)))))
- `tests/acl2/owner-tests.lisp:623` the body did not evaluate: (not (fn-own-relation *own-unstamped*))
- `tests/acl2/owner-tests.lisp:624` the body did not evaluate: (not (fn-clock-observationp (fn-own-group-fact-stamp (car (fn-own-facts *own-unstamped*)))))
- `tests/acl2/owner-tests.lisp:626` the body did not evaluate: (not (fn-clock-observationp (fn-own-group-fact-stamp (fn-own-group-fact-make "fn.other" nil))))
- `tests/acl2/owner-tests.lisp:637` the body did not evaluate: (not (fn-own-relation *own-forged-post*))
- `tests/acl2/owner-tests.lisp:638` the body did not evaluate: (let ((conn (fn-own-find-conn 4 (fn-own-conns *own-forged-post*)))) (equal (car (fn-own-outcome *own
- `tests/acl2/owner-tests.lisp:648` the body did not evaluate: (not (fn-sf-record-has-pairp (car (last (fn-own-ledger *own-forged-post*))) (fn-sf-records (fn-sn-fi
- `tests/acl2/owner-tests.lisp:654` the body did not evaluate: (with-guard-checking :none (let ((conn (fn-own-find-conn 99 (fn-own-conns *own-after-post*)))) (and 
- `tests/acl2/owner-tests.lisp:668` the body did not evaluate: (not (< (fn-own-sub-mark (fn-own-inflight *own-taken*)) (len (fn-own-ledger *own-taken*))))
- `tests/acl2/owner-tests.lisp:673` the body did not evaluate: (not (equal (fn-own-find-conn 4 (fn-own-conns (cdr *own-offer*))) (fn-own-find-conn 4 (fn-own-conns 
- `tests/acl2/owner-tests.lisp:82` the body did not evaluate: (equal (fn-served-reply-octets (car *own-open-a*)) *fn-served-greeting*)
- `tests/acl2/owner-tests.lisp:677` the body did not evaluate: (consp (car *own-240*))
- `tests/acl2/owner-tests.lisp:84` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 0 (fn-own-conns *own-a*))) 0)
- `tests/acl2/owner-tests.lisp:90` the body did not evaluate: (equal (fn-own-pending *own-begun*) 1)
- `tests/acl2/owner-tests.lisp:92` the body did not evaluate: (fn-own-relation *own-posted*)
- `tests/acl2/owner-tests.lisp:93` the body did not evaluate: (equal (fn-own-view-version (fn-own-view *own-posted*)) 1)
- `tests/acl2/owner-tests.lisp:94` the body did not evaluate: (equal (len (fn-own-ledger *own-posted*)) 1)
- `tests/acl2/owner-tests.lisp:95` the body did not evaluate: (null (fn-own-pending *own-posted*))
- `tests/acl2/owner-tests.lisp:96` the body did not evaluate: (equal (fn-sf-phase (fn-sn-files (fn-own-store *own-posted*))) :ready)
- `tests/acl2/owner-tests.lisp:99` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 0 (fn-own-conns *own-posted*))) 0)
- `tests/acl2/owner-tests.lisp:101` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 2 (fn-own-conns *own-c*))) 1)
- `tests/acl2/owner-tests.lisp:109` the body did not evaluate: (not (equal (car *own-read-a*) (car *own-read-c*)))
- `tests/acl2/owner-tests.lisp:110` the body did not evaluate: (equal (fn-served-reply-octets (car *own-read-c*)) (append (fn-nntp-string-octets "211 1 1 1 fn.lett
- `tests/acl2/owner-tests.lisp:112` the body did not evaluate: (equal (fn-own-take 5 (fn-served-reply-octets (car *own-read-a*))) (fn-nntp-string-octets "211 0"))
- `tests/acl2/owner-tests.lisp:114` the body did not evaluate: (not (fn-served-closingp (car *own-read-a*)))
- `tests/acl2/owner-tests.lisp:115` the body did not evaluate: (fn-own-relation (cdr *own-read-a*))
- `tests/acl2/owner-tests.lisp:122` the body did not evaluate: (null (car (fn-own-read *own-c* 2 (fn-own-take 7 *own-group-octets*))))
- `tests/acl2/owner-tests.lisp:123` the body did not evaluate: (equal (car *own-read-cut*) (car *own-read-c*))
- `tests/acl2/owner-tests.lisp:127` the body did not evaluate: (not (equal *own-reply-a* *own-reply-c*))
- `tests/acl2/owner-tests.lisp:128` the body did not evaluate: (equal *own-reply-c* (list (fn-nntp-reply-effect (append (fn-nntp-string-octets "211 1 1 1 fn.letter
- `tests/acl2/owner-tests.lisp:131` the body did not evaluate: (equal (car *own-read-c*) *own-reply-c*)
- `tests/acl2/owner-tests.lisp:134` the body did not evaluate: (let* ((conn (fn-own-find-conn 0 (fn-own-conns *own-c*))) (s (fn-own-store *own-c*))) (equal (car *o
- `tests/acl2/owner-tests.lisp:150` the body did not evaluate: (let* ((conn (fn-own-find-conn 0 (fn-own-conns *own-c*))) (s (fn-own-store *own-c*))) (equal *own-re
- `tests/acl2/owner-tests.lisp:169` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 0 (fn-own-conns *own-advanced*))) 1)
- `tests/acl2/owner-tests.lisp:170` the body did not evaluate: (equal (car (fn-own-read *own-advanced* 0 *own-group-octets*)) (car *own-read-c*))
- `tests/acl2/owner-tests.lisp:178` the body did not evaluate: (fn-own-relation *own-posted-2*)
- `tests/acl2/owner-tests.lisp:179` the body did not evaluate: (equal (fn-own-view-version (fn-own-view *own-posted-2*)) 2)
- `tests/acl2/owner-tests.lisp:180` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 2 (fn-own-conns *own-posted-2*))) 1)
- `tests/acl2/owner-tests.lisp:181` the body did not evaluate: (equal (len (fn-own-ledger *own-posted-2*)) 2)
- `tests/acl2/owner-tests.lisp:187` the body did not evaluate: (null (fn-own-find-conn 1 (fn-own-conns *own-closed*)))
- `tests/acl2/owner-tests.lisp:192` the body did not evaluate: (fn-own-relation *own-reopened*)
- `tests/acl2/owner-tests.lisp:193` the body did not evaluate: (null (fn-own-conns *own-reopened*))
- `tests/acl2/owner-tests.lisp:194` the body did not evaluate: (null (fn-own-clock *own-reopened*))
- `tests/acl2/owner-tests.lisp:195` the body did not evaluate: (equal (fn-own-view-version (fn-own-view *own-reopened*)) 2)
- `tests/acl2/owner-tests.lisp:196` the body did not evaluate: (fn-own-ledger-durablep (fn-own-ledger *own-reopened*) (fn-sf-records (fn-sn-files (fn-own-store *ow
- `tests/acl2/owner-tests.lisp:199` the body did not evaluate: (equal (fn-own-conn-version (fn-own-find-conn 3 (fn-own-conns *own-after*))) 2)
- `tests/acl2/owner-tests.lisp:200` the body did not evaluate: (equal (fn-served-reply-octets (car (fn-own-read *own-after* 3 *own-group-octets*))) (append (fn-nnt
- `tests/acl2/owner-tests.lisp:212` the body did not evaluate: (equal (fn-own-run *own-0* *own-trace*) *own-after*)
- `tests/acl2/owner-tests.lisp:217` the body did not evaluate: (equal (fn-sn-open-kind (fn-sn-open-observed *own-groups* 10 *own-image-frontier* *own-image-records
- `tests/acl2/owner-tests.lisp:227` the body did not evaluate: (equal (fn-sf-phase (fn-sn-files (fn-own-store *own-completing*))) :completing)
- `tests/acl2/owner-tests.lisp:228` the body did not evaluate: (not (equal (fn-own-complete *own-completing*) *own-completing*))
- `tests/acl2/owner-tests.lisp:229` the body did not evaluate: (equal (fn-own-complete (fn-own-complete *own-completing*)) (fn-own-complete *own-completing*))
- `tests/acl2/owner-tests.lisp:231` the body did not evaluate: (equal (fn-own-ledger (fn-own-complete *own-completing*)) (list (fn-sf-completion (fn-sn-files (fn-o
- `tests/acl2/owner-tests.lisp:234` the body did not evaluate: (equal (fn-own-complete *own-posted*) *own-posted*)
- `tests/acl2/owner-tests.lisp:235` the body did not evaluate: (equal (fn-own-complete *own-a*) *own-a*)
- `tests/acl2/owner-tests.lisp:244` the body did not evaluate: (equal (fn-own-reclaim-floor *own-posted-2*) 0)
- `tests/acl2/owner-tests.lisp:245` the body did not evaluate: (equal (fn-own-reclaim-floor *own-closed*) 1)
- `tests/acl2/owner-tests.lisp:246` the body did not evaluate: (equal (fn-own-reclaim-floor *own-after*) 2)
- `tests/acl2/owner-tests.lisp:247` the body did not evaluate: (equal (fn-own-take 1 (fn-sf-records (fn-sn-files (fn-own-store *own-posted-2*)))) (fn-own-take 1 (f
- `tests/acl2/owner-tests.lisp:256` the body did not evaluate: (equal (len (fn-own-conns *own-full*)) 4)
- `tests/acl2/owner-tests.lisp:257` the body did not evaluate: (equal (fn-own-max-conns *own-full*) 4)
- `tests/acl2/owner-tests.lisp:258` the body did not evaluate: (fn-own-conns-boundedp (fn-own-conns *own-full*) *own-groups*)
- `tests/acl2/owner-tests.lisp:259` the body did not evaluate: (null (car (fn-own-open *own-full*)))
- `tests/acl2/owner-tests.lisp:260` the body did not evaluate: (equal (cdr (fn-own-open *own-full*)) *own-full*)
- `tests/acl2/owner-tests.lisp:268` the body did not evaluate: (equal (fn-own-declare-group *own-after* "fn.new") *own-after*)
- `tests/acl2/owner-tests.lisp:270` the body did not evaluate: (equal (fn-own-clock *own-clocked*) *own-obs*)

