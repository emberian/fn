# Ten-second books, fourth pass, 2026-09-26

This lane changed hints in three books: `bp-node-job-offer`,
`bp-node-machine-authorization` and `owner-invariants`. No theorem
statement, name, rule class or exported rule moved. Two local lemmas were
added to `owner-invariants`: one with `:rule-classes nil`, and one rewrite
rule that is disabled right after it and enabled only in two hints. No new
PRF. PKT-326 names what is still open.

## Method

The per-event `Time` and `Rules:` come from these certify logs:
- `certify-20260926T044558Z-333541`: hbox, batch 17, 2 jobs, under three
  lanes' load. It has `bp-node-job-offer`.
- `certify-20260926T050746Z-356474`: hbox, candidate closure, 4 jobs, load 8.
  It has `bp-node-machine-authorization` and `owner-invariants`.

The slow events were then profiled in `tools/proof_repl.py` sessions under
`(accumulated-persistence t)`:
- hbox: `/tank/fn/toolchains/w28/acl2-literal-4g`, cache `/tank/fn/certcache`.
- persvati: `w25/acl2-literal`, cache `/home/ember/fn-certcache`. Only the
  first owner-invariants session ran here (see Environment).

Each variant was admitted after a `ubt` of the original, so that the
original could not prove it as a rewrite rule.

### What the logs say before any change

CPU (the ACL2 summary) against wall (the manifest):
- `bp-node-job-offer` at 11.65 s wall is 6.49 s CPU. Of that, 4.18 s is the
  include of `bp-node-contact-driver`, loaded without its compiled file (the
  farm's "Unable to load compiled file" for `defrecord`), and 1.74 s is proof.
  The 11.6 s is contention plus the uncompiled include. It is not proof work.
- `bp-node-machine-authorization`: 12.55 s wall at 4 jobs is 6.15 s CPU, of
  which 5.30 s is proof.
- `owner-invariants`: 15.61 s wall is 15.54 s CPU, of which 13.38 s is proof.
  This is the real debt.

## What was changed

| Book | Event | Cause | Hint repair | REPL before → after |
| --- | --- | --- | --- | ---: |
| owner-invariants | `fn-own-refresh-preserves-relation` | The hint enabled `fn-midx-correspondencep` and `fn-gidx-build`, so both tries were rebuilt symbolically (`fn-midx-build` 70 k frames, 66 k useless) | Keeps `fn-midx-refresh`, `fn-midx-correspondencep`, `fn-gidx-build` and `fn-snt-ready-or-recovered-node-is-exact-replay` closed. The index then goes through `fn-midx-refresh-preserves-correspondence` and the group index through `fn-ctl-visible-state-of-fields` | 3.43 → 0.39 s (persvati, quiet) |
| owner-invariants | `fn-own-durable-reply-names-a-durable-record` | The whole relation was opened for two conjuncts | A local `:rule-classes nil` lemma `fn-own-relation-conns-and-ledger` (relation ⇒ conns-okp ∧ ledger-durablep, minimal theory). The relation stays closed | 1.66 → 0.64 s (hbox) |
| owner-invariants | `fn-own-read-is-served-step-on-pinned-prefix`, `fn-own-reader-sees-pinned-prefix-replay` | The same | The same lemma | 0.41 → 0.05 s (hbox) |
| owner-invariants | `fn-own-open-peer-preserves-relation`, `fn-own-open-preserves-relation` | The old table's `fn-own-conns-okp` reopened recursively (34 k frames, 30 k useless) | A local rule `fn-own-conns-okp-of-cons`, disabled after admission. These two hints enable it and close `fn-own-conns-okp` | 1.11 → 0.76 s, 0.33 → 0.25 s (hbox) |
| owner-invariants | `fn-own-replace-conn-okp` | `fn-own-conn-okp` opened at every step of the induction | `fn-own-conn-okp` is closed | 0.51 → 0.03 s (hbox) |
| bp-node-machine-authorization | `fn-bpn-find-job-of-find-queued-for-peer` | `fn-bpn-jobp` and its field recognizers opened (354 k steps) | Minimal theory plus the four list definitions. Key uniqueness is all the proof needs | 1.40 → 0.00 s (hbox) |
| bp-node-machine-authorization | `fn-bpn-step-cl-send-is-authorized-by-durable-attempt-record`, `fn-bpn-step-queue-acceptance-is-durable-or-exact-duplicate` | `nth` and `zp` opened (28 to 31 k and 14 to 16 k useless frames) | `nth` and `zp` are left out of the minimal theory. The statement and the step both name the event's fields by `nth` | 1.79 → 0.80 s, 1.85 → 0.90 s (hbox) |
| bp-node-job-offer | `fn-bpnj-found-job-has-its-key` | `fn-bpn-job-key` opened (193 k steps) | Minimal theory plus the search's definition, with its induction. Disabling the key instead made the proof slower: 14.2 s | 0.48 → 0.00 s |
| bp-node-job-offer | `fn-bpnj-result-event-is-a-served-event`, `fn-bpnj-with-base-fields`, `fn-bpnj-with-base-keeps-the-other-fields` | Full theory | Minimal theory with the rules each proof used | 0.25, 0.15 and 0.15 → 0.00 s |

Whole-book REPL event totals:
- `owner-invariants`: 16.2 → 8.1 s on hbox at load 7 to 10. The 16.2 s load
  already had the refresh repair. On persvati, quiet, the original bytes
  totalled 11.9 s.
- `bp-node-machine-authorization`: about 6 → 2.5 s (hbox).
- `bp-node-job-offer`: proof about 1.7 → 0.5 s. The rest is the 1.5 s include
  with the fasl, or 4.2 s without it.

Tried and not kept:
- `fn-own-min-pinned-below-found`: closing the projection recognizers left it
  at 0.34 s. Its time is not in rewriting (29 k steps).
- `fn-bpn-enqueue-step-preserves-lifecycle-invariant`: without `nth`/`zp`
  it took 4.4 s, against 0.7 s before.
- The open-peer variant that also closed `fn-own-view-okp` failed. The new
  connection's okp needs the view's version facts.

## Certification

`run-20260926T064751Z-da0e` ran on hbox, source b5994e34. It used w28
`acl2-literal-4g`, 2 jobs, 300 s, `--affected-by` the three changed books, and
no `--closure`. Result: 119 certified, 0 failed, 316 installed from the cache.
The load average was 7 to 9 before, during and after the run. The box has 24
cores, and the load came from other lanes.
[manifest](manifests/certify-20260926T064807Z-632722.json)

| Book | Before: wall / CPU (run, jobs, load) | After: wall / CPU, 2 jobs, hbox load 7 to 9 |
| --- | ---: | ---: |
| books/bp-node-job-offer | 11.65 / 6.49 s (044558Z, 2 jobs, under load); 7.6 s quiet, its lane | 3.27 / 3.21 s (prove 1.74 → 0.98) |
| books/bp-node-machine-authorization | 12.55 / 6.15 s (050746Z, 4 jobs, load 8) | 3.17 / 3.07 s (prove 5.30 → 2.27) |
| books/owner-invariants | 15.61 / 15.54 s (050746Z, 4 jobs, load 8); 12.94 s at 2 jobs on persvati (010233Z) | 10.29 / 10.19 s (prove 13.38 → 7.93) |
| tests/acl2/bp-node-job-offer-tests | 4.73 s (044558Z) | 2.32 s |
| tests/acl2/bp-node-machine-authorization-tests | 0.97 s (050746Z) | 1.12 s |

`owner-invariants` is still 10.3 s wall at load 8 to 9 on hbox. Its
proof time fell by 41 %. The same bytes' events total 8.1 s in an hbox REPL
at the same load. On persvati, quiet, the unrepaired book's events totalled
11.9 s, and the refresh repair alone takes 3.0 s off that. No quiet 2-job farm
figure exists for these bytes (see Environment), so no quiet figure is claimed.

The load artefacts, from existing manifests with no run spent on them (the
deputy's order):
- bp-node-progress: 4.93 s at 2 jobs in batch 17 (044558Z, under load).
- relay-source-routes-tests: 4.38 s there.
- bp-node-forwarding-teeth-tests: 8.49 s there.

All three are under ten seconds at two jobs even under load, and none was
touched. The same run measured `books/owner-control-read` at 10.34 s wall,
6.48 s CPU: contention, and not this lane's book. `proof_cost` prints it as
`NEAR`.

## Baseline

This lane edited `planning/proof-cost-baseline.json` by hand for the books it
repaired:
- `bp-node-job-offer` is removed (3.27 s).
- `owner-invariants` is lowered from 12.936 s (persvati, 010233Z) to 10.291 s
  (hbox, this run, under load 7 to 9).

`proof_cost`: baseline 1 book, 0 failing.

## Environment

- persvati's ACL2 slot pool (16 slots) was held by 15 idle `proof_repl.py
  serve` sessions from other lanes: reclaim-d13 ×4 (21 h), k0-corollaries
  (25 h), status-join, rep-wave-c, peer-keys, peer-pull, reclaim-lifecycle ×3,
  operator-walk ×2 and consumer-e2-2. A farm submit waited 13 minutes in the
  cache preflight for a slot, with capacity-vector and rollback-history
  queued for the same one. The box's load was 0.5 to 0.9. This lane's
  owner-invariants work moved to hbox. Those sessions are not this lane's to
  stop. PKT-326 item (c).
- hbox's load was 6 to 11 for the whole session, from other lanes' builds.

## Seam with hot-path-scans

hot-path-scans owns the selector's traversal in `bp-node-job-offer`
(`lane/hot-path-scans` 47695837, not on dev at the time of writing). This
lane changed only the hints of four events there, none of `fn-bpnj-select`,
`fn-bpnj-held`, `fn-bpnj-contact-next` or their theorems. `git merge-tree`
of the two branches is clean.

## PKT-326 (not done)

- (a) `owner-invariants` needs a quiet 2-job measurement at b5994e34 or
  later: persvati once its slots free, or hbox at load < 3. Under the
  deputy's 06:50 word (a book over ten seconds only under load is not a
  defect), it leaves the baseline if that figure is under ten seconds.
- (b) The remaining owner-invariants cost is a flat tail. The largest items
  are open-peer at 0.6 s, refresh 0.4 s, durable-reply 0.36 s,
  read-preserves 0.34 s and min-pinned-below-found 0.29 s. The last is not
  rewriting (29 k steps) and was not repaired. No split: every includer
  would pay the 1 to 4 s include of `owner` again.
- (c) persvati's ACL2 slot pool is held by 15 idle REPL sessions of other
  lanes (Environment). This is the deputy's to reap. It starves every lane's
  farm run and REPL there.
- (d) The farm installs `defrecord` without its compiled file (the
  ten-second-2 finding). That is 2.7 of `bp-node-job-offer`'s 6.5 CPU seconds
  before this lane's change (the include took 4.18 s in the farm and 1.5 s in
  the REPL with the fasl).
