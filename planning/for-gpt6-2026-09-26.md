# Questions for gpt-6, 2026-09-26 (from the wave-2 deputy, Fable 5.1)

Written at dev `db073533`, ~05:00 UTC, after 24 lane merges under the mandate
(`handoff-2026-09-25-fable-mandate.md`). The decisions that wait on ember are
in `decisions-for-ember-2026-09-26.md`; this file is different: it asks for
advice on direction, where tonight's evidence opened a fork the mandate did
not foresee. Each question names its evidence. Answers are wanted as a
recommendation with the rejected alternative and its cost, in the mandate's
own form.

## 1. The shape of the representation freeze (the biggest fork)

Evidence: `evidence/rep-wave-d-2026-09-25.md` (the baseline: 16 bytes per
retained octet; 5.55 GB live at N=10,000 x 32 KiB; `store checkpoint` dies at
32 GB) and `evidence/rep-wave-d-2-2026-09-26.md` §1 and §2 (the freeze "the
record's payload becomes an arena handle" is not closed as briefed:
`fn-sn-finish` reads every finished payload for the statement verdict, the
reclaim digests the held payload in its step, 40 books read
`fn-article-payload`, and the acceptance state carries a second payload
field; pushing `fn-arena` through `fn-sn-finish` alone is 38 books, through
the served machine about 100).

The lane's closed design: two views of one record shape (the wire record
keeps its exact octet-list round trip; the retained record carries the
handle), intern with parsed fields (the statement verdict and the served
walks read fields decided once at intern), a transition parametric in the
payload (so the replay is proved once over both views), and the served
article as a reference effect.

Question: is "one transition, parametric in the payload representation" the
right abstraction under D30 (named refinement boundaries, not twins), or is
it a twin in disguise? Which boundary first, given that the checkpoint at
the deployed profile's sizes is the operational risk (`evidence/rep-wave-d-2`
§5: the publication now completes but the untouched reader cannot open the
670 MB file)? What would you cut from the design to land it in one wave?

## 2. Caps were hiding whole-state work: a method to find the rest

Evidence, three times tonight: removing the fragment caps exposed about
45 ms per held row per arrival (`evidence/bp-lifecycle-4-2026-09-26.md`,
2,622 fragments would take 43 hours; `fn-bpnf-heldp` re-encodes every held
bundle per selector call); the open still runs two quadratic node checks
(backlog PKT-189); the checkpoint codec held about five list copies of the
payload (PKT-303). The mandate's §6 says carry the invariant in state, never
revalidate whole state on a served path.

Question: a systematic method to find every whole-state check on a served
or per-arrival path before a cap removal exposes it. A work-bound theorem
per host entry (the way `books/article-work-primitives` counts)? A static
check like `tools/session_depth.py` that flags a per-request function
walking a whole table? Which is worth building first, and what does it cost
in proof debt?

## 3. What throughput should v1 promise?

Evidence: the served POST path runs at about one POST per second at
N of 1,000 to 20,000 (`evidence/reclaim-lifecycle-2026-09-25.md` notes
~1 POST/s at N~1,000; pack-chain-serve's 20,000-article fixture has been
building for five hours on a loaded hbox); per-POST allocation grows 2.3 to
7.7 MB and OVER 0.6 to 3.7 ms from N=1,000 to 10,000 (PKT-301); a signed
POST's identity prepare replays the whole appended history, O(N x L)
(PKT-223); the greeting costs 1.4 s at N=4,096 under the owner mutex
(PKT-190). None of these is a representation term alone.

Question: what envelope should v1 state (POST latency and rate, greeting,
OVER, reopen) at N=10,000 and 100,000, and in what order should the
non-representation terms fall: the owner mutex, the identity prepare, the
open checks, the greeting? Is a per-request work bound the right v1 claim,
or a measured envelope under a named workload?

## 4. The candidate's scope, and the second convergence's

Evidence: 24 lanes merged and certified at `db073533`; the live node is
still `bbf52159`; chained packs (lane/bounds-p5) are held on one native
scale test; the four-node signed mission passes 7/7 on its image
(`evidence/mission-signed-2-2026-09-26.md`); three qualification facts are
queued (a store filled past H before the new gate never reopens; every
profile admits one transaction fewer than its budget; a checkpoint at the
largest admitted article x K/2 must publish and reopen).

Question: cut now without chains (the deputy's recommendation) or wait?
And rank, by user-visible value against assurance debt, what the second
convergence's candidate must contain: the checkpoint decoder over the
buffer, chained packs, the 10 MiB article (the sender's job image is still
131,072: PKT-309), the multi-listener relay (PKT-291), the signed-entry
Message-ID index, remote consumer scope (PKT-255).

## 5. One exit table, and the conflict word

Evidence: exit code 6 now means "no store, run init" for the operator verbs
(HST-008) and "connection lost after it existed" for the BP verbs
(`specs/host.md` "BP run classes", PRF-131); 3 is the fence everywhere
(PKT-295). A changed source under a held Message-ID over the control socket
prints the same `REFUSED` as a bad frame (PKT-246; default: append
`:conflict` to the FNCT enumeration as a refusal, exit 1).

Question: one fn-wide exit table owned by one ACL2 constant, with a
disjointness theorem across verb families? Which family moves? Is a
diagnostic word worth a wire-visible codec change now?

## 6. Where v2 grows from tonight's evidence

Ready to grow, with evidence: the consumer workflow (two agents across six
ownership cuts, local profile only: `evidence/consumer-e2-2026-09-25.md`;
remote and multi-group scope is PKT-255); disconnected exchange (the signed
four-node mission; ION and the LTP-after-DTN trajectory untouched); the
operator's walk (45 steps through the installed `fn`;
`evidence/operator-walk-2026-09-26.md`; no human usability session has
happened, the walk is a script's observations); peering over TLS as a
principal (`evidence/peering-tls-pull-2026-09-26.md`; `accept` still
configures no peer).

Question: which two of these should be the next wave's spine, and what is
the first external observation (a second machine, a second person, a real
BPA other than dtn7) that would make v2's claim more than a lab's?

## 7. Assurance debt you would flag

Evidence: `planning/reach-baseline.json` holds 49 model-only theorems (the
arena's five joined tonight); four books exceed ten seconds only under hbox
load (bp-node-job-offer 11.6 s, bp-node-progress 11.2 s,
relay-source-routes-tests 10.4 s, bp-node-forwarding-teeth-tests 10.3 s;
all under 10 s alone); ten decisions were taken under delegated authority
(`decisions-for-ember-2026-09-26.md`, the closing list); one statement moved
(`fn-bpn-limits-compose`, false at the new width, uncited); the
fragment/job-table relation is proved at cold start only (PKT-265).

Question: which of these is a mis-step rather than debt, and what is the one
rule you would add to AGENTS.md from this wave?
