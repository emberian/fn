# Allocation pause — 2026-09-21

The user requested winding down and quiescence. The autonomous goal is **paused**;
resume only on user instruction. Main checkpoint before this note is
`75639e15b8a904e849a2c16800b47ba5e841d676`. No release or completion claim.
All active lanes were instructed to stop processes, preserve their work, and
launch no successors. Their worktrees remain for resumption; do not delete WIP.
The proposed new Claude/tmux review was never launched.

## Integrated checkpoint

Main includes native hybrid control and shared durable completion, the identity
recovery fault repair, exact-byte compaction interruption tests, tagged-event
append proof repair/teeth, the Message-ID trie foundation, model reclaim suffix
preservation events, the protected peering test/runner, and native v0 peering
matrix accounting with observed process identities. Relevant evidence and
limitations are linked from `planning/now.md`.

The latest full `make check` completed successfully before the final matrix
identity-role hardening (`75639e15`); that hardening had nine passing focused
unit tests in its lane. Checks are scaffolding, not whole-system assurance.
Native protected tests were locally skipped for lack of a qualified image.
Neither those skips nor source evaluation constitutes runtime qualification.

## Frozen integration gate

Gate branch `w29/native-freeze-df217`, clean tip
`5d9181dd309218d3db306fb07556864a089f2ae3`, is exactly `773e9ae3` plus the
proof-only signature-carrier repair. Remote tree:
`persvati:/home/ember/fn-gates/freeze-df21773d`.
Its certification was cancelled with partial logs, no terminal manifest;
no new production/developer/DTN images or runtime gates were started.
Under that tree's `build/acl2/`, the partial directory has timestamp
`20260921T190933Z` and runner PID `3116676` (directory naming convention:
`certify-<timestamp>-<pid>`). Preserve it as interrupted evidence.

On resumption: inspect surviving exact-source cache results; finish the gate's
required closure, build revision-specific immutable images, record all input,
launcher/core/runtime hashes, and run current tests as a separately recorded
test overlay. Do not rebuild an artifact path while another lane measures it.
Qualified persvati wrapper: `/home/ember/fn-gates/toolchains/w25/acl2-literal`.
Qualified hbox wrapper: `/tank/fn/toolchains/w28/acl2-literal-4g`.
The ordinary saved_acl2 launcher is not the qualified farm wrapper.

## Preserved unmerged work

| Lane / branch | Checkpoint | Remaining work |
| --- | --- | --- |
| `w27/bounded-event-profiles` | `2e1ee2414e00c4f6d506f19ee5670a8fb21ccdb5` | General bounded CBOR proofs have focused results; parameterized statement sequence proofs and actual large-record caller lifts remain uncertified. Finish witnesses and archive evidence before registry claims. |
| `w25/prf050-identity-sequence` | `b8cf10e6` | Phase-aware sequence invariant, fast identity/retention callers and both relation teeth are preserved. Dependency guard/provenance failures prevented owned certification. Unique commits are a32b0953, 0ffb8bc2, 51f9eb06, 5db0db25, 47079caa, b6d9d74d; prerequisite cherry-picks are already integrated elsewhere. |
| `w28/nntp-msgid-index` | `0e69b30d` | Actual pinned-reader trie propagation is WIP. External owner-connection constructor arities, full called-step refinement and indexed-chain guards remain open. Main contains only the earlier primitive foundation. |
| `w26/reclaim-keystone-teeth` | `e7d110d570346570f53fb0e643f8167050a2781d` | Main has the general model suffix chain. Followups archive logs and add the crash-image premise tooth; remote certification did not reach owned roots. Register the actual core theorem only after certification and complete teeth, not its selected wrapper corollary. |
| `w29-peer-row-roundtrip` | `5cea98c4f3ac0e7289d24a5a0d98ab0f2153f641` | Name/path component proofs pass source evaluation; transport proof still fails, inbound/outbound/auth and general codec proof remain. This is WIP, not a completed repair. |
| `recovery/portable-hybrid-authorship` | `21ccae85` | Latest Date/projection changes are not ACL2-admitted/certified. Render/receive is a witness, not general theorem. Mutable relay fields are deliberately refused until their grammar/projection composition is implemented; NNTP/BP transport integration remains. |
| `w26/owner-tls-overlay` | `9e97d035` | Store invariant repair and teeth are already main. Actual outer TLS-prefix correspondence and current store proof closure still need qualification against coherent dependencies. |

Lane worktrees are under `build/lanes/`; `git worktree list` gives exact paths.
All reported lane checkpoints above were clean on shutdown. Do not merge entire
old branches or their generated ledgers blindly; integrate unique coherent
changes and regenerate the ledger on main.

## Stopped jobs and retained evidence

The BP sequence run was stopped at process group `3097876`; partial evidence
is under its farm/lane `build/acl2/`, timestamp `20260921T190731Z`, PID `3097890`.
It had not reached owned roots. The latest bounded-profile partial closure is
local timestamp `20260921T191431Z`, PID `33581`; the preceding CBOR result was
completed, while statement completion remains unestablished.

Portable-carrier inherited closures were interrupted on both hosts. Root's
independent final check found persvati orphan group `3104794` with ACL2 child
`3110679` still alive after the lane stopped its SSH wrapper; root terminated
that exact process group. Stopping an SSH client alone is not remote cleanup.
The corresponding tree is `/home/ember/fn-gates/recovery-portable-hybrid-authorship`.
No fn certification/ACL2 workers were observed on hbox during shutdown.
No new tmux review session existed. Runtime tests, proof jobs, and agents must
remain stopped until the user resumes work.
